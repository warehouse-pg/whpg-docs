import{_ as s,c as t,o as a,ag as n}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"gpinitsystem","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/gpinitsystem.md","filePath":"docs/7x/utility_guide/ref/gpinitsystem.md"}'),o={name:"docs/7x/utility_guide/ref/gpinitsystem.md"};function i(r,e,c,p,l,d){return a(),t("div",null,e[0]||(e[0]=[n(`<h1 id="gpinitsystem" tabindex="-1">gpinitsystem <a class="header-anchor" href="#gpinitsystem" aria-label="Permalink to &quot;gpinitsystem&quot;">​</a></h1><p>Initializes a WarehousePG cluster using configuration parameters specified in the <code>gpinitsystem_config</code> file.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpinitsystem -c &lt;cluster_configuration_file&gt; </span></span>
<span class="line"><span>            [-h &lt;hostfile_gpinitsystem&gt;]</span></span>
<span class="line"><span>            [-B &lt;parallel_processes&gt;] </span></span>
<span class="line"><span>            [-p &lt;postgresql_conf_param_file&gt;]</span></span>
<span class="line"><span>            [-s &lt;standby_coordinator_host&gt;</span></span>
<span class="line"><span>                [-P &lt;standby_coordinator_port&gt;]</span></span>
<span class="line"><span>                [-S &lt;standby_coordinator_datadir&gt; | --standby_datadir=&lt;standby_coordinator_datadir&gt;]]</span></span>
<span class="line"><span>            [-m &lt;number&gt; | --max_connections=number&gt;]</span></span>
<span class="line"><span>            [-b &lt;size&gt; | --shared_buffers=&lt;size&gt;]</span></span>
<span class="line"><span>            [-n &lt;locale&gt; | --locale=&lt;locale&gt;] [--lc-collate=&lt;locale&gt;] </span></span>
<span class="line"><span>            [--lc-ctype=&lt;locale&gt;] [--lc-messages=&lt;locale&gt;] </span></span>
<span class="line"><span>            [--lc-monetary=&lt;locale&gt;] [--lc-numeric=&lt;locale&gt;] </span></span>
<span class="line"><span>            [--lc-time=&lt;locale&gt;] [-e &lt;password&gt; | --su_password=&lt;password&gt;] </span></span>
<span class="line"><span>            [--mirror-mode={group|spread}] [-a] [-q] [-l &lt;logfile_directory&gt;] [-D]</span></span>
<span class="line"><span>            [-I &lt;input_configuration_file&gt;]</span></span>
<span class="line"><span>            [-O &lt;output_configuration_file&gt;]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpinitsystem -v | --version</span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpinitsystem -? | --help</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>The <code>gpinitsystem</code> utility creates a WarehousePG instance or writes an input configuration file using the values defined in a cluster configuration file and any command-line options that you provide. See <a href="#section5">Initialization Configuration File Format</a> for more information about the configuration file. Before running this utility, make sure that you have installed the WarehousePG software on all the hosts in the array.</p><p>With the <code>&lt;-O output_configuration_file&gt;</code> option, <code>gpinitsystem</code> writes all provided configuration information to the specified output file. This file can be used with the <code>-I</code> option to create a new cluster or re-create a cluster from a backed up configuration. See <a href="#section5">Initialization Configuration File Format</a> for more information.</p><p>In a WarehousePG DBMS, each database instance (the coordinator instance and all segment instances) must be initialized across all of the hosts in the system in such a way that they can all work together as a unified DBMS. The <code>gpinitsystem</code> utility takes care of initializing the WarehousePG coordinator and each segment instance, and configuring the system as a whole.</p><p>Before running <code>gpinitsystem</code>, you must set the <code>$GPHOME</code> environment variable to point to the location of your WarehousePG installation on the coordinator host and exchange SSH keys between all host addresses in the array using <code>gpssh-exkeys</code>.</p><p>This utility performs the following tasks:</p><ul><li>Verifies that the parameters in the configuration file are correct.</li><li>Ensures that a connection can be established to each host address. If a host address cannot be reached, the utility will exit.</li><li>Verifies the locale settings.</li><li>Displays the configuration that will be used and prompts the user for confirmation.</li><li>Initializes the coordinator instance.</li><li>Initializes the standby coordinator instance (if specified).</li><li>Initializes the primary segment instances.</li><li>Initializes the mirror segment instances (if mirroring is configured).</li><li>Configures the WarehousePG cluster and checks for errors.</li><li>Starts the WarehousePG cluster.</li></ul><blockquote><p><strong>Note</strong> This utility uses secure shell (SSH) connections between systems to perform its tasks. In large WarehousePG deployments, cloud deployments, or deployments with a large number of segments per host, this utility may exceed the host&#39;s maximum threshold for unauthenticated connections. Consider updating the SSH <code>MaxStartups</code> and <code>MaxSessions</code> configuration parameters to increase this threshold. For more information about SSH configuration options, refer to the SSH documentation for your Linux distribution.</p></blockquote><h2 id="options" tabindex="-1"><a id="section4"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>-a : Do not prompt the user for confirmation.</p><p>-B parallel_processes : The number of segments to create in parallel. If not specified, the utility will start up to 4 parallel processes at a time.</p><p>-c cluster_configuration_file : Required. The full path and filename of the configuration file, which contains all of the defined parameters to configure and initialize a new WarehousePG cluster. See <a href="#section5">Initialization Configuration File Format</a> for a description of this file. You must provide either the <code>-c &lt;cluster_configuration_file&gt;</code> option or the <code>-I &lt;input_configuration_file&gt;</code> option to <code>gpinitsystem</code>.</p><p>-D : Sets log output level to debug.</p><p>-h hostfile_gpinitsystem : Optional. The full path and filename of a file that contains the host addresses of your segment hosts. If not specified on the command line, you can specify the host file using the <code>MACHINE_LIST_FILE</code> parameter in the gpinitsystem_config file.</p><p>-I input_configuration_file : The full path and filename of an input configuration file, which defines the WarehousePG host systems, the coordinator instance and segment instances on the hosts, using the <code>QD_PRIMARY_ARRAY</code>, <code>PRIMARY_ARRAY</code>, and <code>MIRROR_ARRAY</code> parameters. The input configuration file is typically created by using <code>gpinitsystem</code> with the <code>-O output\\_configuration\\_file</code> option. Edit those parameters in order to initialize a new cluster or re-create a cluster from a backed up configuration. You must provide either the <code>-c &lt;cluster_configuration_file&gt;</code> option or the <code>-I &lt;input_configuration_file&gt;</code> option to <code>gpinitsystem</code>.</p><p>-n locale | --locale=locale : Sets the default locale used by WarehousePG. If not specified, the default locale is <code>en_US.utf8</code>. A locale identifier consists of a language identifier and a region identifier, and optionally a character set encoding. For example, <code>sv_SE</code> is Swedish as spoken in Sweden, <code>en_US</code> is U.S. English, and <code>fr_CA</code> is French Canadian. If more than one character set can be useful for a locale, then the specifications look like this: <code>en_US.UTF-8</code> (locale specification and character set encoding). On most systems, the command <code>locale</code> will show the locale environment settings and <code>locale -a</code> will show a list of all available locales.</p><p>--lc-collate=locale : Similar to <code>--locale</code>, but sets the locale used for collation (sorting data). The sort order cannot be changed after WarehousePG is initialized, so it is important to choose a collation locale that is compatible with the character set encodings that you plan to use for your data. There is a special collation name of <code>C</code> or <code>POSIX</code> (byte-order sorting as opposed to dictionary-order sorting). The <code>C</code> collation can be used with any character encoding.</p><p>--lc-ctype=locale : Similar to <code>--locale</code>, but sets the locale used for character classification (what character sequences are valid and how they are interpreted). This cannot be changed after WarehousePG is initialized, so it is important to choose a character classification locale that is compatible with the data you plan to store in WarehousePG.</p><p>--lc-messages=locale : Similar to <code>--locale</code>, but sets the locale used for messages output by WarehousePG. The current version of WarehousePG does not support multiple locales for output messages (all messages are in English), so changing this setting will not have any effect.</p><p>--lc-monetary=locale : Similar to <code>--locale</code>, but sets the locale used for formatting currency amounts.</p><p>--lc-numeric=locale : Similar to <code>--locale</code>, but sets the locale used for formatting numbers.</p><p>--lc-time=locale : Similar to <code>--locale</code>, but sets the locale used for formatting dates and times.</p><p>-l logfile_directory : The directory to write the log file. Defaults to <code>~/gpAdminLogs</code>.</p><p>-m number | --max_connections=number : Sets the maximum number of client connections allowed to the coordinator. The default is 250.</p><p>-O output_configuration_file : Optional, used during new cluster initialization. This option writes the <code>cluster_configuration_file</code> information (used with -c) to the specified <code>output_configuration_file</code>. This file defines the WarehousePG members using the <code>QD_PRIMARY_ARRAY</code>, <code>PRIMARY_ARRAY</code>, and <code>MIRROR_ARRAY</code> parameters. Use this file as a template for the <code>-I</code> <code>input_configuration_file</code> option. See <a href="#section6">Examples</a> for more information.</p><p>-p postgresql_conf_param_file : Optional. The name of a file that contains <code>postgresql.conf</code> parameter settings that you want to set for WarehousePG. These settings will be used when the individual coordinator and segment instances are initialized. You can also set parameters after initialization using the <code>gpconfig</code> utility.</p><p>-q : Run in quiet mode. Command output is not displayed on the screen, but is still written to the log file.</p><p>-b size | --shared_buffers=size : Sets the amount of memory a WarehousePG server instance uses for shared memory buffers. You can specify sizing in kilobytes (kB), megabytes (MB) or gigabytes (GB). The default is 125MB.</p><p>-s standby_coordinator_host : Optional. If you wish to configure a backup coordinator instance, specify the host name using this option. The WarehousePG software must already be installed and configured on this host.</p><p>-P standby_coordinator_port : If you configure a standby coordinator instance with <code>-s</code>, specify its port number using this option. The default port is the same as the coordinator port. To run the standby and coordinator on the same host, you must use this option to specify a different port for the standby. The WarehousePG software must already be installed and configured on the standby host.</p><p>-S standby_coordinator_datadir | --standby_dir=standby_coordinator_datadir : If you configure a standby coordinator host with <code>-s</code>, use this option to specify its data directory. If you configure a standby on the same host as the coordinator instance, the coordinator and standby must have separate data directories.</p><p>-e superuser_password | --su_password=superuser_password : Use this option to specify the password to set for the WarehousePG superuser account (such as <code>gpadmin</code>). If this option is not specified, the default password <code>gparray</code> is assigned to the superuser account. You can use the <code>ALTER ROLE</code> command to change the password at a later time.</p><pre><code>Recommended security best practices:

-   Do not use the default password option for production environments.
-   Change the password immediately after installation.
</code></pre><p>--mirror-mode={group|spread} : Use this option to specify the placement of mirror segment instances on the segment hosts. The default, <code>group</code>, groups the mirror segments for all of a host&#39;s primary segments on a single alternate host. <code>spread</code> spreads mirror segments for the primary segments on a host across different hosts in the WarehousePG cluster. Spreading is only allowed if the number of hosts is greater than the number of segment instances per host. See <a href="./../../admin_guide/ha/overview-of-segment-mirroring.html#topic3">Overview of Segment Mirroring</a> for information about WarehousePG mirroring strategies.</p><p>-v | --version : Print the <code>gpinitsystem</code> version and exit.</p><p>-? | --help : Show help about <code>gpinitsystem</code> command line arguments, and exit.</p><h2 id="initialization-configuration-file-format" tabindex="-1"><a id="section5"></a>Initialization Configuration File Format <a class="header-anchor" href="#initialization-configuration-file-format" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Initialization Configuration File Format&quot;">​</a></h2><p><code>gpinitsystem</code> requires a cluster configuration file with the following parameters defined. An example initialization configuration file can be found in <code>$GPHOME/docs/cli_help/gpconfigs/gpinitsystem_config</code>.</p><p>To avoid port conflicts between WarehousePG and other applications, the WarehousePG port numbers should not be in the range specified by the operating system parameter <code>net.ipv4.ip_local_port_range</code>. For example, if <code>net.ipv4.ip_local_port_range = 10000 65535</code>, you could set WarehousePG base port numbers to these values.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PORT_BASE = 6000</span></span>
<span class="line"><span>MIRROR_PORT_BASE = 7000</span></span></code></pre></div><p>MACHINE_LIST_FILE : <strong>Optional.</strong> Can be used in place of the <code>-h</code> option. This specifies the file that contains the list of the segment host address names that comprise the WarehousePG cluster. The coordinator host is assumed to be the host from which you are running the utility and should not be included in this file. If your segment hosts have multiple network interfaces, then this file would include all addresses for the host. Give the absolute path to the file.</p><p>SEG_PREFIX : <strong>Required.</strong> This specifies a prefix that will be used to name the data directories on the coordinator and segment instances. The naming convention for data directories in a WarehousePG cluster is SEG_PREFIXnumber where number starts with 0 for segment instances (the coordinator is always -1). So for example, if you choose the prefix <code>gpseg</code>, your coordinator instance data directory would be named <code>gpseg-1</code>, and the segment instances would be named <code>gpseg0</code>, <code>gpseg1</code>, <code>gpseg2</code>, <code>gpseg3</code>, and so on.</p><p>PORT_BASE : <strong>Required.</strong> This specifies the base number by which primary segment port numbers are calculated. The first primary segment port on a host is set as <code>PORT_BASE</code>, and then incremented by one for each additional primary segment on that host. Valid values range from 1 through 65535.</p><p>DATA_DIRECTORY : <strong>Required.</strong> This specifies the data storage location(s) where the utility will create the primary segment data directories. The number of locations in the list dictate the number of primary segments that will get created per physical host (if multiple addresses for a host are listed in the host file, the number of segments will be spread evenly across the specified interface addresses). It is OK to list the same data storage area multiple times if you want your data directories created in the same location. The user who runs <code>gpinitsystem</code> (for example, the <code>gpadmin</code> user) must have permission to write to these directories. For example, this will create six primary segments per host:</p><p>: \`\`\` declare -a DATA_DIRECTORY=(/data1/primary /data1/primary /data1/primary /data2/primary /data2/primary /data2/primary)</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span></span></span>
<span class="line"><span>COORDINATOR\\_HOSTNAME</span></span>
<span class="line"><span>:   **Required.** The host name of the coordinator instance. This host name must exactly match the configured host name of the machine \\(run the \`hostname\` command to determine the correct hostname\\).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>COORDINATOR\\_DIRECTORY</span></span>
<span class="line"><span>:   **Required.** This specifies the location where the data directory will be created on the coordinator host. You must make sure that the user who runs \`gpinitsystem\` \\(for example, the \`gpadmin\` user\\) has permissions to write to this directory.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>COORDINATOR\\_PORT</span></span>
<span class="line"><span>:   **Required.** The port number for the coordinator instance. This is the port number that users and client connections will use when accessing the WarehousePG cluster.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>TRUSTED\\_SHELL</span></span>
<span class="line"><span>:   **Required.** The shell the \`gpinitsystem\` utility uses to run commands on remote hosts. Allowed values are \`ssh\`. You must set up your trusted host environment before running the \`gpinitsystem\` utility \\(you can use \`gpssh-exkeys\` to do this\\).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>ENCODING</span></span>
<span class="line"><span>:   **Required.** The character set encoding to use. This character set must be compatible with the \`--locale\` settings used, especially \`--lc-collate\` and \`--lc-ctype\`. WarehousePG supports the same character sets as PostgreSQL.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>DATABASE\\_NAME</span></span>
<span class="line"><span>:   **Optional.** The name of a WarehousePG database to create after the system is initialized. You can always create a database later using the \`CREATE DATABASE\` command or the \`createdb\` utility.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>MIRROR\\_PORT\\_BASE</span></span>
<span class="line"><span>:   **Optional.** This specifies the base number by which mirror segment port numbers are calculated. The first mirror segment port on a host is set as \`MIRROR_PORT_BASE\`, and then incremented by one for each additional mirror segment on that host. Valid values range from 1 through 65535 and cannot conflict with the ports calculated by \`PORT_BASE\`.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>MIRROR\\_DATA\\_DIRECTORY</span></span>
<span class="line"><span>:   **Optional.** This specifies the data storage location\\(s\\) where the utility will create the mirror segment data directories. There must be the same number of data directories declared for mirror segment instances as for primary segment instances \\(see the \`DATA_DIRECTORY\` parameter\\). The user who runs \`gpinitsystem\` \\(for example, the \`gpadmin\` user\\) must have permission to write to these directories. For example:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>:   \`\`\`</span></span>
<span class="line"><span>declare -a MIRROR_DATA_DIRECTORY=(/data1/mirror </span></span>
<span class="line"><span>/data1/mirror /data1/mirror /data2/mirror /data2/mirror </span></span>
<span class="line"><span>/data2/mirror)</span></span></code></pre></div><p>QD_PRIMARY_ARRAY, PRIMARY_ARRAY, MIRROR_ARRAY : <strong>Required</strong> when using <code>input_configuration file</code> with <code>-I</code> option. These parameters specify the WarehousePG coordinator host, the primary segment, and the mirror segment hosts respectively. During new cluster initialization, use the <code>gpinitsystem</code> <code>-O output\\_configuration\\_file</code> to populate <code>QD_PRIMARY_ARRAY</code>, <code>PRIMARY_ARRAY</code>, <code>MIRROR_ARRAY</code>.</p><p>: To initialize a new cluster or re-create a cluster from a backed up configuration, edit these values in the input configuration file used with the <code>gpinitsystem</code> <code>-I input\\_configuration\\_file</code> option. Use one of the following formats to specify the host information:</p><pre><code>\`\`\`
&lt;hostname&gt;~&lt;address&gt;~&lt;port&gt;~&lt;data_directory&gt;/&lt;seg_prefix&lt;segment_id&gt;~&lt;dbid&gt;~&lt;content_id&gt;
\`\`\`

or

\`\`\`
&lt;host&gt;~&lt;port&gt;~&lt;data_directory&gt;/&lt;seg_prefix&lt;segment_id&gt;~&lt;dbid&gt;~&lt;content_id&gt;
\`\`\`
</code></pre><p>: The first format populates the <code>hostname</code> and <code>address</code> fields in the <code>gp_segment_configuration</code> catalog table with the hostname and address values provided in the input configuration file. The second format populates <code>hostname</code> and <code>address</code> fields with the same value, derived from host.</p><p>: The WarehousePG coordinator always uses the value -1 for the segment ID and content ID. For example, seg_prefix&lt;segment_id&gt; and dbid values for <code>QD_PRIMARY_ARRAY</code> use <code>-1</code> to indicate the coordinator instance:</p><pre><code>\`\`\`
QD_PRIMARY_ARRAY=cdw~cdw~5432~/gpdata/coordinator/gpseg-1~1~-1
declare -a PRIMARY_ARRAY=(
sdw1~sdw1~40000~/gpdata/data1/gpseg0~2~0
sdw1~sdw1~40001~/gpdata/data2/gpseg1~3~1
sdw2~sdw2~40000~/gpdata/data1/gpseg2~4~2
sdw2~sdw2~40001~/gpdata/data2/gpseg3~5~3
)
declare -a MIRROR_ARRAY=(
sdw2~sdw2~50000~/gpdata/mirror1/gpseg0~6~0
sdw2~sdw2~50001~/gpdata/mirror2/gpseg1~7~1
sdw1~sdw1~50000~/gpdata/mirror1/gpseg2~8~2
sdw1~sdw1~50001~/gpdata/mirror2/gpseg3~9~3
)
\`\`\`
</code></pre><p>: To re-create a cluster using a known WarehousePG cluster configuration, you can edit the segment and content IDs to match the values of the system.</p><p>HEAP_CHECKSUM : <strong>Optional.</strong> This parameter specifies if checksums are enabled for heap data. When enabled, checksums are calculated for heap storage in all databases, enabling WarehousePG to detect corruption in the I/O system. This option is set when the system is initialized and cannot be changed later.</p><p>: The <code>HEAP_CHECKSUM</code> option is on by default and turning it off is strongly discouraged. If you set this option to off, data corruption in storage can go undetected and make recovery much more difficult.</p><p>: To determine if heap checksums are enabled in a WarehousePG cluster, you can query the <code>data_checksums</code> server configuration parameter with the <code>gpconfig</code> management utility:</p><pre><code>\`\`\`
$ gpconfig -s data_checksums
\`\`\`
</code></pre><p>HBA_HOSTNAMES : <strong>Optional.</strong> This parameter controls whether <code>gpinitsystem</code> uses IP addresses or host names in the <code>pg_hba.conf</code> file when updating the file with addresses that can connect to WarehousePG. The default value is <code>0</code>, the utility uses IP addresses when updating the file. When Initializing WarehousePG, specify <code>HBA_HOSTNAMES=1</code> to have the utility use host names in the <code>pg_hba.conf</code> file.</p><p>: For information about how WarehousePG resolves host names in the <code>pg_hba.conf</code> file, see <a href="./../../admin_guide/client_auth.html#topic1">Configuring Client Authentication</a>.</p><h2 id="specifying-hosts-using-hostnames-or-ip-addresses" tabindex="-1"><a id="spechosts"></a>Specifying Hosts using Hostnames or IP Addresses <a class="header-anchor" href="#specifying-hosts-using-hostnames-or-ip-addresses" aria-label="Permalink to &quot;&lt;a id=&quot;spechosts&quot;&gt;&lt;/a&gt;Specifying Hosts using Hostnames or IP Addresses&quot;">​</a></h2><p>When Initializing WarehousePG with <code>gpinitsystem</code>, you can specify segment hosts using either hostnames or IP addresses. For example, you can use hostnames or IP addresses in the file specified with the <code>-h</code> option.</p><ul><li>If you specify a hostname, the resolution of the hostname to an IP address should be done locally for security. For example, you should use entries in a local <code>/etc/hosts</code> file to map a hostname to an IP address. The resolution of a hostname to an IP address should not be performed by an external service such as a public DNS server. You must stop the WarehousePG cluster before you change the mapping of a hostname to a different IP address.</li><li>If you specify an IP address, the address should not be changed after the initial configuration. When segment mirroring is enabled, replication from the primary to the mirror segment will fail if the IP address changes from the configured value. For this reason, you should use a hostname when Initializing WarehousePG unless you have a specific requirement to use IP addresses.</li></ul><p>When initializing the WarehousePG cluster, <code>gpinitsystem</code> uses the initialization information to populate the <a href="./../../ref_guide/system_catalogs/gp_segment_configuration.html">gp_segment_configuration</a> catalog table and adds hosts to the <code>pg_hba.conf</code> file. By default, the host IP address is added to the file. Specify the <code>gpinitsystem</code> configuration file parameter <a href="#hba_hostnames">HBA_HOSTNAMES</a>=1 to add hostnames to the file.</p><p>WarehousePG uses the <code>address</code> value of the <code>gp_segment_configuration</code> catalog table when looking up host systems for WarehousePG interconnect (internal) communication between the coordinator and segment instances and between segment instances, and for other internal communication.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Initialize a WarehousePG cluster by supplying a cluster configuration file and a segment host address file, and set up a spread mirroring (<code>--mirror-mode=spread</code>) configuration:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpinitsystem -c gpinitsystem_config -h hostfile_gpinitsystem --mirror-mode=spread</span></span></code></pre></div><p>Initialize a WarehousePG cluster and set the superuser remote password:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpinitsystem -c gpinitsystem_config -h hostfile_gpinitsystem --su-password=mypassword</span></span></code></pre></div><p>Initialize a WarehousePG cluster with an optional standby coordinator host:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpinitsystem -c gpinitsystem_config -h hostfile_gpinitsystem -s host09</span></span></code></pre></div><p>Initialize a WarehousePG cluster and write the provided configuration to an output file, for example <code>cluster_init.config</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpinitsystem -c gpinitsystem_config -h hostfile_gpinitsystem -O cluster_init.config</span></span></code></pre></div><p>The output file uses the <code>QD_PRIMARY_ARRAY</code> and <code>PRIMARY_ARRAY</code> parameters to define coordinator and segment hosts:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TRUSTED_SHELL=ssh</span></span>
<span class="line"><span>CHECK_POINT_SEGMENTS=8</span></span>
<span class="line"><span>ENCODING=UNICODE</span></span>
<span class="line"><span>SEG_PREFIX=gpseg</span></span>
<span class="line"><span>HEAP_CHECKSUM=on</span></span>
<span class="line"><span>HBA_HOSTNAMES=0</span></span>
<span class="line"><span>QD_PRIMARY_ARRAY=cdw~cdw.local~5433~/data/coordinator1/gpseg-1~1~-1</span></span>
<span class="line"><span>declare -a PRIMARY_ARRAY=(</span></span>
<span class="line"><span>cdw~cdw.local~6001~/data/primary1/gpseg0~2~0</span></span>
<span class="line"><span>)</span></span>
<span class="line"><span>declare -a MIRROR_ARRAY=(</span></span>
<span class="line"><span>cdw~cdw.local~7001~/data/mirror1/gpseg0~3~0</span></span>
<span class="line"><span>)</span></span></code></pre></div><p>Initialize a WarehousePG using an input configuration file (a file that defines the WarehousePG cluster) using <code>QD_PRIMARY_ARRAY</code> and <code>PRIMARY_ARRAY</code> parameters:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpinitsystem -I cluster_init.config</span></span></code></pre></div><p>The following example uses a host system configured with multiple NICs. If host systems are configured with multiple NICs, you can initialize a WarehousePG cluster to use each NIC as a WarehousePG host system. You must ensure that the host systems are configured with sufficient resources to support all the segment instances being added to the host. Also, if high availability is enabled, you must ensure that the WarehousePG cluster configuration supports failover if a host system fails. For information about WarehousePG mirroring schemes, see <a href="./../../best_practices/ha.html#topic_ngz_qf4_tt">../../best_practices/ha.html#topic_ngz_qf4_tt</a>.</p><p>For this simple coordinator and segment instance configuration, the host system <code>gp7c</code> is configured with two NICs <code>gp7c-1</code> and <code>gp7c-2</code>. In the configuration, the <a href="#array_params">QD_PRIMARY_ARRAY</a> parameter defines the coordinator segment using <code>gp7c-1</code>. The <a href="#array_params">PRIMARY_ARRAY</a> and <a href="#array_params">MIRROR_ARRAY</a> parameters use <code>gp7c-2</code> to define a primary and mirror segment instance.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>QD_PRIMARY_ARRAY=gp7c~gp7c-1~5432~/data/coordinator/gpseg-1~1~-1</span></span>
<span class="line"><span>declare -a PRIMARY_ARRAY=(</span></span>
<span class="line"><span>gp7c~gp7c-2~40000~/data/data1/gpseg0~2~0</span></span>
<span class="line"><span>gp7s~gp7s~40000~/data/data1/gpseg1~3~1</span></span>
<span class="line"><span>)</span></span>
<span class="line"><span>declare -a MIRROR_ARRAY=(</span></span>
<span class="line"><span>gp7s~gp7s~50000~/data/mirror1/gpseg0~4~0</span></span>
<span class="line"><span>gp7c~gp7c-2~50000~/data/mirror1/gpseg1~5~1</span></span>
<span class="line"><span>)</span></span></code></pre></div><h2 id="see-also" tabindex="-1"><a id="section7"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./gpssh-exkeys.html">gpssh-exkeys</a>, <a href="./gpdeletesystem.html">gpdeletesystem</a>, <a href="./../../install_guide/init_whpg.html">Initializing WarehousePG</a>.</p>`,86)]))}const g=s(o,[["render",i]]);export{u as __pageData,g as default};
