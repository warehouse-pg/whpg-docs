import{_ as s,c as a,o as n,ag as o}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"gpssh-exkeys","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/gpssh-exkeys.md","filePath":"docs/7x/utility_guide/ref/gpssh-exkeys.md"}'),t={name:"docs/7x/utility_guide/ref/gpssh-exkeys.md"};function i(p,e,d,c,l,h){return n(),a("div",null,e[0]||(e[0]=[o(`<h1 id="gpssh-exkeys" tabindex="-1">gpssh-exkeys <a class="header-anchor" href="#gpssh-exkeys" aria-label="Permalink to &quot;gpssh-exkeys&quot;">​</a></h1><p>Exchanges SSH public keys between hosts.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpssh-exkeys -f &lt;hostfile_exkeys&gt; | -h &lt;hostname&gt; [-h &lt;hostname&gt; ...]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpssh-exkeys -e &lt;hostfile_exkeys&gt; -x &lt;hostfile_gpexpand&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpssh-exkeys -? </span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpssh-exkeys --version</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>The <code>gpssh-exkeys</code> utility exchanges SSH keys between the specified host names (or host addresses). This allows SSH connections between WarehousePG hosts and network interfaces without a password prompt. The utility is used to initially prepare a WarehousePG cluster for passwordless SSH access, and also to prepare additional hosts for passwordless SSH access when expanding a WarehousePG cluster.</p><p>Keys are exchanged as the currently logged in user. You run the utility on the coordinator host as the <code>gpadmin</code> user (the user designated to own your WarehousePG installation). WarehousePG management utilities require that the <code>gpadmin</code> user be created on all hosts in the WarehousePG cluster, and the utilities must be able to connect as that user to all hosts without a password prompt.</p><p>You can also use <code>gpssh-exkeys</code> to enable passwordless SSH for additional users, <code>root</code>, for example.</p><p>The <code>gpssh-exkeys</code> utility has the following prerequisites:</p><ul><li>The user must have an account on the coordinator, standby, and every segment host in the WarehousePG cluster.</li><li>The user must have an <code>id_rsa</code> SSH key pair installed on the coordinator host.</li><li>The user must be able to connect with SSH from the coordinator host to every other host machine without entering a password. (This is called &quot;1-<em>n</em> passwordless SSH.&quot;)</li></ul><p>You can enable 1-<em>n</em> passwordless SSH using the <code>ssh-copy-id</code> command to add the user&#39;s public key to each host&#39;s <code>authorized_keys</code> file. The <code>gpssh-exkeys</code> utility enables &quot;<em>n</em>-<em>n</em> passwordless SSH,&quot; which allows the user to connect with SSH from any host to any other host in the cluster without a password.</p><p>To specify the hosts involved in an SSH key exchange, use the <code>-f</code> option to specify a file containing a list of host names (recommended), or use the <code>-h</code> option to name single host names on the command-line. At least one host name (<code>-h</code>) or a host file (<code>-f</code>) is required. Note that the local host is included in the key exchange by default.</p><p>To specify new expansion hosts to be added to an existing WarehousePG cluster, use the <code>-e</code> and <code>-x</code> options. The <code>-e</code> option specifies a file containing a list of existing hosts in the system that have already exchanged SSH keys. The <code>-x</code> option specifies a file containing a list of new hosts that need to participate in the SSH key exchange.</p><p>The <code>gpssh-exkeys</code> utility performs key exchange using the following steps:</p><ul><li>Adds the user&#39;s public key to the user&#39;s own <code>authorized_keys</code> file on the current host.</li><li>Updates the <code>known_hosts</code> file of the current user with the host key of each host specified using the <code>-h</code>, <code>-f</code>, <code>-e</code>, and <code>-x</code> options.</li><li>Connects to each host using <code>ssh</code> and obtains the user&#39;s <code>authorized_keys</code>, <code>known_hosts</code>, and <code>id_rsa.pub</code> files.</li><li>Adds keys from the <code>id_rsa.pub</code> files obtained from each host to the <code>authorized_keys</code> file of the current user.</li><li>Updates the <code>authorized_keys</code>, <code>known_hosts</code>, and <code>id_rsa.pub</code> files on all hosts with new host information (if any).</li></ul><h2 id="options" tabindex="-1"><a id="section4"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>-e hostfile_exkeys : When doing a system expansion, this is the name and location of a file containing all configured host names and host addresses (interface names) for each host in your current WarehousePG cluster (coordinator, standby coordinator, and segments), one name per line without blank lines or extra spaces. Hosts specified in this file cannot be specified in the host file used with <code>-x</code>.</p><p>-f hostfile_exkeys : Specifies the name and location of a file containing all configured host names and host addresses (interface names) for each host in your WarehousePG cluster (coordinator, standby coordinator and segments), one name per line without blank lines or extra spaces.</p><p>-h hostname : Specifies a single host name (or host address) that will participate in the SSH key exchange. You can use the <code>-h</code> option multiple times to specify multiple host names and host addresses.</p><p>--version : Displays the version of this utility.</p><p>-x hostfile_gpexpand : When doing a system expansion, this is the name and location of a file containing all configured host names and host addresses (interface names) for each new segment host you are adding to your WarehousePG cluster, one name per line without blank lines or extra spaces. Hosts specified in this file cannot be specified in the host file used with <code>-e</code>.</p><p>-? (help) : Displays the online help.</p><h2 id="examples" tabindex="-1"><a id="section5"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Exchange SSH keys between all host names and addresses listed in the file <code>hostfile_exkeys</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpssh-exkeys -f hostfile_exkeys</span></span></code></pre></div><p>Exchange SSH keys between the hosts <code>sdw1</code>, <code>sdw2</code>, and <code>sdw3</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpssh-exkeys -h sdw1 -h sdw2 -h sdw3</span></span></code></pre></div><p>Exchange SSH keys between existing hosts <code>sdw1</code>, <code>sdw2</code>, and <code>sdw3</code>, and new hosts <code>sdw4</code> and <code>sdw5</code> as part of a system expansion operation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ cat hostfile_exkeys</span></span>
<span class="line"><span>cdw</span></span>
<span class="line"><span>cdw-1</span></span>
<span class="line"><span>cdw-2</span></span>
<span class="line"><span>scdw</span></span>
<span class="line"><span>scdw-1</span></span>
<span class="line"><span>scdw-2</span></span>
<span class="line"><span>sdw1</span></span>
<span class="line"><span>sdw1-1</span></span>
<span class="line"><span>sdw1-2</span></span>
<span class="line"><span>sdw2</span></span>
<span class="line"><span>sdw2-1</span></span>
<span class="line"><span>sdw2-2</span></span>
<span class="line"><span>sdw3</span></span>
<span class="line"><span>sdw3-1</span></span>
<span class="line"><span>sdw3-2</span></span>
<span class="line"><span>$ cat hostfile_gpexpand</span></span>
<span class="line"><span>sdw4</span></span>
<span class="line"><span>sdw4-1</span></span>
<span class="line"><span>sdw4-2</span></span>
<span class="line"><span>sdw5</span></span>
<span class="line"><span>sdw5-1</span></span>
<span class="line"><span>sdw5-2</span></span>
<span class="line"><span>$ gpssh-exkeys -e hostfile_exkeys -x hostfile_gpexpand</span></span></code></pre></div><h2 id="see-also" tabindex="-1"><a id="section6"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./gpssh.html">gpssh</a>, <a href="./gpsync.html">gpsync</a></p>`,31)]))}const g=s(t,[["render",i]]);export{u as __pageData,g as default};
