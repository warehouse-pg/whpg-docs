import{_ as a,c as t,o as n,ag as s}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"ALTER SEQUENCE","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/ALTER_SEQUENCE.md","filePath":"docs/7x/ref_guide/sql_commands/ALTER_SEQUENCE.md"}'),o={name:"docs/7x/ref_guide/sql_commands/ALTER_SEQUENCE.md"};function c(i,e,l,d,r,h){return n(),t("div",null,e[0]||(e[0]=[s(`<h1 id="alter-sequence" tabindex="-1">ALTER SEQUENCE <a class="header-anchor" href="#alter-sequence" aria-label="Permalink to &quot;ALTER SEQUENCE&quot;">​</a></h1><p>Changes the definition of a sequence generator.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ALTER SEQUENCE [ IF EXISTS ] &lt;name&gt;</span></span>
<span class="line"><span>     [ AS &lt;data_type&gt; ]</span></span>
<span class="line"><span>     [INCREMENT [ BY ] &lt;increment&gt;] </span></span>
<span class="line"><span>     [MINVALUE &lt;minvalue&gt; | NO MINVALUE] </span></span>
<span class="line"><span>     [MAXVALUE &lt;maxvalue&gt; | NO MAXVALUE] </span></span>
<span class="line"><span>     [START [ WITH ] &lt;start&gt; ]</span></span>
<span class="line"><span>     [RESTART [ [ WITH ] &lt;restart&gt;] ]</span></span>
<span class="line"><span>     [CACHE &lt;cache&gt;] [[ NO ] CYCLE] </span></span>
<span class="line"><span>     [OWNED BY {&lt;table_name.column_name&gt; | NONE}]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>ALTER SEQUENCE [ IF EXISTS ] &lt;name&gt; OWNER TO { &lt;new_owner&gt; | CURRENT_USER | SESSION_USER }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>ALTER SEQUENCE [ IF EXISTS ] &lt;name&gt; RENAME TO &lt;new_name&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>ALTER SEQUENCE [ IF EXISTS ] &lt;name&gt; SET SCHEMA &lt;new_schema&gt;</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>ALTER SEQUENCE</code> changes the parameters of an existing sequence generator. Any parameters not specifically set in the <code>ALTER SEQUENCE</code> command retain their prior settings.</p><p>You must own the sequence to use <code>ALTER SEQUENCE</code>. To change a sequence&#39;s schema, you must also have <code>CREATE</code> privilege on the new schema. Note that superusers have all these privileges automatically.</p><p>To alter the owner, you must be a direct or indirect member of the new owning role, and that role must have <code>CREATE</code> privilege on the sequence&#39;s schema. (These restrictions enforce that altering the owner does not do anything you could not do by dropping and recreating the sequence. However, a superuser can alter ownership of any sequence anyway.)</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>name : The name (optionally schema-qualified) of a sequence to be altered.</p><p>IF EXISTS : Do not throw an error if the sequence does not exist. WarehousePG issues a notice in this case.</p><p>data_type : The optional clause <code>AS data_type</code> changes the data type of the sequence. Valid types are <code>smallint</code>, <code>integer</code>, and <code>bigint</code>.</p><p>: Changing the data type automatically changes the minimum and maximum values of the sequence if and only if the previous minimum and maximum values were the minimum or maximum value of the old data type (in other words, if the sequence had been created using <code>NO MINVALUE</code> or <code>NO MAXVALUE</code>, implicitly or explicitly). Otherwise, the minimum and maximum values are preserved, unless new values are given as part of the same command. If the minimum and maximum values do not fit into the new data type, WarehousePG generates an error.</p><p>increment : The clause <code>INCREMENT BY increment</code> is optional. A positive value will make an ascending sequence, a negative one a descending sequence. If unspecified, the old increment value will be maintained.</p><p>minvalue NO MINVALUE : The optional clause <code>MINVALUE minvalue</code> determines the minimum value a sequence can generate. If <code>NO MINVALUE</code> is specified, the defaults of <code>1</code> and the minimum value of the data type for ascending and descending sequences, respectively, will be used. If neither option is specified, the current minimum value will be maintained.</p><p>maxvalue NO MAXVALUE : The optional clause <code>MAXVALUE maxvalue</code> determines the maximum value for the sequence. If <code>NO MAXVALUE</code> is specified, the defaults of the maximum value of the data type and <code>-1</code> for ascending and descending sequences, respectively, will be used. If neither option is specified, the current maximum value will be maintained.</p><p>start : The optional clause <code>START WITH start</code> changes the recorded start value of the sequence. This has no effect on the <em>current</em> sequence value; it simply sets the value that future <code>ALTER SEQUENCE RESTART</code> commands will use.</p><p>restart : The optional clause <code>RESTART [ WITH restart ]</code> changes the current value of the sequence. This is equivalent to calling the <code>setval(sequence, start_val, is_called)</code> function with <code>is_called = false</code>. The specified value will be returned by the <em>next</em> call of the <code>nextval(sequence)</code> function. Writing <code>RESTART</code> with no restart value is equivalent to supplying the start value that was recorded by <code>CREATE SEQUENCE</code> or last set by <code>ALTER SEQUENCE START WITH</code>. : In contrast to a <code>setval()</code> call, a <code>RESTART</code> operation on a sequence is transactional and blocks concurrent transactions from obtaining numbers from the same sequence. If that&#39;s not the desired mode of operation, <code>setval()</code> should be used</p><p>cache : The clause <code>CACHE cache</code> enables sequence numbers to be preallocated and stored in memory for faster access. The minimum value is 1 (only one value can be generated at a time, i.e., no cache). If unspecified, the old cache value will be maintained. : &gt; <strong>Note</strong> When operating with a cache of sequence numbers (<code>cache &gt; 1</code>), WarehousePG may discard some cached sequence values. If you require consecutive values, you must explicitly set <code>CACHE 1</code> when you create or alter the sequence.</p><p>CYCLE : The optional <code>CYCLE</code> key word may be used to enable the sequence to wrap around when the <code>maxvalue</code> or <code>minvalue</code> has been reached by an ascending or descending sequence. If the limit is reached, the next number generated will be the respective <code>minvalue</code> or <code>maxvalue</code>.</p><p>NO CYCLE : If the optional <code>NO CYCLE</code> key word is specified, any calls to <code>nextval()</code> after the sequence has reached its maximum value will return an error. If neither <code>CYCLE</code> or <code>NO CYCLE</code> are specified, the old cycle behavior will be maintained.</p><p>OWNED BY table_name.column_name OWNED BY NONE : The <code>OWNED BY</code> option causes the sequence to be associated with a specific table column, such that if that column (or its whole table) is dropped, the sequence will be automatically dropped as well. If specified, this association replaces any previously specified association for the sequence. The specified table must have the same owner and be in the same schema as the sequence. Specifying <code>OWNED BY NONE</code> removes any existing table column association.</p><p>new_owner : The user name of the new owner of the sequence.</p><p>new_name : The new name for the sequence.</p><p>new_schema : The new schema for the sequence.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p><code>ALTER SEQUENCE</code> will not immediately affect <code>nextval()</code> results in backends, other than the current one, that have preallocated (cached) sequence values. They will use up all cached values prior to noticing the changed sequence generation parameters. The current backend will be affected immediately.</p><p><code>ALTER SEQUENCE</code> blocks concurrent <code>nextval()</code> and <code>setval()</code> calls.</p><p>For historical reasons, <code>ALTER TABLE</code> can be used with sequences too; but the only variants of <code>ALTER TABLE</code> that are allowed with sequences are equivalent to the forms shown above.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Restart a sequence called <code>serial</code> at <code>105</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ALTER SEQUENCE serial RESTART WITH 105;</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p><code>ALTER SEQUENCE</code> conforms to the SQL standard, except for the <code>AS</code>, <code>START WITH</code>, <code>OWNED BY</code>, <code>OWNER TO</code>, <code>RENAME TO</code>, and <code>SET SCHEMA</code> clauses, which are WarehousePG extensions.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./CREATE_SEQUENCE.html">CREATE SEQUENCE</a>, <a href="./DROP_SEQUENCE.html">DROP SEQUENCE</a>, <a href="./ALTER_TABLE.html">ALTER TABLE</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,37)]))}const m=a(o,[["render",c]]);export{u as __pageData,m as default};
