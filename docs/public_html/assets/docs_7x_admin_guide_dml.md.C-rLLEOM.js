import{_ as a,c as t,o,ag as n}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"DML: Inserting, Updating and Deleting","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/dml.md","filePath":"docs/7x/admin_guide/dml.md"}'),s={name:"docs/7x/admin_guide/dml.md"};function i(d,e,c,l,r,p){return o(),t("div",null,e[0]||(e[0]=[n(`<h1 id="dml-inserting-updating-and-deleting" tabindex="-1">DML: Inserting, Updating and Deleting <a class="header-anchor" href="#dml-inserting-updating-and-deleting" aria-label="Permalink to &quot;DML: Inserting, Updating and Deleting&quot;">​</a></h1><hr><p>This section provides information about manipulating data and concurrent access in WarehousePG.</p><p>This topic includes the following subtopics:</p><ul><li><a href="#topic2">About Concurrency Control in WarehousePG</a></li><li><a href="#topic3">Inserting Rows</a></li><li><a href="#topic4">Updating Existing Rows</a></li><li><a href="#topic5">Deleting Rows</a></li><li><a href="#topic7">Working With Transactions</a></li><li><a href="#topic_gdd">Global Deadlock Detector</a></li><li><a href="#topic9">Vacuuming the Database</a></li><li><a href="#topic11">Running Out of Locks</a></li></ul><p><strong>Parent topic:</strong> <a href="./../admin_guide.html">WarehousePG Administrator Guide</a></p><h2 id="about-concurrency-control-in-warehousepg" tabindex="-1"><a id="topic2"></a>About Concurrency Control in WarehousePG <a class="header-anchor" href="#about-concurrency-control-in-warehousepg" aria-label="Permalink to &quot;&lt;a id=&quot;topic2&quot;&gt;&lt;/a&gt;About Concurrency Control in WarehousePG&quot;">​</a></h2><p>WarehousePG and PostgreSQL do not use locks for concurrency control. They maintain data consistency using a multiversion model, Multiversion Concurrency Control (MVCC). MVCC achieves transaction isolation for each database session, and each query transaction sees a snapshot of data. This ensures the transaction sees consistent data that is not affected by other concurrent transactions.</p><p>Because MVCC does not use explicit locks for concurrency control, lock contention is minimized and WarehousePG maintains reasonable performance in multiuser environments. Locks acquired for querying (reading) data do not conflict with locks acquired for writing data.</p><p>WarehousePG provides multiple lock modes to control concurrent access to data in tables. Most WarehousePG SQL commands automatically acquire the appropriate locks to ensure that referenced tables are not dropped or modified in incompatible ways while a command runs. For applications that cannot adapt easily to MVCC behavior, you can use the <code>LOCK</code> command to acquire explicit locks. However, proper use of MVCC generally provides better performance.</p><table tabindex="0"><thead><tr><th>Lock Mode</th><th>Associated SQL Commands</th><th>Conflicts With</th></tr></thead><tbody><tr><td>ACCESS SHARE</td><td><code>SELECT</code></td><td>ACCESS EXCLUSIVE</td></tr><tr><td>ROW SHARE</td><td><code>SELECT...FOR lock_strength</code></td><td>EXCLUSIVE, ACCESS EXCLUSIVE</td></tr><tr><td>ROW EXCLUSIVE</td><td><code>INSERT</code>, <code>COPY</code></td><td>SHARE, SHARE ROW EXCLUSIVE, EXCLUSIVE, ACCESS EXCLUSIVE</td></tr><tr><td>SHARE UPDATE EXCLUSIVE</td><td><code>VACUUM</code> (without <code>FULL</code>), <code>ANALYZE</code></td><td>SHARE UPDATE EXCLUSIVE, SHARE, SHARE ROW EXCLUSIVE, EXCLUSIVE, ACCESS EXCLUSIVE</td></tr><tr><td>SHARE</td><td><code>CREATE INDEX</code></td><td>ROW EXCLUSIVE, SHARE UPDATE EXCLUSIVE, SHARE ROW EXCLUSIVE, EXCLUSIVE, ACCESS EXCLUSIVE</td></tr><tr><td>SHARE ROW EXCLUSIVE</td><td></td><td>ROW EXCLUSIVE, SHARE UPDATE EXCLUSIVE, SHARE, SHARE ROW EXCLUSIVE, EXCLUSIVE, ACCESS EXCLUSIVE</td></tr><tr><td>EXCLUSIVE</td><td><code>DELETE</code>, <code>UPDATE</code>, <code>SELECT...FOR lock_strength</code>, <code>REFRESH MATERIALIZED VIEW CONCURRENTLY</code></td><td>ROW SHARE, ROW EXCLUSIVE, SHARE UPDATE EXCLUSIVE, SHARE, SHARE ROW EXCLUSIVE, EXCLUSIVE, ACCESS EXCLUSIVE</td></tr><tr><td>ACCESS EXCLUSIVE</td><td><code>ALTER TABLE</code>, <code>DROP TABLE</code>, <code>TRUNCATE</code>, <code>REINDEX</code>, <code>CLUSTER</code>, <code>REFRESH MATERIALIZED VIEW</code> (without <code>CONCURRENTLY</code>), <code>VACUUM FULL</code></td><td>ACCESS SHARE, ROW SHARE, ROW EXCLUSIVE, SHARE UPDATE EXCLUSIVE, SHARE, SHARE ROW EXCLUSIVE, EXCLUSIVE, ACCESS EXCLUSIVE</td></tr></tbody></table><blockquote><p><strong>Note</strong> By default, the Global Deadlock Detector is deactivated, and WarehousePG acquires the more restrictive <code>EXCLUSIVE</code> lock (rather than <code>ROW EXCLUSIVE</code> in PostgreSQL) for <code>UPDATE</code>, <code>DELETE</code>, and <code>SELECT</code> queries with a locking clause (<code>FOR lock_strength</code>).</p></blockquote><p>When the Global Deadlock Detector is enabled:</p><ul><li>The lock mode for some <code>DELETE</code> and <code>UPDATE</code> operations on heap tables is <code>ROW EXCLUSIVE</code>. See <a href="#topic_gdd">Global Deadlock Detector</a>.</li><li>The lock mode for some queries with a locking clause (<code>SELECT...FOR lock_strength</code>) is <code>ROW SHARE</code>. See &quot;The Locking Clause&quot; in <a href="./../ref_guide/sql_commands/SELECT.html">SELECT</a>.</li></ul><h2 id="inserting-rows" tabindex="-1"><a id="topic3"></a>Inserting Rows <a class="header-anchor" href="#inserting-rows" aria-label="Permalink to &quot;&lt;a id=&quot;topic3&quot;&gt;&lt;/a&gt;Inserting Rows&quot;">​</a></h2><p>Use the <code>INSERT</code> command to create rows in a table. This command requires the table name and a value for each column in the table; you may optionally specify the column names in any order. If you do not specify column names, list the data values in the order of the columns in the table, separated by commas.</p><p>For example, to specify the column names and the values to insert:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INSERT INTO products (name, price, product_no) VALUES (&#39;Cheese&#39;, 9.99, 1);</span></span></code></pre></div><p>To specify only the values to insert:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INSERT INTO products VALUES (1, &#39;Cheese&#39;, 9.99);</span></span></code></pre></div><p>Usually, the data values are literals (constants), but you can also use scalar expressions. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INSERT INTO films SELECT * FROM tmp_films WHERE date_prod &lt; </span></span>
<span class="line"><span>&#39;2016-05-07&#39;;</span></span></code></pre></div><p>You can insert multiple rows in a single command. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INSERT INTO products (product_no, name, price) VALUES</span></span>
<span class="line"><span>    (1, &#39;Cheese&#39;, 9.99),</span></span>
<span class="line"><span>    (2, &#39;Bread&#39;, 1.99),</span></span>
<span class="line"><span>    (3, &#39;Milk&#39;, 2.99);</span></span></code></pre></div><p>To insert data into a partitioned table, you specify the root partitioned table, which is the table created with the <code>CREATE TABLE</code> command. You also can specify a leaf partition in an <code>INSERT</code> command. An error is returned if the data is not valid for the specified leaf partition. Specifying a table that is not a leaf partition in the <code>INSERT</code> command is not supported.</p><p>To insert large amounts of data, use external tables or the <code>COPY</code> command. These load mechanisms are more efficient than <code>INSERT</code> for inserting large quantities of rows. See <a href="./load/topics/g-loading-and-unloading-data.html">Loading and Unloading Data</a> for more information about bulk data loading.</p><p>The storage model of append-optimized tables is optimized for bulk data loading. WarehousePG does not recommend single row <code>INSERT</code> statements for append-optimized tables. For append-optimized tables, WarehousePG supports a maximum of 127 concurrent <code>INSERT</code> transactions into a single append-optimized table.</p><h2 id="updating-existing-rows" tabindex="-1"><a id="topic4"></a>Updating Existing Rows <a class="header-anchor" href="#updating-existing-rows" aria-label="Permalink to &quot;&lt;a id=&quot;topic4&quot;&gt;&lt;/a&gt;Updating Existing Rows&quot;">​</a></h2><p>The <code>UPDATE</code> command updates rows in a table. You can update all rows, a subset of all rows, or individual rows in a table. You can update each column separately without affecting other columns.</p><p>To perform an update, you need:</p><ul><li>The name of the table and columns to update</li><li>The new values of the columns</li><li>One or more conditions specifying the row or rows to be updated.</li></ul><p>For example, the following command updates all products that have a price of <em>5</em> to have a price of <em>10</em>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>UPDATE products SET price = 10 WHERE price = 5;</span></span></code></pre></div><p>Using <code>UPDATE</code> in WarehousePG has the following restrictions:</p><ul><li>While GPORCA supports updates to WarehousePG distribution key columns, the Postgres-based planner does not.</li><li>If mirrors are enabled, you cannot use <code>STABLE</code> or <code>VOLATILE</code> functions in an <code>UPDATE</code> statement.</li><li>WarehousePG partitioning columns cannot be updated.</li></ul><h2 id="deleting-rows" tabindex="-1"><a id="topic5"></a>Deleting Rows <a class="header-anchor" href="#deleting-rows" aria-label="Permalink to &quot;&lt;a id=&quot;topic5&quot;&gt;&lt;/a&gt;Deleting Rows&quot;">​</a></h2><p>The <code>DELETE</code> command deletes rows from a table. Specify a <code>WHERE</code> clause to delete rows that match certain criteria. If you do not specify a <code>WHERE</code> clause, all rows in the table are deleted. The result is a valid, but empty, table. For example, to remove all rows from the products table that have a price of <em>10</em>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DELETE FROM products WHERE price = 10;</span></span></code></pre></div><p>To delete all rows from a table:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DELETE FROM products;</span></span></code></pre></div><p>Using <code>DELETE</code> in WarehousePG has similar restrictions to using <code>UPDATE</code>:</p><ul><li>If mirrors are enabled, you cannot use <code>STABLE</code> or <code>VOLATILE</code> functions in an <code>UPDATE</code> statement.</li></ul><h3 id="truncating-a-table" tabindex="-1"><a id="topic6"></a>Truncating a Table <a class="header-anchor" href="#truncating-a-table" aria-label="Permalink to &quot;&lt;a id=&quot;topic6&quot;&gt;&lt;/a&gt;Truncating a Table&quot;">​</a></h3><p>Use the <code>TRUNCATE</code> command to quickly remove all rows in a table. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TRUNCATE mytable;</span></span></code></pre></div><p>This command empties a table of all rows in one operation. Note that <code>TRUNCATE</code> does not scan the table, therefore it does not process inherited child tables or <code>ON DELETE</code> rewrite rules. The command truncates only rows in the named table.</p><h2 id="working-with-transactions" tabindex="-1"><a id="topic7"></a>Working With Transactions <a class="header-anchor" href="#working-with-transactions" aria-label="Permalink to &quot;&lt;a id=&quot;topic7&quot;&gt;&lt;/a&gt;Working With Transactions&quot;">​</a></h2><p>Transactions allow you to bundle multiple SQL statements in one all-or-nothing operation.</p><p>The following are the WarehousePG SQL transaction commands:</p><ul><li><code>BEGIN</code> or <code>START TRANSACTION</code> starts a transaction block.</li><li><code>END</code> or <code>COMMIT</code> commits the results of a transaction.</li><li><code>ROLLBACK</code> abandons a transaction without making any changes.</li><li><code>SAVEPOINT</code> marks a place in a transaction and enables partial rollback. You can roll back commands run after a savepoint while maintaining commands run before the savepoint.</li><li><code>ROLLBACK TO SAVEPOINT</code> rolls back a transaction to a savepoint.</li><li><code>RELEASE SAVEPOINT</code> destroys a savepoint within a transaction.</li></ul><h3 id="transaction-isolation-levels" tabindex="-1"><a id="topic8"></a>Transaction Isolation Levels <a class="header-anchor" href="#transaction-isolation-levels" aria-label="Permalink to &quot;&lt;a id=&quot;topic8&quot;&gt;&lt;/a&gt;Transaction Isolation Levels&quot;">​</a></h3><p>WarehousePG accepts the standard SQL transaction levels as follows:</p><ul><li><code>READ UNCOMMITTED</code> and <code>READ COMMITTED</code> behave like the standard <code>READ COMMITTED</code>.</li><li><code>REPEATABLE READ</code> and <code>SERIALIZABLE</code> behave like <code>REPEATABLE READ</code>.</li></ul><p>The following information describes the behavior of the WarehousePG transaction levels.</p><h4 id="read-uncommitted-and-read-committed" tabindex="-1"><a id="readunc"></a>Read Uncommitted and Read Committed <a class="header-anchor" href="#read-uncommitted-and-read-committed" aria-label="Permalink to &quot;&lt;a id=&quot;readunc&quot;&gt;&lt;/a&gt;Read Uncommitted and Read Committed&quot;">​</a></h4><p>WarehousePG does not allow any command to see an uncommitted update in another concurrent transaction, so <code>READ UNCOMMITTED</code> behaves the same as <code>READ COMMITTED</code>. <code>READ COMMITTED</code> provides fast, simple, partial transaction isolation. <code>SELECT</code>, <code>UPDATE</code>, and <code>DELETE</code> commands operate on a snapshot of the database taken when the query started.</p><p>A <code>SELECT</code> query:</p><ul><li>Sees data committed before the query starts.</li><li>Sees updates run within the transaction.</li><li>Does not see uncommitted data outside the transaction.</li><li>Can possibly see changes that concurrent transactions made if the concurrent transaction is committed after the initial read in its own transaction.</li></ul><p>Successive <code>SELECT</code> queries in the same transaction can see different data if other concurrent transactions commit changes between the successive queries. <code>UPDATE</code> and <code>DELETE</code> commands find only rows committed before the commands started.</p><p><code>READ COMMITTED</code> transaction isolation allows concurrent transactions to modify or lock a row before <code>UPDATE</code> or <code>DELETE</code> find the row. <code>READ COMMITTED</code> transaction isolation may be inadequate for applications that perform complex queries and updates and require a consistent view of the database.</p><h4 id="repeatable-read-and-serializable" tabindex="-1"><a id="repread"></a>Repeatable Read and Serializable <a class="header-anchor" href="#repeatable-read-and-serializable" aria-label="Permalink to &quot;&lt;a id=&quot;repread&quot;&gt;&lt;/a&gt;Repeatable Read and Serializable&quot;">​</a></h4><p><code>SERIALIZABLE</code> transaction isolation, as defined by the SQL standard, ensures that transactions that run concurrently produce the same results as if they were run one after another. If you specify <code>SERIALIZABLE</code> WarehousePG falls back to <code>REPEATABLE READ</code>. <code>REPEATABLE READ</code> transactions prevent dirty reads, non-repeatable reads, and phantom reads without expensive locking, but WarehousePG does not detect all serializability interactions that can occur during concurrent transaction execution. Concurrent transactions should be examined to identify interactions that are not prevented by disallowing concurrent updates of the same data. You can prevent these interactions by using explicit table locks or by requiring the conflicting transactions to update a dummy row introduced to represent the conflict.</p><p>With <code>REPEATABLE READ</code> transactions, a <code>SELECT</code> query:</p><ul><li>Sees a snapshot of the data as of the start of the transaction (not as of the start of the current query within the transaction).</li><li>Sees only data committed before the query starts.</li><li>Sees updates run within the transaction.</li><li>Does not see uncommitted data outside the transaction.</li><li>Does not see changes that concurrent transactions make.</li><li>Successive <code>SELECT</code> commands within a single transaction always see the same data.</li><li><code>UPDATE</code>, <code>DELETE</code>, <code>SELECT FOR UPDATE</code>, and <code>SELECT FOR SHARE</code> commands find only rows committed before the command started. If a concurrent transaction has updated, deleted, or locked a target row, the <code>REPEATABLE READ</code> transaction waits for the concurrent transaction to commit or roll back the change. If the concurrent transaction commits the change, the <code>REPEATABLE READ</code> transaction rolls back. If the concurrent transaction rolls back its change, the <code>REPEATABLE READ</code> transaction can commit its changes.</li></ul><p>The default transaction isolation level in WarehousePG is <code>READ COMMITTED</code>. To change the isolation level for a transaction, declare the isolation level when you <code>BEGIN</code> the transaction or use the <code>SET TRANSACTION</code> command after the transaction starts.</p><h2 id="global-deadlock-detector" tabindex="-1"><a id="topic_gdd"></a>Global Deadlock Detector <a class="header-anchor" href="#global-deadlock-detector" aria-label="Permalink to &quot;&lt;a id=&quot;topic_gdd&quot;&gt;&lt;/a&gt;Global Deadlock Detector&quot;">​</a></h2><p>The WarehousePG Global Deadlock Detector background worker process collects lock information on all segments and uses a directed algorithm to detect the existence of local and global deadlocks. This algorithm allows WarehousePG to relax concurrent update and delete restrictions on heap tables. (WarehousePG still employs table-level locking on AO/CO tables, restricting concurrent <code>UPDATE</code>, <code>DELETE</code>, and <code>SELECT...FOR lock_strength</code> operations.)</p><p>By default, the Global Deadlock Detector is deactivated and WarehousePG runs the concurrent <code>UPDATE</code> and <code>DELETE</code> operations on a heap table serially. You can activate these concurrent updates and have the Global Deadlock Detector determine when a deadlock exists by setting the server configuration parameter <a href="./../ref_guide/config_params/guc-list.html"><code>gp_enable_global_deadlock_detector</code></a>.</p><p>When the Global Deadlock Detector is enabled, the background worker process is automatically started on the coordinator host when you start WarehousePG. You configure the interval at which the Global Deadlock Detector collects and analyzes lock waiting data via the <a href="./../ref_guide/config_params/guc-list.html">gp_global_deadlock_detector_period</a> server configuration parameter.</p><p>If the Global Deadlock Detector determines that deadlock exists, it breaks the deadlock by cancelling one or more backend processes associated with the youngest transaction(s) involved.</p><p>When the Global Deadlock Detector determines a deadlock exists for the following types of transactions, only one of the transactions will succeed. The other transactions will fail with an error indicating that concurrent updates to the same row is not allowed.</p><ul><li>Concurrent transactions on the same row of a heap table where the first transaction is an update operation and a later transaction runs an update or delete and the query plan contains a motion operator.</li><li>Concurrent update transactions on the same distribution key of a heap table that are run by the Postgres-based planner.</li><li>Concurrent update transactions on the same row of a hash table that are run by the GPORCA optimizer.</li></ul><blockquote><p><strong>Note</strong> WarehousePG uses the interval specified in the <a href="./../ref_guide/config_params/guc-list.html">deadlock_timeout</a> server configuration parameter for local deadlock detection. Because the local and global deadlock detection algorithms differ, the cancelled process(es) may differ depending upon which detector (local or global) WarehousePG triggers first.</p></blockquote><blockquote><p><strong>Note</strong> If the <a href="./../ref_guide/config_params/guc-list.html">lock_timeout</a> server configuration parameter is turned on and set to a value smaller than <code>deadlock_timeout</code> and <code>gp_global_deadlock_detector_period</code>, WarehousePG will cancel a statement before it would ever trigger a deadlock check in that session.</p></blockquote><p>To view lock waiting information for all segments, run the <code>gp_dist_wait_status()</code> user-defined function. You can use the output of this function to determine which transactions are waiting on locks, which transactions are holding locks, the lock types and mode, the waiter and holder session identifiers, and which segments are running the transactions. Sample output of the <code>gp_dist_wait_status()</code> function follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * FROM pg_catalog.gp_dist_wait_status();</span></span>
<span class="line"><span>-[ RECORD 1 ]----+--------------</span></span>
<span class="line"><span>segid            | 0</span></span>
<span class="line"><span>waiter_dxid      | 11</span></span>
<span class="line"><span>holder_dxid      | 12</span></span>
<span class="line"><span>holdTillEndXact  | t</span></span>
<span class="line"><span>waiter_lpid      | 31249</span></span>
<span class="line"><span>holder_lpid      | 31458</span></span>
<span class="line"><span>waiter_lockmode  | ShareLock</span></span>
<span class="line"><span>waiter_locktype  | transactionid</span></span>
<span class="line"><span>waiter_sessionid | 8</span></span>
<span class="line"><span>holder_sessionid | 9</span></span>
<span class="line"><span>-[ RECORD 2 ]----+--------------</span></span>
<span class="line"><span>segid            | 1</span></span>
<span class="line"><span>waiter_dxid      | 12</span></span>
<span class="line"><span>holder_dxid      | 11</span></span>
<span class="line"><span>holdTillEndXact  | t</span></span>
<span class="line"><span>waiter_lpid      | 31467</span></span>
<span class="line"><span>holder_lpid      | 31250</span></span>
<span class="line"><span>waiter_lockmode  | ShareLock</span></span>
<span class="line"><span>waiter_locktype  | transactionid</span></span>
<span class="line"><span>waiter_sessionid | 9</span></span>
<span class="line"><span>holder_sessionid | 8</span></span></code></pre></div><p>When it cancels a transaction to break a deadlock, the Global Deadlock Detector reports the following error message:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ERROR:  canceling statement due to user request: &quot;cancelled by global deadlock detector&quot;</span></span></code></pre></div><h3 id="global-deadlock-detector-update-and-delete-compatibility" tabindex="-1"><a id="gdd_example"></a>Global Deadlock Detector UPDATE and DELETE Compatibility <a class="header-anchor" href="#global-deadlock-detector-update-and-delete-compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;gdd_example&quot;&gt;&lt;/a&gt;Global Deadlock Detector UPDATE and DELETE Compatibility&quot;">​</a></h3><p>The Global Deadlock Detector can manage concurrent updates for these types of <code>UPDATE</code> and <code>DELETE</code> commands on heap tables:</p><ul><li><p>Simple <code>UPDATE</code> of a single table. Update a non-distribution key with the Postgres-based planner. The command does not contain a <code>FROM</code> clause, or a sub-query in the <code>WHERE</code> clause.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>UPDATE t SET c2 = c2 + 1 WHERE c1 &gt; 10;</span></span></code></pre></div></li><li><p>Simple <code>DELETE</code> of a single table. The command does not contain a sub-query in the <code>FROM</code> or <code>WHERE</code> clauses.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DELETE FROM t WHERE c1 &gt; 10;</span></span></code></pre></div></li><li><p>Split <code>UPDATE</code>. For the Postgres-based planner, the <code>UPDATE</code> command updates a distribution key.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>UPDATE t SET c = c + 1; -- c is a distribution key</span></span></code></pre></div><p>For GPORCA, the <code>UPDATE</code> command updates a distribution key or references a distribution key.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>UPDATE t SET b = b + 1 WHERE c = 10; -- c is a distribution key</span></span></code></pre></div></li><li><p>Complex <code>UPDATE</code>. The <code>UPDATE</code> command includes multiple table joins.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>UPDATE t1 SET c = t1.c+1 FROM t2 WHERE t1.c = t2.c;</span></span></code></pre></div><p>Or the command contains a sub-query in the <code>WHERE</code> clause.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>UPDATE t SET c = c + 1 WHERE c &gt; ALL(SELECT * FROM t1);</span></span></code></pre></div></li><li><p>Complex <code>DELETE</code>. A complex <code>DELETE</code> command is similar to a complex <code>UPDATE</code>, and involves multiple table joins or a sub-query.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DELETE FROM t USING t1 WHERE t.c &gt; t1.c;</span></span></code></pre></div></li></ul><p>The following table shows the concurrent <code>UPDATE</code> or <code>DELETE</code> commands that are managed by the Global Deadlock Detector. For example, concurrent simple <code>UPDATE</code> commands on the same table row are managed by the Global Deadlock Detector. For a concurrent complex <code>UPDATE</code> and a simple <code>UPDATE</code>, only one <code>UPDATE</code> is performed, and an error is returned for the other <code>UPDATE</code>.</p><table tabindex="0"><thead><tr><th>Command</th><th>Simple <code>UPDATE</code></th><th>Simple <code>DELETE</code></th><th>Split <code>UPDATE</code></th><th>Complex <code>UPDATE</code></th><th>Complex <code>DELETE</code></th></tr></thead><tbody><tr><td>Simple <code>UPDATE</code></td><td>YES</td><td>YES</td><td>NO</td><td>NO</td><td>NO</td></tr><tr><td>Simple <code>DELETE</code></td><td>YES</td><td>YES</td><td>NO</td><td>YES</td><td>YES</td></tr><tr><td>Split <code>UPDATE</code></td><td>NO</td><td>NO</td><td>NO</td><td>NO</td><td>NO</td></tr><tr><td>Complex <code>UPDATE</code></td><td>NO</td><td>YES</td><td>NO</td><td>NO</td><td>NO</td></tr><tr><td>Complex <code>DELETE</code></td><td>NO</td><td>YES</td><td>NO</td><td>NO</td><td>YES</td></tr></tbody></table><h2 id="vacuuming-the-database" tabindex="-1"><a id="topic9"></a>Vacuuming the Database <a class="header-anchor" href="#vacuuming-the-database" aria-label="Permalink to &quot;&lt;a id=&quot;topic9&quot;&gt;&lt;/a&gt;Vacuuming the Database&quot;">​</a></h2><p>Deleted or updated data rows occupy physical space on disk even though new transactions cannot see them. Periodically running the <code>VACUUM</code> command removes these expired rows. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>VACUUM mytable;</span></span></code></pre></div><p>The <code>VACUUM</code> command collects table-level statistics such as the number of rows and pages. Vacuum all tables after loading data, including append-optimized tables. For information about recommended routine vacuum operations, see <a href="./managing/maintain.html">Routine Vacuum and Analyze</a>.</p><blockquote><p><strong>Important</strong> The <code>VACUUM</code>, <code>VACUUM FULL</code>, and <code>VACUUM ANALYZE</code> commands should be used to maintain the data in a WarehousePG especially if updates and deletes are frequently performed on your database data. See the <code>VACUUM</code> command in the <em>WarehousePG Reference Guide</em> for information about using the command.</p></blockquote><h2 id="running-out-of-locks" tabindex="-1"><a id="topic11"></a>Running Out of Locks <a class="header-anchor" href="#running-out-of-locks" aria-label="Permalink to &quot;&lt;a id=&quot;topic11&quot;&gt;&lt;/a&gt;Running Out of Locks&quot;">​</a></h2><p>WarehousePG can potentially run out of locks when a database operation accesses multiple tables in a single transaction. Backup and restore are examples of such operations.</p><p>When WarehousePG runs out of locks, the error message that you may observe references a shared memory error:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>... &quot;WARNING&quot;,&quot;53200&quot;,&quot;out of shared memory&quot;,,,,,,&quot;LOCK TABLE ...</span></span>
<span class="line"><span>... &quot;ERROR&quot;,&quot;53200&quot;,&quot;out of shared memory&quot;,,&quot;You might need to increase max_locks_per_transaction.&quot;,,,,&quot;LOCK TABLE ...</span></span></code></pre></div><blockquote><p><strong>Note</strong> &quot;shared memory&quot; in this context refers to the shared memory of the internal object: the lock slots. &quot;Out of shared memory&quot; does <em>not</em> refer to exhaustion of system- or WarehousePG-level memory resources.</p></blockquote><p>As the hint describes, consider increasing the <a href="./../ref_guide/config_params/guc-list.html"><code>max_locks_per_transaction</code></a> server configuration parameter when you encounter this error.</p>`,94)]))}const m=a(s,[["render",i]]);export{u as __pageData,m as default};
