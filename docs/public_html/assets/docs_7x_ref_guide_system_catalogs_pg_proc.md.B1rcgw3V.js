import{_ as e,c as d,o,ag as r}from"./chunks/framework.Ds6Eueu6.js";const f=JSON.parse('{"title":"pg_proc","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/system_catalogs/pg_proc.md","filePath":"docs/7x/ref_guide/system_catalogs/pg_proc.md"}'),n={name:"docs/7x/ref_guide/system_catalogs/pg_proc.md"};function a(c,t,i,s,l,p){return o(),d("div",null,t[0]||(t[0]=[r('<h1 id="pg-proc" tabindex="-1">pg_proc <a class="header-anchor" href="#pg-proc" aria-label="Permalink to &quot;pg_proc&quot;">​</a></h1><p>The <code>pg_proc</code> system catalog table stores information about functions (or procedures), both built-in functions and those defined by <code>CREATE FUNCTION</code>. The table contains data for aggregate and window functions as well as plain functions. If <code>proisagg</code> is true, there should be a matching row in <code>pg_aggregate</code>.</p><p>For compiled functions, both built-in and dynamically loaded, <code>prosrc</code> contains the function&#39;s C-language name (link symbol). For all other currently-known language types, <code>prosrc</code> contains the function&#39;s source text. <code>probin</code> is unused except for dynamically-loaded C functions, for which it gives the name of the shared library file containing the function.</p><table tabindex="0"><thead><tr><th>column</th><th>type</th><th>references</th><th>description</th></tr></thead><tbody><tr><td><code>oid</code></td><td>oid</td><td></td><td>Row identifier (hidden attribute; must be explicitly selected).</td></tr><tr><td><code>proname</code></td><td>name</td><td></td><td>Name of the function.</td></tr><tr><td><code>pronamespace</code></td><td>oid</td><td>pg_namespace.oid</td><td>The OID of the namespace that contains this function.</td></tr><tr><td><code>proowner</code></td><td>oid</td><td>pg_authid.oid</td><td>Owner of the function.</td></tr><tr><td><code>prolang</code></td><td>oid</td><td>pg_language.oid</td><td>Implementation language or call interface of this function.</td></tr><tr><td><code>procost</code></td><td>float4</td><td></td><td>Estimated execution cost (in cpu_operator_cost units); if <code>proretset</code> is <code>true</code>, identifies the cost per row returned.</td></tr><tr><td><code>prorows</code></td><td>float4</td><td></td><td>Estimated number of result rows (zero if not <code>proretset</code>).</td></tr><tr><td><code>provariadic</code></td><td>oid</td><td>pg_type.oid</td><td>Data type of the variadic array parameter&#39;s elements, or zero if the function does not have a variadic parameter.</td></tr><tr><td><code>prosupport</code></td><td>regproc</td><td>pg_proc.oid</td><td>Planner support function for this function</td></tr><tr><td><code>prokind</code></td><td>char</td><td></td><td><code>f</code> for a normal function, <code>p</code> for a procedure, <code>a</code> for an aggregate function, or <code>w</code> for a window function</td></tr><tr><td><code>prosecdef</code></td><td>boolean</td><td></td><td>Function is a security definer (for example, a &#39;setuid&#39; function).</td></tr><tr><td><code>proleakproof</code></td><td>boolean</td><td></td><td>The function has no side effects. No information about the arguments is conveyed except via the return value. Any function that might throw an error depending on the values of its arguments is not leak-proof.</td></tr><tr><td><code>proisstrict</code></td><td>boolean</td><td></td><td>Function returns NULL if any call argument is NULL. In that case the function will not actually be called at all. Functions that are not strict must be prepared to handle NULL inputs.</td></tr><tr><td><code>proretset</code></td><td>boolean</td><td></td><td>Function returns a set (multiple values of the specified data type).</td></tr><tr><td><code>provolatile</code></td><td>char</td><td></td><td>Tells whether the function&#39;s result depends only on its input arguments, or is affected by outside factors. <code>i</code> = <em>immutable</em> (always delivers the same result for the same inputs), <code>s</code> = <em>stable</em> (results (for fixed inputs) do not change within a scan), or <code>v</code> = <em>volatile</em> (results may change at any time or functions with side-effects).</td></tr><tr><td><code>proparallel</code></td><td>char</td><td></td><td>Indicates whether a function can be safely run in a parallel backend, or only in non-parallel mode</td></tr><tr><td><code>pronargs</code></td><td>smallint</td><td></td><td>Number of arguments.</td></tr><tr><td><code>pronargdefaults</code></td><td>smallint</td><td></td><td>Number of arguments that have default values.</td></tr><tr><td><code>prorettype</code></td><td>oid</td><td>pg_type.oid</td><td>Data type of the return value, or null for a procedure.</td></tr><tr><td><code>proargtypes</code></td><td>oidvector</td><td>pg_type.oid</td><td>An array with the data types of the function arguments. This includes only input arguments (including <code>INOUT</code> and <code>VARIADIC</code> arguments), and thus represents the call signature of the function.</td></tr><tr><td><code>proallargtypes</code></td><td>oid[]</td><td>pg_type.oid</td><td>An array with the data types of the function arguments. This includes all arguments (including <code>OUT</code> and <code>INOUT</code> arguments); however, if all of the arguments are <code>IN</code> arguments, this field will be null. Note that subscripting is 1-based, whereas for historical reasons <code>proargtypes</code> is subscripted from 0.</td></tr><tr><td><code>proargmodes</code></td><td>char[]</td><td></td><td>An array with the modes of the function arguments: <code>i</code> = <code>IN</code>, <code>o</code> = <code>OUT</code> , <code>b</code> = <code>INOUT</code>, <code>v</code> = <code>VARIADIC</code>. If all the arguments are IN arguments, this field will be null. Note that subscripts correspond to positions of <code>proallargtypes</code>, not <code>proargtypes</code>.</td></tr><tr><td><code>proargnames</code></td><td>text[]</td><td></td><td>An array with the names of the function arguments. Arguments without a name are set to empty strings in the array. If none of the arguments have a name, this field will be null. Note that subscripts correspond to positions of <code>proallargtypes</code> not <code>proargtypes</code>.</td></tr><tr><td><code>proargdefaults</code></td><td>pg_node_tree</td><td></td><td>Expression trees (in <code>nodeToString()</code> representation) for default argument values. This is a list with <code>pronargdefaults</code> elements, corresponding to the last N input arguments (i.e., the last N <code>proargtypes</code> positions). If none of the arguments have defaults, this field will be null.</td></tr><tr><td><code>protrftypes</code></td><td>oid[]</td><td></td><td>Types for which to apply transforms</td></tr><tr><td><code>prosrc</code></td><td>text</td><td></td><td>This tells the function handler how to invoke the function. It might be the actual source code of the function for interpreted languages, a link symbol, a file name, or just about anything else, depending on the implementation language/call convention.</td></tr><tr><td><code>probin</code></td><td>text</td><td></td><td>Additional information about how to invoke the function. Again, the interpretation is language-specific.</td></tr><tr><td><code>proconfig</code></td><td>text[]</td><td></td><td>Function&#39;s local settings for run-time configuration variables.</td></tr><tr><td><code>proacl</code></td><td>aclitem[]</td><td></td><td>Access privileges for the function as given by <code>GRANT</code>/<code>REVOKE</code>.</td></tr><tr><td><code>proexeclocation</code></td><td>char</td><td></td><td>Where the function runs when it is invoked: <code>m</code> - coordinator only, <code>a</code> - any segment instance, <code>s</code> - all segment instances, <code>i</code> - initplan.</td></tr></tbody></table><p><strong>Parent topic:</strong> <a href="./../system_catalogs/catalog_ref-html.html">System Catalogs Definitions</a></p>',5)]))}const h=e(n,[["render",a]]);export{f as __pageData,h as default};
