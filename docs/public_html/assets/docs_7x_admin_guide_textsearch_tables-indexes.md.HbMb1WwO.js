import{_ as t,c as a,o as s,ag as n}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"Searching Text in Database Tables","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/textsearch/tables-indexes.md","filePath":"docs/7x/admin_guide/textsearch/tables-indexes.md"}'),i={name:"docs/7x/admin_guide/textsearch/tables-indexes.md"};function o(c,e,d,l,p,r){return s(),a("div",null,e[0]||(e[0]=[n(`<h1 id="searching-text-in-database-tables" tabindex="-1">Searching Text in Database Tables <a class="header-anchor" href="#searching-text-in-database-tables" aria-label="Permalink to &quot;Searching Text in Database Tables&quot;">​</a></h1><hr><p>This topic shows how to use text search operators to search database tables and how to create indexes to speed up text searches.</p><p>The examples in the previous section illustrated full text matching using simple constant strings. This section shows how to search table data, optionally using indexes.</p><p>This section contains the following subtopics:</p><ul><li><a href="#searching">Searching a Table</a></li><li><a href="#indexes">Creating Indexes</a></li></ul><h2 id="searching-a-table" tabindex="-1"><a id="searching"></a>Searching a Table <a class="header-anchor" href="#searching-a-table" aria-label="Permalink to &quot;&lt;a id=&quot;searching&quot;&gt;&lt;/a&gt;Searching a Table&quot;">​</a></h2><p>It is possible to do a full text search without an index. A simple query to print the <code>title</code> of each row that contains the word <code>friend</code> in its <code>body</code> field is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT title</span></span>
<span class="line"><span>FROM pgweb</span></span>
<span class="line"><span>WHERE to_tsvector(&#39;english&#39;, body) @@ to_tsquery(&#39;english&#39;, &#39;friend&#39;);</span></span></code></pre></div><p>This will also find related words such as <code>friends</code> and <code>friendly</code>, since all these are reduced to the same normalized lexeme.</p><p>The query above specifies that the <code>english</code> configuration is to be used to parse and normalize the strings. Alternatively we could omit the configuration parameters:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT title</span></span>
<span class="line"><span>FROM pgweb</span></span>
<span class="line"><span>WHERE to_tsvector(body) @@ to_tsquery(&#39;friend&#39;);</span></span></code></pre></div><p>This query will use the configuration set by <a href="./../../ref_guide/config_params/guc-list.html">default_text_search_config</a>.</p><p>A more complex example is to select the ten most recent documents that contain <code>create</code> and <code>table</code> in the <code>title</code> or <code>body</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT title</span></span>
<span class="line"><span>FROM pgweb</span></span>
<span class="line"><span>WHERE to_tsvector(title || &#39; &#39; || body) @@ to_tsquery(&#39;create &amp; table&#39;)</span></span>
<span class="line"><span>ORDER BY last_mod_date DESC</span></span>
<span class="line"><span>LIMIT 10;</span></span></code></pre></div><p>For clarity we omitted the <code>coalesce</code> function calls which would be needed to find rows that contain <code>NULL</code> in one of the two fields.</p><p>Although these queries will work without an index, most applications will find this approach too slow, except perhaps for occasional ad-hoc searches. Practical use of text searching usually requires creating an index.</p><h2 id="creating-indexes" tabindex="-1"><a id="indexes"></a>Creating Indexes <a class="header-anchor" href="#creating-indexes" aria-label="Permalink to &quot;&lt;a id=&quot;indexes&quot;&gt;&lt;/a&gt;Creating Indexes&quot;">​</a></h2><p>We can create a GIN index (<a href="./gist-gin.html">GiST and GIN Indexes for Text Search</a>) to speed up text searches:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE INDEX pgweb_idx ON pgweb USING GIN (to_tsvector(&#39;english&#39;, body));</span></span></code></pre></div><p>Notice that the two-argument version of <code>to_tsvector</code> is used. Only text search functions that specify a configuration name can be used in expression indexes. This is because the index contents must be unaffected by <a href="./../../ref_guide/config_params/guc-list.html">default_text_search_config</a>. If they were affected, the index contents might be inconsistent because different entries could contain <code>tsvector</code>s that were created with different text search configurations, and there would be no way to guess which was which. It would be impossible to dump and restore such an index correctly.</p><p>Because the two-argument version of <code>to_tsvector</code> was used in the index above, only a query reference that uses the two-argument version of <code>to_tsvector</code> with the same configuration name will use that index. That is, <code>WHERE to_tsvector(&#39;english&#39;, body) @@ &#39;a &amp; b&#39;</code> can use the index, but <code>WHERE to_tsvector(body) @@ &#39;a &amp; b&#39;</code> cannot. This ensures that an index will be used only with the same configuration used to create the index entries.</p><p>It is possible to set up more complex expression indexes wherein the configuration name is specified by another column, e.g.:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE INDEX pgweb_idx ON pgweb USING GIN (to_tsvector(config_name, body));</span></span></code></pre></div><p>where <code>config_name</code> is a column in the <code>pgweb</code> table. This allows mixed configurations in the same index while recording which configuration was used for each index entry. This would be useful, for example, if the document collection contained documents in different languages. Again, queries that are meant to use the index must be phrased to match, e.g., <code>WHERE to_tsvector(config_name, body) @@ &#39;a &amp; b&#39;</code>.</p><p>Indexes can even concatenate columns:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE INDEX pgweb_idx ON pgweb USING GIN (to_tsvector(&#39;english&#39;, title || &#39; &#39; || body));</span></span></code></pre></div><p>Another approach is to create a separate <code>tsvector</code> column to hold the output of <code>to_tsvector</code>. To keep this column automatically up to date with its source data, use a stored generated column. This example is a concatenation of title and body, using <code>coalesce</code> to ensure that one field will still be indexed when the other is NULL:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ALTER TABLE pgweb </span></span>
<span class="line"><span>    ADD COLUMN textsearchable_index_col tsvector</span></span>
<span class="line"><span>        GENERATED ALWAYS AS (to_tsvector(&#39;english&#39;, coalesce(title, &#39;&#39;) || &#39; &#39; || coalesce(body, &#39;&#39;))) STORED;</span></span></code></pre></div><p>Then we create a GIN index to speed up the search:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE INDEX textsearch_idx ON pgweb USING GIN (textsearchable_index_col);</span></span></code></pre></div><p>Now we are ready to perform a fast full text search:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT title FROM pgweb WHERE textsearchable_index_col @@ to_tsquery(&#39;create &amp; table&#39;) </span></span>
<span class="line"><span>ORDER BY last_mod_date DESC LIMIT 10;</span></span></code></pre></div><p>One advantage of the separate-column approach over an expression index is that it is not necessary to explicitly specify the text search configuration in queries in order to make use of the index. As shown in the example above, the query can depend on <code>default_text_search_config</code>. Another advantage is that searches will be faster, since it will not be necessary to redo the <code>to_tsvector</code> calls to verify index matches. (This is more important when using a GiST index than a GIN index; see <a href="./gist-gin.html">GiST and GIN Indexes for Text Search</a>.) The expression-index approach is simpler to set up, however, and it requires less disk space since the <code>tsvector</code> representation is not stored explicitly.</p><p><strong>Parent topic:</strong> <a href="./../textsearch/full-text-search.html">Using Full Text Search</a></p>`,35)]))}const g=t(i,[["render",o]]);export{u as __pageData,g as default};
