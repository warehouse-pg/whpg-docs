import{_ as o,c as a,o as t,ag as s}from"./chunks/framework.Ds6Eueu6.js";const h=JSON.parse('{"title":"CREATE RESOURCE GROUP","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/CREATE_RESOURCE_GROUP.md","filePath":"docs/7x/ref_guide/sql_commands/CREATE_RESOURCE_GROUP.md"}'),i={name:"docs/7x/ref_guide/sql_commands/CREATE_RESOURCE_GROUP.md"};function n(r,e,p,c,l,u){return t(),a("div",null,e[0]||(e[0]=[s(`<h1 id="create-resource-group" tabindex="-1">CREATE RESOURCE GROUP <a class="header-anchor" href="#create-resource-group" aria-label="Permalink to &quot;CREATE RESOURCE GROUP&quot;">​</a></h1><p>Defines a new resource group.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE GROUP &lt;name&gt; WITH (&lt;group_attribute&gt;=&lt;value&gt; [, ... ])</span></span></code></pre></div><p>where group_attribute is one of:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ CONCURRENCY=&lt;integer&gt; ]</span></span>
<span class="line"><span>CPU_MAX_PERCENT=&lt;integer&gt; | CPUSET=&lt;coordinator_cores&gt;;&lt;segment_cores&gt;</span></span>
<span class="line"><span>[ CPU_WEIGHT=&lt;integer&gt; ]</span></span>
<span class="line"><span>[ MEMORY_QUOTA=&lt;integer&gt; ]</span></span>
<span class="line"><span>[ MIN_COST=&lt;integer&gt; ]</span></span>
<span class="line"><span>[ IO_LIMIT=&#39; &lt;tablespace_io_limit_spec&gt; [; ...] &#39; ]</span></span></code></pre></div><p>Where <code>&lt;tablespace_io_limit_spec&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;tablespace_name&gt; | &lt;oid&gt; : &lt;io_limit_option_spec&gt; [, ...]</span></span></code></pre></div><p>Where <code>&lt;io_limit_option_spec&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>wbps=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| rbps=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| wiops=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| riops=&lt;io_limit_option_value&gt;</span></span></code></pre></div><p>Where <code>&lt;io_limit_option_vlaue&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;integer&gt; | max</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>Creates a new resource group for WarehousePG resource management. You can create resource groups to manage resources for roles or to manage the resources of a WarehousePG external component such as PL/Container.</p><p>A resource group that you create to manage a user role identifies concurrent transaction, memory, CPU, and disk I/O limits for the role when resource groups are enabled. You may assign such resource groups to one or more roles.</p><p>A resource group that you create to manage the resources of a WarehousePG external component such as PL/Container identifies the CPU limits for the component when resource groups are enabled. These resource groups use cgroups for both CPU management. Assignment of resource groups to external components is component-specific. For example, you assign a PL/Container resource group when you configure a PL/Container runtime. You cannot assign a resource group that you create for external components to a role, nor can you assign a resource group that you create for roles to an external component.</p><p>You must have <code>SUPERUSER</code> privileges to create a resource group. The maximum number of resource groups allowed in your WarehousePG cluster is 100.</p><p>WarehousePG pre-defines three default resource groups: <code>admin_group</code>, <code>default_group</code>, and <code>system_group</code>. These group names, as well as the group name <code>none</code>, are reserved.</p><p>To set appropriate limits for resource groups, the WarehousePG administrator must be familiar with the queries typically run on the system, as well as the users/roles running those queries and the external components they may be using, such as PL/Containers</p><p>After creating a resource group for a role, assign the group to one or more roles using the <a href="./ALTER_ROLE.html">ALTER ROLE</a> or <a href="./CREATE_ROLE.html">CREATE ROLE</a> commands.</p><p>After you create a resource group to manage the CPU resources of an external component, configure the external component to use the resource group. For example, configure the PL/Container runtime <code>resource_group_id</code>.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>name : The name of the resource group.</p><p>CONCURRENCY integer : Optional. The maximum number of concurrent transactions, including active and idle transactions, that are permitted for this resource group. The <code>CONCURRENCY</code> value must be an integer in the range [0 .. <code>max_connections</code>]. The default <code>CONCURRENCY</code> value for resource groups defined for roles is 20.</p><p>: You must set <code>CONCURRENCY</code> to <code>0</code> for resource groups that you create for external components.</p><p>: &gt; <strong>Note</strong> You cannot set the <code>CONCURRENCY</code> value for the <code>admin_group</code> to <code>0</code>.</p><p>CPU_MAX_PERCENT integer : Optional. The percentage of the maximum available CPU resources that the resource group can use. The value range is <code>1-100</code>.</p><p>CPU_WEIGHT integer : Optional. The scheduling priority of the current group. The value range is <code>1-500</code>, the default is \`100.</p><p>CPUSET &lt;coordinator_cores&gt;;&lt;segment_cores&gt;</p><p>: <code>CPUSET</code> identifies the CPU cores to reserve for this resource group on the coordinator host and on segment hosts. The CPU cores that you specify must be available in the system and cannot overlap with any CPU cores that you specify for other resource groups.</p><p>: &gt; <strong>Note</strong> You must specify either <code>CPU_MAX_PERCENT</code> or <code>CPUSET</code> when you create a resource group, but not both.</p><p>: Specify cores as a comma-separated list of single core numbers or core number intervals. Define the coordinator host cores first, followed by segment host cores, and separate the two with a semicolon. You must enclose the full core configuration in single quotes. For example, &#39;1;1,3-4&#39; configures core 1 for the coordinator host, and cores 1, 3, and 4 for the segment hosts.</p><p>: &gt; <strong>Note</strong> You can configure <code>CPUSET</code> for a resource group only after you have enabled resource group-based resource management for your WarehousePG cluster.</p><p>IO_LIMIT=&#39;&lt;tablespace_io_limit_spec&gt; [; ...]&#39; : Optional. The maximum read/write sequential disk I/O throughput, and the maximum read/write I/O operations per second for the queries assigned to a specific resource group.</p><p>Where <code>&lt;tablespace_io_limit_spec&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;tablespace_name&gt; | &lt;oid&gt; : &lt;io_limit_option_spec&gt; [, ...]</span></span></code></pre></div><p>Where <code>&lt;io_limit_option_spec&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>wbps=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| rbps=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| wiops=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| riops=&lt;io_limit_option_value&gt;</span></span></code></pre></div><p>Where <code>&lt;io_limit_option_vlaue&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;integer&gt; | max</span></span></code></pre></div><p>When you use this parameter, you may speficy:</p><ul><li>The tablespace name or the tablespace object ID (OID) you set the limits for. Use <code>*</code> to set limits for all tablespaces.</li><li>The values for <code>rbps</code> and <code>wbps</code> to limit the maximum read and write sequential disk I/O throughput in the resource group, in MB/S. The default value is <code>max</code>, which means there is no limit.</li><li>The values for <code>riops</code> and <code>wiops</code> to limit the maximum read and write I/O operations per second in the resource group. The default value is <code>max</code>, which means there is no limit.</li></ul><p>If the parameter <code>IO_LIMIT</code> is not set, the default value for <code>rbps</code>, <code>wpbs</code>, <code>riops</code>, and <code>wiops</code>s is set to <code>max</code>, which means that there are no disk I/O limits. In this scenario, the <code>gp_toolkit.gp_resgroup_config</code> system view displays its value as <code>-1</code>.</p><blockquote><p><strong>Note</strong> The parameter <code>IO_LIMIT</code> is only available when you use Linux Control Groups v2. See <a href="./../../admin_guide/workload_mgmt_resgroups.html#topic71717999">Configuring and Using Resource Groups</a> for more information.</p></blockquote><p>MEMORY_QUOTA integer : Optional. The maximum available memory, in MB, to reserve for this resource group. This value determines the total amount of memory that all worker processes within a resource group can consume on a segment host during query execution.</p><p>: The minimum memory quantity you can specify for a resource group is <code>0</code>. The default value is <code>-1</code>.</p><p>: When you specify a <code>MEMORY_QUOTA</code> of <code>-1</code>, <code>MEMORY_QUOTA</code> takes the value of the <code>statement_mem</code> server configuration parameter.</p><p>: &gt; <strong>Note</strong> If the server configuration parameter <code>gp_resgroup_memory_query_fixed_mem</code> is set, its value overrides at the session level the value of <code>MEMORY_QUOTA</code>.</p><p>MIN_COST integer : Optional. The limit on the cost of the query plan generated by a query in this resource group. When the query plan cost of the query is less than this value, the query will be unassigned from the resource group to which it belongs.</p><p>This means that low-cost queries will execute more quickly, as they are not subject to resource constraints.</p><p>The value range is <code>0-500</code>. The default value is <code>0</code>, which means that the cost is not used to bypass the query.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>You cannot submit a <code>CREATE RESOURCE GROUP</code> command in an explicit transaction or sub-transaction.</p><p>Use the <code>gp_toolkit.gp_resgroup_config</code> system view to display the limit settings of all resource groups:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * FROM gp_toolkit.gp_resgroup_config;</span></span></code></pre></div><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Create a resource group with CPU and memory quota of 350 MB:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE GROUP rgroup1 WITH (CPU_MAX_PERCENT=35, MEMORY_QUOTA=350);</span></span></code></pre></div><p>Create a resource group with a concurrent transaction limit of 20, a memory quota of 1500 MB, a CPU limit of 25, and disk I/O limits for the <code>pg_default</code> tablespace:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE GROUP rgroup2 WITH (CONCURRENCY=20, </span></span>
<span class="line"><span>  MEMORY_QUOTA=1500, CPU_MAX_PERCENT=25,</span></span>
<span class="line"><span>  IO_LIMIT=’pg_default: wbps=1000, rbps=1000, wiops=100, riops=100’);</span></span></code></pre></div><p>Create a resource group with a concurrent transaction limit of 20, a memory quota of 1500 MB, a CPU limit of 25, and disk I/O limits for a tablespace with oid 1663:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE GROUP rgroup2 WITH (CONCURRENCY=20,</span></span>
<span class="line"><span>  MEMORY_QUOTA=1500, CPU_MAX_PERCENT=25,</span></span>
<span class="line"><span>  IO_LIMIT=’1663: wbps=1000, rbps=1000, wiops=100, riops=100’);</span></span></code></pre></div><p>Create a resource group with a memory quota of 110 MB to which you assign CPU core 1 on the coordinator host, and cores 1 to 3 on segment hosts:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE GROUP rgroup3 WITH (CPUSET=&#39;1;1-3&#39;, MEMORY_QUOTA=110);</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p><code>CREATE RESOURCE GROUP</code> is a WarehousePG extension. There is no provision for resource groups or resource management in the SQL standard.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./ALTER_ROLE.html">ALTER ROLE</a>, <a href="./CREATE_ROLE.html">CREATE ROLE</a>, <a href="./ALTER_RESOURCE_GROUP.html">ALTER RESOURCE GROUP</a>, <a href="./DROP_RESOURCE_GROUP.html">DROP RESOURCE GROUP</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,69)]))}const g=o(i,[["render",n]]);export{h as __pageData,g as default};
