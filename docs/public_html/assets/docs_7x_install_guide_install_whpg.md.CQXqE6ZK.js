import{_ as a,c as s,o,ag as t}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"Installing WarehousePG","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/install_guide/install_whpg.md","filePath":"docs/7x/install_guide/install_whpg.md"}'),n={name:"docs/7x/install_guide/install_whpg.md"};function i(l,e,r,p,d,h){return o(),s("div",null,e[0]||(e[0]=[t(`<h1 id="installing-warehousepg" tabindex="-1">Installing WarehousePG <a class="header-anchor" href="#installing-warehousepg" aria-label="Permalink to &quot;Installing WarehousePG&quot;">​</a></h1><hr><p>Describes how to install the WarehousePG software binaries on all of the hosts that will comprise your WarehousePG cluster, how to enable passwordless SSH for the <code>gpadmin</code> user, and how to verify the installation.</p><p>Perform the following tasks in order:</p><ol><li><a href="#topic_oy5_21n_1jb">Install WarehousePG</a>: <ul><li><a href="#topic_download">Download the WarehousePG Server Software</a> (WarehousePG)</li><li><a href="#topic_verify_sha">Verify the WarehousePG Software</a> (WarehousePG)</li><li><a href="#topic_install">Install the WarehousePG Software</a></li><li><a href="#topic_dj4_ssr_cmb">(Optional) Install to a Non-Default Directory</a></li></ul></li><li><a href="#topic_xmb_gb5_vhb">Enabling Passwordless SSH</a></li><li><a href="#topic10">Confirm the software installation.</a></li><li><a href="#topic_cwj_hzb_vhb">Next Steps</a></li></ol><p><strong>Parent topic:</strong> <a href="./install_guide/">Installing and Upgrading WarehousePG</a></p><h2 id="installing-warehousepg-1" tabindex="-1"><a id="topic_oy5_21n_1jb"></a>Installing WarehousePG <a class="header-anchor" href="#installing-warehousepg-1" aria-label="Permalink to &quot;&lt;a id=&quot;topic_oy5_21n_1jb&quot;&gt;&lt;/a&gt;Installing WarehousePG&quot;">​</a></h2><p>You must install WarehousePG on each host machine of the WarehousePG cluster.</p><p>VMware distributes the WarehousePG software as a downloadable package that you install on each host system with the operating system&#39;s package management system.</p><p>Open source WarehousePG releases are available as RPM installers for RHEL/Oracle/Rocky Linux. See <a href="https://network.pivotal.io/products/greenplum-database" target="_blank" rel="noreferrer">Open Source WarehousePG by VMware</a> to download pre-built binaries in RPM format.</p><h3 id="downloading-the-warehousepg-server-software-warehousepg" tabindex="-1"><a id="topic_download"></a>Downloading the WarehousePG Server Software (WarehousePG) <a class="header-anchor" href="#downloading-the-warehousepg-server-software-warehousepg" aria-label="Permalink to &quot;&lt;a id=&quot;topic_download&quot;&gt;&lt;/a&gt;Downloading the WarehousePG Server Software (WarehousePG)&quot;">​</a></h3><p>You can download the <em>WarehousePG Server</em> software package from the github repo.</p><p>Be sure to note the name and the file system location of the downloaded file.</p><h3 id="verifying-the-warehousepg-software-warehousepg" tabindex="-1"><a id="topic_verify_sha"></a>Verifying the WarehousePG Software (WarehousePG) <a class="header-anchor" href="#verifying-the-warehousepg-software-warehousepg" aria-label="Permalink to &quot;&lt;a id=&quot;topic_verify_sha&quot;&gt;&lt;/a&gt;Verifying the WarehousePG Software (WarehousePG)&quot;">​</a></h3><p>Follow the instructions in <a href="./verify_sw.html">Verifying the WarehousePG Software Download</a> to verify the integrity of the <em>WarehousePG Server</em> software.</p><h3 id="installing-warehousepg-2" tabindex="-1"><a id="topic_install"></a>Installing WarehousePG <a class="header-anchor" href="#installing-warehousepg-2" aria-label="Permalink to &quot;&lt;a id=&quot;topic_install&quot;&gt;&lt;/a&gt;Installing WarehousePG&quot;">​</a></h3><p>Before you begin installing WarehousePG, be sure you have completed the steps in <a href="./config_os.html">Configure Operating System</a> to configure each of the coordinator, standby coordinator, and segment host machines for WarehousePG.</p><blockquote><p><strong>Important</strong> After installing WarehousePG, you must set WarehousePG environment variables. See <a href="./init_whpg.html">Setting WarehousePG Environment Variables</a>.</p></blockquote><p>Follow these instructions to install WarehousePG from a pre-built binary.</p><blockquote><p><strong>Important</strong> You require sudo or root user access to install from a pre-built RPM or DEB file.</p></blockquote><ol><li><p>Download and copy the WarehousePG package to the <code>gpadmin</code> user&#39;s home directory on the coordinator, standby coordinator, and every segment host machine. The distribution file name has the format <code>greenplum-db-&lt;version&gt;-&lt;platform&gt;.rpm</code> for RHEL, Oracle Linux, or Rocky Linux systems.</p><blockquote><p><strong>Note</strong> For Oracle Linux or Rocky Linux installations, download and install the <code>rhel8-x86_64</code>distribution files.</p></blockquote></li><li><p>With sudo (or as root), install the WarehousePG package on each host machine using your system&#39;s package manager software:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ sudo yum install ./greenplum-db-&lt;version&gt;-&lt;platform&gt;.rpm</span></span></code></pre></div><p>The <code>yum</code> command automatically installs software dependencies, copies the WarehousePG software files into a version-specific directory under <code>/usr/local</code>, <code>/usr/local/greenplum-db-&lt;version&gt;</code>, and creates the symbolic link <code>/usr/local/greenplum-db</code> to the installation directory.</p></li><li><p>Change the owner and group of the installed files to <code>gpadmin</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ sudo chown -R gpadmin:gpadmin /usr/local/greenplum*</span></span>
<span class="line"><span>$ sudo chgrp -R gpadmin /usr/local/greenplum*</span></span></code></pre></div></li></ol><h2 id="optional-installing-to-a-non-default-directory" tabindex="-1"><a id="topic_dj4_ssr_cmb"></a>(Optional) Installing to a Non-Default Directory <a class="header-anchor" href="#optional-installing-to-a-non-default-directory" aria-label="Permalink to &quot;&lt;a id=&quot;topic_dj4_ssr_cmb&quot;&gt;&lt;/a&gt;\\(Optional\\) Installing to a Non-Default Directory&quot;">​</a></h2><p>On RHEL systems, you can use the <code>rpm</code> command with the <code>--prefix</code> option to install WarehousePG to a non-default directory (instead of under <code>/usr/local</code>). Note, however, that using <code>rpm</code> does not automatically install WarehousePG dependencies; you must manually install dependencies to each host system.</p><p>Follow these instructions to install WarehousePG to a specific directory.</p><blockquote><p><strong>Important</strong> You require sudo or root user access to install from a pre-built RPM file.</p></blockquote><ol><li><p>Download and copy the WarehousePG package to the <code>gpadmin</code> user&#39;s home directory on the coordinator, standby coordinator, and every segment host machine. The distribution file name has the format <code>greenplum-db-&lt;version&gt;-rhel8-x86_64.rpm</code> for RHEL/Oracle/Rocky Linux systems.</p></li><li><p>Manually install the WarehousePG dependencies to each host system:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ sudo yum install apr apr-util bash bzip2 curl krb5 libcurl libevent \\</span></span>
<span class="line"><span>libxml2 libyaml zlib openldap openssh openssl openssl-libs perl readline rsync R sed tar zip</span></span></code></pre></div></li><li><p>Use <code>rpm</code> with the <code>--prefix</code> option to install the WarehousePG package to your chosen installation directory on each host machine:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ sudo rpm --install ./greenplum-db-&lt;version&gt;-&lt;platform&gt;.rpm --prefix=&lt;directory&gt;</span></span></code></pre></div><p>The <code>rpm</code> command copies the WarehousePG software files into a version-specific directory under your chosen <code>&lt;directory&gt;</code>, <code>&lt;directory&gt;/greenplum-db-&lt;version&gt;</code>, and creates the symbolic link <code>&lt;directory&gt;/greenplum-db</code> to the versioned directory.</p></li><li><p>Change the owner and group of the installed files to <code>gpadmin</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ sudo chown -R gpadmin:gpadmin &lt;directory&gt;/greenplum*</span></span></code></pre></div></li></ol><blockquote><p><strong>Note</strong> All example procedures in the WarehousePG documentation assume that you installed to the default directory, which is <code>/usr/local</code>. If you install to a non-default directory, substitute that directory for <code>/usr/local</code>.</p></blockquote><p>If you install to a non-default directory using <code>rpm</code>, you will need to continue using <code>rpm</code> (and of <code>yum</code>) to perform minor version upgrades; these changes are covered in the upgrade documentation.</p><h2 id="enabling-passwordless-ssh" tabindex="-1"><a id="topic_xmb_gb5_vhb"></a>Enabling Passwordless SSH <a class="header-anchor" href="#enabling-passwordless-ssh" aria-label="Permalink to &quot;&lt;a id=&quot;topic_xmb_gb5_vhb&quot;&gt;&lt;/a&gt;Enabling Passwordless SSH&quot;">​</a></h2><p>The <code>gpadmin</code> user on each WarehousePG host must be able to SSH from any host in the cluster to any other host in the cluster without entering a password or passphrase (called &quot;passwordless SSH&quot;). If you enable passwordless SSH from the coordinator host to every other host in the cluster (&quot;1-<em>n</em> passwordless SSH&quot;), you can use the WarehousePG <code>gpssh-exkeys</code> command-line utility to enable passwordless SSH from every host to every other host (&quot;<em>n</em>-<em>n</em> passwordless SSH&quot;).</p><ol><li><p>Log in to the coordinator host as the <code>gpadmin</code> user.</p></li><li><p>Source the <code>path</code> file in the WarehousePG installation directory.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ source /usr/local/greenplum-db-&lt;version&gt;/greenplum_path.sh</span></span></code></pre></div><blockquote><p><strong>Note</strong> Add the above <code>source</code> command to the <code>gpadmin</code> user&#39;s <code>.bashrc</code> or other shell startup file so that the WarehousePG path and environment variables are set whenever you log in as <code>gpadmin</code>.</p></blockquote></li><li><p>Use the <code>ssh-copy-id</code> command to add the <code>gpadmin</code> user&#39;s public key to the <code>authorized_hosts</code> SSH file on every other host in the cluster.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ ssh-copy-id scdw</span></span>
<span class="line"><span>$ ssh-copy-id sdw1</span></span>
<span class="line"><span>$ ssh-copy-id sdw2</span></span>
<span class="line"><span>$ ssh-copy-id sdw3</span></span>
<span class="line"><span>. . .</span></span></code></pre></div><p>This enables 1-<em>n</em> passwordless SSH. You will be prompted to enter the <code>gpadmin</code> user&#39;s password for each host. If you have the <code>sshpass</code> command on your system, you can use a command like the following to avoid the prompt.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ SSHPASS=&lt;password&gt; sshpass -e ssh-copy-id scdw</span></span></code></pre></div></li><li><p>In the <code>gpadmin</code> home directory, create a file named <code>hostfile_exkeys</code> that has the machine configured host names and host addresses (interface names) for each host in your WarehousePG cluster (coordinator, standby coordinator, and segment hosts). Make sure there are no blank lines or extra spaces. Check the <code>/etc/hosts</code> file on your systems for the correct host names to use for your environment. For example, if you have a coordinator, standby coordinator, and three segment hosts with two unbonded network interfaces per host, your file would look something like this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cdw</span></span>
<span class="line"><span>cdw-1</span></span>
<span class="line"><span>cdw-2</span></span>
<span class="line"><span>scdw</span></span>
<span class="line"><span>scdw-1</span></span>
<span class="line"><span>scdw-2</span></span>
<span class="line"><span>sdw1</span></span>
<span class="line"><span>sdw1-1</span></span>
<span class="line"><span>sdw1-2</span></span>
<span class="line"><span>sdw2</span></span>
<span class="line"><span>sdw2-1</span></span>
<span class="line"><span>sdw2-2</span></span>
<span class="line"><span>sdw3</span></span>
<span class="line"><span>sdw3-1</span></span>
<span class="line"><span>sdw3-2</span></span></code></pre></div></li><li><p>Run the <code>gpssh-exkeys</code> utility with your <code>hostfile_exkeys</code> file to enable <em>n</em>-<em>n</em> passwordless SSH for the <code>gpadmin</code> user.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpssh-exkeys -f hostfile_exkeys</span></span></code></pre></div></li></ol><h2 id="confirming-your-installation" tabindex="-1"><a id="topic10"></a>Confirming Your Installation <a class="header-anchor" href="#confirming-your-installation" aria-label="Permalink to &quot;&lt;a id=&quot;topic10&quot;&gt;&lt;/a&gt;Confirming Your Installation&quot;">​</a></h2><p>To make sure the WarehousePG software was installed and configured correctly, run the following confirmation steps from your WarehousePG coordinator host. If necessary, correct any problems before continuing on to the next task.</p><ol><li><p>Log in to the coordinator host as <code>gpadmin</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ su - gpadmin</span></span></code></pre></div></li><li><p>Use the <code>gpssh</code> utility to see if you can log in to all hosts without a password prompt, and to confirm that the WarehousePG software was installed on all hosts. Use the <code>hostfile_exkeys</code> file you used to set up passwordless SSH. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpssh -f hostfile_exkeys -e &#39;ls -l /usr/local/greenplum-db-&lt;version&gt;&#39;</span></span></code></pre></div><p>If the installation was successful, you should be able to log in to all hosts without a password prompt. All hosts should show that they have the same contents in their installation directories, and that the directories are owned by the <code>gpadmin</code> user.</p><p>If you are prompted for a password, run the following command to redo the ssh key exchange:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpssh-exkeys -f hostfile_exkeys</span></span></code></pre></div></li></ol><h2 id="about-your-warehousepg-installation" tabindex="-1"><a id="topic_zdf_1f5_vhb"></a>About Your WarehousePG Installation <a class="header-anchor" href="#about-your-warehousepg-installation" aria-label="Permalink to &quot;&lt;a id=&quot;topic_zdf_1f5_vhb&quot;&gt;&lt;/a&gt;About Your WarehousePG Installation&quot;">​</a></h2><ul><li><code>greenplum_path.sh</code> — This file contains the environment variables for WarehousePG. See <a href="./init_whpg.html">Setting WarehousePG Environment Variables</a>.</li><li><strong>bin</strong> — This directory contains the WarehousePG management utilities. This directory also contains the PostgreSQL client and server programs, most of which are also used in WarehousePG.</li><li><strong>docs/cli_help</strong> — This directory contains help files for WarehousePG command-line utilities.</li><li><strong>docs/cli_help/gpconfigs</strong> — This directory contains sample <code>gpinitsystem</code> configuration files and host files that can be modified and used when installing and Initializing WarehousePG.</li><li><strong>ext</strong> — Bundled programs (such as Python) used by some WarehousePG utilities.</li><li><strong>include</strong> — The C header files for WarehousePG.</li><li><strong>lib</strong> — WarehousePG and PostgreSQL library files.</li><li><strong>sbin</strong> — Supporting/Internal scripts and programs.</li><li><strong>share</strong> — Shared files for WarehousePG.</li></ul><h2 id="next-steps" tabindex="-1"><a id="topic_cwj_hzb_vhb"></a>Next Steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;&lt;a id=&quot;topic_cwj_hzb_vhb&quot;&gt;&lt;/a&gt;Next Steps&quot;">​</a></h2><ul><li><a href="./create_data_dirs.html">Creating the Data Storage Areas</a></li><li><a href="./validate.html">Validating the WHPG Cluster</a></li><li><a href="./init_whpg.html">Initializing WarehousePG</a></li></ul>`,38)]))}const g=a(n,[["render",i]]);export{u as __pageData,g as default};
