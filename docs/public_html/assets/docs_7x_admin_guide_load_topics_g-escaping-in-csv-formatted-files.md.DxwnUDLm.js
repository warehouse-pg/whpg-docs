import{_ as t,c as a,o,ag as s}from"./chunks/framework.Ds6Eueu6.js";const h=JSON.parse('{"title":"Escaping in CSV Formatted Files","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/load/topics/g-escaping-in-csv-formatted-files.md","filePath":"docs/7x/admin_guide/load/topics/g-escaping-in-csv-formatted-files.md"}'),i={name:"docs/7x/admin_guide/load/topics/g-escaping-in-csv-formatted-files.md"};function c(d,e,r,n,l,p){return o(),a("div",null,e[0]||(e[0]=[s('<h1 id="escaping-in-csv-formatted-files" tabindex="-1">Escaping in CSV Formatted Files <a class="header-anchor" href="#escaping-in-csv-formatted-files" aria-label="Permalink to &quot;Escaping in CSV Formatted Files&quot;">​</a></h1><hr><p>By default, the escape character is a <code>&quot;</code> (double quote) for CSV-formatted files. If you want to use a different escape character, use the <code>ESCAPE</code> clause of <code>COPY</code>, <code>CREATE EXTERNAL TABLE</code> or <code>gpload</code> to declare a different escape character. In cases where your selected escape character is present in your data, you can use it to escape itself.</p><p>For example, suppose you have a table with three columns and you want to load the following three fields:</p><ul><li><code>Free trip to A,B</code></li><li><code>5.89</code></li><li><code>Special rate &quot;1.79&quot;</code></li></ul><p>Your designated delimiter character is <code>,</code> (comma), and your designated escape character is <code>&quot;</code> (double quote). The formatted row in your data file looks like this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&quot;Free trip to A,B&quot;,&quot;5.89&quot;,&quot;Special rate &quot;&quot;1.79&quot;&quot;&quot;</span></span></code></pre></div><p>The data value with a comma character that is part of the data is enclosed in double quotes. The double quotes that are part of the data are escaped with a double quote even though the field value is enclosed in double quotes.</p><p>Embedding the entire field inside a set of double quotes guarantees preservation of leading and trailing whitespace characters:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&quot;Free trip to A,B &quot;,&quot;5.89 &quot;,&quot;Special rate &quot;&quot;1.79&quot;&quot; &quot;</span></span></code></pre></div><blockquote><p><strong>Note</strong> In CSV mode, all characters are significant. A quoted value surrounded by white space, or any characters other than <code>DELIMITER</code>, includes those characters. This can cause errors if you import data from a system that pads CSV lines with white space to some fixed width. In this case, preprocess the CSV file to remove the trailing white space before importing the data into WarehousePG.</p></blockquote><p><strong>Parent topic:</strong> <a href="./../../load/topics/g-escaping.html">Escaping</a></p>',12)]))}const g=t(i,[["render",c]]);export{h as __pageData,g as default};
