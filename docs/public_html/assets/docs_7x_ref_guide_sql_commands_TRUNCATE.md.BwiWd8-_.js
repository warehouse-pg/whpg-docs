import{_ as t,c as a,o,ag as s}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"TRUNCATE","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/TRUNCATE.md","filePath":"docs/7x/ref_guide/sql_commands/TRUNCATE.md"}'),i={name:"docs/7x/ref_guide/sql_commands/TRUNCATE.md"};function n(c,e,r,l,d,h){return o(),a("div",null,e[0]||(e[0]=[s(`<h1 id="truncate" tabindex="-1">TRUNCATE <a class="header-anchor" href="#truncate" aria-label="Permalink to &quot;TRUNCATE&quot;">​</a></h1><p>Empties a table or set of tables of all rows.</p><blockquote><p><strong>Note</strong> WarehousePG does not enforce referential integrity syntax (foreign key constraints). <code>TRUNCATE</code> truncates a table that is referenced in a foreign key constraint even if the <code>CASCADE</code> option is omitted.</p></blockquote><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TRUNCATE [TABLE] [ONLY] &lt;name&gt; [ * ] [, ...] </span></span>
<span class="line"><span>    [ RESTART IDENTITY | CONTINUE IDENTITY ] [CASCADE | RESTRICT]</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>TRUNCATE</code> quickly removes all rows from a table or set of tables. It has the same effect as an unqualified <a href="./DELETE.html">DELETE</a> on each table, but since it does not actually scan the tables it is faster. Furthermore, it reclaims disk space immediately, rather than requiring a subsequent <a href="./VACUUM.html">VACUUM</a> operation. This is most useful on large tables.</p><p>You must have the <code>TRUNCATE</code> privilege on the table to truncate it.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>name : The name (optionally schema-qualified) of a table to truncate. If <code>ONLY</code> is specified before the table name, only that table is truncated. If <code>ONLY</code> is not specified, the table and all its descendant tables (if any) are truncated. Optionally, you can specify <code>*</code> after the table name to explicitly indicate that descendant tables are included.</p><p>RESTART IDENTITY : Automatically restart sequences owned by columns of the truncated table(s).</p><p>CONTINUE IDENTITY : Do not change the values of sequences. This is the default.</p><p>CASCADE : Because this key word applies to foreign key references (which are not supported in WarehousePG) it has no effect.</p><p>RESTRICT : Because this key word applies to foreign key references (which are not supported in WarehousePG) it has no effect.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p><code>TRUNCATE</code> acquires an <code>ACCESS EXCLUSIVE</code> lock on each table that it operates on, which blocks all other concurrent operations on the table. When <code>RESTART IDENTITY</code> is specified, any sequences that are to be restarted are likewise locked exclusively. If you require concurrent access to a table, use the <code>DELETE</code> command instead.</p><p><code>TRUNCATE</code> will not fire any user-defined <code>ON DELETE</code> triggers that might exist for the tables.</p><p><code>TRUNCATE</code> will not truncate any tables that inherit from the named table. Only the named table is truncated, not its child tables.</p><p><code>TRUNCATE</code> is not MVCC-safe. After truncation, the table will appear empty to concurrent transactions, if they are using a snapshot taken before the truncation occurred.</p><p><code>TRUNCATE</code> is transaction-safe with respect to the data in the tables: the truncation will be safely rolled back if the surrounding transaction does not commit.</p><p>When <code>RESTART IDENTITY</code> is specified, the implied <code>ALTER SEQUENCE RESTART</code> operations are also done transactionally; that is, they will be rolled back if the surrounding transaction does not commit. Be aware that if any additional sequence operations are done on the restarted sequences before the transaction rolls back, the effects of these operations on the sequences will be rolled back.</p><p><code>TRUNCATE</code> is not currently supported for foreign tables. This implies that if a specified table has any descendant tables that are foreign, the command will fail.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Empty the tables <code>films</code> and <code>distributors</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TRUNCATE films, distributors;</span></span></code></pre></div><p>The same, and also reset any associated sequence generators:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TRUNCATE films, distributors RESTART IDENTITY;</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p>The SQL:2008 standard includes a <code>TRUNCATE</code> command with the syntax <code>TRUNCATE TABLE tablename</code>. The clauses <code>CONTINUE IDENTITY</code>/<code>RESTART IDENTITY</code> also appear in that standard, but have slightly different though related meanings. Some of the concurrency behavior of this command is left implementation-defined by the standard, so the above notes should be considered and compared with other implementations if necessary.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./DELETE.html">DELETE</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,32)]))}const T=t(i,[["render",n]]);export{u as __pageData,T as default};
