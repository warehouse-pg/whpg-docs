import{_ as t,c as a,o,ag as s}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"gpreload","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/gpreload.md","filePath":"docs/7x/utility_guide/ref/gpreload.md"}'),i={name:"docs/7x/utility_guide/ref/gpreload.md"};function n(l,e,d,r,p,c){return o(),a("div",null,e[0]||(e[0]=[s(`<h1 id="gpreload" tabindex="-1">gpreload <a class="header-anchor" href="#gpreload" aria-label="Permalink to &quot;gpreload&quot;">​</a></h1><p>Reloads WarehousePG table data sorting the data based on specified columns.</p><blockquote><p><strong>Note</strong> The <code>gpreload</code> utility is deprecated and will be removed in the next major release of WarehousePG. Use <a href="./../../ref_guide/sql_commands/ALTER_TABLE.html">ALTER TABLE REPACK BY COLUMNS</a> to physically reorder a table based on one or more columns.</p></blockquote><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpreload -d &lt;database&gt; [-p &lt;port&gt;] {-t | --table-file} &lt;path_to_file&gt; [-a]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpreload -h </span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpreload --version</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>The <code>gpreload</code> utility reloads table data with column data sorted. For tables that were created with the table storage option <code>appendoptimized=TRUE</code> and compression enabled, reloading the data with sorted data can improve table compression. You specify a list of tables to be reloaded and the table columns to be sorted in a text file.</p><p>Compression is improved by sorting data when the data in the column has a relatively low number of distinct values when compared to the total number of rows.</p><p>For a table being reloaded, the order of the columns to be sorted might affect compression. The columns with the fewest distinct values should be listed first. For example, listing state then city would generally result in better compression than listing city then state.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>public.cust_table: state, city</span></span>
<span class="line"><span>public.cust_table: city, state</span></span></code></pre></div><p>For information about the format of the file used with <code>gpreload</code>, see the <code>--table-file</code> option.</p><h2 id="notes" tabindex="-1"><a id="section4"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>To improve reload performance, indexes on tables being reloaded should be removed before reloading the data.</p><p>Running the <code>ANALYZE</code> command after reloading table data might query performance because of a change in the data distribution of the reloaded data.</p><p>For each table, the utility copies table data to a temporary table, truncates the existing table data, and inserts data from the temporary table to the table in the specified sort order. Each table reload is performed in a single transaction.</p><p>For a partitioned table, you can reload the data of a leaf partition. However, data is inserted from the root partitioned table, which acquires a <code>ROW EXCLUSIVE</code> lock on the entire table.</p><h2 id="options" tabindex="-1"><a id="section5"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>-a (do not prompt) : Optional. If specified, the <code>gpreload</code> utility does not prompt the user for confirmation.</p><p>-d database : The database that contains the tables to be reloaded. The <code>gpreload</code> utility connects to the database as the user running the utility.</p><p>-p port : The WarehousePG coordinator port. If not specified, the value of the <code>PGPORT</code> environment variable is used. If the value is not available, an error is returned.</p><p>{-t | --table-file } path_to_file : The location and name of file containing list of schema qualified table names to reload and the column names to reorder from the WarehousePG. Only user defined tables are supported. Views or system catalog tables are not supported.</p><p>: If indexes are defined on table listed in the file, <code>gpreload</code> prompts to continue.</p><p>: Each line specifies a table name and the list of columns to sort. This is the format of each line in the file:</p><p>: <code>schema.table_name: column [desc] [, column2 [desc] ... ]</code></p><p>: The table name is followed by a colon ( : ) and then at least one column name. If you specify more than one column, separate the column names with a comma. The columns are sorted in ascending order. Specify the keyword <code>desc</code> after the column name to sort the column in descending order.</p><p>: Wildcard characters are not supported.</p><p>: If there are errors in the file, <code>gpreload</code> reports the first error and exits. No data is reloaded.</p><p>: The following example reloads three tables:</p><pre><code>\`\`\`
public.clients: region, state, rep_id desc
public.merchants: region, state
test.lineitem: group, assy, whse 
\`\`\`
</code></pre><p>: In the first table <code>public.clients</code>, the data in the <code>rep_id</code> column is sorted in descending order. The data in the other columns are sorted in ascending order.</p><p>--version (show utility version) : Displays the version of this utility.</p><p>-? (help) : Displays the online help.</p><h2 id="example" tabindex="-1"><a id="section6"></a>Example <a class="header-anchor" href="#example" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Example&quot;">​</a></h2><p>This example command reloads the tables in the database <code>mytest</code> that are listed in the file <code>data-tables.txt</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpreload -d mytest --table-file data-tables.txt</span></span></code></pre></div><h2 id="see-also" tabindex="-1"><a id="section7"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><code>CREATE TABLE</code> in the <em>WarehousePG Reference Guide</em></p>`,37)]))}const m=t(i,[["render",n]]);export{u as __pageData,m as default};
