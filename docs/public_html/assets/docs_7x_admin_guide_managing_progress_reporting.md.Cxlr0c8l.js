import{_ as t,c as o,o as a,ag as s}from"./chunks/framework.Ds6Eueu6.js";const g=JSON.parse('{"title":"Monitoring Long-Running Operations","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/managing/progress_reporting.md","filePath":"docs/7x/admin_guide/managing/progress_reporting.md"}'),r={name:"docs/7x/admin_guide/managing/progress_reporting.md"};function d(i,e,n,c,p,l){return a(),o("div",null,e[0]||(e[0]=[s(`<h1 id="monitoring-long-running-operations" tabindex="-1">Monitoring Long-Running Operations <a class="header-anchor" href="#monitoring-long-running-operations" aria-label="Permalink to &quot;Monitoring Long-Running Operations&quot;">​</a></h1><p>WarehousePG can report the progress of <code>ANALYZE</code>, <code>CLUSTER</code>, <code>COPY</code>, <code>CREATE INDEX</code>, <code>REINDEX</code>, and <code>VACUUM</code> commands during command execution. WarehousePG can also report the progress of a running base backup (initiated during <a href="./../../utility_guide/ref/gprecoverseg.html">gprecoverseg -F</a>) command invocation, allowing you to monitor the progress of these possibly long-running operations.</p><p>WarehousePG reports the command progress via ephemeral system views, which return data only while the operations are running. Two sets of progress reporting views are provided:</p><ul><li><code>gp_stat_progress_&lt;command&gt;</code> - displays the progress of running <code>&lt;command&gt;</code> invocations on the coordinator and all segments, with a row per segment instance</li><li><code>gp_stat_progress_&lt;command&gt;_summary</code> - aggregates <code>&lt;command&gt;</code> progress on the coordinator and all segments, and displays one row per running <code>&lt;command&gt;</code> invocation</li></ul><p>WarehousePG reports progress in phases, where the phases are specific to the command. For example, <code>acquiring sample rows</code> is an analyze progress phase, while <code>building index</code> is an index creation progress phase. WarehousePG reports the progress for both heap and AO/CO tables. For most commands, heap and AO/CO table share the same phases. For vacuum and cluster operations, WarehousePG reports heap and AO/CO table progress in separate phases.</p><h2 id="analyze-progress-reporting" tabindex="-1"><a id="analyze_progress"></a>ANALYZE Progress Reporting <a class="header-anchor" href="#analyze-progress-reporting" aria-label="Permalink to &quot;&lt;a id=&quot;analyze_progress&quot;&gt;&lt;/a&gt;ANALYZE Progress Reporting&quot;">​</a></h2><p>The <a href="./../../ref_guide/system_catalogs/catalog_ref-views.html#gp_stat_progress_analyze">gp_stat_progress_analyze</a> system view reports the progress of running <code>ANALYZE</code> and <code>analyzedb</code> operations. The view displays a row per segment instance that is currently servicing an analyze operation.</p><p>For each active analyze operation, the <code>gp_stat_progress_analyze_summary</code> view aggregates across the WarehousePG cluster the metrics reported by <code>gp_stat_progress_analyze</code>.</p><p>The table below describes how to interpret the phase-specific information reported in the views:</p><table tabindex="0"><thead><tr><th>Phase</th><th>Description</th></tr></thead><tbody><tr><td><code>initializing</code></td><td>The command is preparing to begin scanning the heap. This phase is expected to be very brief.</td></tr><tr><td><code>acquiring sample rows</code></td><td>The command is currently scanning the table given by <code>relid</code> to obtain sample rows.</td></tr><tr><td><code>acquiring inherited sample rows</code></td><td>The command is currently scanning child tables to obtain sample rows. Columns <code>child_tables_total</code>, <code>child_tables_done</code>, and <code>current_child_table_relid</code> contain the progress information for this phase.</td></tr><tr><td><code>computing statistics</code></td><td>The command is computing statistics from the sample rows obtained during the table scan.</td></tr><tr><td><code>computing extended statistics</code></td><td>The command is computing extended statistics from the sample rows obtained during the table scan.</td></tr><tr><td><code>finalizing analyze</code></td><td>The command is updating <code>pg_class</code>. When this phase is completed, <code>ANALYZE</code> ends.</td></tr></tbody></table><h2 id="cluster-and-vacuum-full-progress-reporting" tabindex="-1"><a id="cluster_progress"></a>CLUSTER and VACUUM FULL Progress Reporting <a class="header-anchor" href="#cluster-and-vacuum-full-progress-reporting" aria-label="Permalink to &quot;&lt;a id=&quot;cluster_progress&quot;&gt;&lt;/a&gt;CLUSTER and VACUUM FULL Progress Reporting&quot;">​</a></h2><p>The <a href="./../../ref_guide/system_catalogs/catalog_ref-views.html#gp_stat_progress_cluster">gp_stat_progress_cluster</a> system view reports the progress of running <code>CLUSTER</code>, <code>clusterdb</code>, and <code>VACUUM FULL</code> (on a heap table) operations. (<code>VACUUM FULL</code> on a heap table is similar to <code>CLUSTER</code> in that WarehousePG performs a re-write of the table.) The view displays a row per segment instance that is currently servicing any of the mentioned commands.</p><p>For each active cluster or vacuum full operation, the <code>gp_stat_progress_cluster_summary</code> view aggregates across the WarehousePG cluster the metrics reported by <code>gp_stat_progress_cluster</code>.</p><h3 id="heap-table-cluster-phases" tabindex="-1"><a id="cluster_progress_heap"></a>Heap Table Cluster Phases <a class="header-anchor" href="#heap-table-cluster-phases" aria-label="Permalink to &quot;&lt;a id=&quot;cluster_progress_heap&quot;&gt;&lt;/a&gt;Heap Table Cluster Phases&quot;">​</a></h3><p>The table below describes how to interpret the <em>heap table</em> phase-specific information reported in the views:</p><table tabindex="0"><thead><tr><th>Phase</th><th>Description</th></tr></thead><tbody><tr><td><code>initializing</code></td><td>The command is preparing to begin scanning the heap. This phase is expected to be very brief.</td></tr><tr><td><code>seq scanning heap</code></td><td>The command is currently scanning the table using a sequential scan.</td></tr><tr><td><code>index scanning heap</code></td><td><code>CLUSTER</code> is currently scanning the table using an index scan.</td></tr><tr><td><code>sorting tuples</code></td><td><code>CLUSTER</code> is currently sorting tuples.</td></tr><tr><td><code>writing new heap</code></td><td><code>CLUSTER</code> is currently writing the new heap.</td></tr><tr><td><code>swapping relation files</code></td><td>The command is currently swapping newly-built files into place.</td></tr><tr><td><code>rebuilding index</code></td><td>The command is currently rebuilding an index.</td></tr><tr><td><code>performing final cleanup</code></td><td>The command is performing final cleanup. When this phase is completed, <code>CLUSTER</code> or <code>VACUUM FULL</code> ends.</td></tr></tbody></table><h3 id="ao-co-table-cluster-phases" tabindex="-1"><a id="cluster_progress_ao"></a>AO/CO Table Cluster Phases <a class="header-anchor" href="#ao-co-table-cluster-phases" aria-label="Permalink to &quot;&lt;a id=&quot;cluster_progress_ao&quot;&gt;&lt;/a&gt;AO/CO Table Cluster Phases&quot;">​</a></h3><p>The table below describes how to interpret the <em>AO/CO table</em> phase-specific information reported in the views:</p><table tabindex="0"><thead><tr><th>Phase</th><th>Description</th></tr></thead><tbody><tr><td><code>initializing</code></td><td>The command is preparing to begin scanning. This phase is expected to be very brief.</td></tr><tr><td><code>seq scanning append-optimized</code></td><td>The command is currently scanning the AO/CO table using a sequential scan.</td></tr><tr><td><code>index scanning heap</code></td><td><code>CLUSTER</code> is currently scanning the table using an index scan.</td></tr><tr><td><code>sorting tuples</code></td><td><code>CLUSTER</code> is currently sorting tuples.</td></tr><tr><td><code>writing new append-optimized</code></td><td><code>CLUSTER</code> is currently writing the new AO/CO table.</td></tr><tr><td><code>swapping relation files</code></td><td>The command is currently swapping newly-built files into place.</td></tr><tr><td><code>performing final cleanup</code></td><td>The command is performing final cleanup. When this phase is completed, <code>CLUSTER</code> or <code>VACUUM FULL</code> ends.</td></tr></tbody></table><h2 id="copy-progress-reporting" tabindex="-1"><a id="copy_progress"></a>COPY Progress Reporting <a class="header-anchor" href="#copy-progress-reporting" aria-label="Permalink to &quot;&lt;a id=&quot;copy_progress&quot;&gt;&lt;/a&gt;COPY Progress Reporting&quot;">​</a></h2><p>The <a href="./../../ref_guide/system_catalogs/catalog_ref-views.html#gp_stat_progress_copy">gp_stat_progress_copy</a> system view reports the progress of running <code>COPY</code> operations. The view displays a row per segment instance that is currently servicing a copy operation.</p><p>For each active copy operation, the <code>gp_stat_progress_copy_summary</code> aggregates across the WarehousePG cluster the metrics reported by <code>gp_stat_progress_copy</code>.</p><p>You can also use these views to monitor the data movement progress of utilities that use <code>COPY</code> under the hood, such as <code>gpbackup/gprestore</code>.</p><p>WarehousePG calculates the <code>bytes_processed</code>, <code>bytes_total</code>, <code>tuples_processed</code>, and <code>tuples_excluded</code> column values differently for the <code>gp_stat_progress_copy_summary</code> view depending on the type of table and type of <code>COPY</code> operation. The table below identifies the table types, the types of <code>COPY</code> operations, and which of <code>sum()</code> or <code>average()</code> WarehousePG uses to calculate the final value:</p><table tabindex="0"><thead><tr><th>Table type</th><th>COPY TO</th><th>COPY FROM</th><th>COPY TO/FROM ON SEGMENT</th></tr></thead><tbody><tr><td>Distributed table</td><td><code>sum()</code></td><td><code>sum()</code></td><td><code>sum()</code></td></tr><tr><td>Replicated table</td><td><code>sum()</code></td><td><code>average()</code></td><td><code>sum()</code></td></tr></tbody></table><p>WarehousePG uses <code>sum()</code> for <code>COPY ... ON SEGMENT</code> as the command explicitly operates on each segment irrespective of the table type.</p><p>WarehousePG uses <code>sum()</code> for <code>COPY TO</code> with replicated tables, as the actual copy originates from only a single segment.</p><h2 id="create-index-progress-reporting" tabindex="-1"><a id="create_index_progress"></a>CREATE INDEX Progress Reporting <a class="header-anchor" href="#create-index-progress-reporting" aria-label="Permalink to &quot;&lt;a id=&quot;create_index_progress&quot;&gt;&lt;/a&gt;CREATE INDEX Progress Reporting&quot;">​</a></h2><p>The <a href="./../../ref_guide/system_catalogs/catalog_ref-views.html#gp_stat_progress_create_index">gp_stat_progress_create_index</a> system view reports the progress of running <code>CREATE INDEX</code> and <code>REINDEX</code> operations. The view displays a row per segment instance that is currently servicing either command.</p><p>For each active index operation, the <code>gp_stat_progress_create_index_summary</code> view aggregates across the WarehousePG cluster the metrics reported by <code>gp_stat_progress_create_index</code>.</p><p>The table below describes how to interpret the phase-specific information reported in the views:</p><table tabindex="0"><thead><tr><th>Phase</th><th>Description</th></tr></thead><tbody><tr><td><code>initializing</code></td><td><code>CREATE INDEX</code> or <code>REINDEX</code> is preparing to create the index. This phase is expected to be very brief.</td></tr><tr><td><code>waiting for writers before build</code></td><td>This phase is not applicable, WarehousePG does not support <code>CREATE INDEX CONCURRENTLY</code> or <code>REINDEX CONCURRENTLY</code>.</td></tr><tr><td><code>building index</code></td><td>The index is being built by the access method-specific code. In this phase, access methods that support progress reporting fill in their own progress data, and the subphase is indicated in this column. Typically, <code>blocks_total</code> and <code>blocks_done</code> will contain progress data, as well as potentially <code>tuples_total</code> and <code>tuples_done</code>.</td></tr><tr><td><code>waiting for writers before validation</code></td><td>This phase is not applicable, WarehousePG does not support <code>CREATE INDEX CONCURRENTLY</code> or <code>REINDEX CONCURRENTLY</code>.</td></tr><tr><td><code>index validation: scanning index</code></td><td>This phase is not applicable, WarehousePG does not support <code>CREATE INDEX CONCURRENTLY</code> or <code>REINDEX CONCURRENTLY</code>.</td></tr><tr><td><code>index validation: sorting tuples</code></td><td>This phase is not applicable, WarehousePG does not support <code>CREATE INDEX CONCURRENTLY</code> or <code>REINDEX CONCURRENTLY</code>.</td></tr><tr><td><code>index validation: scanning table</code></td><td>This phase is not applicable, WarehousePG does not support <code>CREATE INDEX CONCURRENTLY</code> or <code>REINDEX CONCURRENTLY</code>.</td></tr><tr><td><code>waiting for old snapshots</code></td><td>This phase is not applicable, WarehousePG does not support <code>CREATE INDEX CONCURRENTLY</code> or <code>REINDEX CONCURRENTLY</code>.</td></tr><tr><td><code>waiting for readers before marking dead</code></td><td>This phase is not applicable, WarehousePG does not support <code>REINDEX CONCURRENTLY</code>.</td></tr><tr><td><code>waiting for readers before dropping</code></td><td>This phase is not applicable, WarehousePG does not support <code>REINDEX CONCURRENTLY</code>.</td></tr></tbody></table><p>WarehousePG skips several phases because it does not support concurrent index creation or concurrent reindexing.</p><h2 id="vacuum-progress-reporting" tabindex="-1"><a id="vacuum_progress"></a>VACUUM Progress Reporting <a class="header-anchor" href="#vacuum-progress-reporting" aria-label="Permalink to &quot;&lt;a id=&quot;vacuum_progress&quot;&gt;&lt;/a&gt;VACUUM Progress Reporting&quot;">​</a></h2><p>The <a href="./../../ref_guide/system_catalogs/catalog_ref-views.html#gp_stat_progress_vacuum">gp_stat_progress_vacuum</a> system view reports the progress of running <code>VACUUM</code> and <code>vacuumdb</code> operations on AO/CO and heap tables, and <code>VACUUM FULL</code> operations on AO/CO tables. The view displays a row per segment instance that is currently servicing a vacuum operation.</p><p>For each active vacuum operation, the <code>gp_stat_progress_vacuum_summary</code> view aggregates across the WarehousePG cluster the metrics reported by <code>gp_stat_progress_vacuum</code>.</p><p>Regular <code>VACUUM</code> modifies a heap table in place. <code>VACUUM FULL</code> on a heap table rewrites the table as does a <code>CLUSTER</code> operation. For information about progress reporting for <code>VACUUM FULL</code> on a heap table, see <a href="#cluster_progress">CLUSTER and VACUUM FULL Progress Reporting</a>.</p><p>In WarehousePG, an AO/CO table vacuum behaves differently than a heap table vacuum. Because WarehousePG stores the logical EOF for each segment file, it does not need to scan physical blocks after the logical EOF, so WarehousePG can truncate them. WarehousePG always rewrites data into new segment files to get rid of dead tuples for AO/CO tables, and performs the same operations for both <code>VACUUM FULL</code> and <code>VACUUM</code>. Due to these difference, WarehousePG reports vacuum progress on heap and AO/CO tables using different phases.</p><h3 id="heap-table-vacuum-phases" tabindex="-1"><a id="vacuum_progress_heap"></a>Heap Table Vacuum Phases <a class="header-anchor" href="#heap-table-vacuum-phases" aria-label="Permalink to &quot;&lt;a id=&quot;vacuum_progress_heap&quot;&gt;&lt;/a&gt;Heap Table Vacuum Phases&quot;">​</a></h3><p>The table below describes how to interpret the <em>heap table</em> phase-specific information reported in the views:</p><table tabindex="0"><thead><tr><th>Heap Table Phase</th><th>Description</th></tr></thead><tbody><tr><td><code>initializing</code></td><td><code>VACUUM</code> is preparing to begin scanning the heap. This phase is expected to be very brief.</td></tr><tr><td><code>scanning heap</code></td><td><code>VACUUM</code> is currently scanning the heap. It prunes and defragment seach page if required, and possibly performs freezing activity. Use the <code>heap_blks_scanned</code> column to monitor the progress of the scan.</td></tr><tr><td><code>vacuuming indexes</code></td><td><code>VACUUM</code> is currently vacuuming the indexes. If a table has any indexes, this will happen at least once per vacuum, after the heap has been completely scanned. It may happen multiple times per vacuum if <code>maintenance_work_mem</code> (or, in the case of autovacuum, <code>autovacuum_work_mem</code> if set) is insufficient to store the number of dead tuples found.</td></tr><tr><td><code>vacuuming heap</code></td><td><code>VACUUM</code> is currently vacuuming the heap. Vacuuming the heap is distinct from scanning the heap, and occurs after each instance of vacuuming indexes. If <code>heap_blks_scanned</code> is less than <code>heap_blks_total</code>, the system returns to scanning the heap after this phase is completed; otherwise, it begins cleaning up indexes after this phase is completed.</td></tr><tr><td><code>cleaning up indexes</code></td><td><code>VACUUM</code> is currently cleaning up indexes. This occurs after the heap has been completely scanned and all vacuuming of the indexes and the heap has been completed.</td></tr><tr><td><code>truncating heap</code></td><td><code>VACUUM</code> is currently truncating the heap so as to return empty pages at the end of the relation to the operating system. This occurs after cleaning up indexes.</td></tr><tr><td><code>performing final cleanup</code></td><td><code>VACUUM</code> is performing final cleanup. During this phase, <code>VACUUM</code> will vacuum the free space map, update statistics in <code>pg_class</code>, and report statistics to the statistics collector. When this phase is completed, the <code>VACUUM</code> operation ends.</td></tr></tbody></table><h3 id="ao-co-table-vacuum-phases" tabindex="-1"><a id="vacuum_progress_ao"></a>AO/CO Table Vacuum Phases <a class="header-anchor" href="#ao-co-table-vacuum-phases" aria-label="Permalink to &quot;&lt;a id=&quot;vacuum_progress_ao&quot;&gt;&lt;/a&gt;AO/CO Table Vacuum Phases&quot;">​</a></h3><p>The table below describes how to interpret the <em>AO/CO table</em> phase-specific information reported in the views:</p><table tabindex="0"><thead><tr><th>AO/CO Table Phase</th><th>Description</th></tr></thead><tbody><tr><td><code>initializing</code></td><td><code>VACUUM</code> is preparing to begin scanning AO/CO tables. This phase is expected to be very brief.</td></tr><tr><td><code>vacuuming indexes</code></td><td>For AO/CO tables, the <code>vacuuming indexes</code> phase is a sub-phase that may occur during both <code>append-optimized pre-cleanup</code> and <code>append-optimized post-cleanup</code> phases if the relation has an index and there are invisible awaiting-drop segment files.</td></tr><tr><td><code>append-optimized pre-cleanup</code></td><td><code>VACUUM</code> is performing pre-cleanup of the AO/CO table; this includes recycling <code>AWAITING_DROP</code> segments that are no longer visible to anyone from previous <code>VACUUM</code> operations, and truncating all live segment files to their logcial EOFs. Use <code>heap_blks_vacuumed</code> and <code>index_vacuum_count</code> to monitor the progress.</td></tr><tr><td><code>append-optimized compact</code></td><td><code>VACUUM</code> is vacuuming (or &quot;compacting&quot;) the AO/CO table by scanning the segment files and rewriting only the surviving tuples into new segment files. Use <code>heap_blks_scanned</code> and <code>num_dead_tuples</code> to monitor the progress.</td></tr><tr><td><code>append-optimized post-cleanup</code></td><td><code>VACUUM</code> is performing post-cleanup of the AO/CO table; this includes recycling old segments that are no longer visible after the &quot;compact&quot; phase, and truncating all live segment files to their logcial EOFs. Use <code>heap_blks_vacuumed</code> and <code>index_vacuum_count</code> to monitor the progress.</td></tr></tbody></table><h2 id="base-backup-progress-reporting" tabindex="-1"><a id="basebackup_progress"></a>Base Backup Progress Reporting <a class="header-anchor" href="#base-backup-progress-reporting" aria-label="Permalink to &quot;&lt;a id=&quot;basebackup_progress&quot;&gt;&lt;/a&gt;Base Backup Progress Reporting&quot;">​</a></h2><p>The <a href="./../../ref_guide/system_catalogs/catalog_ref-views.html#gp_stat_progress_basebackup">gp_stat_progress_basebackup</a> system view reports the progress of running base backup operations, as is performed by <code>gprecoverseg -F</code> (full recovery). The view displays a row per segment instance that is currently servicing replication commands.</p><p>For each active base backup operation, the <code>gp_stat_progress_basebackup_summary</code> view aggregates across the WarehousePG cluster the metrics reported by <code>gp_stat_progress_basebackup</code>.</p><p>The table below describes how to interpret the phase-specific information reported in the views:</p><table tabindex="0"><thead><tr><th>Phase</th><th>Description</th></tr></thead><tbody><tr><td><code>initializing</code></td><td>The WAL sender process is preparing to begin the backup. This phase is expected to be very brief.</td></tr><tr><td><code>waiting for checkpoint to finish</code></td><td>The WAL sender process is currently performing <code>pg_backup_start</code> to prepare to take a base backup, and waiting for the start-of-backup checkpoint to finish.</td></tr><tr><td><code>estimating backup size</code></td><td>The WAL sender process is currently estimating the total amount of database files that will be streamed as a base backup.</td></tr><tr><td><code>streaming database files</code></td><td>The WAL sender process is currently streaming database files as a base backup.</td></tr><tr><td><code>waiting for wal archiving to finish</code></td><td>The WAL sender process is currently performing <code>pg_backup_stop</code> to finish the backup, and waiting for all the WAL files required for the base backup to be successfully archived. The backup ends when this phase is completed.</td></tr><tr><td><code>transferring wal files</code></td><td>The WAL sender process is currently transferring all WAL logs generated during the backup. This phase may occur after <code>waiting for wal archiving to finish</code> phase. The backup ends when this phase is completed.</td></tr></tbody></table><h2 id="example-viewing-real-time-command-progress" tabindex="-1"><a id="realtime_progress"></a>Example: Viewing Real-Time Command Progress <a class="header-anchor" href="#example-viewing-real-time-command-progress" aria-label="Permalink to &quot;&lt;a id=&quot;realtime_progress&quot;&gt;&lt;/a&gt;Example: Viewing Real-Time Command Progress&quot;">​</a></h2><p>The following example commands display the real-time progress of all running <code>VACUUM</code> commands every 0.5 seconds:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>host$ cat &gt; viewer.sql &lt;&lt; EOF</span></span>
<span class="line"><span>SELECT * FROM gp_stat_progress_vacuum ORDER BY gp_segment_id;</span></span>
<span class="line"><span>SELECT * FROM gp_stat_progress_vacuum_summary;</span></span>
<span class="line"><span>EOF</span></span>
<span class="line"><span></span></span>
<span class="line"><span>host$ watch -n 0.5 &quot;psql -af viewer.sql&quot;</span></span></code></pre></div><h2 id="additional-considerations" tabindex="-1"><a id="consider"></a>Additional Considerations <a class="header-anchor" href="#additional-considerations" aria-label="Permalink to &quot;&lt;a id=&quot;consider&quot;&gt;&lt;/a&gt;Additional Considerations&quot;">​</a></h2><p>Take note of these additional considerations related to progress reporting in WarehousePG:</p><ul><li>Progress phases and their interpretation of attributes are slightly different for AO/AOCO tables and heap tables for the <code>CREATE INDEX/REINDEX</code>, <code>VACUUM</code>, and <code>CLUSTER</code> operations. Refer to the individual command topics for more information.</li><li>The progress views for an <code>ANALYZE</code> operation on a partitioned table display the progress of each individual leaf partition. WarehousePG does not currently provide a measure of the combined progress of all partitions.</li><li>WarehousePG does not support the <code>CLUSTER</code> operation on a partitioned table.</li></ul>`,55)]))}const u=t(r,[["render",d]]);export{g as __pageData,u as default};
