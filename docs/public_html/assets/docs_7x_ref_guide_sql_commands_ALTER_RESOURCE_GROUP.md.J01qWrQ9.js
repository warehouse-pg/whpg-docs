import{_ as t,c as a,o,ag as s}from"./chunks/framework.Ds6Eueu6.js";const h=JSON.parse('{"title":"ALTER RESOURCE GROUP","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/ALTER_RESOURCE_GROUP.md","filePath":"docs/7x/ref_guide/sql_commands/ALTER_RESOURCE_GROUP.md"}'),i={name:"docs/7x/ref_guide/sql_commands/ALTER_RESOURCE_GROUP.md"};function n(r,e,p,c,l,u){return o(),a("div",null,e[0]||(e[0]=[s(`<h1 id="alter-resource-group" tabindex="-1">ALTER RESOURCE GROUP <a class="header-anchor" href="#alter-resource-group" aria-label="Permalink to &quot;ALTER RESOURCE GROUP&quot;">​</a></h1><p>Changes the limits of a resource group.</p><h2 id="synopsis" tabindex="-1"><a id="synopsis"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;synopsis&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ALTER RESOURCE GROUP &lt;name&gt; SET &lt;group_attribute&gt; &lt;value&gt;</span></span></code></pre></div><p>where group_attribute is one of:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ CONCURRENCY=&lt;integer&gt; ]</span></span>
<span class="line"><span>CPU_MAX_PERCENT=&lt;integer&gt; | CPUSET=&lt;coordinator_cores&gt;;&lt;segment_cores&gt;</span></span>
<span class="line"><span>[ CPU_WEIGHT=&lt;integer&gt; ]</span></span>
<span class="line"><span>[ MEMORY_QUOTA=&lt;integer&gt; ]</span></span>
<span class="line"><span>[ MIN_COST=&lt;integer&gt; ]</span></span>
<span class="line"><span>[ IO_LIMIT=&#39; &lt;tablespace_io_limit_spec&gt; [; ...] &#39; ]</span></span></code></pre></div><p>Where <code>&lt;tablespace_io_limit_spec&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;tablespace_name&gt; | &lt;oid&gt; : &lt;io_limit_option_spec&gt; [, ...]</span></span></code></pre></div><p>Where <code>&lt;io_limit_option_spec&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>wbps=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| rbps=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| wiops=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| riops=&lt;io_limit_option_value&gt;</span></span></code></pre></div><p>Where <code>&lt;io_limit_option_vlaue&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;integer&gt; | max</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="description"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;description&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>ALTER RESOURCE GROUP</code> changes the limits of a resource group. Only a superuser can alter a resource group.</p><p>You can set or reset the concurrency limit of a resource group that you create for roles to control the maximum number of active concurrent statements in that group. You can also reset the memory or CPU resources of a resource group to control the amount of memory or CPU resources that all queries submitted through the group can consume on each segment host.</p><p>When you alter the CPU resource management mode or limit of a resource group, the new mode or limit is immediately applied.</p><p>When you alter a memory quota of a resource group that you create for roles, the new resource limit is immediately applied if current resource usage is less than or equal to the new value and there are no running transactions in the resource group. If the current resource usage exceeds the new memory quota value, or if there are running transactions in other resource groups that hold some of the resource, then WarehousePG defers assigning the new limit until resource usage falls within the range of the new value.</p><p>When you increase the memory quota of a resource group that you create for external components, the new resource limit is phased in as system memory resources become available. If you decrease the memory quota of a resource group that you create for external components, the behavior is component-specific.</p><p>You can alter one limit type in a single <code>ALTER RESOURCE GROUP</code> call.</p><h2 id="parameters" tabindex="-1"><a id="parameters"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;parameters&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>name : The name of the resource group to alter.</p><p>CONCURRENCY integer : The maximum number of concurrent transactions, including active and idle transactions, that are permitted for this resource group. The <code>CONCURRENCY</code> value must be an integer in the range [0 .. <code>max_connections</code>]. The default <code>CONCURRENCY</code> value for resource groups defined for roles is 20.</p><p>: You must set <code>CONCURRENCY</code> to <code>0</code> for resource groups that you create for external components.</p><p>: &gt; <strong>Note</strong> You cannot set the <code>CONCURRENCY</code> value for the <code>admin_group</code> to <code>0</code>.</p><p>CPU_MAX_PERCENT integer : The percentage of the maximum available CPU resources that the resource group can use. The value range is <code>1-100</code>.</p><p>CPU_WEIGHT integer : The scheduling priority of the current group. The value range is <code>1-500</code>, the default is \`100.</p><p>CPUSET &lt;coordinator_cores&gt;;&lt;segment_cores&gt;</p><p>: <code>CPUSET</code> identifies the CPU cores to reserve for this resource group on the coordinator host and on segment hosts. The CPU cores that you specify must be available in the system and cannot overlap with any CPU cores that you specify for other resource groups.</p><p>: &gt; <strong>Note</strong> You must specify either <code>CPU_MAX_PERCENT</code> or <code>CPUSET</code> when you create a resource group, but not both.</p><p>: Specify cores as a comma-separated list of single core numbers or core number intervals. Define the coordinator host cores first, followed by segment host cores, and separate the two with a semicolon. You must enclose the full core configuration in single quotes. For example, &#39;1;1,3-4&#39; configures core 1 for the coordinator host, and cores 1, 3, and 4 for the segment hosts.</p><p>: &gt; <strong>Note</strong> You can configure <code>CPUSET</code> for a resource group only after you have enabled resource group-based resource management for your WarehousePG cluster.</p><p>IO_LIMIT=&#39;&lt;tablespace_io_limit_spec&gt; [; ...]&#39; : Optional. The maximum read/write sequential disk I/O throughput, and the maximum read/write I/O operations per second for the queries assigned to a specific resource group.</p><p>Where <code>&lt;tablespace_io_limit_spec&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;tablespace_name&gt; | &lt;oid&gt; : &lt;io_limit_option_spec&gt; [, ...]</span></span></code></pre></div><p>Where <code>&lt;io_limit_option_spec&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>wbps=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| rbps=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| wiops=&lt;io_limit_option_value&gt;</span></span>
<span class="line"><span>| riops=&lt;io_limit_option_value&gt;</span></span></code></pre></div><p>Where <code>&lt;io_limit_option_vlaue&gt;</code> is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;integer&gt; | max</span></span></code></pre></div><p>When you use this parameter, you may speficy:</p><ul><li>The tablespace name or the tablespace object ID (OID) you set the limits for. Use <code>*</code> to set limits for all tablespaces.</li><li>The values for <code>rbps</code> and <code>wbps</code> to limit the maximum read and write sequential disk I/O throughput in the resource group, in MB/S. The default value is <code>max</code>, which means there is no limit.</li><li>The values for <code>riops</code> and <code>wiops</code> to limit the maximum read and write I/O operations per second in the resource group. The default value is <code>max</code>, which means there is no limit.</li></ul><p>If the parameter <code>IO_LIMIT</code> is not set, the default value for <code>rbps</code>, <code>wpbs</code>, <code>riops</code>, and <code>wiops</code>s is set to <code>max</code>, which means that there are no disk I/O limits. In this scenario, the <code>gp_toolkit.gp_resgroup_config</code> system view displays its value as <code>-1</code>.</p><blockquote><p><strong>Note</strong> The parameter <code>IO_LIMIT</code> is only available when you use Linux Control Groups v2. See <a href="./../../admin_guide/workload_mgmt_resgroups.html#topic71717999">Configuring and Using Resource Groups</a> for more information.</p></blockquote><p>MEMORY_QUOTA integer : The maximum available memory, in MB, to reserve for this resource group. This value determines the total amount of memory that all worker processes within a resource group can consume on a segment host during query execution.</p><p>: The minimum memory quantity you can specify for a resource group is <code>0</code>. The default value is <code>-1</code>.</p><p>: When you specify a <code>MEMORY_QUOTA</code> of <code>-1</code>, <code>MEMORY QUOTA</code> takes the value of the <code>statement_mem</code> server configuration parameter.</p><p>: &gt; <strong>Note</strong> If the server configuration parameter <code>gp_resgroup_memory_query_fixed_mem</code> is set, its value overrides at the session level the value of <code>MEMORY_QUOTA</code>.</p><p>MIN_COST integer : The limit on the cost of the query plan generated by a query in this resource group. When the query plan cost of the query is less than this value, the query will be unassigned from the resource group to which it belongs.</p><p>This means that low-cost queries will execute more quickly, as they are not subject to resource constraints.</p><p>The value range is <code>0-500</code>. The default value is <code>0</code>, which means that the cost is not used to bypass the query.</p><h2 id="notes" tabindex="-1"><a id="notes"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;notes&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>Use <a href="./CREATE_ROLE.html">CREATE ROLE</a> or <a href="./ALTER_ROLE.html">ALTER ROLE</a> to assign a specific resource group to a role (user).</p><p>You cannot submit an <code>ALTER RESOURCE GROUP</code> command in an explicit transaction or sub-transaction.</p><h2 id="examples" tabindex="-1"><a id="examples"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;examples&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Change the active transaction limit for a resource group:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ALTER RESOURCE GROUP rgroup1 SET CONCURRENCY 13;</span></span></code></pre></div><p>Update the CPU limit for a resource group:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ALTER RESOURCE GROUP rgroup2 SET CPU_MAX_PERCENT 45;</span></span></code></pre></div><p>Update the memory quota for a resource group:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ALTER RESOURCE GROUP rgroup3 SET MEMORY_QUOTA 300;</span></span></code></pre></div><p>Reserve CPU core 1 for a resource group on the coordinator host and all segment hosts:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ALTER RESOURCE GROUP rgroup5 SET CPUSET &#39;1;1&#39;;</span></span></code></pre></div><p>Set disk I/O limits for tablespaces <code>tablespace1</code> and a tablespace with oid 1663:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ALTER RESOURCE GROUP admin_group SET IO_LIMIT &#39;tablespace1:wbps=2000,wiops=2000;1663:rbps=2024,riops=2024&#39;;</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="compatibility"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;compatibility&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p>The <code>ALTER RESOURCE GROUP</code> statement is a WarehousePG extension. This command does not exist in standard PostgreSQL.</p><h2 id="see-also" tabindex="-1"><a id="see_also"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;see_also&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./CREATE_RESOURCE_GROUP.html">CREATE RESOURCE GROUP</a>, <a href="./DROP_RESOURCE_GROUP.html">DROP RESOURCE GROUP</a>, <a href="./CREATE_ROLE.html">CREATE ROLE</a>, <a href="./ALTER_ROLE.html">ALTER ROLE</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,68)]))}const g=t(i,[["render",n]]);export{h as __pageData,g as default};
