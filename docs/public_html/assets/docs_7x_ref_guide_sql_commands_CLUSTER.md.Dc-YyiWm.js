import{_ as a,c as t,o as s,ag as o}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"CLUSTER","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/CLUSTER.md","filePath":"docs/7x/ref_guide/sql_commands/CLUSTER.md"}'),i={name:"docs/7x/ref_guide/sql_commands/CLUSTER.md"};function n(r,e,d,l,c,h){return s(),t("div",null,e[0]||(e[0]=[o(`<h1 id="cluster" tabindex="-1">CLUSTER <a class="header-anchor" href="#cluster" aria-label="Permalink to &quot;CLUSTER&quot;">​</a></h1><p>Physically reorders a table on disk according to an index.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CLUSTER &lt;indexname&gt; ON &lt;table_name&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CLUSTER [VERBOSE] &lt;table_name&gt; [ USING &lt;index_name&gt; ]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CLUSTER [VERBOSE]</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>CLUSTER</code> instructs WarehousePG to order the table specified by <code>&lt;table_name&gt;</code> based on the index specified by <code>&lt;index_name&gt;</code>. The index must already have been defined on <code>&lt;table_name&gt;</code>.</p><p>WarehousePG supports <code>CLUSTER</code> operations on append-optimized tables only for B-tree indexes.</p><blockquote><p><strong>Note</strong> WarehousePG 7 does not support <code>CLUSTER</code>ing a partitioned table.</p></blockquote><p>When a table is clustered, it is physically reordered on disk based on the index information. Clustering is a one-time operation: when the table is subsequently updated, the changes are not clustered. That is, no attempt is made to store new or updated rows according to their index order. If you wish, you can periodically recluster by issuing the command again. Setting the table&#39;s <code>FILLFACTOR</code> storage parameter to less than 100% can aid in preserving cluster ordering during updates, because updated rows are kept on the same page if enough space is available there.</p><p>When a table is clustered using this command, WarehousePG remembers on which index it was clustered. The form <code>CLUSTER &lt;table_name&gt;</code> reclusters the table on the same index that it was clustered before. You can use the <code>CLUSTER</code> or <code>SET WITHOUT CLUSTER</code> forms of <a href="./ALTER_TABLE.html">ALTER TABLE</a> to set the index to use for future cluster operations, or to clear any previous setting. <code>CLUSTER</code> without any parameter reclusters all previously clustered tables in the current database that the calling user owns, or all tables if called by a superuser. This form of <code>CLUSTER</code> cannot be run inside a transaction block.</p><p>When a table is being clustered, an <code>ACCESS EXCLUSIVE</code> lock is acquired on it. This prevents any other database operations (both reads and writes) from operating on the table until the <code>CLUSTER</code> is finished.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>index_name : The name of an index.</p><p><code>VERBOSE</code> : Prints a progress report as each table is clustered.</p><p>table_name : The name (optionally schema-qualified) of a table.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>If the records you need are distributed randomly on disk, then the database has to seek across the disk to get the records requested. If those records are stored more closely together, then the fetching from disk is more sequential. In cases where you are accessing single rows randomly within a table, the actual order of the data in the table is unimportant. However, if you tend to access some data more than others, and there is an index that groups them together, you will benefit from using <code>CLUSTER</code>. If you are requesting a range of indexed values from a table, or a single indexed value that has multiple rows that match, <code>CLUSTER</code> will help because once the index identifies the table page for the first row that matches, all other rows that match are probably already on the same table page, and so you save disk accesses and speed up the query. A good example for a clustered index is on a date column where the data is ordered sequentially by date. A query against a specific date range will result in an ordered fetch from the disk, which leverages faster sequential access.</p><p><code>CLUSTER</code> can re-sort the table using either an index scan on the specified index, or (if the index is a b-tree) a sequential scan followed by sorting. It will attempt to choose the method that will be faster, based on planner cost parameters and available statistical information.</p><p>When an index scan is used, a temporary copy of the table is created that contains the table data in the index order. Temporary copies of each index on the table are created as well. Therefore, you need free space on disk at least equal to the sum of the table size and the index sizes.</p><p>When a sequential scan and sort is used, a temporary sort file is also created, so that the peak temporary space requirement is as much as double the table size, plus the index sizes. This method is often faster than the index scan method, but if the disk space requirement is intolerable, you can deactivate this choice by temporarily setting the <a href="./../config_params/guc-list.html">enable_sort</a> configuration parameter to <code>off</code>.</p><p>It is advisable to set the <a href="./../config_params/guc-list.html">maintenance_work_mem</a> configuration parameter to a reasonably large value (but not more than the amount of RAM you can dedicate to the <code>CLUSTER</code> operation) before clustering.</p><p>Because the query optimizer records statistics about the ordering of tables, it is advisable to run <a href="./ANALYZE.html">ANALYZE</a> on the newly clustered table. Otherwise, the planner may make poor query plan choices.</p><p>Because <code>CLUSTER</code> remembers which indexes are clustered, you can cluster the tables you want clustered manually the first time, then set up a periodic maintenance script that runs <code>CLUSTER</code> without any parameters, so that the desired tables are periodically reclustered.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Cluster the table <code>employees</code> on the basis of its index <code>emp_ind</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CLUSTER emp_ind ON emp;</span></span></code></pre></div><p>Cluster the <code>employees</code> table using the same index that was used before:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CLUSTER employees;</span></span></code></pre></div><p>Cluster all tables in the database that have previously been clustered:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CLUSTER;</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p>There is no <code>CLUSTER</code> statement in the SQL standard.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./CREATE_TABLE_AS.html">CREATE TABLE AS</a>, <a href="./CREATE_INDEX.html">CREATE INDEX</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,35)]))}const m=a(i,[["render",n]]);export{u as __pageData,m as default};
