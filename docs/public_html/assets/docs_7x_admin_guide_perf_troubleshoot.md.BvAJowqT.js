import{_ as t,c as a,o,ag as s}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"Investigating a Performance Problem","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/perf_troubleshoot.md","filePath":"docs/7x/admin_guide/perf_troubleshoot.md"}'),i={name:"docs/7x/admin_guide/perf_troubleshoot.md"};function n(r,e,c,l,p,d){return o(),a("div",null,e[0]||(e[0]=[s(`<h1 id="investigating-a-performance-problem" tabindex="-1">Investigating a Performance Problem <a class="header-anchor" href="#investigating-a-performance-problem" aria-label="Permalink to &quot;Investigating a Performance Problem&quot;">​</a></h1><hr><p>This section provides guidelines for identifying and troubleshooting performance problems in a WarehousePG cluster.</p><p>This topic lists steps you can take to help identify the cause of a performance problem. If the problem affects a particular workload or query, you can focus on tuning that particular workload. If the performance problem is system-wide, then hardware problems, system failures, or resource contention may be the cause.</p><p><strong>Parent topic:</strong> <a href="./performance.thml.html">Managing Performance</a></p><h2 id="checking-system-state" tabindex="-1"><a id="topic2"></a>Checking System State <a class="header-anchor" href="#checking-system-state" aria-label="Permalink to &quot;&lt;a id=&quot;topic2&quot;&gt;&lt;/a&gt;Checking System State&quot;">​</a></h2><p>Use the <code>gpstate</code> utility to identify failed segments. A WarehousePG cluster will incur performance degradation when segment instances are down because other hosts must pick up the processing responsibilities of the down segments.</p><p>Failed segments can indicate a hardware failure, such as a failed disk drive or network card. WarehousePG provides the hardware verification tool <code>gpcheckperf</code> to help identify the segment hosts with hardware issues.</p><h2 id="checking-database-activity" tabindex="-1"><a id="topic3"></a>Checking Database Activity <a class="header-anchor" href="#checking-database-activity" aria-label="Permalink to &quot;&lt;a id=&quot;topic3&quot;&gt;&lt;/a&gt;Checking Database Activity&quot;">​</a></h2><ul><li><a href="#topic4">Checking for Active Sessions (Workload)</a></li><li><a href="#topic5">Checking for Locks (Contention)</a></li><li><a href="#topic6">Checking Query Status and System Utilization</a></li></ul><h3 id="checking-for-active-sessions-workload" tabindex="-1"><a id="topic4"></a>Checking for Active Sessions (Workload) <a class="header-anchor" href="#checking-for-active-sessions-workload" aria-label="Permalink to &quot;&lt;a id=&quot;topic4&quot;&gt;&lt;/a&gt;Checking for Active Sessions \\(Workload\\)&quot;">​</a></h3><p>The <em>pg_stat_activity</em> system catalog view shows one row per server process; it shows the database OID, database name, process ID, user OID, user name, current query, time at which the current query began execution, time at which the process was started, client address, and port number. To obtain the most information about the current system workload, query this view as the database superuser. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * FROM pg_stat_activity;</span></span></code></pre></div><p>Note that the information does not update instantaneously.</p><h3 id="checking-for-locks-contention" tabindex="-1"><a id="topic5"></a>Checking for Locks (Contention) <a class="header-anchor" href="#checking-for-locks-contention" aria-label="Permalink to &quot;&lt;a id=&quot;topic5&quot;&gt;&lt;/a&gt;Checking for Locks \\(Contention\\)&quot;">​</a></h3><p>The <em>pg_locks</em> system catalog view allows you to see information about outstanding locks. If a transaction is holding a lock on an object, any other queries must wait for that lock to be released before they can continue. This may appear to the user as if a query is hanging.</p><p>Examine <em>pg_locks</em> for ungranted locks to help identify contention between database client sessions. <em>pg_locks</em> provides a global view of all locks in the database system, not only those relevant to the current database. You can join its relation column against <code>pg_class.oid</code> to identify locked relations (such as tables), but this works correctly only for relations in the current database. You can join the <code>pid</code> column to the <code>pg_stat_activity.pid</code> to see more information about the session holding or waiting to hold a lock. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT locktype, database, c.relname, l.relation, </span></span>
<span class="line"><span>l.transactionid, l.pid, l.mode, l.granted, </span></span>
<span class="line"><span>a.query </span></span>
<span class="line"><span>        FROM pg_locks l, pg_class c, pg_stat_activity a </span></span>
<span class="line"><span>        WHERE l.relation=c.oid AND l.pid=a.pid</span></span>
<span class="line"><span>        ORDER BY c.relname;</span></span></code></pre></div><p>If you use resource groups, queries that are waiting will also show in <em>pg_locks</em>. To see how many queries are waiting to run in a resource group, use the<em>gp_resgroup_status</em>system catalog view. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * FROM gp_toolkit.gp_resgroup_status;</span></span></code></pre></div><p>Similarly, if you use resource queues, queries that are waiting in a queue also show in <em>pg_locks</em>. To see how many queries are waiting to run from a resource queue, use the<em>gp_resqueue_status</em>system catalog view. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * FROM gp_toolkit.gp_resqueue_status;</span></span></code></pre></div><h3 id="checking-query-status-and-system-utilization" tabindex="-1"><a id="topic6"></a>Checking Query Status and System Utilization <a class="header-anchor" href="#checking-query-status-and-system-utilization" aria-label="Permalink to &quot;&lt;a id=&quot;topic6&quot;&gt;&lt;/a&gt;Checking Query Status and System Utilization&quot;">​</a></h3><p>You can use system monitoring utilities such as <code>ps</code>, <code>top</code>, <code>iostat</code>, <code>vmstat</code>, <code>netstat</code> and so on to monitor database activity on the hosts in your WarehousePG cluster. These tools can help identify WarehousePG processes (<code>postgres</code> processes) currently running on the system and the most resource intensive tasks with regards to CPU, memory, disk I/O, or network activity. Look at these system statistics to identify queries that degrade database performance by overloading the system and consuming excessive resources. WarehousePG&#39;s management tool <code>gpssh</code> allows you to run these system monitoring commands on several hosts simultaneously.</p><p>You can create and use the WarehousePG <em>session_level_memory_consumption</em> view that provides information about the current memory utilization and idle time for sessions that are running queries on WarehousePG. For information about the view, see <a href="./managing/monitor.html">Viewing Session Memory Usage Information</a>.</p><h2 id="troubleshooting-problem-queries" tabindex="-1"><a id="topic7"></a>Troubleshooting Problem Queries <a class="header-anchor" href="#troubleshooting-problem-queries" aria-label="Permalink to &quot;&lt;a id=&quot;topic7&quot;&gt;&lt;/a&gt;Troubleshooting Problem Queries&quot;">​</a></h2><p>If a query performs poorly, look at its query plan to help identify problems. The <code>EXPLAIN</code> command shows the query plan for a given query. See <a href="./query/topics/query-profiling.html">Query Profiling</a> for more information about reading query plans and identifying problems.</p><p>When an out of memory event occurs during query execution, the WarehousePG memory accounting framework reports detailed memory consumption of every query running at the time of the event. The information is written to the WarehousePG segment logs.</p><h2 id="investigating-error-messages" tabindex="-1"><a id="topic8"></a>Investigating Error Messages <a class="header-anchor" href="#investigating-error-messages" aria-label="Permalink to &quot;&lt;a id=&quot;topic8&quot;&gt;&lt;/a&gt;Investigating Error Messages&quot;">​</a></h2><p>WarehousePG log messages are written to files in the <code>log</code> directory within the coordinator&#39;s or segment&#39;s data directory. Because the coordinator log file contains the most information, you should always check it first. Log files roll over daily and use the naming convention: <code>gpdb-</code><em><code>YYYY</code></em><code>-</code><em><code>MM</code></em><code>-</code><em><code>DD_hhmmss.csv</code></em>. To locate the log files on the coordinator host:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ cd $COORDINATOR_DATA_DIRECTORY/log</span></span></code></pre></div><p>Log lines have the format of:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;timestamp&gt; | &lt;user&gt; | &lt;database&gt; | &lt;statement_id&gt; | &lt;con#&gt;&lt;cmd#&gt; </span></span>
<span class="line"><span>|:-&lt;LOG_LEVEL&gt;: &lt;log_message&gt;</span></span></code></pre></div><p>You may want to focus your search for <code>WARNING</code>, <code>ERROR</code>, <code>FATAL</code> or <code>PANIC</code> log level messages. You can use the WarehousePG utility <code>gplogfilter</code> to search through WarehousePG log files. For example, when you run the following command on the coordinator host, it checks for problem log messages in the standard logging locations:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gplogfilter -t</span></span></code></pre></div><p>To search for related log entries in the segment log files, you can run <code>gplogfilter</code> on the segment hosts using <code>gpssh</code>. You can identify corresponding log entries by the <em><code>statement_id</code></em> or <em><code>con</code></em><code>#</code> (session identifier). For example, to search for log messages in the segment log files containing the string <code>con6</code> and save output to a file:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpssh -f seg_hosts_file -e &#39;source </span></span>
<span class="line"><span>/usr/local/greenplum-db/greenplum_path.sh ; gplogfilter -f </span></span>
<span class="line"><span>con6 /gpdata/*/log/gpdb*.csv&#39; &gt; seglog.out</span></span></code></pre></div>`,37)]))}const g=t(i,[["render",n]]);export{u as __pageData,g as default};
