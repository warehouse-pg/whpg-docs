import{_ as a,c as t,o as s,ag as o}from"./chunks/framework.Ds6Eueu6.js";const m=JSON.parse('{"title":"CREATE DATABASE","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/CREATE_DATABASE.md","filePath":"docs/7x/ref_guide/sql_commands/CREATE_DATABASE.md"}'),n={name:"docs/7x/ref_guide/sql_commands/CREATE_DATABASE.md"};function i(c,e,d,l,r,p){return s(),t("div",null,e[0]||(e[0]=[o(`<h1 id="create-database" tabindex="-1">CREATE DATABASE <a class="header-anchor" href="#create-database" aria-label="Permalink to &quot;CREATE DATABASE&quot;">​</a></h1><p>Creates a new database.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE DATABASE name</span></span>
<span class="line"><span>    [ [WITH] [OWNER [=] &lt;user_name&gt;]</span></span>
<span class="line"><span>          [TEMPLATE [=] &lt;template&gt;]</span></span>
<span class="line"><span>          [ENCODING [=] &lt;encoding&gt;]</span></span>
<span class="line"><span>          [LC_COLLATE [=] &lt;lc_collate&gt;]</span></span>
<span class="line"><span>          [LC_CTYPE [=] &lt;lc_ctype&gt;]</span></span>
<span class="line"><span>          [TABLESPACE [=] &lt;tablespace_name&gt;]</span></span>
<span class="line"><span>          [ALLOW_CONNECTIONS [=] &lt;allowconn&gt;]</span></span>
<span class="line"><span>          [CONNECTION LIMIT [=] &lt;connlimit&gt; ]</span></span>
<span class="line"><span>          [IS_TEMPLATE [=] &lt;istemplate&gt; ] ]</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>CREATE DATABASE</code> creates a new database.</p><p>To create a database, you must be a superuser or have the special <code>CREATEDB</code> privilege. See <a href="./CREATE_ROLE.html">CREATE ROLE</a>.</p><p>The creator becomes the owner of the new database by default. Superusers can create databases owned by other users by using the <code>OWNER</code> clause. They can even create databases owned by users with no special privileges. Non-superusers with <code>CREATEDB</code> privilege can only create databases owned by themselves.</p><p>By default, the new database will be created by cloning the standard system database <code>template1</code>. A different template can be specified by writing <code>TEMPLATE name</code>. In particular, by writing <code>TEMPLATE template0</code>, you can create a clean database containing only the standard objects predefined by WarehousePG. This is useful if you wish to avoid copying any installation-local objects that may have been added to <code>template1</code>.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>name : The name of a database to create.</p><p>user_name : The name of the database user who will own the new database, or <code>DEFAULT</code> to use the default owner (the user running the command). To create a database owned by another role, you must be a direct or indirect member of that role, or be a superuser.</p><p>template : The name of the template from which to create the new database, or <code>DEFAULT</code> to use the default template (template1).</p><p>encoding : Character set encoding to use in the new database. Specify a string constant (such as <code>&#39;SQL_ASCII&#39;</code>), an integer encoding number, or <code>DEFAULT</code> to use the default encoding (namely, the encoding of the template database). For more information, see <a href="./../character_sets.html">Character Set Support</a>. Refer to the <a href="#section5">Notes</a> for additional restrictions.</p><p>lc_collate : The collation order (<code>LC_COLLATE</code>) to use in the new database. This affects the sort order applied to strings, e.g. in queries with <code>ORDER BY</code>, as well as the order used in indexes on text columns. The default is to use the collation order of the template database. See the <a href="#section5">Notes</a> for additional restrictions.</p><p>lc_ctype : The character classification (<code>LC_CTYPE</code>) to use in the new database. This affects the categorization of characters, e.g. lower, upper, and digit. The default is to use the character classification of the template database. See the <a href="#section5">Notes</a> for additional restrictions.</p><p>tablespace_name : The name of the tablespace that will be associated with the new database, or <code>DEFAULT</code> to use the template database&#39;s tablespace. This tablespace will be the default tablespace used for objects created in this database. See <a href="./CREATE_TABLESPACE.html">CREATE TABLESPACE</a> for more information.</p><p>allowconn : If <code>false</code>, then no one can connect to this database. The default is <code>true</code>, allowing connections (except as restricted by other mechanisms, such as <code>GRANT/REVOKE CONNECT</code>).</p><p>connlimit : The maximum number of concurrent connections allowed to this database on the coordinator. The default is <code>-1</code>, no limit. WarehousePG superusers are exempt from this limit.</p><p>istemplate : If <code>true</code>, then this database can be cloned by any user with <code>CREATEDB</code> privileges; if <code>false</code> (the default), then only superusers or the owner of the database can clone it. Note that template databases cannot be dropped.</p><p>Optional parameters can be written in any order, not only the order illustrated above.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p><code>CREATE DATABASE</code> cannot be run inside a transaction block.</p><p>Errors along the line of “could not initialize database directory” are most likely related to insufficient permissions on the data directory, a full disk, or other file system problems.</p><p>Use <a href="./DROP_DATABASE.HTML.html">DROP DATABASE</a> to remove a database.</p><p>The program <a href="./../../utility_guide/ref/createdb.html">createdb</a> is a wrapper program around this command, provided for convenience.</p><p>Database-level configuration parameters (set via <a href="./ALTER_DATABASE.html">ALTER DATABASE</a> and database-level permissions (set via <a href="./GRANT.html">GRANT</a>) are not copied from the template database.</p><p>Although it is possible to copy a database other than <code>template1</code> by specifying its name as the template, this is not (yet) intended as a general-purpose <code>“COPY DATABASE”</code> facility. The principal limitation is that no other sessions can be connected to the template database while it is being copied. <code>CREATE DATABASE</code> will fail if any other connection exists when it starts; otherwise, new connections to the template database are locked out until <code>CREATE DATABASE</code> completes.</p><p>The character set encoding specified for the new database must be compatible with the chosen locale settings (<code>LC_COLLATE</code> and <code>LC_CTYPE</code>). If the locale is <code>C</code> (or equivalently <code>POSIX</code>), then all encodings are allowed, but for other locale settings there is only one encoding that will work properly. <code>CREATE DATABASE</code> will allow superusers to specify <code>SQL_ASCII</code> encoding regardless of the locale settings, but this choice is deprecated and may result in misbehavior of character-string functions if data that is not encoding-compatible with the locale is stored in the database.</p><p>The encoding and locale settings must match those of the template database, except when <code>template0</code> is used as template. This is because other databases might contain data that does not match the specified encoding, or might contain indexes whose ordering is affected by <code>LC_COLLATE</code> and <code>LC_CTYPE</code>. Copying such data would result in a database that is corrupt according to the new settings. <code>template0</code>, however, is known to not contain any data or indexes that would be affected.</p><p>The <code>CONNECTION LIMIT</code> option is enforced approximately; if two new sessions start at about the same time when just one connection “slot” remains for the database, it is possible that both will fail. Also, the limit is not enforced against superusers or background worker processes.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>To create a new database:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE DATABASE gpdb;</span></span></code></pre></div><p>To create a database <code>sales</code> owned by user <code>salesapp</code> with a default tablespace of <code>salesspace</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE DATABASE sales OWNER salesapp TABLESPACE salesspace;</span></span></code></pre></div><p>To create a database <code>music</code> with a different locale:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE DATABASE music</span></span>
<span class="line"><span>    LC_COLLATE &#39;sv_SE.utf8&#39; LC_CTYPE &#39;sv_SE.utf8&#39;</span></span>
<span class="line"><span>    TEMPLATE template0;</span></span></code></pre></div><p>In this example, the <code>TEMPLATE template0</code> clause is required if the specialized locale is different from the one in <code>template1</code>. (If it is not, then specifying the locale explicitly is redundant.)</p><p>To create a database <code>music2</code> with a different locale and a different character set encoding:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE DATABASE music2</span></span>
<span class="line"><span>    LC_COLLATE &#39;sv_SE.iso885915&#39; LC_CTYPE &#39;sv_SE.iso885915&#39;</span></span>
<span class="line"><span>    ENCODING LATIN9</span></span>
<span class="line"><span>    TEMPLATE template0;</span></span></code></pre></div><p>The specified locale and encoding settings must match, or an error will be reported.</p><p>Note that locale names are specific to the operating system, so that the above commands might not work in the same way everywhere.</p><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p>There is no <code>CREATE DATABASE</code> statement in the SQL standard. Databases are equivalent to catalogs, whose creation is implementation-defined.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./ALTER_DATABASE.html">ALTER DATABASE</a>, <a href="./DROP_DATABASE.html">DROP DATABASE</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,48)]))}const u=a(n,[["render",i]]);export{m as __pageData,u as default};
