import{_ as t,c as a,o as s,ag as o}from"./chunks/framework.Ds6Eueu6.js";const m=JSON.parse('{"title":"CREATE TABLESPACE","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/CREATE_TABLESPACE.md","filePath":"docs/7x/ref_guide/sql_commands/CREATE_TABLESPACE.md"}'),i={name:"docs/7x/ref_guide/sql_commands/CREATE_TABLESPACE.md"};function n(c,e,r,l,p,d){return s(),a("div",null,e[0]||(e[0]=[o(`<h1 id="create-tablespace" tabindex="-1">CREATE TABLESPACE <a class="header-anchor" href="#create-tablespace" aria-label="Permalink to &quot;CREATE TABLESPACE&quot;">​</a></h1><p>Defines a new tablespace.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE TABLESPACE &lt;tablespace_name&gt;</span></span>
<span class="line"><span>   [ OWNER { &lt;owner_name&gt; | CURRENT_USER | SESSION_USER } ]</span></span>
<span class="line"><span>   LOCATION &#39;&lt;directory&gt;&#39; </span></span>
<span class="line"><span>   [ WITH (content&lt;ID_1&gt;=&#39;&lt;directory&gt;&#39;[, content&lt;ID_2&gt;=&#39;&lt;directory&gt;&#39; ... ] [, &lt;tablespace_option = value [, ... ] ] ) ]</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>CREATE TABLESPACE</code> registers and configures a new tablespace for your WarehousePG cluster. The tablespace name must be distinct from the name of any existing tablespace in the system. A tablespace is a WarehousePG cluster object (a global object), you can use a tablespace from any database if you have appropriate privileges.</p><p>A tablespace allows superusers to define an alternative host file system location where the data files containing database objects (such as tables and indexes) reside.</p><p>A user with appropriate privileges can pass tablespace_name to <a href="./CREATE_DATABASE.html">CREATE DATABASE</a>, <a href="./CREATE_TABLE.html">CREATE TABLE</a>, or <a href="./CREATE_INDEX.html">CREATE INDEX</a> to direct WarehousePG to store the data files for these objects within the specified tablespace.</p><p>In WarehousePG, the file system location must exist on all hosts including the hosts running the coordinator, standby mirror, each primary segment, and each mirror segment.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>tablespace_name : The name of a tablespace to be created. The name cannot begin with <code>pg_</code> or <code>gp_</code>, as such names are reserved for system tablespaces.</p><p>owner_name : The name of the user who will own the tablespace. If omitted, defaults to the user running the command. Only superusers can create tablespaces, but they can assign ownership of tablespaces to non-superusers.</p><p>LOCATION &#39;directory&#39; : The directory that will be used for the tablespace. The directory should be empty and must be owned by the WarehousePG cluster user. You must specify the absolute path of the directory, and the path name must not be greater than 100 characters in length. (The location is used to create a symlink target in the pg_tblspc directory, and symlink targets are truncated to 100 characters when sending to <code>tar</code> from utilities such as <code>pg_basebackup</code>.)</p><p>: You can specify a different tablespace directory for any WarehousePG segment instance in the <code>WITH</code> clause.</p><p>contentID_i=&#39;directory_i&#39; : The value ID_i is the content ID for the segment instance. directory_i is the absolute path to the host system file location that the segment instance uses as the root directory for the tablespace. You cannot specify the content ID of the coordinator instance (<code>-1</code>). You can specify the same directory for multiple segments.</p><p>: If a segment instance is not listed in the <code>WITH</code> clause, WarehousePG uses the tablespace directory specified in the <code>LOCATION</code> clause.</p><p>: The restrictions identified for the <code>LOCATION</code> directory also hold for directory_i.</p><p>tablespace_option : A tablespace parameter to set or reset. Currently, the only available parameters are <code>seq_page_cost</code> and <code>random_page_cost</code>. Setting either value for a particular tablespace will override the planner&#39;s usual estimate of the cost of reading pages from tables in that tablespace, as established by the configuration parameters of the same name (see <a href="./../config_params/guc-list.html#seq_page_cost">seq_page_cost</a>, <a href="./../config_params/guc-list.html#random_page_cost">random_page_cost</a>). This may be useful if one tablespace is located on a disk which is faster or slower than the remainder of the I/O subsystem.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>Because <code>CREATE TABLESPACE</code> creates symbolic links from the <code>pg_tblspc</code> directory in the coordinator and segment instance data directory to the directories specified in the command, WarehousePG supports tablespaces only on systems that support symbolic links.</p><p>You cannot run <code>CREATE TABLESPACE</code> inside a transaction block.</p><p>When creating tablespaces, ensure that file system locations have sufficient I/O speed and available disk space.</p><blockquote><p><strong>Note</strong> WarehousePG does not support different tablespace locations for a primary-mirror pair with the same content ID. It is only possible to configure different locations for different content IDs. Do not modify symbolic links under the <code>pg_tblspc</code> directory so that primary-mirror pairs point to different file locations; this will lead to erroneous behavior.</p></blockquote><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Create a new tablespace and specify the file system location for the coordinator and all segment instances:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE TABLESPACE mytblspace LOCATION &#39;/gpdbtspc/mytestspace&#39;;</span></span></code></pre></div><p>Create a tablespace <code>indexspace</code> at <code>/data/indexes</code> owned by user <code>genevieve</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE TABLESPACE indexspace OWNER genevieve LOCATION &#39;/data/indexes&#39;;</span></span></code></pre></div><p>Create a new tablespace and specify a location for segment instances with content ID 0 and 1. For the coordinator and segment instances not listed in the <code>WITH</code> clause, the file system location for the tablespace is the directory specified in the <code>LOCATION</code> clause.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE TABLESPACE mytblspace LOCATION &#39;/gpdbtspc/mytestspace&#39; WITH (content0=&#39;/temp/mytest&#39;, content1=&#39;/temp/mytest&#39;);</span></span></code></pre></div><p>The example specifies the same location for the two segment instances. You can a specify different location for each segment.</p><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p><code>CREATE TABLESPACE</code> is a WarehousePG extension.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./CREATE_DATABASE.html">CREATE DATABASE</a>, <a href="./CREATE_TABLE.html">CREATE TABLE</a>, <a href="./CREATE_INDEX.html">CREATE INDEX</a>, <a href="./DROP_TABLESPACE.html">DROP TABLESPACE</a>, <a href="./ALTER_TABLESPACE.html">ALTER TABLESPACE</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,36)]))}const u=t(i,[["render",n]]);export{m as __pageData,u as default};
