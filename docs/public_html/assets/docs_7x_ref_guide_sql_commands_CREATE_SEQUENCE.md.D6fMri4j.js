import{_ as a,c as t,o as s,ag as n}from"./chunks/framework.Ds6Eueu6.js";const p=JSON.parse('{"title":"CREATE SEQUENCE","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/CREATE_SEQUENCE.md","filePath":"docs/7x/ref_guide/sql_commands/CREATE_SEQUENCE.md"}'),o={name:"docs/7x/ref_guide/sql_commands/CREATE_SEQUENCE.md"};function i(c,e,l,d,r,u){return s(),t("div",null,e[0]||(e[0]=[n(`<h1 id="create-sequence" tabindex="-1">CREATE SEQUENCE <a class="header-anchor" href="#create-sequence" aria-label="Permalink to &quot;CREATE SEQUENCE&quot;">​</a></h1><p>Defines a new sequence generator.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE [TEMPORARY | TEMP] SEQUENCE [IF NOT EXISTS] &lt;name&gt;</span></span>
<span class="line"><span>       [ AS data_type ]</span></span>
<span class="line"><span>       [INCREMENT [BY] &lt;increment&gt;] </span></span>
<span class="line"><span>       [MINVALUE &lt;minvalue&gt; | NO MINVALUE] </span></span>
<span class="line"><span>       [MAXVALUE &lt;maxvalue&gt; | NO MAXVALUE] </span></span>
<span class="line"><span>       [START [ WITH ] &lt;start&gt;] </span></span>
<span class="line"><span>       [CACHE &lt;cache&gt;] </span></span>
<span class="line"><span>       [[NO] CYCLE] </span></span>
<span class="line"><span>       [OWNED BY { &lt;table_name&gt;.&lt;column_name&gt; | NONE }]</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>CREATE SEQUENCE</code> creates a new sequence number generator. This involves creating and initializing a new special single-row table with the same name. The generator will be owned by the user issuing the command.</p><p>If a schema name is given, then the sequence is created in the specified schema. Otherwise it is created in the current schema. Temporary sequences exist in a special schema, so you may not provide a schema name when creating a temporary sequence. The sequence name must be distinct from the name of any other sequence, table, index, view, or foreign table in the same schema.</p><p>After a sequence is created, you use the <code>nextval()</code> function to operate on the sequence. For example, to insert a row into a table that gets the next value of a sequence:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INSERT INTO distributors VALUES (nextval(&#39;myserial&#39;), &#39;acme&#39;);</span></span></code></pre></div><p>You can also use the function <code>setval()</code> to operate on a sequence, but only for queries that do not operate on distributed data. For example, the following query is allowed because it resets the sequence counter value for the sequence generator process on the coordinator:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT setval(&#39;myserial&#39;, 201);</span></span></code></pre></div><p>But the following query will be rejected in WarehousePG because it operates on distributed data:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INSERT INTO product VALUES (setval(&#39;myserial&#39;, 201), &#39;gizmo&#39;);</span></span></code></pre></div><p>In a regular (non-distributed) database, functions that operate on the sequence go to the local sequence table to get values as they are needed. In WarehousePG, however, keep in mind that each segment is its own distinct database process. Therefore the segments need a single point of truth to go for sequence values so that all segments get incremented correctly and the sequence moves forward in the right order. A sequence server process runs on the coordinator and is the point-of-truth for a sequence in a WarehousePG distributed database. Segments get sequence values at runtime from the coordinator.</p><p>Because of this distributed sequence design, there are some limitations on the functions that operate on a sequence in WarehousePG:</p><ul><li><code>lastval()</code> and <code>currval()</code> functions are not supported.</li><li><code>setval()</code> can only be used to set the value of the sequence generator on the coordinator, it cannot be used in subqueries to update records on distributed table data.</li><li><code>nextval()</code> sometimes grabs a block of values from the coordinator for a segment to use, depending on the query. So values may sometimes be skipped in the sequence if all of the block turns out not to be needed at the segment level. Note that a regular PostgreSQL database does this too, so this is not something unique to WarehousePG.</li></ul><blockquote><p><strong>Note</strong> The default sequence cache size in WarehousePG is <code>20</code>.</p></blockquote><p>Although you cannot update a sequence directly, you can use a query like:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * FROM &lt;sequence_name&gt;;</span></span></code></pre></div><p>to examine the parameters and current state of a sequence. In particular, the last_value field of the sequence shows the last value allocated by any session. (Note that this value might be obsolete by the time it&#39;s printed, if other sessions are actively doing <code>nextval()</code> calls.)</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>TEMPORARY | TEMP : If specified, the sequence object is created only for this session, and is automatically dropped on session exit. Existing permanent sequences with the same name are not visible (in this session) while the temporary sequence exists, unless they are referenced with schema-qualified names.</p><p>IF NOT EXISTS : Do not throw an error if a relation with the same name already exists. WarehousePG issues a notice in this case. Note that there is no guarantee that the existing relation is anything like the sequence that would have been created - it might not even be a sequence.</p><p>name : The name (optionally schema-qualified) of the sequence to be created.</p><p>data_type : The optional clause <code>AS data_type</code> specifies the data type of the sequence. Valid types are <code>smallint</code>, <code>integer</code>, and <code>bigint</code>. <code>bigint</code> is the default. The data type determines the default minimum and maximum values of the sequence.</p><p>increment : Specifies which value is added to the current sequence value to create a new value. A positive value will make an ascending sequence, a negative one a descending sequence. The default value is <code>1</code>.</p><p>minvalue NO MINVALUE : Determines the minimum value a sequence can generate. If this clause is not supplied or <code>NO MINVALUE</code> is specified, then the default values will be used. The default for an ascending sequence is the maximum value of the data type. The default for a descending sequence is <code>-1</code>.</p><p>maxvalue NO MAXVALUE : Determines the maximum value for the sequence. If this clause is not supplied or <code>NO MAXVALUE</code> is specified, then default values will be used. The defaults are 263-1 and -1 for ascending and descending sequences, respectively.</p><p>start : Allows the sequence to begin anywhere. The default starting value is <code>minvalue</code> for ascending sequences and <code>maxvalue</code> for descending ones.</p><p>cache : Specifies how many sequence numbers are to be preallocated and stored in memory for faster access. The default value is 20. The minimum value is 1 (no cache). : &gt; <strong>Note</strong> When operating with a cache of sequence numbers (<code>cache &gt; 1</code>), WarehousePG may discard some cached sequence values. If you require consecutive values, you must explicitly set <code>CACHE 1</code> when you create or alter the sequence.</p><p>CYCLE NO CYCLE : Allows the sequence to wrap around when the <code>maxvalue</code> (for ascending) or <code>minvalue</code> (for descending) has been reached. If the limit is reached, the next number generated will be the <code>minvalue</code> (for ascending) or <code>maxvalue</code> (for descending). If <code>NO CYCLE</code> is specified, any calls to <code>nextval()</code> after the sequence has reached its maximum value will return an error. If neither <code>CYCLE</code> or <code>NO CYCLE</code> are specified, <code>NO CYCLE</code> is the default.</p><p>OWNED BY table_name.colume_name OWNED BY NONE : Causes the sequence to be associated with a specific table column, such that if that column (or its whole table) is dropped, the sequence will be automatically dropped as well. The specified table must have the same owner and be in the same schema as the sequence. <code>OWNED BY NONE</code>, the default, specifies that there is no such association.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>Use <code>DROP SEQUENCE</code> to remove a sequence.</p><p>Sequences are based on bigint arithmetic, so the range cannot exceed the range of an eight-byte integer (-9223372036854775808 to 9223372036854775807).</p><p>Because <code>nextval()</code> and <code>setval()</code> calls are never rolled back, sequence objects cannot be used if &quot;gapless&quot; assignment of sequence numbers is needed. It is possible to build gapless assignment by using exclusive locking of a table containing a counter; but this solution is much more expensive than sequence objects, especially if many transactions need sequence numbers concurrently.</p><p>Although multiple sessions are guaranteed to allocate distinct sequence values, the values may be generated out of sequence when all the sessions are considered. For example, with a <code>cache</code> setting of <code>10</code>, session A might reserve values 1..10 and return <code>nextval=1</code>, then session B might reserve values 11..20 and return <code>nextval=11</code> before session A has generated nextval=2. Thus, with a <code>cache</code> setting of one it is safe to assume that <code>nextval()</code> values are generated sequentially; with a <code>cache</code> setting greater than one you should only assume that the <code>nextval()</code> values are all distinct, not that they are generated purely sequentially.</p><p>Another consideration is that a <code>setval()</code> executed on such a sequence will not be noticed by other sessions until they have used up any preallocated values they have cached.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Create an ascending sequence named <code>myseq</code>, starting at 101:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE SEQUENCE myseq START 101;</span></span></code></pre></div><p>Insert a row into a table that gets the next value of the sequence named <code>myseq</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INSERT INTO distributors VALUES (nextval(&#39;myseq&#39;), &#39;acme&#39;);</span></span></code></pre></div><p>Reset the sequence counter value on the WarehousePG coordinator:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT setval(&#39;myseq&#39;, 201);</span></span></code></pre></div><p>Illegal use of <code>setval()</code> in WarehousePG (setting sequence values on distributed data):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INSERT INTO product VALUES (setval(&#39;myseq&#39;, 201), &#39;gizmo&#39;);</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p><code>CREATE SEQUENCE</code> conforms to the SQL standard, with the following exceptions:</p><ul><li>You obtain the next value using the <code>nextval()</code> function instead of the <code>NEXT VALUE FOR</code> expression specified in the SQL standard.</li><li>The <code>OWNED BY</code> clause is a WarehousePG extension.</li></ul><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./ALTER_SEQUENCE.html">ALTER SEQUENCE</a>, <a href="./DROP_SEQUENCE.html">DROP SEQUENCE</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,53)]))}const m=a(o,[["render",i]]);export{p as __pageData,m as default};
