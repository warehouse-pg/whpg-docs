import{_ as e,c as a,o,ag as n}from"./chunks/framework.Ds6Eueu6.js";const p=JSON.parse('{"title":"pgbouncer-admin","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/pgbouncer-admin.md","filePath":"docs/7x/utility_guide/ref/pgbouncer-admin.md"}'),d={name:"docs/7x/utility_guide/ref/pgbouncer-admin.md"};function r(s,t,i,c,l,h){return o(),a("div",null,t[0]||(t[0]=[n(`<h1 id="pgbouncer-admin" tabindex="-1">pgbouncer-admin <a class="header-anchor" href="#pgbouncer-admin" aria-label="Permalink to &quot;pgbouncer-admin&quot;">​</a></h1><hr><p>PgBouncer Administration Console.</p><h2 id="synopsis" tabindex="-1"><a id="syn"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;syn&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>psql -p &lt;port&gt; pgbouncer</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="desc"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;desc&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>The PgBouncer Administration Console is available via <code>psql</code>. Connect to the PgBouncer <code>&lt;port&gt;</code> and the virtual database named <code>pgbouncer</code> to log in to the console.</p><p>Users listed in the <code>pgbouncer.ini</code> configuration parameters <code>admin_users</code> and <code>stats_users</code> have privileges to log in to the PgBouncer Administration Console. When <code>auth_type=any</code>, then any user is allowed in as a stats_user.</p><p>Additionally, the user name <code>pgbouncer</code> is allowed to log in without password when the login comes via the Unix socket and the client has same Unix user UID as the running process.</p><p>The admin console currently only supports the simple query protocol. Some drivers use the extended query protocol for all commands; these drivers will not work for this console.</p><p>You can control connections between PgBouncer and WarehousePG from the console. You can also set PgBouncer configuration parameters.</p><h2 id="options" tabindex="-1"><a id="opt"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;opt&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>-p &lt;port&gt; : The PgBouncer port number.</p><h2 id="command-syntax" tabindex="-1"><a id="topic_bk3_3jc_dt"></a>Command Syntax <a class="header-anchor" href="#command-syntax" aria-label="Permalink to &quot;&lt;a id=&quot;topic_bk3_3jc_dt&quot;&gt;&lt;/a&gt;Command Syntax&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>pgbouncer=# SHOW help;</span></span>
<span class="line"><span>NOTICE:  Console usage</span></span>
<span class="line"><span>DETAIL:  </span></span>
<span class="line"><span>  SHOW HELP|CONFIG|DATABASES|POOLS|CLIENTS|SERVERS|USERS|VERSION</span></span>
<span class="line"><span>  SHOW PEERS|PEER_POOLS</span></span>
<span class="line"><span>  SHOW FDS|SOCKETS|ACTIVE_SOCKETS|LISTS|MEM|STATE</span></span>
<span class="line"><span>  SHOW DNS_HOSTS|DNS_ZONES</span></span>
<span class="line"><span>  SHOW STATS|STATS_TOTALS|STATS_AVERAGES|TOTALS</span></span>
<span class="line"><span>  SET key = arg</span></span>
<span class="line"><span>  RELOAD</span></span>
<span class="line"><span>  PAUSE [&lt;db&gt;]</span></span>
<span class="line"><span>  RESUME [&lt;db&gt;]</span></span>
<span class="line"><span>  DISABLE &lt;db&gt;</span></span>
<span class="line"><span>  ENABLE &lt;db&gt;</span></span>
<span class="line"><span>  RECONNECT [&lt;db&gt;]</span></span>
<span class="line"><span>  KILL &lt;db&gt;</span></span>
<span class="line"><span>  SUSPEND</span></span>
<span class="line"><span>  SHUTDOWN</span></span>
<span class="line"><span>  WAIT_CLOSE [&lt;db&gt;]</span></span></code></pre></div><h2 id="administration-commands" tabindex="-1"><a id="topic_bf5_jcl_gs"></a>Administration Commands <a class="header-anchor" href="#administration-commands" aria-label="Permalink to &quot;&lt;a id=&quot;topic_bf5_jcl_gs&quot;&gt;&lt;/a&gt;Administration Commands&quot;">​</a></h2><p>The following PgBouncer administration commands control the running <code>pgbouncer</code> process.</p><p>PAUSE [&lt;db&gt;] : If no database is specified, PgBouncer tries to disconnect from all servers, first waiting for all queries to be released according to the server pool’s pooling mode (in transaction pooling mode, the transaction must complete, in statement mode, the statement must complete, and in session pooling mode the client must disconnect). The command will not return before all queries are finished. This command is to be used to prepare to restart the database.</p><p>: If a database name is specified, PgBouncer pauses only that database.</p><p>: New client connections to a paused database will wait until a <code>RESUME</code> command is invoked.</p><p>DISABLE &lt;db&gt; : Reject all new client connections on the database.</p><p>ENABLE &lt;db&gt; : Allow new client connections after a previous <code>DISABLE</code> command.</p><p>RECONNECT : Close each open server connection for the given database, or all databases, after it is released (according to the pooling mode), even if its lifetime is not up yet. New server connections can be made immediately and will connect as necessary according to the pool size settings.</p><p>: This command is useful when the server connection setup has changed, for example to perform a gradual switchover to a new server. It is not necessary to run this command when the connection string in <code>pgbouncer.ini</code> has been changed and reloaded (see <code>RELOAD</code>) or when DNS resolution has changed, because then the equivalent of this command will be run automatically. This command is only necessary if something downstream of PgBouncer routes the connections.</p><p>: After this command is run, there could be an extended period where some server connections go to an old destination and some server connections go to a new destination. This is likely only sensible when switching read-only traffic between read-only replicas, or when switching between nodes of a multimaster replication setup. If all connections need to be switched at the same time, <code>PAUSE</code> is recommended instead. To close server connections without waiting (for example, in emergency failover rather than gradual switchover scenarios), also consider <code>KILL</code>.</p><p>KILL &lt;db&gt; : Immediately drop all client and server connections to the named database.</p><p>: New client connections to a killed database will wait until <code>RESUME</code> is called.</p><p>SUSPEND : All socket buffers are flushed and PgBouncer stops listening for data on them. The command will not return before all buffers are empty. To be used when rebooting PgBouncer online.</p><p>: New client connections to a suspended database will wait until <code>RESUME</code> is called.</p><p>RESUME [&lt;db&gt;] : Resume work from a previous <code>KILL</code>, <code>PAUSE</code>, or <code>SUSPEND</code> command.</p><p>SHUTDOWN : The PgBouncer process will exit. To exit from the <code>psql</code> command line session, enter <code>\\q</code>.</p><p>RELOAD : The PgBouncer process reloads the current configuration file and updates the changeable settings.</p><p>: PgBouncer notices when a configuration file reload changes the connection parameters of a database definition. An existing server connection to the old destination will be closed when the server connection is next released (according to the pooling mode), and new server connections will immediately use the updated connection parameters</p><p>WAIT_CLOSE [&lt;db&gt;] : Wait until all server connections, either of the specified database or of all databases, have cleared the “close_needed” state (see <code>SHOW SERVERS</code>). This can be called after a <code>RECONNECT</code> or <code>RELOAD</code> to wait until the respective configuration change has been fully activated, for example in switchover scripts.</p><p>SET key = value : Changes the specified configuration setting. See the <a href="#CONFIG"><code>SHOW CONFIG;</code></a> command.</p><p>: (Note that this command is run on the PgBouncer admin console and sets PgBouncer settings. A <code>SET</code> command run on another database will be passed to the PostgreSQL backend like any other SQL command.)</p><h2 id="show-command" tabindex="-1"><a id="topic_zfh_2dl_gs"></a>SHOW Command <a class="header-anchor" href="#show-command" aria-label="Permalink to &quot;&lt;a id=&quot;topic_zfh_2dl_gs&quot;&gt;&lt;/a&gt;SHOW Command&quot;">​</a></h2><p>The <code>SHOW &lt;category&gt;</code> command displays different types of PgBouncer information. You can specify one of the following categories:</p><ul><li><a href="#CLIENTS">CLIENTS</a></li><li><a href="#CONFIG">CONFIG</a></li><li><a href="#DATABASES">DATABASES</a></li><li><a href="#DNS_HOSTS">DNS_HOSTS</a></li><li><a href="#DNS_ZONES">DNS_ZONES</a></li><li><a href="#FDS">FDS</a></li><li><a href="#LISTS">LISTS</a></li><li><a href="#MEM">MEM</a></li><li><a href="#POOLS">POOLS</a></li><li><a href="#PEERS">PEERS</a></li><li><a href="#PEER_POOLS">PEER_POOLS</a></li><li><a href="#SERVERS">SERVERS</a></li><li><a href="#SOCKETS">SOCKETS, ACTIVE_SOCKETS</a>,</li><li><a href="#STATS">STATS</a></li><li><a href="#STATS_TOTALS">STATS_TOTALS</a></li><li><a href="#STATS_AVERAGES">STATS_AVERAGES</a></li><li><a href="#TOTALS">TOTALS</a></li><li><a href="#USERS">USERS</a></li><li><a href="#VERSION">VERSION</a></li></ul><h3 id="clients" tabindex="-1"><a id="CLIENTS"></a>CLIENTS <a class="header-anchor" href="#clients" aria-label="Permalink to &quot;&lt;a id=&quot;CLIENTS&quot;&gt;&lt;/a&gt;CLIENTS&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>type</td><td>C, for client.</td></tr><tr><td>user</td><td>Client connected user.</td></tr><tr><td>database</td><td>Database name.</td></tr><tr><td>state</td><td>State of the client connection, one of <code>active</code>, <code>waiting</code>, <code>active_cancel_req</code>, or <code>waiting_cancel_req</code>.</td></tr><tr><td>addr</td><td>IP address of client.</td></tr><tr><td>port</td><td>Port client is connected to.</td></tr><tr><td>local_addr</td><td>Connection end address on local machine.</td></tr><tr><td>local_port</td><td>Connection end port on local machine.</td></tr><tr><td>connect_time</td><td>Timestamp of connect time.</td></tr><tr><td>request_time</td><td>Timestamp of latest client request.</td></tr><tr><td>wait</td><td>Current Time waiting in seconds.</td></tr><tr><td>wait_us</td><td>Microsecond part of the current waiting time.</td></tr><tr><td>close_needed</td><td>Not used for clients.</td></tr><tr><td>ptr</td><td>Address of internal object for this connection. Used as unique ID.</td></tr><tr><td>link</td><td>Address of server connection the client is paired with.</td></tr><tr><td>remote_pid</td><td>Process ID, if client connects with Unix socket and the OS supports getting it.</td></tr><tr><td>tls</td><td>A string with TLS connection information, or empty if not using TLS.</td></tr><tr><td>application_name</td><td>A string containing the <code>application_name</code> set by the client for this connection, or empty if not set.</td></tr><tr><td>prepared_statements</td><td>The amount of prepared statements that the client has prepared.</td></tr></tbody></table><h3 id="config" tabindex="-1"><a id="CONFIG"></a>CONFIG <a class="header-anchor" href="#config" aria-label="Permalink to &quot;&lt;a id=&quot;CONFIG&quot;&gt;&lt;/a&gt;CONFIG&quot;">​</a></h3><p>Show the current PgBouncer configuration settings, one per row, with the following columns:</p><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>key</td><td>Configuration variable name</td></tr><tr><td>value</td><td>Configuration value</td></tr><tr><td>default</td><td>Configuration default value</td></tr><tr><td>changeable</td><td>Either <code>yes</code> or <code>no</code>. Shows whether the variable can be changed while running. If <code>no</code>, the variable can be changed only at boot time. Use <code>SET</code> to change a variable at run time.</td></tr></tbody></table><h3 id="databases" tabindex="-1"><a id="DATABASES"></a>DATABASES <a class="header-anchor" href="#databases" aria-label="Permalink to &quot;&lt;a id=&quot;DATABASES&quot;&gt;&lt;/a&gt;DATABASES&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>name</td><td>Name of configured database entry.</td></tr><tr><td>host</td><td>Host pgbouncer connects to.</td></tr><tr><td>port</td><td>Port pgbouncer connects to.</td></tr><tr><td>database</td><td>Actual database name pgbouncer connects to.</td></tr><tr><td>force_user</td><td>When user is part of the connection string, the connection between pgbouncer and the database server is forced to the given user, whatever the client user.</td></tr><tr><td>pool_size</td><td>Maximum number of server connections.</td></tr><tr><td>min_pool_size</td><td>Minimum number of server connections.</td></tr><tr><td>reserve_pool</td><td>The maximum number of additional connections for this database.</td></tr><tr><td>pool_mode</td><td>The database&#39;s override <code>pool_mode</code> or NULL if the default will be used instead.</td></tr><tr><td>max_connections</td><td>Maximum number of allowed connections for this database, as set by <code>max_db_connections</code>, either globally or per-database.</td></tr><tr><td>current_connections</td><td>The current number of connections for this database.</td></tr><tr><td>paused</td><td>Paused/unpaused state of the database. 1 if this database is currently paused, else 0.</td></tr><tr><td>disabled</td><td>Enabled/disabled state of the database. 1 if this database is currently disabled, else 0.</td></tr></tbody></table><h3 id="dns-hosts" tabindex="-1"><a id="DNS_HOSTS"></a>DNS_HOSTS <a class="header-anchor" href="#dns-hosts" aria-label="Permalink to &quot;&lt;a id=&quot;DNS_HOSTS&quot;&gt;&lt;/a&gt;DNS\\_HOSTS&quot;">​</a></h3><p>Show host names in DNS cache.</p><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>hostname</td><td>Host name</td></tr><tr><td>ttl</td><td>How many seconds until next lookup.</td></tr><tr><td>addrs</td><td>Comma-separated list of addresses.</td></tr></tbody></table><h3 id="dns-zones" tabindex="-1"><a id="DNS_ZONES"></a>DNS_ZONES <a class="header-anchor" href="#dns-zones" aria-label="Permalink to &quot;&lt;a id=&quot;DNS_ZONES&quot;&gt;&lt;/a&gt;DNS\\_ZONES&quot;">​</a></h3><p>Show DNS zones in cache.</p><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>zonename</td><td>Zone name</td></tr><tr><td>serial</td><td>Current DNS serial number</td></tr><tr><td>count</td><td>Hostnames belonging to this zone</td></tr></tbody></table><h3 id="fds" tabindex="-1"><a id="FDS"></a>FDS <a class="header-anchor" href="#fds" aria-label="Permalink to &quot;&lt;a id=&quot;FDS&quot;&gt;&lt;/a&gt;FDS&quot;">​</a></h3><p><code>SHOW FDS</code> is an internal command used for an online restart, for example when upgrading to a new PgBouncer version. It displays a list of file descriptors in use with the internal state attached to them. This command blocks the internal event loop, so it should not be used while PgBouncer is in use.</p><p>When the connected user has username &quot;pgbouncer&quot;, connects through a Unix socket, and has the same UID as the running process, the actual file descriptors are passed over the connection. This mechanism is used to do an online restart.</p><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>fd</td><td>File descriptor numeric value.</td></tr><tr><td>task</td><td>One of <code>pooler</code>, <code>client</code>, or <code>server</code>.</td></tr><tr><td>user</td><td>User of the connection using the file descriptor.</td></tr><tr><td>database</td><td>Database of the connection using the file descriptor.</td></tr><tr><td>addr</td><td>IP address of the connection using the file descriptor, <code>unix</code> if a Unix socket is used.</td></tr><tr><td>port</td><td>Port used by the connection using the file descriptor.</td></tr><tr><td>cancel</td><td>Cancel key for this connection.</td></tr><tr><td>link</td><td>File descriptor for corresponding server/client. NULL if idle.</td></tr></tbody></table><h3 id="lists" tabindex="-1"><a id="LISTS"></a>LISTS <a class="header-anchor" href="#lists" aria-label="Permalink to &quot;&lt;a id=&quot;LISTS&quot;&gt;&lt;/a&gt;LISTS&quot;">​</a></h3><p>Shows the following PgBouncer internal information, in columns (not rows):</p><table tabindex="0"><thead><tr><th>Item</th><th>Description</th></tr></thead><tbody><tr><td>databases</td><td>Count of databases.</td></tr><tr><td>users</td><td>Count of users.</td></tr><tr><td>pools</td><td>Count of pools.</td></tr><tr><td>free_clients</td><td>Count of free clients.</td></tr><tr><td>used_clients</td><td>Count of used clients.</td></tr><tr><td>login_clients</td><td>Count of clients in <code>login</code> state.</td></tr><tr><td>free_servers</td><td>Count of free servers.</td></tr><tr><td>used_servers</td><td>Count of used servers.</td></tr><tr><td>dns_names</td><td>Count of DNS names in the cache.</td></tr><tr><td>dns_zones</td><td>Count of DNS zones in the cache.</td></tr><tr><td>dns_queries</td><td>Count of in-flight DNS queries.</td></tr><tr><td>dns_pending</td><td>not used</td></tr></tbody></table><h3 id="mem" tabindex="-1"><a id="MEM"></a>MEM <a class="header-anchor" href="#mem" aria-label="Permalink to &quot;&lt;a id=&quot;MEM&quot;&gt;&lt;/a&gt;MEM&quot;">​</a></h3><p>Shows low-level information about the current sizes of various internal memory allocations. The information presented is subject to change.</p><h3 id="peers" tabindex="-1"><a id="PEERS"></a>PEERS <a class="header-anchor" href="#peers" aria-label="Permalink to &quot;&lt;a id=&quot;PEERS&quot;&gt;&lt;/a&gt;PEERS&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>peer_id</td><td>ID of the configured peer entry.</td></tr><tr><td>host</td><td>Host PgBouncer connects to.</td></tr><tr><td>port</td><td>Port PgBouncer connects to.</td></tr><tr><td>pool_size</td><td>Maximum number of server connections that can be made to this peer.</td></tr></tbody></table><h3 id="peer-pools" tabindex="-1"><a id="PEER_POOLS"></a>PEER_POOLS <a class="header-anchor" href="#peer-pools" aria-label="Permalink to &quot;&lt;a id=&quot;PEER_POOLS&quot;&gt;&lt;/a&gt;PEER_POOLS&quot;">​</a></h3><p>A new <code>peer_pool</code> entry is made for each configured peer.</p><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>Database</td><td>ID of the configured peer entry.</td></tr><tr><td>cl_active_cancel_req</td><td>Client connections that have forwarded query cancellations to the server and are waiting for the server response.</td></tr><tr><td>cl_waiting_cancel_req</td><td>Client connections that have not forwarded query cancellations to the server yet.</td></tr><tr><td>sv_active_cancel</td><td>Server connections that are currently forwarding a cancel request.</td></tr><tr><td>sv_login</td><td>Server connections currently in the process of logging in.</td></tr></tbody></table><h3 id="pools" tabindex="-1"><a id="POOLS"></a>POOLS <a class="header-anchor" href="#pools" aria-label="Permalink to &quot;&lt;a id=&quot;POOLS&quot;&gt;&lt;/a&gt;POOLS&quot;">​</a></h3><p>A new pool entry is made for each pair of (database, user).</p><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>database</td><td>Database name.</td></tr><tr><td>user</td><td>User name.</td></tr><tr><td>cl_active</td><td>Client connections that are linked to server connection and can process queries.</td></tr><tr><td>cl_waiting</td><td>Client connections that have sent queries but have not yet got a server connection.</td></tr><tr><td>cl_active_cancel_req</td><td>Client connections that have forwarded query cancellations to the server and are waiting for the server response.</td></tr><tr><td>cl_waiting_cancel_req</td><td>Client connections that have not forwarded query cancellations to the server yet.</td></tr><tr><td>sv_active</td><td>Server connections that are linked to client.</td></tr><tr><td>sv_active_cancel</td><td>Server connections that are currently forwarding a cancel request.</td></tr><tr><td>sv_being_canceled</td><td>Servers that normally could become idle but are waiting to do so until all in-flight cancel requests have completed that were sent to cancel a query on this server.</td></tr><tr><td>sv_idle</td><td>Server connections that are unused and immediately usable for client queries.</td></tr><tr><td>sv_used</td><td>Server connections that have been idle more than <code>server_check_delay</code>. The <code>server_check_query</code> query must be run on them before they can be used again.</td></tr><tr><td>sv_tested</td><td>Server connections that are currently running either <code>server_reset_query</code> or <code>server_check_query</code>.</td></tr><tr><td>sv_login</td><td>Server connections currently in the process of logging in.</td></tr><tr><td>maxwait</td><td>How long the first (oldest) client in the queue has waited, in seconds. If this begins to increase, the current pool of servers does not handle requests quickly enough. The cause may be either an overloaded server or the <code>pool_size</code> setting is too small.</td></tr><tr><td>maxwait_us</td><td>Microsecond part of the maximum waiting time.</td></tr><tr><td>pool_mode</td><td>The pooling mode in use.</td></tr></tbody></table><h3 id="servers" tabindex="-1"><a id="SERVERS"></a>SERVERS <a class="header-anchor" href="#servers" aria-label="Permalink to &quot;&lt;a id=&quot;SERVERS&quot;&gt;&lt;/a&gt;SERVERS&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>type</td><td>S, for server.</td></tr><tr><td>user</td><td>User name that <code>pgbouncer</code> uses to connect to server.</td></tr><tr><td>database</td><td>Database name.</td></tr><tr><td>state</td><td>State of the <code>pgbouncer</code> server connection, one of <code>active</code>, <code>idle</code>, <code>used</code>, <code>tested</code>, or <code>new</code>.</td></tr><tr><td>addr</td><td>IP address of the WarehousePG or PostgreSQL server.</td></tr><tr><td>port</td><td>Port of the WarehousePG or PostgreSQL server.</td></tr><tr><td>local_addr</td><td>Connection start address on local machine.</td></tr><tr><td>local_port</td><td>Connection start port on local machine.</td></tr><tr><td>connect_time</td><td>When the connection was made.</td></tr><tr><td>request_time</td><td>When the last request was issued.</td></tr><tr><td>wait</td><td>Current waiting time in seconds.</td></tr><tr><td>wait_us</td><td>Microsecond part of the current waiting time.</td></tr><tr><td>close_needed</td><td>1 if the connection will be closed as soon as possible, because a configuration file reload or DNS update changed the connection information or <code>RECONNECT</code> was issued.</td></tr><tr><td>ptr</td><td>Address of the internal object for this connection. Used as unique ID.</td></tr><tr><td>link</td><td>Address of the client connection the server is paired with.</td></tr><tr><td>remote_pid</td><td>Pid of backend server process. If the connection is made over Unix socket and the OS supports getting process ID info, it is the OS pid. Otherwise it is extracted from the cancel packet the server sent, which should be PID in case server is PostgreSQL, but it is a random number in case server is another PgBouncer.</td></tr><tr><td>tls</td><td>A string with TLS connection information, or empty if not using TLS.</td></tr><tr><td>application_name</td><td>A string containing the application_name set on the linked client connection, or empty if this is not set, or if there is no linked connection.</td></tr><tr><td>prepared_statements</td><td>The amount of prepared statements that are prepared on the server. This number is limited by the max_prepared_statements setting.</td></tr></tbody></table><h3 id="sockets-active-sockets" tabindex="-1"><a id="SOCKETS"></a>SOCKETS, ACTIVE_SOCKETS <a class="header-anchor" href="#sockets-active-sockets" aria-label="Permalink to &quot;&lt;a id=&quot;SOCKETS&quot;&gt;&lt;/a&gt;SOCKETS, ACTIVE\\_SOCKETS&quot;">​</a></h3><p>Shows low-level information about sockets or only active sockets. This includes the information shown under <code>SHOW CLIENTS</code> and <code>SHOW SERVERS</code> as well as other more low-level information.</p><h3 id="stats" tabindex="-1"><a id="STATS"></a>STATS <a class="header-anchor" href="#stats" aria-label="Permalink to &quot;&lt;a id=&quot;STATS&quot;&gt;&lt;/a&gt;STATS&quot;">​</a></h3><p>Shows statistics. In this and related commands, the total figures are since process start, the averages are updated every <code>stats_period</code>.</p><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>database</td><td>Statistics are presented per database.</td></tr><tr><td>total_xact_count</td><td>Total number of SQL transactions pooled by PgBouncer.</td></tr><tr><td>total_query_count</td><td>Total number of SQL queries pooled by PgBouncer.</td></tr><tr><td>total_received</td><td>Total volume in bytes of network traffic received by <code>pgbouncer</code>.</td></tr><tr><td>total_sent</td><td>Total volume in bytes of network traffic sent by <code>pgbouncer</code>.</td></tr><tr><td>total_xact_time</td><td>Total number of microseconds spent by PgBouncer when connected to WarehousePG in a transaction, either idle in transaction or executing queries.</td></tr><tr><td>total_query_time</td><td>Total number of microseconds spent by <code>pgbouncer</code> when actively connected to the database server.</td></tr><tr><td>total_wait_time</td><td>Time spent (in microseconds) by clients waiting for a server.</td></tr><tr><td>avg_xact_count</td><td>Average number of transactions per second in the last stat period.</td></tr><tr><td>avg_query_count</td><td>Average queries per second in the last stats period.</td></tr><tr><td>avg_recv</td><td>Average received (from clients) bytes per second.</td></tr><tr><td>avg_sent</td><td>Average sent (to clients) bytes per second.</td></tr><tr><td>avg_xact_time</td><td>Average transaction duration in microseconds.</td></tr><tr><td>avg_query_time</td><td>Average query duration in microseconds.</td></tr><tr><td>avg_wait_time</td><td>Time spent by clients waiting for a server in microseconds (average per second).</td></tr></tbody></table><h3 id="stats-averages" tabindex="-1"><a id="STATS_AVERAGES"></a>STATS_AVERAGES <a class="header-anchor" href="#stats-averages" aria-label="Permalink to &quot;&lt;a id=&quot;STATS_AVERAGES&quot;&gt;&lt;/a&gt;STATS\\_AVERAGES&quot;">​</a></h3><p>Subset of <code>SHOW STATS</code> showing the average values for selected statistics (<code>avg_</code>)</p><h3 id="stats-totals" tabindex="-1"><a id="STATS_TOTALS"></a>STATS_TOTALS <a class="header-anchor" href="#stats-totals" aria-label="Permalink to &quot;&lt;a id=&quot;STATS_TOTALS&quot;&gt;&lt;/a&gt;STATS\\_TOTALS&quot;">​</a></h3><p>Subset of <code>SHOW STATS</code> showing the total values for selected statistics (<code>total_</code>)</p><h3 id="totals" tabindex="-1"><a id="TOTALS"></a>TOTALS <a class="header-anchor" href="#totals" aria-label="Permalink to &quot;&lt;a id=&quot;TOTALS&quot;&gt;&lt;/a&gt;TOTALS&quot;">​</a></h3><p>Like <code>SHOW STATS</code> but aggregated across all databases.</p><h3 id="users" tabindex="-1"><a id="USERS"></a>USERS <a class="header-anchor" href="#users" aria-label="Permalink to &quot;&lt;a id=&quot;USERS&quot;&gt;&lt;/a&gt;USERS&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>name</td><td>The user name</td></tr><tr><td>pool_mode</td><td>The user&#39;s override pool_mode, or NULL if the default will be used instead.</td></tr></tbody></table><h3 id="version" tabindex="-1"><a id="VERSION"></a>VERSION <a class="header-anchor" href="#version" aria-label="Permalink to &quot;&lt;a id=&quot;VERSION&quot;&gt;&lt;/a&gt;VERSION&quot;">​</a></h3><p>Display PgBouncer version information.</p><blockquote><p><strong>Note</strong> This reference documentation is based on the PgBouncer 1.21 documentation.</p></blockquote><h2 id="signals" tabindex="-1"><a id="signals"></a>Signals <a class="header-anchor" href="#signals" aria-label="Permalink to &quot;&lt;a id=&quot;signals&quot;&gt;&lt;/a&gt;Signals&quot;">​</a></h2><p>SIGHUP : Reload config. Same as issuing the command <code>RELOAD</code> on the console.</p><p>SIGINT : Safe shutdown. Same as issuing <code>PAUSE</code> and <code>SHUTDOWN</code> on the console.</p><p>SIGTERM : Immediate shutdown. Same as issuing <code>SHUTDOWN</code> on the console.</p><p>SIGUSR1 : Same as issuing <code>PAUSE</code> on the console.</p><p>SIGUSR2 : Same as issuing <code>RESUME</code> on the console.</p><h2 id="libevent-settings" tabindex="-1"><a id="libevent"></a>Libevent Settings <a class="header-anchor" href="#libevent-settings" aria-label="Permalink to &quot;&lt;a id=&quot;libevent&quot;&gt;&lt;/a&gt;Libevent Settings&quot;">​</a></h2><p>From the Libevent documentation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>It is possible to disable support for epoll, kqueue, devpoll, poll or select by</span></span>
<span class="line"><span>setting the environment variable EVENT_NOEPOLL, EVENT_NOKQUEUE, EVENT_NODEVPOLL,</span></span>
<span class="line"><span>EVENT_NOPOLL or EVENT_NOSELECT, respectively.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>By setting the environment variable EVENT_SHOW_METHOD, libevent displays the</span></span>
<span class="line"><span>kernel notification method that it uses.</span></span></code></pre></div><h2 id="see-also" tabindex="-1"><a id="seealso"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;seealso&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./pgbouncer.html">pgbouncer</a>, <a href="./pgbouncer-ini.html">pgbouncer.ini</a></p>`,98)]))}const m=e(d,[["render",r]]);export{p as __pageData,m as default};
