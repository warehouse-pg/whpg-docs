import{_ as a,c as t,o as s,ag as n}from"./chunks/framework.Ds6Eueu6.js";const m=JSON.parse('{"title":"CREATE OPERATOR CLASS","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/CREATE_OPERATOR_CLASS.md","filePath":"docs/7x/ref_guide/sql_commands/CREATE_OPERATOR_CLASS.md"}'),o={name:"docs/7x/ref_guide/sql_commands/CREATE_OPERATOR_CLASS.md"};function i(r,e,p,c,l,d){return s(),t("div",null,e[0]||(e[0]=[n(`<h1 id="create-operator-class" tabindex="-1">CREATE OPERATOR CLASS <a class="header-anchor" href="#create-operator-class" aria-label="Permalink to &quot;CREATE OPERATOR CLASS&quot;">​</a></h1><p>Defines a new operator class.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE OPERATOR CLASS &lt;name&gt; [DEFAULT] FOR TYPE &lt;data_type&gt;  </span></span>
<span class="line"><span>  USING &lt;index_method&gt; [ FAMILY &lt;family_name&gt; ] AS </span></span>
<span class="line"><span>  { OPERATOR &lt;strategy_number&gt; &lt;operator_name&gt; [ ( &lt;op_type&gt;, &lt;op_type&gt; ) ] [ FOR SEARCH | FOR ORDER BY &lt;sort_family_name&gt; ]</span></span>
<span class="line"><span>  | FUNCTION &lt;support_number&gt; [ ( &lt;op_type&gt; [ , &lt;op_type&gt; ] ) ] &lt;function_name&gt; (&lt;argument_type&gt; [, ...] )</span></span>
<span class="line"><span>  | STORAGE &lt;storage_type&gt;</span></span>
<span class="line"><span>  } [, ... ]</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>CREATE OPERATOR CLASS</code> creates a new operator class. An operator class defines how a particular data type can be used with an index. The operator class specifies that certain operators will fill particular roles or strategies for this data type and this index method. The operator class also specifies the support functions to be used by the index method when the operator class is selected for an index column. All of the operators and functions used by an operator class must be defined before the operator class is created. Any functions used to implement the operator class must be defined as <code>IMMUTABLE</code>.</p><p>If a schema name is given, then the operator class is created in the specified schema. Otherwise, it is created in the current schema. Two operator classes in the same schema can have the same name only if they are for different index methods.</p><p>The user who defines an operator class becomes its owner. Presently, the creating user must be a superuser. (This restriction is made because an erroneous operator class definition could confuse or even crash the server.)</p><p><code>CREATE OPERATOR CLASS</code> does not presently check whether the operator class definition includes all the operators and functions required by the index method, nor whether the operators and functions form a self-consistent set. It is the user&#39;s responsibility to define a valid operator class.</p><p>Related operator classes can be grouped into operator families. To add a new operator class to an existing family, specify the <code>FAMILY</code> option in <code>CREATE OPERATOR CLASS</code>. Without this option, the new class is placed into a family named the same as the new class (WarehousePG creates that family if it doesn&#39;t already exist).</p><p>Refer to <a href="https://www.postgresql.org/docs/12/xindex.html" target="_blank" rel="noreferrer">Interfacing Extensions to Indexes</a> in the PostgreSQL documentation for more information.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>name : The (optionally schema-qualified) name of the operator class to be defined. Two operator classes in the same schema can have the same name only if they are for different index methods.</p><p>DEFAULT : If present, makes the operator class the default operator class for its data type. At most one operator class can be the default for a specific data type and index method.</p><p>data_type : The column data type that this operator class is for.</p><p>index_method : The name of the index method that this operator class is for.</p><p>family_name : The name of the existing operator family to add this operator class to. If not specified, a family named the same as the operator class is used (creating it, if it doesn&#39;t already exist).</p><p>strategy_number : The index method&#39;s strategy number for an operator associated with the operator class. The operators associated with an operator class are identified by <em>strategy numbers</em>, which serve to identify the semantics of each operator within the context of its operator class.</p><p>operator_name : The name (optionally schema-qualified) of an operator associated with the operator class.</p><p>op_type : In an <code>OPERATOR</code> clause, the operand data type(s) of the operator, or <code>NONE</code> to signify a left-unary or right-unary operator. The operand data types can be omitted in the normal case where they are the same as the operator class&#39;s data type.</p><p>: In a <code>FUNCTION</code> clause, the operand data type(s) the function is intended to support, if different from the input data type(s) of the function (for B-tree comparison functions and hash functions) or the class&#39;s data type (for B-tree sort support functions and all functions in GiST, SP-GiST, GIN, and BRIN operator classes). These defaults are correct, and so op_type need not be specified in <code>FUNCTION</code> clauses, except for the case of a B-tree sort support function that is meant to support cross-data-type comparisons.</p><p>sort_family_name : The name (optionally schema-qualified) of an existing <code>btree</code> operator family that describes the sort ordering associated with an ordering operator.</p><p>: If neither <code>FOR SEARCH</code> nor <code>FOR ORDER BY</code> is specified, <code>FOR SEARCH</code> is the default.</p><p>support_number : The index method&#39;s support function number for a function associated with the operator class.</p><p>function_name : The name (optionally schema-qualified) of a function that is an index method support function for the operator class.</p><p>argument_type : The parameter data type(s) of the function.</p><p>storage_type : The data type actually stored in the index. Normally this is the same as the column data type, but some index methods (currently GiST, GIN, and BRIN) allow it to be different. The <code>STORAGE</code> clause must be omitted unless the index method allows a different type to be used. If the column data_type is specified as <code>anyarray</code>, the storage_type can be declared as <code>anyelement</code> to indicate that the index entries are members of the element type belonging to the actual array type that each particular index is created for.</p><p>The <code>OPERATOR</code>, <code>FUNCTION</code>, and <code>STORAGE</code> clauses can appear in any order.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>Because the index machinery does not check access permissions on functions before using them, including a function or operator in an operator class is the same as granting public execute permission on it. This is usually not an issue for the sorts of functions that are useful in an operator class.</p><p>The operators should not be defined by SQL functions. A SQL function is likely to be inlined into the calling query, which will prevent the optimizer from recognizing that the query matches an index.</p><p>Any functions used to implement the operator class must be defined as <code>IMMUTABLE</code>.</p><p>Before WarehousePG 6.0, the <code>OPERATOR</code> clause could include a <code>RECHECK</code> option. This option is no longer supported. WarehousePG now determines whether an index operator is &quot;lossy&quot; on-the-fly at run time. This allows more efficient handling of cases where an operator might or might not be lossy.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>The following example command defines a GiST index operator class for the data type <code>_int4</code> (array of int4). See the <code>intarray</code> contrib module for the complete example.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE OPERATOR CLASS gist__int_ops</span></span>
<span class="line"><span>    DEFAULT FOR TYPE _int4 USING gist AS</span></span>
<span class="line"><span>        OPERATOR 3 &amp;&amp;,</span></span>
<span class="line"><span>        OPERATOR 6 = (anyarray, anyarray),</span></span>
<span class="line"><span>        OPERATOR 7 @&gt;,</span></span>
<span class="line"><span>        OPERATOR 8 &lt;@,</span></span>
<span class="line"><span>        OPERATOR 20 @@ (_int4, query_int),</span></span>
<span class="line"><span>        FUNCTION 1 g_int_consistent (internal, _int4, smallint, oid, internal),</span></span>
<span class="line"><span>        FUNCTION 2 g_int_union (internal, internal),</span></span>
<span class="line"><span>        FUNCTION 3 g_int_compress (internal),</span></span>
<span class="line"><span>        FUNCTION 4 g_int_decompress (internal),</span></span>
<span class="line"><span>        FUNCTION 5 g_int_penalty (internal, internal, internal),</span></span>
<span class="line"><span>        FUNCTION 6 g_int_picksplit (internal, internal),</span></span>
<span class="line"><span>        FUNCTION 7 g_int_same (_int4, _int4, internal);</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p><code>CREATE OPERATOR CLASS</code> is a WarehousePG extension. There is no <code>CREATE OPERATOR CLASS</code> statement in the SQL standard.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./ALTER_OPERATOR_CLASS.html">ALTER OPERATOR CLASS</a>, <a href="./DROP_OPERATOR_CLASS.html">DROP OPERATOR CLASS</a>, <a href="./CREATE_OPERATOR_FAMILY.html">CREATE OPERATOR FAMILY</a>, <a href="./ALTER_OPERATOR_FAMILY.html">ALTER OPERATOR FAMILY</a>, <a href="./CREATE_FUNCTION.html">CREATE FUNCTION</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,41)]))}const u=a(o,[["render",i]]);export{m as __pageData,u as default};
