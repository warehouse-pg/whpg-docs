import{_ as t,c as a,o as i,ag as n}from"./chunks/framework.Ds6Eueu6.js";const g=JSON.parse('{"title":"Initializing New Segments","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/expand/expand-initialize.md","filePath":"docs/7x/admin_guide/expand/expand-initialize.md"}'),s={name:"docs/7x/admin_guide/expand/expand-initialize.md"};function o(p,e,r,d,l,c){return i(),a("div",null,e[0]||(e[0]=[n(`<h1 id="initializing-new-segments" tabindex="-1">Initializing New Segments <a class="header-anchor" href="#initializing-new-segments" aria-label="Permalink to &quot;Initializing New Segments&quot;">​</a></h1><hr><p>Use the <code>gpexpand</code> utility to create and initialize the new segment instances and create the expansion schema.</p><p>The first time you run <a href="./../../utility_guide/ref/gpexpand.html">gpexpand</a> with a valid input file it creates and initializes segment instances and creates the expansion schema. After these steps are completed, running <code>gpexpand</code> detects if the expansion schema has been created and, if so, performs table redistribution.</p><blockquote><p><strong>Note</strong> To prevent catalog inconsistency across existing and new segments, be sure that no DDL operations are running during the initialization phase.</p></blockquote><ul><li><a href="#topic23">Creating an Input File for System Expansion</a></li><li><a href="#topic26">Running gpexpand to Initialize New Segments</a></li><li><a href="#topic27">Rolling Back a Failed Expansion Setup</a></li></ul><p><strong>Parent topic:</strong> <a href="./../expand/expand-main.html">Expanding a WarehousePG cluster</a></p><h2 id="creating-an-input-file-for-system-expansion" tabindex="-1"><a id="topic23"></a>Creating an Input File for System Expansion <a class="header-anchor" href="#creating-an-input-file-for-system-expansion" aria-label="Permalink to &quot;&lt;a id=&quot;topic23&quot;&gt;&lt;/a&gt;Creating an Input File for System Expansion&quot;">​</a></h2><p>To begin expansion, <code>gpexpand</code> requires an input file containing information about the new segments and hosts. If you run <code>gpexpand</code> without specifying an input file, the utility displays an interactive interview that collects the required information and automatically creates an input file.</p><p>If you create the input file using the interactive interview, you may specify a file with a list of expansion hosts in the interview prompt. If your platform or command shell limits the length of the host list, specifying the hosts with <code>-f</code> may be mandatory.</p><h3 id="creating-an-input-file-in-interactive-mode" tabindex="-1"><a id="topic24"></a>Creating an input file in Interactive Mode <a class="header-anchor" href="#creating-an-input-file-in-interactive-mode" aria-label="Permalink to &quot;&lt;a id=&quot;topic24&quot;&gt;&lt;/a&gt;Creating an input file in Interactive Mode&quot;">​</a></h3><p>Before you run <code>gpexpand</code> to create an input file in interactive mode, ensure you know:</p><ul><li>The number of new hosts (or a hosts file)</li><li>The new hostnames (or a hosts file)</li><li>The mirroring strategy used in existing hosts, if any</li><li>The number of segments to add per host, if any</li></ul><p>The utility automatically generates an input file based on this information, <code>dbid</code>, <code>content</code> ID, and data directory values stored in <em>gp_segment_configuration</em>, and saves the file in the current directory.</p><h4 id="to-create-an-input-file-in-interactive-mode" tabindex="-1"><a id="no163887"></a>To create an input file in interactive mode <a class="header-anchor" href="#to-create-an-input-file-in-interactive-mode" aria-label="Permalink to &quot;&lt;a id=&quot;no163887&quot;&gt;&lt;/a&gt;To create an input file in interactive mode&quot;">​</a></h4><ol><li><p>Log in on the coordinator host as the user who will run your WarehousePG cluster; for example, <code>gpadmin</code>.</p></li><li><p>Run <code>gpexpand</code>. The utility displays messages about how to prepare for an expansion operation, and it prompts you to quit or continue.</p><p>Optionally, specify a hosts file using <code>-f</code>. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpexpand -f /home/gpadmin/&lt;new_hosts_file&gt;</span></span></code></pre></div></li><li><p>At the prompt, select <code>Y</code> to continue.</p></li><li><p>Unless you specified a hosts file using <code>-f</code>, you are prompted to enter hostnames. Enter a comma separated list of the hostnames of the new expansion hosts. Do not include interface hostnames. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&gt; sdw4, sdw5, sdw6, sdw7</span></span></code></pre></div><p>To add segments to existing hosts only, enter a blank line at this prompt. Do not specify <code>localhost</code> or any existing host name.</p></li><li><p>Enter the mirroring strategy used in your system, if any. Options are <code>spread|grouped|none</code>. The default setting is <code>grouped</code>.</p><p>Ensure you have enough hosts for the selected grouping strategy. For more information about mirroring, see <a href="./expand-planning.html">Planning Mirror Segments</a>.</p></li><li><p>Enter the number of new primary segments to add, if any. By default, new hosts are initialized with the same number of primary segments as existing hosts. Increase segments per host by entering a number greater than zero. The number you enter will be the number of additional segments initialized on all hosts. For example, if existing hosts currently have two segments each, entering a value of <code>2</code> initializes two more segments on existing hosts, and four segments on new hosts.</p></li><li><p>If you are adding new primary segments, enter the new primary data directory root for the new segments. Do not specify the actual data directory name, which is created automatically by <code>gpexpand</code> based on the existing data directory names.</p><p>For example, if your existing data directories are as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/gpdata/primary/gp0</span></span>
<span class="line"><span>/gpdata/primary/gp1</span></span></code></pre></div><p>then enter the following (one at each prompt) to specify the data directories for two new primary segments:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/gpdata/primary</span></span>
<span class="line"><span>/gpdata/primary</span></span></code></pre></div><p>When the initialization runs, the utility creates the new directories <code>gp2</code> and <code>gp3</code> under <code>/gpdata/primary</code>.</p></li><li><p>If you are adding new mirror segments, enter the new mirror data directory root for the new segments. Do not specify the data directory name; it is created automatically by <code>gpexpand</code> based on the existing data directory names.</p><p>For example, if your existing data directories are as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/gpdata/mirror/gp0</span></span>
<span class="line"><span>/gpdata/mirror/gp1</span></span></code></pre></div><p>enter the following (one at each prompt) to specify the data directories for two new mirror segments:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/gpdata/mirror</span></span>
<span class="line"><span>/gpdata/mirror</span></span></code></pre></div><p>When the initialization runs, the utility will create the new directories <code>gp2</code> and <code>gp3</code> under <code>/gpdata/mirror</code>.</p><p>These primary and mirror root directories for new segments must exist on the hosts, and the user running <code>gpexpand</code> must have permissions to create directories in them.</p><p>After you have entered all required information, the utility generates an input file and saves it in the current directory. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpexpand_inputfile_yyyymmdd_145134</span></span></code></pre></div><p>If the WarehousePG cluster is configured with tablespaces, the utility automatically generates an additional tablespace mapping file. This file is required for later parsing by the utility so make sure it is present before proceeding with the next step. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpexpand_inputfile_yyyymmdd_145134.ts</span></span></code></pre></div></li></ol><h3 id="expansion-input-file-format" tabindex="-1"><a id="topic25"></a>Expansion Input File Format <a class="header-anchor" href="#expansion-input-file-format" aria-label="Permalink to &quot;&lt;a id=&quot;topic25&quot;&gt;&lt;/a&gt;Expansion Input File Format&quot;">​</a></h3><p>Use the interactive interview process to create your own input file unless your expansion scenario has atypical needs.</p><p>The format for expansion input files is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>hostname|address|port|datadir|dbid|content|preferred_role</span></span></code></pre></div><p>For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sdw5|sdw5-1|50011|/gpdata/primary/gp9|11|9|p</span></span>
<span class="line"><span>sdw5|sdw5-2|50012|/gpdata/primary/gp10|12|10|p</span></span>
<span class="line"><span>sdw5|sdw5-2|60011|/gpdata/mirror/gp9|13|9|m</span></span>
<span class="line"><span>sdw5|sdw5-1|60012|/gpdata/mirror/gp10|14|10|m</span></span></code></pre></div><p>For each new segment, this format of expansion input file requires the following:</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Valid Values</th><th>Description</th></tr></thead><tbody><tr><td>hostname</td><td>Hostname</td><td>Hostname for the segment host.</td></tr><tr><td>port</td><td>An available port number</td><td>Database listener port for the segment, incremented on the existing segment <em>port</em> base number.</td></tr><tr><td>datadir</td><td>Directory name</td><td>The data directory location for a segment as per the gp_segment_configuration system catalog.</td></tr><tr><td>dbid</td><td>Integer. Must not conflict with existing <em>dbid</em> values.</td><td>Database ID for the segment. The values you enter should be incremented sequentially from existing <em>dbid</em> values shown in the system catalog <code>gp_segment_configuration</code>. For example, to add four segment instances to an existing ten-segment array with <em>dbid</em> values of 1-10, list new <em>dbid</em> values of 11, 12, 13 and 14.</td></tr><tr><td>content</td><td>Integer. Must not conflict with existing <em>content</em> values.</td><td>The content ID of the segment. A primary segment and its mirror should have the same content ID, incremented sequentially from existing values. For more information, see <em>content</em> in the reference for <code>gp_segment_configuration</code>.</td></tr><tr><td>preferred_role</td><td><code>p</code> or <code>m</code></td><td>Determines whether this segment is a primary or mirror. Specify <code>p</code> for primary and <code>m</code> for mirror.</td></tr></tbody></table><h2 id="running-gpexpand-to-initialize-new-segments" tabindex="-1"><a id="topic26"></a>Running gpexpand to Initialize New Segments <a class="header-anchor" href="#running-gpexpand-to-initialize-new-segments" aria-label="Permalink to &quot;&lt;a id=&quot;topic26&quot;&gt;&lt;/a&gt;Running gpexpand to Initialize New Segments&quot;">​</a></h2><p>After you have created an input file, run <code>gpexpand</code> to initialize new segment instances.</p><h3 id="to-run-gpexpand-with-an-input-file" tabindex="-1"><a id="no160378"></a>To run gpexpand with an input file <a class="header-anchor" href="#to-run-gpexpand-with-an-input-file" aria-label="Permalink to &quot;&lt;a id=&quot;no160378&quot;&gt;&lt;/a&gt;To run gpexpand with an input file&quot;">​</a></h3><ol><li><p>Log in on the coordinator host as the user who will run your WarehousePG cluster; for example, <code>gpadmin</code>.</p></li><li><p>Run the <code>gpexpand</code> utility, specifying the input file with <code>-i</code>. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpexpand -i input_file</span></span></code></pre></div><p>The utility detects if an expansion schema exists for the WarehousePG cluster. If a <em>gpexpand</em> schema exists, remove it with <code>gpexpand -c</code> before you start a new expansion operation. See <a href="./expand-post.html">Removing the Expansion Schema</a>.</p><p>When the new segments are initialized and the expansion schema is created, the utility prints a success message and exits.</p></li></ol><p>When the initialization process completes, you can connect to WarehousePG and view the expansion schema. The <em>gpexpand</em> schema resides in the postgres database. For more information, see <a href="./expand-planning.html">About the Expansion Schema</a>.</p><p>After segment initialization is complete, <a href="./expand-redistribute.html#expand-redistribute">redistribute the tables</a> to balance existing data over the new segments.</p><h2 id="monitoring-the-cluster-expansion-state" tabindex="-1"><a id="topic_wkv_kkd_chb"></a>Monitoring the Cluster Expansion State <a class="header-anchor" href="#monitoring-the-cluster-expansion-state" aria-label="Permalink to &quot;&lt;a id=&quot;topic_wkv_kkd_chb&quot;&gt;&lt;/a&gt;Monitoring the Cluster Expansion State&quot;">​</a></h2><p>At any time, you can check the state of cluster expansion by running the <code>gpstate</code> utility with the <code>-x</code> flag:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpstate -x</span></span></code></pre></div><p>If the expansion schema exists in the postgres database, <code>gpstate -x</code> reports on the progress of the expansion. During the first expansion phase, <code>gpstate</code> reports on the progress of new segment initialization. During the second phase, <code>gpstate</code> reports on the progress of table redistribution, and whether redistribution is paused or active.</p><p>You can also query the expansion schema to see expansion status. See <a href="./expand-redistribute.html">Monitoring Table Redistribution</a> for more information.</p><h2 id="rolling-back-a-failed-expansion-setup" tabindex="-1"><a id="topic27"></a>Rolling Back a Failed Expansion Setup <a class="header-anchor" href="#rolling-back-a-failed-expansion-setup" aria-label="Permalink to &quot;&lt;a id=&quot;topic27&quot;&gt;&lt;/a&gt;Rolling Back a Failed Expansion Setup&quot;">​</a></h2><p>You can roll back an expansion setup operation (adding segment instances and segment hosts) only if the operation fails.</p><p>If the expansion fails during the initialization step, while the database is down, you must first restart the database in coordinator-only mode by running the <code>gpstart -m</code> command.</p><p>Roll back the failed expansion with the following command:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpexpand --rollback</span></span></code></pre></div>`,40)]))}const u=t(s,[["render",o]]);export{g as __pageData,u as default};
