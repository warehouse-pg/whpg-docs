import{_ as e,c as a,o as d,ag as n}from"./chunks/framework.Ds6Eueu6.js";const g=JSON.parse('{"title":"BRIN Indexes","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/ddl/ddl-brin.md","filePath":"docs/7x/admin_guide/ddl/ddl-brin.md"}'),r={name:"docs/7x/admin_guide/ddl/ddl-brin.md"};function i(o,t,s,l,m,c){return d(),a("div",null,t[0]||(t[0]=[n('<h1 id="brin-indexes" tabindex="-1">BRIN Indexes <a class="header-anchor" href="#brin-indexes" aria-label="Permalink to &quot;BRIN Indexes&quot;">​</a></h1><hr><p>Use Block Range Index (BRIN) for handling very large tables in which certain columns have some natural correlation with their physical location within the table. BRIN works in terms of block ranges (or page ranges), which are groups of pages that are physically adjacent in the table. BRIN stores summary information for each block range. For example, a table storing a store&#39;s sale orders might have a date column on which each order was placed, and most of the time the entries for earlier orders will appear earlier in the table as well; a table storing a ZIP code column might have all codes for a city grouped together naturally.</p><p>BRIN indexes can satisfy queries via regular bitmap index scans, and return all tuples in all pages within each range if the summary info stored by the index is consistent with the query conditions. The query executor is in charge of rechecking these tuples and discarding those that do not match the query conditions — in other words, these indexes are lossy. Because a BRIN index is very small, scanning the index adds little overhead compared to a sequential scan, but may avoid scanning large parts of the table that are known not to contain matching tuples.</p><p>The specific data that a BRIN index stores, as well as the specific queries that the index will be able to satisfy, depend on the operator class selected for each column of the index. Data types that have a linear sort order can have operator classes that store the minimum and maximum value within each block range, for instance; geometrical types store the bounding box for all the objects in the block range.</p><p>The size of the block range is determined at index creation time by the <code>pages_per_range</code> storage parameter. The number of index entries is equal to the size of the relation in pages divided by the selected value for <code>pages_per_range</code>. Therefore, the smaller the number, the larger the index becomes (because of the need to store more index entries), but at the same time the summary data stored can be more precise and more data blocks can be skipped during an index scan. The default value for <code>pages_per_range</code> is 32 for heap tables, and 1 for append-optimized tables.</p><h2 id="index-maintenance" tabindex="-1"><a id="maint"></a>Index Maintenance <a class="header-anchor" href="#index-maintenance" aria-label="Permalink to &quot;&lt;a id=&quot;maint&quot;&gt;&lt;/a&gt;Index Maintenance&quot;">​</a></h2><p>At the time of creation, WarehousePG scans all existing heap pages and creates a summary index tuple for each range, including the possibly-incomplete range at the end. As new pages are filled with data, page ranges that are already summarized will cause the summary information to be updated with data from the new tuples. When a new page is created that does not fall within the last summarized range, the range that the new page belongs into does not automatically acquire a summary tuple; those tuples remain unsummarized until a summarization run is invoked later, creating the initial summary for that range.</p><p>There are several ways to trigger the initial summarization of a page range. If the table is vacuumed, all existing unsummarized page ranges are summarized.</p><p>You may use the following functions:</p><p><code>brin_summarize_new_values(regclass)</code> summarizes all unsummarized ranges; <code>brin_summarize_range(regclass, bigint)</code> summarizes only the range containing the given page, if it is unsummarized.</p><p>Conversely, you may de-summarize a range using the <code>brin_desummarize_range(regclass, bigint)</code> function, which is useful when the index tuple is no longer a very good representation because the existing values have changed.</p><p>The following table lists the functions available for index maintenance tasks. You cannot execute these functions cannot during recovery. Use of these functions is restricted to superusers and the owner of the given index.</p><table tabindex="0"><thead><tr><th>Name</th><th>Return Type</th><th>Description</th></tr></thead><tbody><tr><td>brin_summarize_new_values(index regclass)</td><td>integer</td><td>summarize page ranges not already summarized</td></tr><tr><td>brin_summarize_range(index regclass, blockNumber bigint)</td><td>integer</td><td>summarize the page range covering the given block, if not already summarized</td></tr><tr><td>brin_desummarize_range(index regclass, blockNumber bigint)</td><td>integer</td><td>de-summarize the page range covering the given block, if summarized</td></tr></tbody></table><p><code>brin_summarize_new_values</code> accepts the OID or name of a BRIN index and inspects the index to find page ranges in the base table that are not currently summarized by the index; for any such range it creates a new summary index tuple by scanning the table pages. It returns the number of new page range summaries that were inserted into the index. <code>brin_summarize_range</code> does the same, except it only summarizes the range that covers the given block number.</p><blockquote><p><strong>Note</strong> <code>brin_summarize_range</code> and <code>brin_desummarize_range</code> only operate on ranges that exist. If you provide a block number that does not exist in the table, the functions return 0. Additionally, a specific range may only exist on some segments, due to data skew. In this case, the functions return the number of segments for which they were able to summarize or desummarize the range.</p></blockquote><h2 id="built-in-operator-classes" tabindex="-1"><a id="opclasses"></a>Built-in Operator Classes <a class="header-anchor" href="#built-in-operator-classes" aria-label="Permalink to &quot;&lt;a id=&quot;opclasses&quot;&gt;&lt;/a&gt;Built-in Operator Classes&quot;">​</a></h2><p>WarehousePG includes the BRIN operator classes shown in the table below.</p><p>The <code>minmax</code> operator classes store the minimum and the maximum values appearing in the indexed column within the range. The <code>inclusion</code> operator classes store a value which includes the values in the indexed column within the range.</p><table tabindex="0"><thead><tr><th>Name</th><th>Indexed Data Type</th><th>Indexable Operators</th></tr></thead><tbody><tr><td>int8_minmax_ops</td><td>bigint</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>bit_minmax_ops</td><td>bit</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>varbit_minmax_ops</td><td>bit varying</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>box_inclusion_ops</td><td>box</td><td>`&lt;&lt; &amp;&lt; &amp;&amp; &amp;&gt; &gt;&gt; ~= @&gt; &lt;@ &amp;&lt;</td></tr><tr><td>bytea_minmax_ops</td><td>bytea</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>bpchar_minmax_ops</td><td>character</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>char_minmax_ops</td><td>&quot;char&quot;</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>date_minmax_ops</td><td>date</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>float8_minmax_ops</td><td>double precision</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>inet_minmax_ops</td><td>inet</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>network_inclusion_ops</td><td>inet</td><td><code>&amp;&amp; &gt;&gt;= &lt;&lt;= = &gt;&gt; &lt;&lt;</code></td></tr><tr><td>int4_minmax_ops</td><td>integer</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>interval_minmax_ops</td><td>interval</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>macaddr_minmax_ops</td><td>macaddr</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>macaddr8_minmax_op</td><td>macaddr8</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>name_minmax_ops</td><td>name</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>numeric_minmax_ops</td><td>numeric</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>pg_lsn_minmax_ops</td><td>pg_lsn</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>oid_minmax_ops</td><td>oid</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>range_inclusion_ops</td><td>any range type</td><td>`&lt;&lt; &amp;&lt; &amp;&amp; &amp;&gt; &gt;&gt; @&gt; &lt;@ -</td></tr><tr><td>float4_minmax_ops</td><td>real</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>int2_minmax_ops</td><td>smallint</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>text_minmax_ops</td><td>text</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>tid_minmax_ops</td><td>tid</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>timestamp_minmax_ops</td><td>timestamp without time zone</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>timestamptz_minmax_ops</td><td>timestamp with time zone</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>time_minmax_ops</td><td>time without time zone</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>timetz_minmax_ops</td><td>time with time zone</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr><tr><td>uuid_minmax_ops</td><td>uuid</td><td><code>&lt; &lt;= = &gt;= &gt;</code></td></tr></tbody></table>',20)]))}const u=e(r,[["render",i]]);export{g as __pageData,u as default};
