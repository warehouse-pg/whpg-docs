import{_ as a,c as t,o as s,ag as n}from"./chunks/framework.Ds6Eueu6.js";const b=JSON.parse('{"title":"analyzedb","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/analyzedb.md","filePath":"docs/7x/utility_guide/ref/analyzedb.md"}'),o={name:"docs/7x/utility_guide/ref/analyzedb.md"};function i(l,e,p,d,c,r){return s(),t("div",null,e[0]||(e[0]=[n(`<h1 id="analyzedb" tabindex="-1">analyzedb <a class="header-anchor" href="#analyzedb" aria-label="Permalink to &quot;analyzedb&quot;">​</a></h1><p>A utility that performs <code>ANALYZE</code> operations on tables incrementally and concurrently. For append optimized tables, analyzedb updates statistics only if the statistics are not current.</p><h2 id="synopsis" tabindex="-1"><a id="syn"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;syn&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>analyzedb -d &lt;dbname&gt;</span></span>
<span class="line"><span>   { -s &lt;schema&gt;  | </span></span>
<span class="line"><span>   { -t &lt;schema&gt;.&lt;table&gt; </span></span>
<span class="line"><span>     [ -i &lt;col1&gt;[,&lt;col2&gt;, ...] | </span></span>
<span class="line"><span>       -x &lt;col1&gt;[,&lt;col2&gt;, ...] ] } |</span></span>
<span class="line"><span>     { -f | --file} &lt;config-file&gt; }</span></span>
<span class="line"><span>   [ -l | --list ]</span></span>
<span class="line"><span>   [ --gen_profile_only ]   </span></span>
<span class="line"><span>   [ -p &lt;parallel-level&gt; ]</span></span>
<span class="line"><span>   [ --full ]</span></span>
<span class="line"><span>   [ --skip_root_stats ]</span></span>
<span class="line"><span>   [ --skip_orca_root_stats ]</span></span>
<span class="line"><span>   [ -v | --verbose ]</span></span>
<span class="line"><span>   [ -a ]</span></span>
<span class="line"><span>   [ --skip-locked ]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>analyzedb { --clean_last | --clean_all }</span></span>
<span class="line"><span>analyzedb --version</span></span>
<span class="line"><span>analyzedb { -? | -h | --help }</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="desc"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;desc&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>The analyzedb utility updates statistics on table data for the specified tables in a WarehousePG incrementally and concurrently.</p><p>While performing <a href="./../../ref_guide/sql_commands/ANALYZE.html">ANALYZE</a> operations, analyzedb creates a snapshot of the table metadata and stores it on disk on the coordinator host. An <code>ANALYZE</code> operation is performed only if the table has been modified. If a table or partition has not been modified since the last time it was analyzed, analyzedb automatically skips the table or partition because it already contains up-to-date statistics.</p><ul><li>For append optimized tables, analyzedb updates statistics incrementally, if the statistics are not current. For example, if table data is changed after statistics were collected for the table. If there are no statistics for the table, statistics are collected.</li><li>For heap tables, statistics are always updated.</li></ul><p>Specify the <code>--full</code> option to update append-optimized table statistics even if the table statistics are current.</p><p>By default, analyzedb creates a maximum of 5 concurrent sessions to analyze tables in parallel. For each session, analyzedb issues an ANALYZE command to the database and specifies different table names. The <code>-p</code> option controls the maximum number of concurrent sessions.</p><p><strong>Partitioned Append-Optimized Tables</strong></p><p>For a partitioned, append-optimized table, analyzedb checks the root partitioned table and the leaf partitions. If needed, the utility updates statistics for non-current partitions and the root partition. For information about how statistics are collected for partitioned tables, see <a href="./../../ref_guide/sql_commands/ANALYZE.html">ANALYZE</a>.</p><p><code>analyzedb</code> must sample additional partitions within a partitioned table when it encounters a stale partition, even when statistics are already collected. VMware recommends that you run <code>analyzedb</code> on the root partition any time that you add a new partition(s) to a partitioned table. This operation both analyzes the leaf partitions in parallel and merges any updated statistics into the root partition.</p><h2 id="notes" tabindex="-1"><a id="notes"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;notes&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>The analyzedb utility updates append optimized table statistics if the table has been modified by DML or DDL commands, including INSERT, DELETE, UPDATE, CREATE TABLE, ALTER TABLE and TRUNCATE. The utility determines if a table has been modified by comparing catalog metadata of tables with the previous snapshot of metadata taken during a previous analyzedb operation. The snapshots of table metadata are stored as state files in the directory <code>db_analyze/&lt;db_name&gt;/&lt;timestamp&gt;</code> in the WarehousePG coordinator data directory.</p><p>The utility preserves old snapshot information from the past 8 days, and the 3 most recent state directories regardless of age, while all other directories are automatically removed. You can also specify the <code>--clean_last</code> or <code>--clean_all</code> option to remove state files generated by analyzedb.</p><p>If you do not specify a table, set of tables, or schema, the analyzedb utility collects the statistics as needed on all system catalog tables and user-defined tables in the database.</p><p>External tables are not affected by analyzedb.</p><p>Table names that contain spaces are not supported.</p><p>Running the <code>ANALYZE</code> command on a table, not using the <code>analyzedb</code> utility, does not update the table metadata that the <code>analyzedb</code> utility uses to determine whether table statistics are up to date.</p><h2 id="options" tabindex="-1"><a id="opts"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;opts&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>--clean_last : Remove the state files generated by last analyzedb operation. All other options except -d are ignored.</p><p>--clean_all : Remove all the state files generated by analyzedb. All other options except -d are ignored.</p><p>-d dbname : Specifies the name of the database that contains the tables to be analyzed. If this option is not specified, the database name is read from the environment variable <code>PGDATABASE</code>. If <code>PGDATABASE</code> is not set, the user name specified for the connection is used.</p><p>-f config-file | --file config-file : Text file that contains a list of tables to be analyzed. A relative file path from current directory can be specified.</p><p>: The file lists one table per line. Table names must be qualified with a schema name. Optionally, a list of columns can be specified using the -i or -x. No other options are allowed in the file. Other options such as <code>--full</code> must be specified on the command line.</p><p>: Only one of the options can be used to specify the files to be analyzed: <code>-f</code> or <code>--file</code>, <code>-t</code> , or <code>-s</code>.</p><p>: When performing ANALYZE operations on multiple tables, analyzedb creates concurrent sessions to analyze tables in parallel. The <code>-p</code> option controls the maximum number of concurrent sessions.</p><p>: In the following example, the first line performs an ANALYZE operation on the table <code>public.nation</code>, the second line performs an ANALYZE operation only on the columns <code>l_shipdate</code> and <code>l_receiptdate</code> in the table <code>public.lineitem</code>. <code> public.nation public.lineitem -i l_shipdate,l_receiptdate </code></p><p>--full : Perform an ANALYZE operation on all the specified tables. The operation is performed even if the statistics are up to date.</p><p>--gen_profile_only : Update the <code>analyzedb</code> snapshot of table statistics information without performing any <code>ANALYZE</code> operations. If other options specify tables or a schema, the utility updates the snapshot information only for the specified tables.</p><p>: Specify this option if the <code>ANALYZE</code> command was run on database tables and you want to update the <code>analyzedb</code> snapshot for the tables.</p><p>-i col1,col2, ... : Optional. Must be specified with the -t option. For the table specified with the -t option, collect statistics only for the specified columns.</p><p>: Only -i, or -x can be specified. Both options cannot be specified.</p><p>-l | --list : Lists the tables that would have been analyzed with the specified options. The ANALYZE operations are not performed.</p><p>-p parallel-level : The number of tables that are analyzed in parallel. parallel level can be an integer between 1 and 10, inclusive. Default value is 5.</p><p>--skip_root_stats : This option is no longer used, you may remove it from your scripts.</p><p><code>--skip_orca_root_stats</code> : &gt; <strong>Note</strong> Do not use this option if GPORCA is enabled.</p><p>: Use this option if you find that <code>ANALYZE ROOTPARTITION</code> commands take a very long time to complete.</p><p>: &gt; <strong>Caution</strong> After you run <code>analyzedb</code> with this option, subsequent <code>analyzedb</code> executions will not update root partition statistics except when changes have been made to the table.</p><p>-s schema : Specify a schema to analyze. All tables in the schema will be analyzed. Only a single schema name can be specified on the command line.</p><p>: Only one of the options can be used to specify the files to be analyzed: <code>-f</code> or <code>--file</code>, <code>-t</code> , or <code>-s</code>.</p><p>-t schema.table : Collect statistics only on schema.table. The table name must be qualified with a schema name. Only a single table name can be specified on the command line. You can specify the <code>-f</code> option to specify multiple tables in a file or the <code>-s</code> option to specify all the tables in a schema.</p><p>: Only one of these options can be used to specify the files to be analyzed: <code>-f</code> or <code>--file</code>, <code>-t</code> , or <code>-s</code>.</p><p>-x col1,col2, ... : Optional. Must be specified with the -t option. For the table specified with the -t option, exclude statistics collection for the specified columns. Statistics are collected only on the columns that are not listed.</p><p>: Only -i, or -x can be specified. Both options cannot be specified.</p><p>-a : Quiet mode. Do not prompt for user confirmation.</p><p>--skip-locked : Skip relations that cannot be immediately locked.</p><p>-h | -? | --help : Displays the online help.</p><p>-v | --verbose : If specified, sets the logging level to verbose to write additional information the log file and to the command line during command execution. The information includes a list of all the tables to be analyzed (including leaf partitions of partitioned tables). Output also includes the duration of each <code>ANALYZE</code> operation.</p><p>--version : Displays the version of this utility.</p><h2 id="examples" tabindex="-1"><a id="exs"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;exs&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>An example that collects statistics only on a set of table columns. In the database <code>mytest</code>, collect statistics on the columns <code>shipdate</code> and <code>receiptdate</code> in the table <code>public.orders</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>analyzedb -d mytest -t public.orders -i shipdate,receiptdate</span></span></code></pre></div><p>An example that collects statistics on a table and exclude a set of columns. In the database <code>mytest</code>, collect statistics on the table <code>public.foo</code>, and do not collect statistics on the columns <code>bar</code> and <code>test2</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>analyzedb -d mytest -t public.foo -x bar,test2</span></span></code></pre></div><p>An example that specifies a file that contains a list of tables. This command collect statistics on the tables listed in the file <code>analyze-tables</code> in the database named <code>mytest</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>analyzedb -d mytest -f analyze-tables</span></span></code></pre></div><p>If you do not specify a table, set of tables, or schema, the analyzedb utility collects the statistics as needed on all catalog tables and user-defined tables in the specified database. This command refreshes table statistics on the system catalog tables and user-defined tables in the database <code>mytest</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>analyzedb -d mytest</span></span></code></pre></div><p>You can create a PL/Python function to run the <code>analyzedb</code> utility as a WarehousePG function. This example <code>CREATE FUNCTION</code> command creates a user defined PL/Python function that runs the <code>analyzedb</code> utility and displays output on the command line. Specify <code>analyzedb</code> options as the function parameter.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE OR REPLACE FUNCTION analyzedb(params TEXT)</span></span>
<span class="line"><span>  RETURNS VOID AS</span></span>
<span class="line"><span>$BODY$</span></span>
<span class="line"><span>    import subprocess</span></span>
<span class="line"><span>    cmd = [&#39;analyzedb&#39;, &#39;-a&#39; ] + params.split()</span></span>
<span class="line"><span>    p = subprocess.Popen(cmd, stdout=subprocess.PIPE, stderr=subprocess.STDOUT)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    # verbose output of process</span></span>
<span class="line"><span>    for line in iter(p.stdout.readline, &#39;&#39;):</span></span>
<span class="line"><span>        plpy.info(line);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    p.wait()</span></span>
<span class="line"><span>$BODY$</span></span>
<span class="line"><span>LANGUAGE plpython3u VOLATILE;</span></span></code></pre></div><p>When this <code>SELECT</code> command is run by the gpadmin user, the <code>analyzedb</code> utility performs an analyze operation on the table <code>public.mytable</code> that is in the database <code>mytest</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT analyzedb(&#39;-d mytest -t public.mytable&#39;) ;</span></span></code></pre></div><blockquote><p><strong>Note</strong> To create a PL/Python function, the PL/Python procedural language must be registered as a language in the database. For example, this <code>CREATE LANGUAGE</code> command run as gpadmin registers PL/Python as an untrusted language:</p></blockquote><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE LANGUAGE plpython3u;</span></span></code></pre></div><h2 id="see-also" tabindex="-1"><a id="seealso"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;seealso&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./../../ref_guide/sql_commands/ANALYZE.html">ANALYZE</a></p>`,68)]))}const u=a(o,[["render",i]]);export{b as __pageData,u as default};
