import{_ as t,c as a,o,ag as s}from"./chunks/framework.Ds6Eueu6.js";const p=JSON.parse('{"title":"About Database Statistics in WarehousePG","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/intro/about_statistics.md","filePath":"docs/7x/admin_guide/intro/about_statistics.md"}'),i={name:"docs/7x/admin_guide/intro/about_statistics.md"};function n(c,e,r,l,d,h){return o(),a("div",null,e[0]||(e[0]=[s('<h1 id="about-database-statistics-in-warehousepg" tabindex="-1">About Database Statistics in WarehousePG <a class="header-anchor" href="#about-database-statistics-in-warehousepg" aria-label="Permalink to &quot;About Database Statistics in WarehousePG&quot;">​</a></h1><hr><p>An overview of statistics gathered by the ANALYZE command in WarehousePG.</p><p>Statistics are metadata that describe the data stored in the database. The query optimizer needs up-to-date statistics to choose the best execution plan for a query. For example, if a query joins two tables and one of them must be broadcast to all segments, the optimizer can choose the smaller of the two tables to minimize network traffic.</p><p>The statistics used by the optimizer are calculated and saved in the system catalog by the <code>ANALYZE</code> command. There are three ways to initiate an analyze operation:</p><ul><li>You can run the <code>ANALYZE</code> command directly.</li><li>You can run the <code>analyzedb</code> management utility outside of the database, at the command line.</li><li>An automatic analyze operation can be triggered when DML operations are performed on tables that have no statistics or when a DML operation modifies a number of rows greater than a specified threshold.</li></ul><p>These methods are described in the following sections. The <code>VACUUM ANALYZE</code> command is another way to initiate an analyze operation, but its use is discouraged because vacuum and analyze are different operations with different purposes.</p><p>Calculating statistics consumes time and resources, so WarehousePG produces estimates by calculating statistics on samples of large tables. In most cases, the default settings provide the information needed to generate correct execution plans for queries. If the statistics produced are not producing optimal query execution plans, the administrator can tune configuration parameters to produce more accurate statistics by increasing the sample size or the granularity of statistics saved in the system catalog. Producing more accurate statistics has CPU and storage costs and may not produce better plans, so it is important to view explain plans and test query performance to ensure that the additional statistics-related costs result in better query performance.</p><p><strong>Parent topic:</strong> <a href="./../intro/concepts.html">WarehousePG Concepts</a></p><h2 id="system-statistics" tabindex="-1"><a id="topic_oq3_qxj_3s"></a>System Statistics <a class="header-anchor" href="#system-statistics" aria-label="Permalink to &quot;&lt;a id=&quot;topic_oq3_qxj_3s&quot;&gt;&lt;/a&gt;System Statistics&quot;">​</a></h2><h3 id="table-size" tabindex="-1"><a id="tabsize"></a>Table Size <a class="header-anchor" href="#table-size" aria-label="Permalink to &quot;&lt;a id=&quot;tabsize&quot;&gt;&lt;/a&gt;Table Size&quot;">​</a></h3><p>The query planner seeks to minimize the disk I/O and network traffic required to run a query, using estimates of the number of rows that must be processed and the number of disk pages the query must access. The data from which these estimates are derived are the <code>pg_class</code> system table columns <code>reltuples</code> and <code>relpages</code>, which contain the number of rows and pages at the time a <code>VACUUM</code> or <code>ANALYZE</code> command was last run. As rows are added or deleted, the numbers become less accurate. However, an accurate count of disk pages is always available from the operating system, so as long as the ratio of <code>reltuples</code> to <code>relpages</code> does not change significantly, the optimizer can produce an estimate of the number of rows that is sufficiently accurate to choose the correct query execution plan.</p><p>When the <code>reltuples</code> column differs significantly from the row count returned by <code>SELECT COUNT(*)</code>, an analyze should be performed to update the statistics.</p><p>When a <code>REINDEX</code> command finishes recreating an index, the <code>relpages</code> and <code>reltuples</code> columns are set to zero. The <code>ANALYZE</code> command should be run on the base table to update these columns.</p><h3 id="the-pg-statistic-system-table-and-pg-stats-view" tabindex="-1"><a id="pgstattab"></a>The pg_statistic System Table and pg_stats View <a class="header-anchor" href="#the-pg-statistic-system-table-and-pg-stats-view" aria-label="Permalink to &quot;&lt;a id=&quot;pgstattab&quot;&gt;&lt;/a&gt;The pg\\_statistic System Table and pg\\_stats View&quot;">​</a></h3><p>The <code>pg_statistic</code> system table holds the results of the last <code>ANALYZE</code> operation on each database table. There is a row for each column of every table. It has the following columns:</p><p>starelid : The object ID of the table or index the column belongs to.</p><p>staattnum : The number of the described column, beginning with 1.</p><p>stainherit : If true, the statistics include inheritance child columns, not just the values in the specified relation.</p><p>stanullfrac : The fraction of the column&#39;s entries that are null.</p><p>stawidth : The average stored width, in bytes, of non-null entries.</p><p>stadistinct : A positive number is an estimate of the number of distinct values in the column; the number is not expected to vary with the number of rows. A negative value is the number of distinct values divided by the number of rows, that is, the ratio of rows with distinct values for the column, negated. This form is used when the number of distinct values increases with the number of rows. A unique column, for example, has an <code>n_distinct</code> value of -1.0. Columns with an average width greater than 1024 are considered unique.</p><p>stakind<i>N</i> : A code number indicating the kind of statistics stored in the <i>N</i>th slot of the <code>pg_statistic</code> row.</p><p>staop<i>N</i> : An operator used to derive the statistics stored in the <i>N</i>th slot. For example, a histogram slot would show the &lt; operator that defines the sort order of the data.</p><p>stanumbers<i>N</i> : float4 array containing numerical statistics of the appropriate kind for the <i>N</i>th slot, or <code>NULL</code> if the slot kind does not involve numerical values.</p><p>stavalues<i>N</i> : Column data values of the appropriate kind for the <i>N</i>th slot, or <code>NULL</code> if the slot kind does not store any data values. Each array&#39;s element values are actually of the specific column&#39;s data type, so there is no way to define these columns&#39; types more specifically than <i>anyarray</i>.</p><p>The statistics collected for a column vary for different data types, so the <code>pg_statistic</code> table stores statistics that are appropriate for the data type in four <i>slots</i>, consisting of four columns per slot. For example, the first slot, which normally contains the most common values for a column, consists of the columns <code>stakind1</code>, <code>staop1</code>, <code>stanumbers1</code>, and <code>stavalues1</code>.</p><p>The <code>stakindN</code> columns each contain a numeric code to describe the type of statistics stored in their slot. The <code>stakind</code> code numbers from 1 to 99 are reserved for core PostgreSQL data types. WarehousePG uses code numbers 1, 2, 3, 4, 5, and 99. A value of 0 means the slot is unused. The following table describes the kinds of statistics stored for the three codes.</p><table class="table frame-all" id="topic_oq3_qxj_3s__table_upf_1yc_nt"><caption><span class="table--title-label">Table 1. </span><span class="title">Contents of pg_statistic &quot;slots&quot;</span></caption><colgroup><col style="width:14.285714285714285%;"><col style="width:85.71428571428571%;"></colgroup><thead class="thead"><tr class="row"><th class="entry" id="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__1">stakind Code</th><th class="entry" id="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__2">Description</th></tr></thead><tbody class="tbody"><tr class="row"><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__1">1</td><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__2"><em class="ph i">Most CommonValues (MCV) Slot</em><ul class="ul" id="topic_oq3_qxj_3s__ul_ipg_gyc_nt"><li class="li"><code class="ph codeph">staop</code> contains the object ID of the &quot;=&quot; operator, used to decide whether values are the same or not.</li><li class="li"><code class="ph codeph">stavalues</code> contains an array of the <var class="keyword varname">K</var> most common non-null values appearing in the column.</li><li class="li"><code class="ph codeph">stanumbers</code> contains the frequencies (fractions of total row count) of the values in the <code class="ph codeph">stavalues</code> array. </li></ul>The values are ordered in decreasing frequency. Since the arrays are variable-size, <var class="keyword varname">K</var> can be chosen by the statistics collector. Values must occur more than once to be added to the <code class="ph codeph">stavalues</code> array; a unique column has no MCV slot.</td></tr><tr class="row"><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__1">2</td><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__2"><em class="ph i">Histogram Slot</em> – describes the distribution of scalar data.<ul class="ul" id="topic_oq3_qxj_3s__ul_t2f_zyc_nt"><li class="li"><code class="ph codeph">staop</code> is the object ID of the &quot;&lt;&quot; operator, which describes the sort ordering. </li><li class="li"><code class="ph codeph">stavalues</code> contains <var class="keyword varname">M</var> (where <code class="ph codeph"><var class="keyword varname">M</var>&gt;=2</code>) non-null values that divide the non-null column data values into <code class="ph codeph"><var class="keyword varname">M</var>-1</code> bins of approximately equal population. The first <code class="ph codeph">stavalues</code> item is the minimum value and the last is the maximum value. </li><li class="li"><code class="ph codeph">stanumbers</code> is not used and should be <code class="ph codeph">NULL</code>. </li></ul><p class="p">If a Most Common Values slot is also provided, then the histogram describes the data distribution after removing the values listed in the MCV array. (It is a <em class="ph i">compressed histogram</em> in the technical parlance). This allows a more accurate representation of the distribution of a column with some very common values. In a column with only a few distinct values, it is possible that the MCV list describes the entire data population; in this case the histogram reduces to empty and should be omitted.</p></td></tr><tr class="row"><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__1">3</td><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__2"><em class="ph i">Correlation Slot</em> – describes the correlation between the physical order of table tuples and the ordering of data values of this column. <ul class="ul" id="topic_oq3_qxj_3s__ul_yvj_sfd_nt"><li class="li"><code class="ph codeph">staop</code> is the object ID of the &quot;&lt;&quot; operator. As with the histogram, more than one entry could theoretically appear.</li><li class="li"><code class="ph codeph">stavalues</code> is not used and should be <code class="ph codeph">NULL</code>. </li><li class="li"><code class="ph codeph">stanumbers</code> contains a single entry, the correlation coefficient between the sequence of data values and the sequence of their actual tuple positions. The coefficient ranges from +1 to -1.</li></ul></td></tr><tr class="row"><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__1">4</td><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__2"><em class="ph i">Most Common Elements Slot</em> - is similar to a Most Common Values (MCV) Slot, except that it stores the most common non-null <em class="ph i">elements</em> of the column values. This is useful when the column datatype is an array or some other type with identifiable elements (for instance, <code class="ph codeph">tsvector</code>). <ul class="ul" id="topic_oq3_qxj_3s__ul_kj4_wnm_y2b"><li class="li"><code class="ph codeph">staop</code> contains the equality operator appropriate to the element type. </li><li class="li"><code class="ph codeph">stavalues</code> contains the most common element values.</li><li class="li"><code class="ph codeph">stanumbers</code> contains common element frequencies. </li></ul><p class="p">Frequencies are measured as the fraction of non-null rows the element value appears in, not the frequency of all rows. Also, the values are sorted into the element type&#39;s default order (to support binary search for a particular value). Since this puts the minimum and maximum frequencies at unpredictable spots in <code class="ph codeph">stanumbers</code>, there are two extra members of <code class="ph codeph">stanumbers</code> that hold copies of the minimum and maximum frequencies. Optionally, there can be a third extra member that holds the frequency of null elements (the frequency is expressed in the same terms: the fraction of non-null rows that contain at least one null element). If this member is omitted, the column is presumed to contain no <code class="ph codeph">NULL</code> elements. </p><div class="note note note_note"><span class="note__title">Note:</span> For <code class="ph codeph">tsvector</code> columns, the <code class="ph codeph">stavalues</code> elements are of type <code class="ph codeph">text</code>, even though their representation within <code class="ph codeph">tsvector</code> is not exactly <code class="ph codeph">text</code>.</div></td></tr><tr class="row"><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__1">5</td><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__2"><em class="ph i">Distinct Elements Count Histogram Slot</em> - describes the distribution of the number of distinct element values present in each row of an array-type column. Only non-null rows are considered, and only non-null elements.<ul class="ul" id="topic_oq3_qxj_3s__ul_gmr_jnm_y2b"><li class="li"><code class="ph codeph">staop</code> contains the equality operator appropriate to the element type. </li><li class="li"><code class="ph codeph">stavalues</code> is not used and should be <code class="ph codeph">NULL</code>. </li><li class="li"><code class="ph codeph">stanumbers</code> contains information about distinct elements. The last member of <code class="ph codeph">stanumbers</code> is the average count of distinct element values over all non-null rows. The preceding <var class="keyword varname">M</var> (where <code class="ph codeph"><var class="keyword varname">M</var> &gt;=2</code>) members form a histogram that divides the population of distinct-elements counts into <code class="ph codeph"><var class="keyword varname">M</var>-1</code> bins of approximately equal population. The first of these is the minimum observed count, and the last the maximum.</li></ul></td></tr><tr class="row"><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__1">99</td><td class="entry" headers="topic_oq3_qxj_3s__table_upf_1yc_nt__entry__2"><em class="ph i">Hyperloglog Slot</em> - for leaf partitions of a partitioned table, stores the <code class="ph codeph">hyperloglog_counter</code> created for the sampled data. The <code class="ph codeph">hyperloglog_counter</code> data structure is converted into a <code class="ph codeph">bytea</code> and stored in a <code class="ph codeph">stavalues5</code> slot of the <code class="ph codeph">pg_statistic</code> catalog table.</td></tr></tbody></table><p>The <code>pg_stats</code> view presents the contents of <code>pg_statistic</code> in a friendlier format. The <code>pg_stats</code> view has the following columns:</p><p>schemaname : The name of the schema containing the table.</p><p>tablename : The name of the table.</p><p>attname : The name of the column this row describes.</p><p>inherited : If true, the statistics include inheritance child columns.</p><p>null_frac : The fraction of column entries that are null.</p><p>avg_width : The average storage width in bytes of the column&#39;s entries, calculated as <code>avg(pg_column_size(column_name))</code>.</p><p>n_distinct : A positive number is an estimate of the number of distinct values in the column; the number is not expected to vary with the number of rows. A negative value is the number of distinct values divided by the number of rows, that is, the ratio of rows with distinct values for the column, negated. This form is used when the number of distinct values increases with the number of rows. A unique column, for example, has an <code>n_distinct</code> value of -1.0. Columns with an average width greater than 1024 are considered unique.</p><p>most_common_vals : An array containing the most common values in the column, or null if no values seem to be more common. If the <code>n_distinct</code> column is -1, <code>most_common_vals</code> is null. The length of the array is the lesser of the number of actual distinct column values or the value of the <code>default_statistics_target</code> configuration parameter. The number of values can be overridden for a column using <code>ALTER TABLE table SET COLUMN column SET STATISTICS N</code>.</p><p>most_common_freqs : An array containing the frequencies of the values in the <code>most_common_vals</code> array. This is the number of occurrences of the value divided by the total number of rows. The array is the same length as the <code>most_common_vals</code> array. It is null if <code>most_common_vals</code> is null.</p><p>histogram_bounds : An array of values that divide the column values into groups of approximately the same size. A histogram can be defined only if there is a <code>max()</code> aggregate function for the column. The number of groups in the histogram is the same as the <code>most_common_vals</code> array size.</p><p>correlation : WarehousePG computes correlation statistics for both heap and AO/AOCO tables, but the Postgres-based planner uses these statistics only for heap tables.</p><p>most_common_elems : An array that contains the most common element values.</p><p>most_common_elem_freqs : An array that contains common element frequencies.</p><p>elem_count_histogram : An array that describes the distribution of the number of distinct element values present in each row of an array-type column.</p><p>Newly created tables and indexes have no statistics. You can check for tables with missing statistics using the <code>gp_stats_missing</code> view, which is in the <code>gp_toolkit</code> schema:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * from gp_toolkit.gp_stats_missing;</span></span></code></pre></div><h3 id="sampling" tabindex="-1"><a id="section_wsy_1rv_mt"></a>Sampling <a class="header-anchor" href="#sampling" aria-label="Permalink to &quot;&lt;a id=&quot;section_wsy_1rv_mt&quot;&gt;&lt;/a&gt;Sampling&quot;">​</a></h3><p>When calculating statistics for large tables, WarehousePG creates a smaller table by sampling the base table. If the table is partitioned, samples are taken from all partitions.</p><h3 id="updating-statistics" tabindex="-1"><a id="section_u5p_brv_mt"></a>Updating Statistics <a class="header-anchor" href="#updating-statistics" aria-label="Permalink to &quot;&lt;a id=&quot;section_u5p_brv_mt&quot;&gt;&lt;/a&gt;Updating Statistics&quot;">​</a></h3><p>Running <code>ANALYZE</code> with no arguments updates statistics for all tables in the database. This could take a very long time, so it is better to analyze tables selectively after data has changed. You can also analyze a subset of the columns in a table, for example columns used in joins, <code>WHERE</code> clauses, <code>SORT</code> clauses, <code>GROUP BY</code> clauses, or <code>HAVING</code> clauses.</p><p>Analyzing a severely bloated table can generate poor statistics if the sample contains empty pages, so it is good practice to vacuum a bloated table before analyzing it.</p><p>See the <em>SQL Command Reference</em> in the <em>WarehousePG Reference Guide</em> for details of running the <code>ANALYZE</code> command.</p><p>Refer to the <em>WarehousePG Management Utility Reference</em> for details of running the <code>analyzedb</code> command.</p><h3 id="analyzing-partitioned-tables" tabindex="-1"><a id="section_cv2_crv_mt"></a>Analyzing Partitioned Tables <a class="header-anchor" href="#analyzing-partitioned-tables" aria-label="Permalink to &quot;&lt;a id=&quot;section_cv2_crv_mt&quot;&gt;&lt;/a&gt;Analyzing Partitioned Tables&quot;">​</a></h3><p>When the <code>ANALYZE</code> command is run on a partitioned table, it analyzes each leaf partition, one at a time. You can run <code>ANALYZE</code> on just new or changed partitioned tables to avoid analyzing partitions that have not changed.</p><p>The <code>analyzedb</code> command-line utility skips unchanged partitions automatically. It also runs concurrent sessions so it can analyze several partitions concurrently. It runs five sessions by default, but the number of sessions can be set from 1 to 10 with the <code>-p</code> command-line option. Each time <code>analyzedb</code> runs, it saves state information for append-optimized tables and partitions in the <code>db_analyze</code> directory in the coordinator data directory. The next time it runs, <code>analyzedb</code> compares the current state of each table with the saved state and skips analyzing a table or partition if it is unchanged. Heap tables are always analyzed.</p><p>If GPORCA is enabled (the default), you also need to run <code>ANALYZE</code> or <code>ANALYZE ROOTPARTITION</code> on the root partitioned table (not a leaf partition) to refresh the root partition statistics. GPORCA requires statistics at the root level for partitioned tables. The Postgres-based planner does not use these statistics.</p><p>The time to analyze a partitioned table is similar to the time to analyze a non-partitioned table with the same data. When all the leaf partitions have statistics, performing <code>ANALYZE ROOTPARTITION</code> to generate root partition statistics should be quick (a few seconds depending on the number of partitions and table columns). If some of the leaf partitions do not have statistics, then all the table data is sampled to generate root partition statistics. Sampling table data takes longer and results in lower quality root partition statistics.</p><p>The WarehousePG server configuration parameter <a href="./../../ref_guide/config_params/guc-list.html">optimizer_analyze_root_partition</a> affects when statistics are collected on the root partitioned table. If the parameter is <code>on</code> (the default), the <code>ROOTPARTITION</code> keyword is not required to collect statistics on the root partition when you run <code>ANALYZE</code>. Root partition statistics are collected when you run <code>ANALYZE</code> on the root partition, or when you run <code>ANALYZE</code> on a leaf partition of the partitioned table and the other leaf partitions have statistics. If the parameter is <code>off</code>, you must run <code>ANALYZE ROOTPARTITION</code> to collect root partition statistics.</p><p>If you do not intend to run queries on partitioned tables with GPORCA (setting the server configuration parameter <a href="./../../ref_guide/config_params/guc-list.html">optimizer</a> to <code>off</code>), you can also set the server configuration parameter <code>optimizer_analyze_root_partition</code> to <code>off</code> to limit when <code>ANALYZE</code> updates the root partition statistics.</p><h2 id="configuring-statistics" tabindex="-1"><a id="topic_gyb_qrd_2t"></a>Configuring Statistics <a class="header-anchor" href="#configuring-statistics" aria-label="Permalink to &quot;&lt;a id=&quot;topic_gyb_qrd_2t&quot;&gt;&lt;/a&gt;Configuring Statistics&quot;">​</a></h2><p>There are several options for configuring WarehousePG statistics collection.</p><h3 id="statistics-target" tabindex="-1"><a id="stattarg"></a>Statistics Target <a class="header-anchor" href="#statistics-target" aria-label="Permalink to &quot;&lt;a id=&quot;stattarg&quot;&gt;&lt;/a&gt;Statistics Target&quot;">​</a></h3><p>The statistics target is the size of the <code>most_common_vals</code>, <code>most_common_freqs</code>, and <code>histogram_bounds</code> arrays for an individual column. By default, the target is 25. The default target can be changed by setting a server configuration parameter and the target can be set for any column using the <code>ALTER TABLE</code> command. Larger values increase the time needed to do <code>ANALYZE</code>, but may improve the quality of the Postgres-based planner estimates.</p><p>Set the system default statistics target to a different value by setting the <code>default_statistics_target</code> server configuration parameter. The default value is usually sufficient, and you should only raise or lower it if your tests demonstrate that query plans improve with the new target. For example, to raise the default statistics target from 100 to 150 you can use the <code>gpconfig</code> utility:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpconfig -c default_statistics_target -v 150</span></span></code></pre></div><p>The statistics target for individual columns can be set with the <code>ALTER TABLE</code> command. For example, some queries can be improved by increasing the target for certain columns, especially columns that have irregular distributions. You can set the target to zero for columns that never contribute to query optimization. When the target is 0, <code>ANALYZE</code> ignores the column. For example, the following <code>ALTER TABLE</code> command sets the statistics target for the <code>notes</code> column in the <code>emp</code> table to zero:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ALTER TABLE emp ALTER COLUMN notes SET STATISTICS 0;</span></span></code></pre></div><p>The statistics target can be set in the range 0 to 1000, or set it to -1 to revert to using the system default statistics target.</p><p>Setting the statistics target on a parent partitioned table affects the child partitions. If you set statistics to 0 on some columns on the parent table, the statistics for the same columns are set to 0 for all children partitions. However, if you later add or exchange another child partition, the new child partition will use either the default statistics target or, in the case of an exchange, the previous statistics target. Therefore, if you add or exchange child partitions, you should set the statistics targets on the new child table.</p><h3 id="automatic-statistics-collection" tabindex="-1"><a id="section_j3p_drv_mt"></a>Automatic Statistics Collection <a class="header-anchor" href="#automatic-statistics-collection" aria-label="Permalink to &quot;&lt;a id=&quot;section_j3p_drv_mt&quot;&gt;&lt;/a&gt;Automatic Statistics Collection&quot;">​</a></h3><p>WarehousePG can be set to automatically run <code>ANALYZE</code> on a table that either has no statistics or has changed significantly when certain operations are performed on the table. For partitioned tables, automatic statistics collection is only triggered when the operation is run directly on a leaf table, and then only the leaf table is analyzed.</p><p>Automatic statistics collection is governed by a server configuration parameter, and has three modes:</p><ul><li><code>none</code> deactivates automatic statistics collection.</li><li><code>on_no_stats</code> triggers an analyze operation for a table with no existing statistics when any of the commands <code>CREATE TABLE AS SELECT</code>, <code>INSERT</code>, or <code>COPY</code> are run on the table by the table owner.</li><li><code>on_change</code> triggers an analyze operation when any of the commands <code>CREATE TABLE AS SELECT</code>, <code>UPDATE</code>, <code>DELETE</code>, <code>INSERT</code>, or <code>COPY</code> are run on the table by the table owner, and the number of rows affected exceeds the threshold defined by the <code>gp_autostats_on_change_threshold</code> configuration parameter.</li></ul><p>The automatic statistics collection mode is set separately for commands that occur within a procedural language function and commands that run outside of a function:</p><ul><li>The <code>gp_autostats_mode</code> configuration parameter controls automatic statistics collection behavior outside of functions and is set to <code>on_no_stats</code> by default.</li><li>The <code>gp_autostats_mode_in_functions</code> parameter controls the behavior when table operations are performed within a procedural language function and is set to <code>none</code> by default.</li></ul><p>With the <code>on_change</code> mode, <code>ANALYZE</code> is triggered only if the number of rows affected exceeds the threshold defined by the <code>gp_autostats_on_change_threshold</code> configuration parameter. The default value for this parameter is a very high value, 2147483647, which effectively deactivates automatic statistics collection; you must set the threshold to a lower number to enable it. The <code>on_change</code> mode could trigger large, unexpected analyze operations that could disrupt the system, so it is not recommended to set it globally. It could be useful in a session, for example to automatically analyze a table following a load.</p><p>Setting the <code>gp_autostats_allow_nonowner</code> server configuration parameter to <code>true</code> also instructs WarehousePG to trigger automatic statistics collection on a table when:</p><ul><li><code>gp_autostats_mode=on_change</code> and the table is modified by a non-owner.</li><li><code>gp_autostats_mode=on_no_stats</code> and the first user to <code>INSERT</code> or <code>COPY</code> into the table is a non-owner.</li></ul><p>To deactivate automatic statistics collection outside of functions, set the <code>gp_autostats_mode</code> parameter to <code>none</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpconfigure -c gp_autostats_mode -v none</span></span></code></pre></div><p>To enable automatic statistics collection in functions for tables that have no statistics, change <code>gp_autostats_mode_in_functions</code> to <code>on_no_stats</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpconfigure -c gp_autostats_mode_in_functions -v on_no_stats</span></span></code></pre></div><p>Set the <code>log_autostats</code> system configuration parameter to on if you want to log automatic statistics collection operations.</p>',84)]))}const m=t(i,[["render",n]]);export{p as __pageData,m as default};
