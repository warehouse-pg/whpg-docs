import{_ as a,c as t,o as n,ag as s}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"CREATE FOREIGN TABLE","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/CREATE_FOREIGN_TABLE.md","filePath":"docs/7x/ref_guide/sql_commands/CREATE_FOREIGN_TABLE.md"}'),o={name:"docs/7x/ref_guide/sql_commands/CREATE_FOREIGN_TABLE.md"};function i(r,e,l,p,c,h){return n(),t("div",null,e[0]||(e[0]=[s(`<h1 id="create-foreign-table" tabindex="-1">CREATE FOREIGN TABLE <a class="header-anchor" href="#create-foreign-table" aria-label="Permalink to &quot;CREATE FOREIGN TABLE&quot;">​</a></h1><p>Defines a new foreign table.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE FOREIGN TABLE [ IF NOT EXISTS ] &lt;table_name&gt; ( [</span></span>
<span class="line"><span>  { &lt;column_name&gt; &lt;data_type&gt; [ OPTIONS ( &lt;option&gt; &#39;&lt;value&gt;&#39; [, ... ] ) ] [ COLLATE &lt;collation&gt; ] [ &lt;column_constraint&gt; [ ... ] ]</span></span>
<span class="line"><span>    | &lt;table_constraint&gt; }</span></span>
<span class="line"><span>    [, ... ]</span></span>
<span class="line"><span>] )</span></span>
<span class="line"><span>[ INHERITS ( &lt;parent_table&gt; [, ... ] ) ]</span></span>
<span class="line"><span>  SERVER &lt;server_name&gt;</span></span>
<span class="line"><span>  [ OPTIONS ( [ mpp_execute { &#39;coordinator&#39; | &#39;any&#39; | &#39;all segments&#39; } [, ] ] &lt;option&gt; &#39;&lt;value&gt;&#39; [, ... ] ) ]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CREATE FOREIGN TABLE [ IF NOT EXISTS ] &lt;table_name&gt;</span></span>
<span class="line"><span>  PARTITION OF &lt;parent_table&gt; [ (</span></span>
<span class="line"><span>  { &lt;column_name&gt; [ WITH OPTIONS ] [ &lt;column_constraint&gt; [ ... ] ]</span></span>
<span class="line"><span>    | &lt;table_constraint&gt; }</span></span>
<span class="line"><span>    [, ... ]</span></span>
<span class="line"><span>) ]</span></span>
<span class="line"><span>{ FOR VALUES &lt;partition_bound_spec&gt; | DEFAULT }</span></span>
<span class="line"><span>  SERVER &lt;server_name&gt;</span></span>
<span class="line"><span>  [ OPTIONS ( [ mpp_execute { &#39;coordinator&#39; | &#39;any&#39; | &#39;all segments&#39; } [, ] ] &lt;option&gt; &#39;&lt;value&gt;&#39; [, ... ] ) ]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>where &lt;column_constraint&gt; is:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[ CONSTRAINT &lt;constraint_name&gt; ]</span></span>
<span class="line"><span>{ NOT NULL |</span></span>
<span class="line"><span>  NULL |</span></span>
<span class="line"><span>  DEFAULT &lt;default_expr&gt; |</span></span>
<span class="line"><span>  GENERATED ALWAYS AS ( &lt;generation_expr&gt; ) STORED}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>and &lt;table_constraint&gt; is:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[ CONSTRAINT &lt;constraint_name&gt; ]</span></span>
<span class="line"><span>CHECK ( &lt;expression&gt; ) [ NO INHERIT ]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>and &lt;partition_bound_spec&gt; is:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>IN ( &lt;partition_bound_expr&gt; [, ...] ) |</span></span>
<span class="line"><span>FROM ( { &lt;partition_bound_expr&gt; | MINVALUE | MAXVALUE } [, ...] )</span></span>
<span class="line"><span>  TO ( { &lt;partition_bound_expr&gt; | MINVALUE | MAXVALUE } [, ...] ) |</span></span>
<span class="line"><span>WITH ( MODULUS &lt;numeric_literal&gt;, REMAINDER &lt;numeric_literal&gt; )</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>CREATE FOREIGN TABLE</code> creates a new foreign table in the current database. The user who creates the foreign table becomes its owner.</p><p>If you schema-qualify the table name (for example, <code>CREATE FOREIGN TABLE myschema.mytable ...</code>), WarehousePG creates the table in the specified schema. Otherwise, the foreign table is created in the current schema. The name of the foreign table must be distinct from the name of any other foreign table, table, sequence, index, view, or materialized view in the same schema.</p><p>Because <code>CREATE FOREIGN TABLE</code> automatically creates a data type that represents the composite type corresponding to one row of the foreign table, foreign tables cannot have the same name as any existing data type in the same schema.</p><p>If the <code>PARTITION OF</code> clause is specified, then the table is created as a partition of parent_table with specified bounds.</p><p>To create a foreign table, you must have <code>USAGE</code> privilege on the foreign server, as well as <code>USAGE</code> privilege on all column types used in the table.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>IF NOT EXISTS : Do not throw an error if a relation with the same name already exists. WarehousePG issues a notice in this case. Note that there is no guarantee that the existing relation is anything like the one that would have been created.</p><p>table_name : The name (optionally schema-qualified) of the foreign table to create.</p><p>column_name : The name of a column to create in the new foreign table.</p><p>data_type : The data type of the column, including array specifiers.</p><p>COLLATE collation : The <code>COLLATE</code> clause assigns a collation to the column (which must be of a collatable data type). If not specified, the column data type&#39;s default collation is used.</p><p>INHERITS ( parent_table [, ... ] ) : The optional <code>INHERITS</code> clause specifies a list of tables from which the new foreign table automatically inherits all columns. Parent tables can be plain tables or foreign tables. See the similar form of <a href="./CREATE_TABLE.html">CREATE TABLE</a> for more details.</p><p>PARTITION OF parent_table { FOR VALUES partition_bound_spec | DEFAULT } : This form can be used to create the foreign table as partition of the given parent table with specified partition bound values. See the similar form of <a href="./CREATE_TABLE.html">CREATE TABLE</a> for more details. Note that it is currently not allowed to create the foreign table as a partition of the parent table if there are <code>UNIQUE</code> indexes on the parent table. (See also <a href="./ALTER_TABLE.html">ALTER TABLE ATTACH PARTITION</a>.)</p><p>CONSTRAINT constraint_name : An optional name for a column or table constraint. If the constraint is violated, the constraint name is present in error messages, so constraint names like <code>col must be positive</code> can be used to communicate helpful constraint information to client applications. (Double-quotes are needed to specify constraint names that contain spaces.) If a constraint name is not specified, the system generates a name.</p><p>NOT NULL : The column is not allowed to contain null values.</p><p>NULL : The column is allowed to contain null values. This is the default.</p><pre><code>This clause is provided only for compatibility with non-standard SQL databases. Its use is discouraged in new applications.
</code></pre><p>CHECK ( expression ) [ NO INHERIT ] : The <code>CHECK</code> clause specifies an expression producing a Boolean result which each row in the foreign table is expected to satisfy; that is, the expression should produce TRUE or UNKNOWN, never FALSE, for all rows in the foreign table. A check constraint specified as a column constraint should reference that column&#39;s value only, while an expression appearing in a table constraint can reference multiple columns. : Currently, <code>CHECK</code> expressions cannot contain subqueries nor refer to variables other than columns of the current row. The system column <code>tableoid</code> may be referenced, but not any other system column. : A constraint marked with <code>NO INHERIT</code> will not propagate to child tables.</p><p>DEFAULT default_expr : The <code>DEFAULT</code> clause assigns a default value for the column whose definition it appears within. The value is any variable-free expression; WarehousePG does not allow subqueries and cross-references to other columns in the current table. The data type of the default expression must match the data type of the column. : WarehousePG uses the default expression in any insert operation that does not specify a value for the column. If there is no default for a column, then the default is null.</p><p>GENERATED ALWAYS AS ( generation_expr ) STORED : This clause creates the column as a generated column. The column cannot be written to, and when read the result of the specified expression will be returned. : The keyword <code>STORED</code> is required to signify that the column will be computed on write. (The computed value will be presented to the foreign-data wrapper for storage and must be returned on reading.) : The generation expression can refer to other columns in the table, but not other generated columns. Any functions and operators used must be immutable. References to other tables are not allowed.</p><p>server_name : The name of an existing server to use for the foreign table. For details on defining a server, see <a href="./CREATE_SERVER.html">CREATE SERVER</a>.</p><p>OPTIONS ( option &#39;value&#39; [, ... ] ) : The options for the new foreign table or one of its columns. While option names must be unique, a table option and a column option may have the same name. The option names and values are foreign-data wrapper-specific. WarehousePG validates the options and values using the foreign-data wrapper&#39;s validator_function.</p><p>mpp_execute { &#39;coordinator&#39; | &#39;any&#39; | &#39;all segments&#39; } : A WarehousePG-specific option that identifies the host from which the foreign-data wrapper reads or writes data:</p><pre><code>-   \`coordinator\` \\(the default\\)—Read or write data from the coordinator host.
-   \`any\`—Read data from either the coordinator host or any one segment, depending on which path costs less.
-   \`all segments\`—Read or write data from all segments. To support this option value, the foreign-data wrapper must have a policy that matches the segments to data.

&gt; **Note** WarehousePG supports parallel writes to foreign tables only when you set \`mpp_execute &#39;all segments&#39;\`.

Support for the foreign table \`mpp_execute\` option, and the specific modes, is foreign-data wrapper-specific.

The \`mpp_execute\` option can be specified in multiple commands: \`CREATE FOREIGN TABLE\`, \`CREATE SERVER\`, and \`CREATE FOREIGN DATA WRAPPER\`. The foreign table setting takes precedence over the foreign server setting, followed by the foreign-data wrapper setting.
</code></pre><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>Constraints on foreign tables (such as <code>CHECK</code> or <code>NOT NULL</code> clauses) are not enforced by WarehousePG, and most foreign-data wrappers do not attempt to enforce them either; that is, the constraint is simply assumed to hold true. There would be little point in such enforcement since it would only apply to rows inserted or updated via the foreign table, and not to rows modified by other means, such as directly on the remote server. Instead, a constraint attached to a foreign table should represent a constraint that is being enforced by the remote server.</p><p>Some special-purpose foreign-data wrappers might be the only access mechanism for the data they access, and in that case it might be appropriate for the foreign-data wrapper itself to perform constraint enforcement. But you should not assume that a wrapper does that unless its documentation says so.</p><p>Although WarehousePG does not attempt to enforce constraints on foreign tables, it does assume that they are correct for purposes of query optimization. If there are rows visible in the foreign table that do not satisfy a declared constraint, queries on the table might produce errors or incorrect answers. It is the user&#39;s responsibility to ensure that the constraint definition matches reality.</p><div class="note">When a foreign table is used as a partition of a partitioned table, there is an implicit constraint that its contents must satisfy the partitioning rule. Again, it is the user&#39;s responsibility to ensure that that is true, which is best done by installing a matching constraint on the remote server.</div><p>Within a partitioned table containing foreign-table partitions, an <code>UPDATE</code> that changes the partition key value can cause a row to be moved from a local partition to a foreign-table partition, provided the foreign-data wrapper supports tuple routing. However it is not currently possible to move a row from a foreign-table partition to another partition. An <code>UPDATE</code> that would require doing that will fail due to the partitioning constraint, assuming that that is properly enforced by the remote server.</p><p>Similar considerations apply to generated columns. Stored generated columns are computed on insert or update on the local WarehousePG server and handed to the foreign-data wrapper for writing out to the foreign data store, but it is not enforced that a query of the foreign table returns values for stored generated columns that are consistent with the generation expression. Again, this might result in incorrect query results.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Create a foreign table named <code>films</code> with the server named <code>film_server</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE FOREIGN TABLE films (</span></span>
<span class="line"><span>    code        char(5) NOT NULL,</span></span>
<span class="line"><span>    title       varchar(40) NOT NULL,</span></span>
<span class="line"><span>    did         integer NOT NULL,</span></span>
<span class="line"><span>    date_prod   date,</span></span>
<span class="line"><span>    kind        varchar(10),</span></span>
<span class="line"><span>    len         interval hour to minute</span></span>
<span class="line"><span>)</span></span>
<span class="line"><span>SERVER film_server;</span></span></code></pre></div><p>Create foreign table <code>measurement_y2016m07</code>, which will be accessed through the server <code>server_07</code>, as a partition of the range partitioned table measurement:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE FOREIGN TABLE measurement_y2016m07</span></span>
<span class="line"><span>    PARTITION OF measurement FOR VALUES FROM (&#39;2016-07-01&#39;) TO (&#39;2016-08-01&#39;)</span></span>
<span class="line"><span>    SERVER server_07;</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p><code>CREATE FOREIGN TABLE</code> largely conforms to the SQL standard; however, much as with <a href="./CREATE_TABLE.html">CREATE TABLE</a>, WarehousePG permits <code>NULL</code> constraints and zero-column foreign tables. The ability to specify column default values is a WarehousePG extension, as is the <code>mpp_execute</code> option. Table inheritance, in the form defined by WarehousePG, is nonstandard.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./ALTER_FOREIGN_TABLE.html">ALTER FOREIGN TABLE</a>, <a href="./DROP_FOREIGN_TABLE.html">DROP FOREIGN TABLE</a>, <a href="./CREATE_SERVER.html">CREATE SERVER</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,46)]))}const m=a(o,[["render",i]]);export{u as __pageData,m as default};
