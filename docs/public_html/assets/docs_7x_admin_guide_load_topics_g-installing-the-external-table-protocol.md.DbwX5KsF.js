import{_ as o,c as t,o as a,ag as c}from"./chunks/framework.Ds6Eueu6.js";const m=JSON.parse('{"title":"Installing the External Table Protocol","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/load/topics/g-installing-the-external-table-protocol.md","filePath":"docs/7x/admin_guide/load/topics/g-installing-the-external-table-protocol.md"}'),r={name:"docs/7x/admin_guide/load/topics/g-installing-the-external-table-protocol.md"};function n(l,e,i,s,d,p){return a(),t("div",null,e[0]||(e[0]=[c('<h1 id="installing-the-external-table-protocol" tabindex="-1">Installing the External Table Protocol <a class="header-anchor" href="#installing-the-external-table-protocol" aria-label="Permalink to &quot;Installing the External Table Protocol&quot;">​</a></h1><hr><p>To use the example external table protocol, you use the C compiler <code>cc</code> to compile and link the source code to create a shared object that can be dynamically loaded by WarehousePG. The commands to compile and link the source code on a Linux system are similar to this:</p><p><code>cc -fpic -c gpextprotocal.c cc -shared -o gpextprotocal.so gpextprotocal.o</code></p><p>The option <code>-fpic</code> specifies creating position-independent code (PIC) and the <code>-c</code> option compiles the source code without linking and creates an object file. The object file needs to be created as position-independent code (PIC) so that it can be loaded at any arbitrary location in memory by WarehousePG.</p><p>The flag <code>-shared</code> specifies creating a shared object (shared library) and the <code>-o</code> option specifies the shared object file name <code>gpextprotocal.so</code>. Refer to the GCC manual for more information on the <code>cc</code> options.</p><p>The header files that are declared as include files in <code>gpextprotocal.c</code> are located in subdirectories of <code>$GPHOME/include/postgresql/.</code></p><p>For more information on compiling and linking dynamically-loaded functions and examples of compiling C source code to create a shared library on other operating systems, see the PostgreSQL documentation at <a href="https://www.postgresql.org/docs/12/xfunc-c.html#DFUNC" target="_blank" rel="noreferrer">https://www.postgresql.org/docs/12/xfunc-c.html#DFUNC</a>.</p><p>The manual pages for the C compiler <code>cc</code> and the link editor <code>ld</code> for your operating system also contain information on compiling and linking source code on your system.</p><p>The compiled code (shared object file) for the custom protocol must be placed in the same location on every host in your WarehousePG cluster (coordinator and all segments). This location must also be in the <code>LD_LIBRARY_PATH</code> so that the server can locate the files. It is recommended to locate shared libraries either relative to <code>$libdir</code> (which is located at <code>$GPHOME/lib</code>) or through the dynamic library path (set by the <code>dynamic_library_path</code> server configuration parameter) on all coordinator segment instances in the WarehousePG cluster. You can use the WarehousePG utilities gpssh and <code>gpsync</code> to update segments.</p><ul><li><strong><a href="./../../load/topics/g-gpextprotocalc.html">gpextprotocal.c</a></strong></li></ul><p><strong>Parent topic:</strong> <a href="./../../load/topics/g-example-custom-data-access-protocol.html">Example Custom Data Access Protocol</a></p>',12)]))}const g=o(r,[["render",n]]);export{m as __pageData,g as default};
