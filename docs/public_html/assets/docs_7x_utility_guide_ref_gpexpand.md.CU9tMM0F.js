import{_ as t,c as s,o as a,ag as n}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"gpexpand","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/gpexpand.md","filePath":"docs/7x/utility_guide/ref/gpexpand.md"}'),o={name:"docs/7x/utility_guide/ref/gpexpand.md"};function i(r,e,p,d,c,l){return a(),s("div",null,e[0]||(e[0]=[n('<h1 id="gpexpand" tabindex="-1">gpexpand <a class="header-anchor" href="#gpexpand" aria-label="Permalink to &quot;gpexpand&quot;">​</a></h1><p>Expands an existing WarehousePG across new hosts in the system.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpexpand [{-f|--hosts-file} &lt;hosts_file&gt;]</span></span>\n<span class="line"><span>        | {-i|--input} &lt;input_file&gt; [-B &lt;batch_size&gt;]</span></span>\n<span class="line"><span>        | [{-d | --duration} &lt;hh:mm:ss&gt; | {-e|--end} &#39;&lt;YYYY-MM-DD hh:mm:ss&gt;&#39;] </span></span>\n<span class="line"><span>          [-a|-analyze] </span></span>\n<span class="line"><span>          [-n  &lt;parallel_processes&gt;]</span></span>\n<span class="line"><span>        | {-r|--rollback}</span></span>\n<span class="line"><span>        | {-c|--clean}</span></span>\n<span class="line"><span>        [-v|--verbose] [-s|--silent]</span></span>\n<span class="line"><span>        [{-t|--tardir} &lt;directory&gt; ]</span></span>\n<span class="line"><span>        [-S|--simple-progress ]</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>gpexpand -? | -h | --help </span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>gpexpand --version</span></span></code></pre></div><h2 id="prerequisites" tabindex="-1"><a id="section3"></a>Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Prerequisites&quot;">​</a></h2><ul><li>You are logged in as the WarehousePG superuser (<code>gpadmin</code>).</li><li>The new segment hosts have been installed and configured as per the existing segment hosts. This involves: <ul><li>Configuring the hardware and OS</li><li>Installing the WarehousePG software</li><li>Creating the <code>gpadmin</code> user account</li><li>Exchanging SSH keys.</li></ul></li><li>Enough disk space on your segment hosts to temporarily hold a copy of your largest table.</li><li>When redistributing data, WarehousePG must be running in production mode. WarehousePG cannot be running in restricted mode or in coordinator mode. The <code>gpstart</code> options <code>-R</code> or <code>-m</code> cannot be specified to start WarehousePG.</li></ul><blockquote><p><strong>Note</strong> These utilities cannot be run while <code>gpexpand</code> is performing segment initialization.</p></blockquote><ul><li><code>gpbackup</code></li><li><code>gpcheckcat</code></li><li><code>gpconfig</code></li><li><code>gppkg</code></li><li><code>gprestore</code></li></ul><blockquote><p><strong>Important</strong> When expanding a WarehousePG cluster, you must deactivate WarehousePG interconnect proxies before adding new hosts and segment instances to the system, and you must update the <code>gp_interconnect_proxy_addresses</code> parameter with the newly-added segment instances before you re-enable interconnect proxies. For information about WarehousePG interconnect proxies, see <a href="./../../admin_guide/managing/proxy-ic.html">Configuring Proxies for the WarehousePG Interconnect</a>.</p></blockquote><p>For information about preparing a system for expansion, see <a href="./../../admin_guide/expand/expand-main.html">Expanding a WarehousePG cluster</a>in the <em>WarehousePG Administrator Guide</em>.</p><h2 id="description" tabindex="-1"><a id="section4"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>The <code>gpexpand</code> utility performs system expansion in two phases: segment instance initialization and then table data redistribution.</p><p>In the initialization phase, <code>gpexpand</code> runs with an input file that specifies data directories, dbid values, and other characteristics of the new segment instances. You can create the input file manually, or by following the prompts in an interactive interview.</p><p>If you choose to create the input file using the interactive interview, you can optionally specify a file containing a list of expansion system hosts. If your platform or command shell limits the length of the list of hostnames that you can type when prompted in the interview, specifying the hosts with <code>-f</code> may be mandatory.</p><p>In addition to initializing the segment instances, the initialization phase performs these actions:</p><ul><li>Creates an expansion schema named <em>gpexpand</em> in the postgres database to store the status of the expansion operation, including detailed status for tables.</li></ul><p>In the table data redistribution phase, <code>gpexpand</code> redistributes table data to rebalance the data across the old and new segment instances.</p><blockquote><p><strong>Note</strong> Data redistribution should be performed during low-use hours. Redistribution can be divided into batches over an extended period.</p></blockquote><p>To begin the redistribution phase, run <code>gpexpand</code> with no options or with the <code>-d</code> (duration), <code>-e</code> (end time), or <code>-i</code> options. If you specify an end time or duration, then the utility redistributes tables in the expansion schema until the specified end time or duration is reached. If you specify <code>-i</code> or no options, then the utility redistribution phase continues until all tables in the expansion schema are reorganized. Each table is reorganized using <code>ALTER TABLE</code> commands to rebalance the tables across new segments, and to set tables to their original distribution policy. If <code>gpexpand</code> completes the reorganization of all tables, it displays a success message and ends.</p><blockquote><p><strong>Note</strong> This utility uses secure shell (SSH) connections between systems to perform its tasks. In large WarehousePG deployments, cloud deployments, or deployments with a large number of segments per host, this utility may exceed the host&#39;s maximum threshold for unauthenticated connections. Consider updating the SSH <code>MaxStartups</code> and <code>MaxSessions</code> configuration parameters to increase this threshold. For more information about SSH configuration options, refer to the SSH documentation for your Linux distribution.</p></blockquote><h2 id="options" tabindex="-1"><a id="section5"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>-a | --analyze : Run <code>ANALYZE</code> to update the table statistics after expansion. The default is to not run <code>ANALYZE</code>.</p><p>-B batch_size : Batch size of remote commands to send to a given host before making a one-second pause. Default is <code>16</code>. Valid values are 1-128.</p><p>: The <code>gpexpand</code> utility issues a number of setup commands that may exceed the host&#39;s maximum threshold for unauthenticated connections as defined by <code>MaxStartups</code> in the SSH daemon configuration. The one-second pause allows authentications to be completed before <code>gpexpand</code> issues any more commands.</p><p>: The default value does not normally need to be changed. However, it may be necessary to reduce the maximum number of commands if <code>gpexpand</code> fails with connection errors such as <code>&#39;ssh_exchange_identification: Connection closed by remote host.&#39;</code></p><p>-c | --clean : Remove the expansion schema.</p><p>-d | --duration hh:mm:ss : Duration of the expansion session from beginning to end.</p><p>-e | --end &#39;YYYY-MM-DD hh:mm:ss&#39; : Ending date and time for the expansion session.</p><p>-f | --hosts-file filename : Specifies the name of a file that contains a list of new hosts for system expansion. Each line of the file must contain a single host name.</p><p>: This file can contain hostnames with or without network interfaces specified. The <code>gpexpand</code> utility handles either case, adding interface numbers to end of the hostname if the original nodes are configured with multiple network interfaces.</p><pre><code>&gt; **Note** The WarehousePG segment host naming convention is `sdwN` where `sdw` is a prefix and `N` is an integer. For example, `sdw1`, `sdw2` and so on. For hosts with multiple interfaces, the convention is to append a dash \\(`-`\\) and number to the host name. For example, `sdw1-1` and `sdw1-2` are the two interface names for host `sdw1`.\n</code></pre><p>: For information about using a hostname or IP address, see <a href="#host_ip">Specifying Hosts using Hostnames or IP Addresses</a>. Also, see <a href="#multi_nic">Using Host Systems with Multiple NICs</a>.</p><p>-i | --input input_file : Specifies the name of the expansion configuration file, which contains one line for each segment to be added in the format of:</p><p>: hostname|address|port|datadir|dbid|content|preferred_role</p><p>-n parallel_processes : The number of tables to redistribute simultaneously. Valid values are 1 - 96.</p><p>: Each table redistribution process requires two database connections: one to alter the table, and another to update the table&#39;s status in the expansion schema. Before increasing <code>-n</code>, check the current value of the server configuration parameter <code>max_connections</code> and make sure the maximum connection limit is not exceeded.</p><p>-r | --rollback : Roll back a failed expansion setup operation.</p><p>-s | --silent : Runs in silent mode. Does not prompt for confirmation to proceed on warnings.</p><p>-S | --simple-progress : If specified, the <code>gpexpand</code> utility records only the minimum progress information in the WarehousePG table <em>gpexpand.expansion_progress</em>. The utility does not record the relation size information and status information in the table <em>gpexpand.status_detail</em>.</p><p>: Specifying this option can improve performance by reducing the amount of progress information written to the <em>gpexpand</em> tables.</p><p>[-t | --tardir] directory : The fully qualified path to a directory on segment hosts where the <code>gpexpand</code> utility copies a temporary tar file. The file contains WarehousePG files that are used to create segment instances. The default directory is the user home directory.</p><p>-v | --verbose : Verbose debugging output. With this option, the utility will output all DDL and DML used to expand the database.</p><p>--version : Display the utility&#39;s version number and exit.</p><p>-? | -h | --help : Displays the online help.</p><h2 id="specifying-hosts-using-hostnames-or-ip-addresses" tabindex="-1"><a id="host_ip"></a>Specifying Hosts using Hostnames or IP Addresses <a class="header-anchor" href="#specifying-hosts-using-hostnames-or-ip-addresses" aria-label="Permalink to &quot;&lt;a id=&quot;host_ip&quot;&gt;&lt;/a&gt;Specifying Hosts using Hostnames or IP Addresses&quot;">​</a></h2><p>When expanding a WarehousePG cluster, you can specify either a hostname or an IP address for the value.</p><ul><li>If you specify a hostname, the resolution of the hostname to an IP address should be done locally for security. For example, you should use entries in a local <code>/etc/hosts</code> file to map a hostname to an IP address. The resolution of a hostname to an IP address should not be performed by an external service such as a public DNS server. You must stop the WarehousePG cluster before you change the mapping of a hostname to a different IP address.</li><li>If you specify an IP address, the address should not be changed after the initial configuration. When segment mirroring is enabled, replication from the primary to the mirror segment will fail if the IP address changes from the configured value. For this reason, you should use a hostname when expanding a WarehousePG cluster unless you have a specific requirement to use IP addresses.</li></ul><p>When expanding a WarehousePG cluster, <code>gpexpand</code> populates <a href="./../../ref_guide/system_catalogs/gp_segment_configuration.html">gp_segment_configuration</a> catalog table with the new segment instance information. WarehousePG uses the <code>address</code> value of the <code>gp_segment_configuration</code> catalog table when looking up host systems for WarehousePG interconnect (internal) communication between the coordinator and segment instances and between segment instances, and for other internal communication.</p><h2 id="using-host-systems-with-multiple-nics" tabindex="-1"><a id="multi_nic"></a>Using Host Systems with Multiple NICs <a class="header-anchor" href="#using-host-systems-with-multiple-nics" aria-label="Permalink to &quot;&lt;a id=&quot;multi_nic&quot;&gt;&lt;/a&gt;Using Host Systems with Multiple NICs&quot;">​</a></h2><p>If host systems are configured with multiple NICs, you can expand a WarehousePG cluster to use each NIC as a WarehousePG host system. You must ensure that the host systems are configured with sufficient resources to support all the segment instances being added to the host. Also, if you enable segment mirroring, you must ensure that the expanded WarehousePG cluster configuration supports failover if a host system fails. For information about WarehousePG mirroring schemes, see <a href="./../../best_practices/ha.html#topic_ngz_qf4_tt">../../best_practices/ha.html#topic_ngz_qf4_tt</a>.</p><p>For example, this is a <code>gpexpand</code> configuration file for a simple WarehousePG cluster. The segment host <code>gp7s1</code> and <code>gp7s2</code> are configured with two NICs, <code>-s1</code> and <code>-s2</code>, where the WarehousePG cluster uses each NIC as a host system.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gp7s1-s2|gp7s1-s2|40001|/data/data1/gpseg2|6|2|p</span></span>\n<span class="line"><span>gp7s2-s1|gp7s2-s1|50000|/data/mirror1/gpseg2|9|2|m</span></span>\n<span class="line"><span>gp7s2-s1|gp7s2-s1|40000|/data/data1/gpseg3|7|3|p</span></span>\n<span class="line"><span>gp7s1-s2|gp7s1-s2|50001|/data/mirror1/gpseg3|8|3|m</span></span></code></pre></div><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Run <code>gpexpand</code> with an input file to initialize new segments and create the expansion schema in the postgres database:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpexpand -i input_file</span></span></code></pre></div><p>Run <code>gpexpand</code> for sixty hours maximum duration to redistribute tables to new segments:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpexpand -d 60:00:00</span></span></code></pre></div><h2 id="see-also" tabindex="-1"><a id="section7"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./gpssh-exkeys.html">gpssh-exkeys</a>, <a href="./../../admin_guide/expand/expand-main.html">Expanding a WarehousePG cluster</a></p>',59)]))}const m=t(o,[["render",i]]);export{u as __pageData,m as default};
