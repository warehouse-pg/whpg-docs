import{_ as a,c as t,o,ag as s}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"createdb","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/createdb.md","filePath":"docs/7x/utility_guide/ref/createdb.md"}'),n={name:"docs/7x/utility_guide/ref/createdb.md"};function i(d,e,c,r,p,l){return o(),t("div",null,e[0]||(e[0]=[s(`<h1 id="createdb" tabindex="-1">createdb <a class="header-anchor" href="#createdb" aria-label="Permalink to &quot;createdb&quot;">​</a></h1><p>Creates a new database.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>createdb [&lt;connection-option&gt; ...] [&lt;option&gt; ...] [&lt;dbname&gt; [&#39;&lt;description&gt;&#39;]]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>createdb -? | --help</span></span>
<span class="line"><span></span></span>
<span class="line"><span>createdb -V | --version</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>createdb</code> creates a new database in a WarehousePG cluster.</p><p>Normally, the database user who runs this command becomes the owner of the new database. However, a different owner can be specified via the <code>-O</code> option, if the executing user has appropriate privileges.</p><p><code>createdb</code> is a wrapper around the SQL command <a href="./../../ref_guide/sql_commands/CREATE_DATABASE.html">CREATE DATABASE</a> .</p><h2 id="options" tabindex="-1"><a id="section4"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>dbname : The name of the database to be created. The name must be unique among all other databases in the WarehousePG cluster. If not specified, reads from the environment variable <code>PGDATABASE</code>, then <code>PGUSER</code> or defaults to the current system user.</p><p>description : A comment to be associated with the newly created database. Descriptions containing white space must be enclosed in quotes.</p><p>-D tablespace | --tablespace=tablespace : Specifies the default tablespace for the database. (This name is processed as a double-quoted identifier.)</p><p>-e echo : Echo the commands that <code>createdb</code> generates and sends to the server.</p><p>-E encoding | --encoding encoding : Character set encoding to use in the new database. Specify a string constant (such as <code>&#39;UTF8&#39;</code>), an integer encoding number, or <code>DEFAULT</code> to use the default encoding. See the WarehousePG Reference Guide for information about supported character sets.</p><p>-l locale | --locale locale : Specifies the locale to be used in this database. This is equivalent to specifying both <code>--lc-collate</code> and <code>--lc-ctype</code>.</p><p>--lc-collate locale : Specifies the <code>LC_COLLATE</code> setting to be used in this database.</p><p>--lc-ctype locale : Specifies the <code>LC_CTYPE</code> setting to be used in this database.</p><p>-O owner | --owner=owner : The name of the database user who will own the new database. Defaults to the user running this command. (This name is processed as a double-quoted identifier.)</p><p>-T template | --template=template : The name of the template from which to create the new database. Defaults to <code>template1</code>. (This name is processed as a double-quoted identifier.)</p><p>-V | --version : Print the <code>createdb</code> version and exit.</p><p>-? | --help : Show help about <code>createdb</code> command line arguments, and exit.</p><p>The options <code>-D</code>, <code>-l</code>, <code>-E</code>, <code>-O</code>, and <code>-T</code> correspond to options of the underlying SQL command <code>CREATE DATABASE</code>; see <a href="./../../ref_guide/sql_commands/CREATE_DATABASE.html">CREATE DATABASE</a> in the <em>WarehousePG Reference Guide</em> for more information about them.</p><p><strong>Connection Options</strong></p><p>-h host | --host=host : The host name of the machine on which the WarehousePG coordinator database server is running. If not specified, reads from the environment variable <code>PGHOST</code> or defaults to localhost.</p><p>-p port | --port=port : The TCP port on which the WarehousePG coordinator database server is listening for connections. If not specified, reads from the environment variable <code>PGPORT</code> or defaults to 5432.</p><p>-U username | --username=username : The database role name to connect as. If not specified, reads from the environment variable <code>PGUSER</code> or defaults to the current system role name.</p><p>-w | --no-password : Never issue a password prompt. If the server requires password authentication and a password is not available by other means such as a <code>.pgpass</code> file, the connection attempt will fail. This option can be useful in batch jobs and scripts where no user is present to enter a password.</p><p>-W | --password : Force a password prompt.</p><p>--maintenance-db=dbname : Specifies the name of the database to connect to when creating the new database. If not specified, the <code>postgres</code> database will be used; if that does not exist (or if it is the name of the new database being created), <code>template1</code> will be used.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>To create the database <code>test</code> using the default options:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>createdb test</span></span></code></pre></div><p>To create the database <code>demo</code> using the WarehousePG coordinator on host <code>gpcoord</code>, port <code>54321</code>, using the <code>LATIN1</code> encoding scheme:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>createdb -p 54321 -h gpcoord -E LATIN1 demo</span></span></code></pre></div><h2 id="see-also" tabindex="-1"><a id="section7"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./../../ref_guide/sql_commands/CREATE_DATABASE.html">CREATE DATABASE</a>, <a href="./dropdb.html">dropdb</a></p>`,36)]))}const b=a(n,[["render",i]]);export{u as __pageData,b as default};
