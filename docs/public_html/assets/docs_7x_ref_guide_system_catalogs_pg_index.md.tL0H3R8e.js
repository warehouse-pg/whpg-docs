import{_ as e,c as d,o,ag as i}from"./chunks/framework.Ds6Eueu6.js";const p=JSON.parse('{"title":"pg_index","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/system_catalogs/pg_index.md","filePath":"docs/7x/ref_guide/system_catalogs/pg_index.md"}'),n={name:"docs/7x/ref_guide/system_catalogs/pg_index.md"};function r(s,t,a,c,l,h){return o(),d("div",null,t[0]||(t[0]=[i('<h1 id="pg-index" tabindex="-1">pg_index <a class="header-anchor" href="#pg-index" aria-label="Permalink to &quot;pg_index&quot;">​</a></h1><p>The <code>pg_index</code> system catalog table contains part of the information about indexes. The rest is mostly in <a href="./pg_class.html">pg_class</a>.</p><table tabindex="0"><thead><tr><th>column</th><th>type</th><th>references</th><th>description</th></tr></thead><tbody><tr><td><code>indexrelid</code></td><td>oid</td><td>pg_class.oid</td><td>The OID of the pg_class entry for this index</td></tr><tr><td><code>indrelid</code></td><td>oid</td><td>pg_class.oid</td><td>The OID of the pg_class entry for the table this index is for</td></tr><tr><td><code>indnatts</code></td><td>smallint</td><td></td><td>The number of columns in the index (duplicates pg_class.relnatts)</td></tr><tr><td><code>indnkeyatts</code></td><td>smallint</td><td></td><td>The number of key columns in the index</td></tr><tr><td><code>indisunique</code></td><td>boolean</td><td></td><td>If true, this is a unique index</td></tr><tr><td><code>indisprimary</code></td><td>boolean</td><td></td><td>If true, this index represents the primary key of the table. (indisunique should always be true when this is true.)</td></tr><tr><td><code>indisexclusion</code></td><td>boolean</td><td></td><td>If true, this index supports an exclusion constraint</td></tr><tr><td>indimmediate</td><td>boolean</td><td></td><td>If true, the uniqueness check is enforced immediately on insertion (irrelevant if <code>indisunique</code> is not true)</td></tr><tr><td><code>indisclustered</code></td><td>boolean</td><td></td><td>If true, the table was last clustered on this index via the <code>CLUSTER</code> command</td></tr><tr><td><code>indisvalid</code></td><td>boolean</td><td></td><td>If true, the index is currently valid for queries. False means the index is possibly incomplete: it must still be modified by <code>INSERT</code>/<code>UPDATE</code> operations, but it cannot safely be used for queries.</td></tr><tr><td><code>indcheckxmin</code></td><td>boolean</td><td></td><td>If true, queries must not use the index until the <code>xmin</code> of this <code>pg_index</code> row is below their <code>TransactionXmin</code> event horizon, because the table may contain broken HOT chains with incompatible rows that they can see</td></tr><tr><td><code>indisready</code></td><td>boolean</td><td></td><td>If true, the index is currently ready for inserts. False means the index must be ignored by <code>INSERT</code>/<code>UPDATE</code> operations</td></tr><tr><td><code>indislive</code></td><td>boolean</td><td></td><td>If false, the index is in process of being dropped, and should be ignored for all purposes</td></tr><tr><td><code>indisreplident</code></td><td>boolean</td><td></td><td>If true this index has been chosen as &quot;replica identity&quot; using <code>ALTER TABLE ... REPLICA IDENTITY USING INDEX ...</code></td></tr><tr><td><code>indkey</code></td><td>int2vector</td><td>pg_attribute.attnum</td><td>This is an array of <code>indnatts</code> values that indicate which table columns this index indexes. For example a value of 1 3 would mean that the first and the third table columns make up the index key. A zero in this array indicates that the corresponding index attribute is an expression over the table columns, rather than a simple column reference.</td></tr><tr><td><code>indcollation</code></td><td>oidvector</td><td></td><td>For each column in the index key, this contains the OID of the collation to use for the index.</td></tr><tr><td><code>indclass</code></td><td>oidvector</td><td>pg_opclass.oid</td><td>For each column in the index key this contains the OID of the operator class to use</td></tr><tr><td><code>indoption</code></td><td>int2vector</td><td></td><td>This is an array of <code>indnatts</code> values that store per-column flag bits. The meaning of the bits is defined by the index&#39;s access method.</td></tr><tr><td><code>indexprs</code></td><td>pg_node_tree</td><td></td><td>Expression trees (in <code>nodeToString()</code> representation) for index attributes that are not simple column references. This is a list with one element for each zero entry in indkey. NULL if all index attributes are simple references.</td></tr><tr><td><code>indpred</code></td><td>pg_node_tree</td><td></td><td>Expression tree (in <code>nodeToString()</code> representation) for partial index predicate. NULL if not a partial index.</td></tr></tbody></table><p><strong>Parent topic:</strong> <a href="./../system_catalogs/catalog_ref-html.html">System Catalogs Definitions</a></p>',4)]))}const f=e(n,[["render",r]]);export{p as __pageData,f as default};
