import{_ as a,c as t,o as s,ag as n}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"CREATE DOMAIN","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/CREATE_DOMAIN.md","filePath":"docs/7x/ref_guide/sql_commands/CREATE_DOMAIN.md"}'),o={name:"docs/7x/ref_guide/sql_commands/CREATE_DOMAIN.md"};function i(l,e,d,p,r,c){return s(),t("div",null,e[0]||(e[0]=[n(`<h1 id="create-domain" tabindex="-1">CREATE DOMAIN <a class="header-anchor" href="#create-domain" aria-label="Permalink to &quot;CREATE DOMAIN&quot;">​</a></h1><p>Defines a new domain.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE DOMAIN &lt;name&gt; [AS] &lt;data_type&gt;</span></span>
<span class="line"><span>       [ COLLATE &lt;collation&gt; ] </span></span>
<span class="line"><span>       [ DEFAULT &lt;expression&gt; ]</span></span>
<span class="line"><span>       [ &lt;constraint&gt; [ ... ] ]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>where &lt;constraint&gt; is:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[ CONSTRAINT &lt;constraint_name&gt; ]</span></span>
<span class="line"><span>{ NOT NULL | NULL | CHECK (&lt;expression&gt;)  }</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>CREATE DOMAIN</code> creates a new domain. A domain is essentially a data type with optional constraints (restrictions on the allowed set of values). The user who defines a domain becomes its owner.</p><p>If a schema name is given (for example, <code>CREATE DOMAIN myschema.mydomain ...</code>) then the domain is created in the specified schema. Otherwise it is created in the current schema. The domain name must be unique among the data types and domains existing in its schema.</p><p>Domains are useful for abstracting common constraints on fields into a single location for maintenance. For example, several tables might contain email address columns, all requiring the same <code>CHECK</code> constraint to verify the address syntax. Define a domain rather than setting up each table&#39;s constraint individually.</p><p>To be able to create a domain, you must have <code>USAGE</code> privilege on the underlying type.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>name : The name (optionally schema-qualified) of a domain to be created.</p><p>data_type : The underlying data type of the domain. This may include array specifiers.</p><p>collation : An optional collation for the domain. If no collation is specified, the domain has the same collation behavior as its underlying data type. The underlying type must be collatable if <code>COLLATE</code> is specified.</p><p>DEFAULT expression : Specifies a default value for columns of the domain data type. The value is any variable-free expression (but subqueries are not allowed). The data type of the default expression must match the data type of the domain. If no default value is specified, then the default value is the null value. : The default expression will be used in any insert operation that does not specify a value for the column. If a default value is defined for a particular column, it overrides any default associated with the domain. In turn, the domain default overrides any default value associated with the underlying data type.</p><p>CONSTRAINT constraint_name : An optional name for a constraint. If not specified, the system generates a name.</p><p>NOT NULL : Values of this domain are prevented from being null (but see the Notes below).</p><p>NULL : Values of this domain are allowed to be null. This is the default. : This clause is only intended for compatibility with nonstandard SQL databases. Its use is discouraged in new applications.</p><p>CHECK (expression) : <code>CHECK</code> clauses specify integrity constraints or tests which values of the domain must satisfy. Each constraint must be an expression producing a Boolean result. It should use the key word <code>VALUE</code> to refer to the value being tested. Expressions evaluating to TRUE or UNKNOWN succeed. If the expression produces a FALSE result, an error is reported and the value is not allowed to be converted to the domain type. : Currently, <code>CHECK</code> expressions cannot contain subqueries nor refer to variables other than <code>VALUE</code>. : When a domain has multiple <code>CHECK</code> constraints, they will be tested in alphabetical order by name. (WarehousePG versions before 7.0 did not honor any particular firing order for <code>CHECK</code> constraints.)</p><h2 id="notes" tabindex="-1"><a id="section4a"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section4a&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>Domain constraints, particularly <code>NOT NULL</code>, are checked when converting a value to the domain type. It is possible for a column that is nominally of the domain type to read as null despite there being such a constraint. For example, this can happen in an outer-join query, if the domain column is on the nullable side of the outer join. A more subtle example is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INSERT INTO tab (domcol) VALUES ((SELECT domcol FROM tab WHERE false));</span></span></code></pre></div><p>The empty scalar sub-SELECT will produce a null value that is considered to be of the domain type, so no further constraint checking is applied to it, and the insertion will succeed.</p><p>It is very difficult to avoid such problems, because of SQL&#39;s general assumption that a null value is a valid value of every data type. Best practice therefore is to design a domain&#39;s constraints so that a null value is allowed, and then to apply column <code>NOT NULL</code> constraints to columns of the domain type as needed, rather than directly to the domain type.</p><p>Greeplum Database assumes that <code>CHECK</code> constraints&#39; conditions are immutable, that is, they will always give the same result for the same input value. This assumption is what justifies examining <code>CHECK</code> constraints only when a value is first converted to be of a domain type, and not at other times. (This is essentially the same as the treatment of table <code>CHECK</code> constraints.)</p><p>An example of a common way to break this assumption is to reference a user-defined function in a <code>CHECK</code> expression, and then change the behavior of that function. WarehousePG does not disallow that, but it will not notice if there are stored values of the domain type that now violate the <code>CHECK</code> constraint. That would cause a subsequent database dump and restore to fail. The recommended way to handle such a change is to drop the constraint (using <code>ALTER DOMAIN</code>), adjust the function definition, and re-add the constraint, thereby rechecking it against stored data.</p><h2 id="examples" tabindex="-1"><a id="section5"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>This example creates the <code>us_postal_code</code> data type and then uses the type in a table definition. A regular expression test is used to verify that the value looks like a valid US postal code.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE DOMAIN us_postal_code AS TEXT</span></span>
<span class="line"><span>CHECK(</span></span>
<span class="line"><span>   VALUE ~ &#39;^\\d{5}$&#39;</span></span>
<span class="line"><span>OR VALUE ~ &#39;^\\d{5}-\\d{4}$&#39;</span></span>
<span class="line"><span>);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CREATE TABLE us_snail_addy (</span></span>
<span class="line"><span>  address_id SERIAL PRIMARY KEY,</span></span>
<span class="line"><span>  street1 TEXT NOT NULL,</span></span>
<span class="line"><span>  street2 TEXT,</span></span>
<span class="line"><span>  street3 TEXT,</span></span>
<span class="line"><span>  city TEXT NOT NULL,</span></span>
<span class="line"><span>  postal us_postal_code NOT NULL</span></span>
<span class="line"><span>);</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section6"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p><code>CREATE DOMAIN</code> conforms to the SQL standard.</p><h2 id="see-also" tabindex="-1"><a id="section7"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./ALTER_DOMAIN.html">ALTER DOMAIN</a>, <a href="./DROP_DOMAIN.html">DROP DOMAIN</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,33)]))}const m=a(o,[["render",i]]);export{u as __pageData,m as default};
