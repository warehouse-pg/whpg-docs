import{_ as a,c as t,o as i,ag as s}from"./chunks/framework.Ds6Eueu6.js";const E=JSON.parse('{"title":"REFRESH MATERIALIZED VIEW","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/REFRESH_MATERIALIZED_VIEW.md","filePath":"docs/7x/ref_guide/sql_commands/REFRESH_MATERIALIZED_VIEW.md"}'),o={name:"docs/7x/ref_guide/sql_commands/REFRESH_MATERIALIZED_VIEW.md"};function n(d,e,l,r,c,h){return i(),t("div",null,e[0]||(e[0]=[s(`<h1 id="refresh-materialized-view" tabindex="-1">REFRESH MATERIALIZED VIEW <a class="header-anchor" href="#refresh-materialized-view" aria-label="Permalink to &quot;REFRESH MATERIALIZED VIEW&quot;">​</a></h1><p>Replaces the contents of a materialized view.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>REFRESH MATERIALIZED VIEW [ CONCURRENTLY ] &lt;name&gt;</span></span>
<span class="line"><span>    [ WITH [ NO ] DATA ]</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>REFRESH MATERIALIZED VIEW</code> completely replaces the contents of a materialized view. The old contents are discarded. To run this command you must be the owner of the materialized view. If <code>WITH DATA</code> is specified (or defaults), the backing query is executed to provide the new data, and the materialized view is left in a scannable state. If <code>WITH NO DATA</code> is specified, no new data is generated, and the materialized view is left in an unscannable state.</p><p>You may not specify <code>CONCURRENTLY</code> and <code>WITH NO DATA</code> together.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>CONCURRENTLY : Refresh the materialized view without locking out concurrent selects on the materialized view. Without this option, a refresh that affects a lot of rows tends to use fewer resources and completes more quickly, but could block other connections which are trying to read from the materialized view. This option might be faster in cases where a small number of rows are affected. : This option is only allowed if there is at least one <code>UNIQUE</code> index on the materialized view which uses only column names and includes all rows; that is, it must not index on any expressions nor include a <code>WHERE</code> clause. : You can not use this option when the materialized view is not already populated. : Even with this option, only one <code>REFRESH</code> at a time may run against any one materialized view.</p><p>name : The name (optionally schema-qualified) of the materialized view to refresh.</p><p>WITH [ NO ] DATA : <code>WITH DATA</code> is the default and specifies that the materialized view query is run to provide new data, and the materialized view is left in a scannable state. If <code>WITH NO DATA</code> is specified, no new data is generated and the materialized view is left in an unscannable state.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>If there is an <code>ORDER BY</code> clause in the materialized view&#39;s defining query, the original contents of the materialized view will be ordered that way; but <code>REFRESH MATERIALIZED VIEW</code> does not guarantee to preserve that ordering.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>This command replaces the contents of the materialized view <code>order_summary</code> using the query from the materialized view&#39;s definition, and leaves it in a scannable state.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>REFRESH MATERIALIZED VIEW order_summary;</span></span></code></pre></div><p>This command frees storage associated with the materialized view <code>annual_statistics_basis</code> and leaves it in an unscannable state.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>REFRESH MATERIALIZED VIEW annual_statistics_basis WITH NO DATA;</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p><code>REFRESH MATERIALIZED VIEW</code> is a WarehousePG extension of the SQL standard.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./ALTER_MATERIALIZED_VIEW.html">ALTER MATERIALIZED VIEW</a>, <a href="./CREATE_MATERIALIZED_VIEW.html">CREATE MATERIALIZED VIEW</a>, <a href="./DROP_MATERIALIZED_VIEW.html">DROP MATERIALIZED VIEW</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,23)]))}const m=a(o,[["render",n]]);export{E as __pageData,m as default};
