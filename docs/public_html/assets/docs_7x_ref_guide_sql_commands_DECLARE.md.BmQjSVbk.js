import{_ as t,c as a,o,ag as r}from"./chunks/framework.Ds6Eueu6.js";const p=JSON.parse('{"title":"DECLARE","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/DECLARE.md","filePath":"docs/7x/ref_guide/sql_commands/DECLARE.md"}'),s={name:"docs/7x/ref_guide/sql_commands/DECLARE.md"};function n(i,e,c,l,d,u){return o(),a("div",null,e[0]||(e[0]=[r('<h1 id="declare" tabindex="-1">DECLARE <a class="header-anchor" href="#declare" aria-label="Permalink to &quot;DECLARE&quot;">​</a></h1><p>Defines a cursor.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DECLARE &lt;name&gt; [BINARY] [INSENSITIVE] [NO SCROLL] [PARALLEL RETRIEVE] CURSOR </span></span>\n<span class="line"><span>     [{WITH | WITHOUT} HOLD] </span></span>\n<span class="line"><span>     FOR &lt;query&gt;</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>DECLARE</code> allows a user to create a cursor, which can be used to retrieve a small number of rows at a time out of a larger query. Cursors can return data using <a href="./FETCH.html">FETCH</a>.</p><blockquote><p><strong>Note</strong> This page describes usage of cursors at the SQL command level. If you are trying to use cursors inside a PL/pgSQL function, the rules are different, see <a href="./../../analytics/pl_sql.html">PL/pgSQL</a>.</p></blockquote><p>Normal cursors return data in text format, the same as a <code>SELECT</code> would produce. Since data is stored natively in binary format, the system must do a conversion to produce the text format. Once the information comes back in text form, the client application may need to convert it to a binary format to manipulate it. In addition, data in the text format is often larger in size than in the binary format. Binary cursors return the data in a binary representation that may be more easily manipulated. Nevertheless, if you intend to display the data as text anyway, retrieving it in text form will save you some effort on the client side.</p><p>As an example, if a query returns a value of one from an integer column, you would get a string of 1 with a default cursor whereas with a binary cursor you would get a 4-byte field containing the internal representation of the value (in big-endian byte order).</p><p>Binary cursors should be used carefully. Many applications, including <code>psql</code>, are not prepared to handle binary cursors and expect data to come back in the text format.</p><blockquote><p><strong>Note</strong> When the client application uses the &#39;extended query&#39; protocol to issue a <code>FETCH</code> command, the Bind protocol message specifies whether data is to be retrieved in text or binary format. This choice overrides the way that the cursor is defined. The concept of a binary cursor as such is thus obsolete when using extended query protocol — any cursor can be treated as either text or binary.</p></blockquote><p>A cursor can be specified in the <code>WHERE CURRENT OF</code> clause of the <a href="./UPDATE.html">UPDATE</a> or <a href="./DELETE.html">DELETE</a> statement to update or delete table data. The <code>UPDATE</code> or <code>DELETE</code> statement can only be run on the server, for example in an interactive <code>psql</code> session or a script.</p><p><strong>Parallel Retrieve Cursors</strong></p><p>WarehousePG supports a special type of cursor, a <em>parallel retrieve cursor</em>. You can use a parallel retrieve cursor to retrieve query results, in parallel, directly from the WarehousePG segments, bypassing the WarehousePG coordinator.</p><p>Parallel retrieve cursors do not support the <code>WITH HOLD</code> clause. WarehousePG ignores the <code>BINARY</code> clause when you declare a parallel retrieve cursor.</p><p>You open a special retrieve session to each parallel retrieve cursor endpoint, and use the <a href="./RETRIEVE.html">RETRIEVE</a> command to retrieve the query results from a parallel retrieve cursor.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>name : The name of the cursor to be created.</p><p>BINARY : Causes the cursor to return data in binary rather than in text format.</p><pre><code>&gt; **Note** WarehousePG ignores the `BINARY` clause when you declare a `PARALLEL RETRIEVE` cursor.\n</code></pre><p>INSENSITIVE : Indicates that data retrieved from the cursor should be unaffected by updates to the table(s) underlying the cursor that occur after the cursor is created. In WarehousePG, all cursors are insensitive. This key word currently has no effect and is present only for compatibility with the SQL standard.</p><p>NO SCROLL : The cursor cannot be used to retrieve rows in a nonsequential fashion. This is the default behavior in WarehousePG; scrollable cursors (<code>SCROLL</code>) are not supported.</p><p>PARALLEL RETRIEVE : Declare a parallel retrieve cursor. A parallel retrieve cursor is a special type of cursor that you can use to retrieve results directly from WarehousePG segments, in parallel.</p><p>WITH HOLD WITHOUT HOLD : <code>WITH HOLD</code> specifies that the cursor may continue to be used after the transaction that created it successfully commits. <code>WITHOUT HOLD</code> specifies that the cursor cannot be used outside of the transaction that created it. <code>WITHOUT HOLD</code> is the default.</p><pre><code>&gt; **Note** WarehousePG does not support declaring a `PARALLEL RETRIEVE` cursor with the `WITH HOLD` clause. `WITH HOLD` also cannot not be specified when the `query` includes a `FOR UPDATE` or `FOR SHARE` clause.\n</code></pre><p>query : A <a href="./SELECT.html">SELECT</a> or <a href="./VALUES.html">VALUES</a> command which will provide the rows to be returned by the cursor.</p><p>: If the cursor is used in the <code>WHERE CURRENT OF</code> clause of the <a href="./UPDATE.html">UPDATE</a> or <a href="./DELETE.html">DELETE</a> command, the <code>SELECT</code> command must satisfy the following conditions:</p><pre><code>-   Cannot reference a view or external table.\n-   References only one table.\n    &lt;br/&gt;&lt;br/&gt;The table must be a heap table, and it must not be replicated-distributed (must not be a view, external table, or append-optimized column-oriented table).\n\n-   Cannot contain any of the following:\n    -   A grouping clause\n    -   A set operation such as `UNION ALL` or `UNION DISTINCT`\n    -   A sorting clause\n    -   A windowing clause\n    -   A join or a self-join\n    &lt;br/&gt;&lt;br/&gt;Specifying the `FOR UPDATE` clause in the `SELECT` command prevents other sessions from changing the rows between the time they are fetched and the time they are updated. Without the `FOR UPDATE` clause, a subsequent use of the `UPDATE` or `DELETE` command with the `WHERE CURRENT OF` clause has no effect if the row was changed since the cursor was created.\n    &lt;br/&gt;&lt;br/&gt;&gt; **Note** Specifying the `FOR UPDATE` clause in the `SELECT` command locks the entire table, not just the selected rows.\n</code></pre><p>The key words <code>BINARY</code>, <code>INSENSITIVE</code>, and <code>NO SCROLL</code> can appear in any order.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>Unless <code>WITH HOLD</code> is specified, the cursor created by this command can only be used within the current transaction. Thus, <code>DECLARE</code> without <code>WITH HOLD</code> is useless outside a transaction block: the cursor would survive only to the completion of the statement. Therefore WarehousePG reports an error if this command is used outside a transaction block. Use <code>BEGIN</code> and <code>COMMIT</code> (or <code>ROLLBACK</code>) to define a transaction block.</p><p>If <code>WITH HOLD</code> is specified and the transaction that created the cursor successfully commits, the cursor can continue to be accessed by subsequent transactions in the same session. (But if the creating transaction ends prematurely, the cursor is removed.) A cursor created with <code>WITH HOLD</code> is closed when an explicit <code>CLOSE</code> command is issued on it, or the session ends. In the current implementation, the rows represented by a held cursor are copied into a temporary file or memory area so that they remain available for subsequent transactions.</p><p>If you create a cursor with the <code>DECLARE</code> command in a transaction, you cannot use the <code>SET</code> command in the transaction until you close the cursor with the <code>CLOSE</code> command.</p><p>Scrollable cursors are not currently supported in WarehousePG. You can only use <code>FETCH</code> or <code>RETRIEVE</code> to move the cursor position forward, not backwards.</p><p><code>DECLARE...FOR UPDATE</code> is not supported with append-optimized tables.</p><p>You can see all available cursors by querying the <a href="./../system_catalogs/catalog_ref-views.html#pg_cursors">pg_cursors</a> system view.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Declare a cursor:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DECLARE mycursor CURSOR FOR SELECT * FROM mytable;</span></span></code></pre></div><p>Declare a parallel retrieve cursor for the same query:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DECLARE myprcursor PARALLEL RETRIEVE CURSOR FOR SELECT * FROM mytable;</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p>SQL standard allows cursors only in embedded SQL and in modules. WarehousePG permits cursors to be used interactively.</p><p>WarehousePG does not implement an <code>OPEN</code> statement for cursors. A cursor is considered to be open when it is declared.</p><p>The SQL standard allows cursors to move both forward and backward. All WarehousePG cursors are forward moving only (not scrollable).</p><p>Binary cursors are a WarehousePG extension.</p><p>The SQL standard makes no provisions for parallel retrieve cursors.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./CLOSE.html">CLOSE</a>, <a href="./DELETE.html">DELETE</a>, <a href="./FETCH.html">FETCH</a>, <a href="./MOVE.html">MOVE</a>, <a href="./RETRIEVE.html">RETRIEVE</a>, <a href="./SELECT.html">SELECT</a>, <a href="./UPDATE.html">UPDATE</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>',50)]))}const m=t(s,[["render",n]]);export{p as __pageData,m as default};
