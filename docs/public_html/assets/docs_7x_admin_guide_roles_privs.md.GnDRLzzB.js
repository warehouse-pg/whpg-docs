import{_ as s,c as t,o as a,ag as o}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"Managing Roles and Privileges","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/roles_privs.md","filePath":"docs/7x/admin_guide/roles_privs.md"}'),c={name:"docs/7x/admin_guide/roles_privs.md"};function i(r,e,d,n,p,l){return a(),t("div",null,e[0]||(e[0]=[o(`<h1 id="managing-roles-and-privileges" tabindex="-1">Managing Roles and Privileges <a class="header-anchor" href="#managing-roles-and-privileges" aria-label="Permalink to &quot;Managing Roles and Privileges&quot;">​</a></h1><hr><p>The WarehousePG authorization mechanism stores roles and permissions to access database objects in the database and is administered using SQL statements or command-line utilities.</p><p>WarehousePG manages database access permissions using <em>roles</em>. The concept of roles subsumes the concepts of <em>users</em> and <em>groups</em>. A role can be a database user, a group, or both. Roles can own database objects (for example, tables) and can assign privileges on those objects to other roles to control access to the objects. Roles can be members of other roles, thus a member role can inherit the object privileges of its parent role.</p><p>Every WarehousePG cluster contains a set of database roles (users and groups). Those roles are separate from the users and groups managed by the operating system on which the server runs. However, for convenience you may want to maintain a relationship between operating system user names and WarehousePG role names, since many of the client applications use the current operating system user name as the default.</p><p>In WarehousePG, users log in and connect through the coordinator instance, which then verifies their role and access privileges. The coordinator then issues commands to the segment instances behind the scenes as the currently logged in role.</p><p>Roles are defined at the system level, meaning they are valid for all databases in the system.</p><p>In order to bootstrap the WarehousePG cluster, a freshly initialized system always contains one predefined <em>superuser</em> role (also referred to as the system user). This role will have the same name as the operating system user that initialized the WarehousePG cluster. Customarily, this role is named <code>gpadmin</code>. In order to create more roles you first have to connect as this initial role.</p><p><strong>Parent topic:</strong> <a href="./getting_started.html">Managing WarehousePG Access</a></p><h2 id="security-best-practices-for-roles-and-privileges" tabindex="-1"><a id="topic2"></a>Security Best Practices for Roles and Privileges <a class="header-anchor" href="#security-best-practices-for-roles-and-privileges" aria-label="Permalink to &quot;&lt;a id=&quot;topic2&quot;&gt;&lt;/a&gt;Security Best Practices for Roles and Privileges&quot;">​</a></h2><ul><li><strong>Secure the gpadmin system user.</strong> WarehousePG requires a UNIX user id to install and initialize the WarehousePG cluster. This system user is referred to as <code>gpadmin</code> in the WarehousePG documentation. This <code>gpadmin</code> user is the default database superuser in WarehousePG, as well as the file system owner of the WarehousePG installation and its underlying data files. This default administrator account is fundamental to the design of WarehousePG. The system cannot run without it, and there is no way to limit the access of this gpadmin user id. Use roles to manage who has access to the database for specific purposes. You should only use the <code>gpadmin</code> account for system maintenance tasks such as expansion and upgrade. Anyone who logs on to a WarehousePG host as this user id can read, alter or delete any data; including system catalog data and database access rights. Therefore, it is very important to secure the gpadmin user id and only provide access to essential system administrators. Administrators should only log in to WarehousePG as <code>gpadmin</code> when performing certain system maintenance tasks (such as upgrade or expansion). Database users should never log on as <code>gpadmin</code>, and ETL or production workloads should never run as <code>gpadmin</code>.</li><li><strong>Assign a distinct role to each user that logs in.</strong> For logging and auditing purposes, each user that is allowed to log in to WarehousePG should be given their own database role. For applications or web services, consider creating a distinct role for each application or service. See <a href="#topic3">Creating New Roles (Users)</a>.</li><li><strong>Use groups to manage access privileges.</strong> See <a href="#topic5">Role Membership</a>.</li><li><strong>Limit users who have the SUPERUSER role attribute.</strong> Roles that are superusers bypass all access privilege checks in WarehousePG, as well as resource queuing. Only system administrators should be given superuser rights. See <a href="#topic4">Altering Role Attributes</a>.</li></ul><h2 id="creating-new-roles-users" tabindex="-1"><a id="topic3"></a>Creating New Roles (Users) <a class="header-anchor" href="#creating-new-roles-users" aria-label="Permalink to &quot;&lt;a id=&quot;topic3&quot;&gt;&lt;/a&gt;Creating New Roles \\(Users\\)&quot;">​</a></h2><p>A user-level role is considered to be a database role that can log in to the database and initiate a database session. Therefore, when you create a new user-level role using the <code>CREATE ROLE</code> command, you must specify the <code>LOGIN</code> privilege. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# CREATE ROLE jsmith WITH LOGIN;</span></span></code></pre></div><p>A database role may have a number of attributes that define what sort of tasks that role can perform in the database. You can set these attributes when you create the role, or later using the <code>ALTER ROLE</code> command.</p><h3 id="altering-role-attributes" tabindex="-1"><a id="topic4"></a>Altering Role Attributes <a class="header-anchor" href="#altering-role-attributes" aria-label="Permalink to &quot;&lt;a id=&quot;topic4&quot;&gt;&lt;/a&gt;Altering Role Attributes&quot;">​</a></h3><p>A database role may have a number of attributes that define what sort of tasks that role can perform in the database.</p><table tabindex="0"><thead><tr><th>Attributes</th><th>Description</th></tr></thead><tbody><tr><td><code>SUPERUSER</code> or <code>NOSUPERUSER</code></td><td>Determines if the role is a superuser. You must yourself be a superuser to create a new superuser. <code>NOSUPERUSER</code> is the default.</td></tr><tr><td><code>CREATEDB</code> or <code>NOCREATEDB</code></td><td>Determines if the role is allowed to create databases. <code>NOCREATEDB</code> is the default.</td></tr><tr><td><code>CREATEROLE</code> or <code>NOCREATEROLE</code></td><td>Determines if the role is allowed to create and manage other roles. <code>NOCREATEROLE</code> is the default.</td></tr><tr><td><code>INHERIT</code> or <code>NOINHERIT</code></td><td>Determines whether a role inherits the privileges of roles it is a member of. A role with the <code>INHERIT</code> attribute can automatically use whatever database privileges have been granted to all roles it is directly or indirectly a member of. <code>INHERIT</code> is the default.</td></tr><tr><td><code>LOGIN</code> or <code>NOLOGIN</code></td><td>Determines whether a role is allowed to log in. A role having the <code>LOGIN</code> attribute can be thought of as a user. Roles without this attribute are useful for managing database privileges (groups). <code>NOLOGIN</code> is the default.</td></tr><tr><td><code>CONNECTION LIMIT *connlimit*</code></td><td>If role can log in, this specifies how many concurrent connections the role can make. -1 (the default) means no limit.</td></tr><tr><td><code>CREATEEXTTABLE</code> or <code>NOCREATEEXTTABLE</code></td><td>Determines whether a role is allowed to create external tables. <code>NOCREATEEXTTABLE</code> is the default. For a role with the <code>CREATEEXTTABLE</code> attribute, the default external table <code>type</code> is <code>readable</code> and the default <code>protocol</code> is <code>gpfdist</code>. Note that external tables that use the <code>file</code> or <code>execute</code> protocols can only be created by superusers.</td></tr><tr><td><code>PASSWORD &#39;*password*&#39;</code></td><td>Sets the role&#39;s password. If you do not plan to use password authentication you can omit this option. If no password is specified, the password will be set to null and password authentication will always fail for that user. A null password can optionally be written explicitly as <code>PASSWORD NULL</code>.</td></tr><tr><td><code>ENCRYPTED</code></td><td>The password is always stored encrypted in the system catalogs. The <code>ENCRYPTED</code> keyword has no effect, but is accepted for backwards compatibility. The method of encryption is determined by the configuration parameter <code>password_encryption</code>. If the presented password string is already in MD5-encrypted or SCRAM-encrypted format, then it is stored as-is regardless of <code>password_encryption</code>, since the system cannot decrypt the specified encrypted password string, to encrypt it in a different format). This allows reloading of encrypted passwords during dump/restore.<br>See <a href="#topic9">Protecting Passwords in WarehousePG</a> for additional information about protecting login passwords.</td></tr><tr><td><code>VALID UNTIL &#39;timestamp&#39;</code></td><td>Sets a date and time after which the role&#39;s password is no longer valid. If omitted the password will be valid for all time.</td></tr><tr><td><code>RESOURCE QUEUE queue_name</code></td><td>Assigns the role to the named resource queue for workload management. Any statement that role issues is then subject to the resource queue&#39;s limits. Note that the <code>RESOURCE QUEUE</code> attribute is not inherited; it must be set on each user-level (<code>LOGIN</code>) role.</td></tr><tr><td><code>DENY deny_interval</code> or <code>DENY deny_point</code></td><td>Restricts access during an interval, specified by day or day and time. For more information see <a href="#topic13">Time-based Authentication</a>.</td></tr></tbody></table><p>You can set these attributes when you create the role, or later using the <code>ALTER ROLE</code> command. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# ALTER ROLE jsmith WITH PASSWORD &#39;passwd123&#39;;</span></span>
<span class="line"><span>=# ALTER ROLE admin VALID UNTIL &#39;infinity&#39;;</span></span>
<span class="line"><span>=# ALTER ROLE jsmith LOGIN;</span></span>
<span class="line"><span>=# ALTER ROLE jsmith RESOURCE QUEUE adhoc;</span></span>
<span class="line"><span>=# ALTER ROLE jsmith DENY DAY &#39;Sunday&#39;;</span></span></code></pre></div><p>A role can also have role-specific defaults for many of the server configuration settings. For example, to set the default schema search path for a role:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# ALTER ROLE admin SET search_path TO myschema, public;</span></span></code></pre></div><h2 id="role-membership" tabindex="-1"><a id="topic5"></a>Role Membership <a class="header-anchor" href="#role-membership" aria-label="Permalink to &quot;&lt;a id=&quot;topic5&quot;&gt;&lt;/a&gt;Role Membership&quot;">​</a></h2><p>It is frequently convenient to group users together to ease management of object privileges: that way, privileges can be granted to, or revoked from, a group as a whole. In WarehousePG this is done by creating a role that represents the group, and then granting membership in the group role to individual user roles.</p><p>Use the <code>CREATE ROLE</code> SQL command to create a new group role. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# CREATE ROLE admin CREATEROLE CREATEDB;</span></span></code></pre></div><p>Once the group role exists, you can add and remove members (user roles) using the <code>GRANT</code> and <code>REVOKE</code> commands. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# GRANT admin TO john, sally;</span></span>
<span class="line"><span>=# REVOKE admin FROM bob;</span></span></code></pre></div><p>For managing object privileges, you would then grant the appropriate permissions to the group-level role only (see <a href="#topic6">Managing Object Privileges</a>). The member user roles then inherit the object privileges of the group role. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# GRANT ALL ON TABLE mytable TO admin;</span></span>
<span class="line"><span>=# GRANT ALL ON SCHEMA myschema TO admin;</span></span>
<span class="line"><span>=# GRANT ALL ON DATABASE mydb TO admin;</span></span></code></pre></div><p>The role attributes <code>LOGIN</code>, <code>SUPERUSER</code>, <code>CREATEDB</code>, <code>CREATEROLE</code>, <code>CREATEEXTTABLE</code>, and <code>RESOURCE QUEUE</code> are never inherited as ordinary privileges on database objects are. User members must actually <code>SET ROLE</code> to a specific role having one of these attributes in order to make use of the attribute. In the above example, we gave <code>CREATEDB</code> and <code>CREATEROLE</code> to the <code>admin</code> role. If <code>sally</code> is a member of <code>admin</code>, then <code>sally</code> could issue the following command to assume the role attributes of the parent role:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=&gt; SET ROLE admin;</span></span></code></pre></div><h2 id="managing-object-privileges" tabindex="-1"><a id="topic6"></a>Managing Object Privileges <a class="header-anchor" href="#managing-object-privileges" aria-label="Permalink to &quot;&lt;a id=&quot;topic6&quot;&gt;&lt;/a&gt;Managing Object Privileges&quot;">​</a></h2><p>When an object (table, view, sequence, database, function, language, schema, or tablespace) is created, it is assigned an owner. The owner is normally the role that ran the creation statement. For most kinds of objects, the initial state is that only the owner (or a superuser) can do anything with the object. To allow other roles to use it, privileges must be granted. WarehousePG supports the following privileges for each object type:</p><table class="table" id="topic6__iq139925"><caption><span class="table--title-label">Table 2. </span><span class="title">Object Privileges</span></caption><colgroup><col style="width:66.66666666666666%;"><col style="width:33.33333333333333%;"></colgroup><thead class="thead"><tr class="row"><th class="entry" id="topic6__iq139925__entry__1">Object Type</th><th class="entry" id="topic6__iq139925__entry__2">Privileges</th></tr></thead><tbody class="tbody"><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Tables, External Tables, Views</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">SELECT</code></p><p class="p"><code class="ph codeph">INSERT</code></p><p class="p"><code class="ph codeph">UPDATE</code></p><p class="p"><code class="ph codeph">DELETE</code></p><p class="p"><code class="ph codeph">REFERENCES</code></p><p class="p"><code class="ph codeph">TRIGGER</code></p><p class="p"><code class="ph codeph">TRUNCATE</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Columns</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">SELECT</code></p><p class="p"><code class="ph codeph">INSERT</code></p><p class="p"><code class="ph codeph">UPDATE</code></p><p class="p"><code class="ph codeph">REFERENCES</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Sequences</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">USAGE</code></p><p class="p"><code class="ph codeph">SELECT</code></p><p class="p"><code class="ph codeph">UPDATE</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Databases</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">CREATE</code></p><p class="p"><code class="ph codeph">CONNECT</code></p><p class="p"><code class="ph codeph">TEMPORARY</code></p><p class="p"><code class="ph codeph">TEMP</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Domains</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">USAGE</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Foreign Data Wrappers</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">USAGE</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Foreign Servers</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">USAGE</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Functions</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">EXECUTE</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Procedural Languages</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">USAGE</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Schemas</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">CREATE</code></p><p class="p"><code class="ph codeph">USAGE</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Tablespaces</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">CREATE</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Types</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">USAGE</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr><tr class="row"><td class="entry" headers="topic6__iq139925__entry__1">Protocols</td><td class="entry" headers="topic6__iq139925__entry__2"><p class="p"><code class="ph codeph">SELECT</code></p><p class="p"><code class="ph codeph">INSERT</code></p><p class="p"><code class="ph codeph">ALL</code></p></td></tr></tbody></table><blockquote><p><strong>Note</strong> You must grant privileges for each object individually. For example, granting <code>ALL</code> on a database does not grant full access to the objects within that database. It only grants all of the database-level privileges (<code>CONNECT</code>, <code>CREATE</code>, <code>TEMPORARY</code>) to the database itself.</p></blockquote><p>Use the <code>GRANT</code> SQL command to give a specified role privileges on an object. For example, to grant the role named <code>jsmith</code> insert privileges on the table named <code>mytable</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# GRANT INSERT ON mytable TO jsmith;</span></span></code></pre></div><p>Similarly, to grant <code>jsmith</code> select privileges only to the column named <code>col1</code> in the table named <code>table2</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# GRANT SELECT (col1) on TABLE table2 TO jsmith;</span></span></code></pre></div><p>To revoke privileges, use the <code>REVOKE</code> command. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# REVOKE ALL PRIVILEGES ON mytable FROM jsmith;</span></span></code></pre></div><p>You can also use the <code>DROP OWNED</code> and <code>REASSIGN OWNED</code> commands for managing objects owned by deprecated roles (Note: only an object&#39;s owner or a superuser can drop an object or reassign ownership). For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# REASSIGN OWNED BY sally TO bob;</span></span>
<span class="line"><span>=# DROP OWNED BY visitor;</span></span></code></pre></div><h2 id="encrypting-data" tabindex="-1"><a id="topic8"></a>Encrypting Data <a class="header-anchor" href="#encrypting-data" aria-label="Permalink to &quot;&lt;a id=&quot;topic8&quot;&gt;&lt;/a&gt;Encrypting Data&quot;">​</a></h2><p>WarehousePG is installed with an optional module of encryption/decryption functions called <code>pgcrypto</code>. The <code>pgcrypto</code> functions allow database administrators to store certain columns of data in encrypted form. This adds an extra layer of protection for sensitive data, as data stored in WarehousePG in encrypted form cannot be read by anyone who does not have the encryption key, nor can it be read directly from the disks.</p><blockquote><p><strong>Note</strong> The <code>pgcrypto</code> functions run inside the database server, which means that all the data and passwords move between <code>pgcrypto</code> and the client application in clear-text. For optimal security, consider also using SSL connections between the client and the WarehousePG coordinator server.</p></blockquote><p>To use <code>pgcrypto</code> functions, register the <code>pgcrypto</code> extension in each database in which you want to use the functions. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>psql -d testdb -c &quot;CREATE EXTENSION pgcrypto&quot;</span></span></code></pre></div><p>See <a href="https://www.postgresql.org/docs/12/pgcrypto.html" target="_blank" rel="noreferrer">pgcrypto</a> in the PostgreSQL documentation for more information about individual functions.</p><h2 id="protecting-passwords-in-warehousepg" tabindex="-1"><a id="topic9"></a>Protecting Passwords in WarehousePG <a class="header-anchor" href="#protecting-passwords-in-warehousepg" aria-label="Permalink to &quot;&lt;a id=&quot;topic9&quot;&gt;&lt;/a&gt;Protecting Passwords in WarehousePG&quot;">​</a></h2><p>In its default configuration, WarehousePG saves MD5 or SCRAM-SHA-256 hashes of login users&#39; passwords in the <code>pg_authid</code> system catalog rather than saving clear text passwords. Anyone who is able to view the <code>pg_authid</code> table can see hash strings, but no passwords. This also ensures that passwords are obscured when the database is dumped to backup files.</p><p>The hash function runs when the password is set by using any of the following commands:</p><ul><li><code>CREATE USER name WITH PASSWORD &#39;password&#39;</code></li><li><code>CREATE ROLE name WITH LOGIN PASSWORD &#39;password&#39;</code></li><li><code>ALTER USER name WITH PASSWORD &#39;password&#39;</code></li><li><code>ALTER ROLE name WITH PASSWORD &#39;password&#39;</code></li></ul><blockquote><p><strong>Note</strong> The SQL command syntax and <code>password_encryption</code> configuration variable include the term <em>encrypt</em>, but the passwords are not technically encrypted. They are <em>hashed</em> and therefore cannot be decrypted.</p></blockquote><p>The hash is calculated on the concatenated clear text password and role name. The MD5 hash produces a 32-byte hexadecimal string prefixed with the characters <code>md5</code> while the SCRAM-SHA-256 hash produces a 64-byte hexadecimal string prefixed with the characters <code>SCRAM-SHA-256</code>. The hashed password is saved in the <code>rolpassword</code> column of the <code>pg_authid</code> system table.</p><p>To set <code>password_encryption</code> globally, run these commands in a shell as the <code>gpadmin</code> user:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ gpconfig -c password_encryption -v &#39;scram-sha-256&#39;</span></span>
<span class="line"><span>$ gpstop -u</span></span></code></pre></div><p>To set <code>password_encryption</code> in a session, use the SQL <code>SET</code> command:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SET password_encryption = &#39;md5&#39;;</span></span></code></pre></div><h2 id="time-based-authentication" tabindex="-1"><a id="topic13"></a>Time-based Authentication <a class="header-anchor" href="#time-based-authentication" aria-label="Permalink to &quot;&lt;a id=&quot;topic13&quot;&gt;&lt;/a&gt;Time-based Authentication&quot;">​</a></h2><p>WarehousePG enables the administrator to restrict access to certain times by role. Use the <code>CREATE ROLE</code> or <code>ALTER ROLE</code> commands to specify time-based constraints.</p><p>For details, refer to the <em>WarehousePG Security Configuration Guide</em>.</p>`,63)]))}const g=s(c,[["render",i]]);export{u as __pageData,g as default};
