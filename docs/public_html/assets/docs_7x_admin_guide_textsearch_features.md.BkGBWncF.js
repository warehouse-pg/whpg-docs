import{_ as t,c as s,o as a,ag as n}from"./chunks/framework.Ds6Eueu6.js";const h=JSON.parse('{"title":"Additional Text Search Features","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/textsearch/features.md","filePath":"docs/7x/admin_guide/textsearch/features.md"}'),o={name:"docs/7x/admin_guide/textsearch/features.md"};function i(r,e,c,p,l,d){return a(),s("div",null,e[0]||(e[0]=[n(`<h1 id="additional-text-search-features" tabindex="-1">Additional Text Search Features <a class="header-anchor" href="#additional-text-search-features" aria-label="Permalink to &quot;Additional Text Search Features&quot;">​</a></h1><hr><p>WarehousePG has additional functions and operators you can use to manipulate search and query vectors, and to rewrite search queries.</p><p>This section contains the following subtopics:</p><ul><li><a href="#manipulating-documents">Manipulating Documents</a></li><li><a href="#manipulate_queries">Manipulating Queries</a></li><li><a href="#rewriting">Rewriting Queries</a></li><li><a href="#statistics">Gathering Document Statistics</a></li></ul><h2 id="manipulating-documents" tabindex="-1"><a id="manipulating-documents"></a>Manipulating Documents <a class="header-anchor" href="#manipulating-documents" aria-label="Permalink to &quot;&lt;a id=&quot;manipulating-documents&quot;&gt;&lt;/a&gt;Manipulating Documents&quot;">​</a></h2><p><a href="./controlling.html#parsing-documents">Parsing Documents</a> showed how raw textual documents can be converted into <code>tsvector</code> values. WarehousePG also provides functions and operators that can be used to manipulate documents that are already in <code>tsvector</code> form.</p><p><code>tsvector || tsvector</code> : The <code>tsvector</code> concatenation operator returns a vector which combines the lexemes and positional information of the two vectors given as arguments. Positions and weight labels are retained during the concatenation. Positions appearing in the right-hand vector are offset by the largest position mentioned in the left-hand vector, so that the result is nearly equivalent to the result of performing <code>to_tsvector</code> on the concatenation of the two original document strings. (The equivalence is not exact, because any stop-words removed from the end of the left-hand argument will not affect the result, whereas they would have affected the positions of the lexemes in the right-hand argument if textual concatenation were used.)</p><p>One advantage of using concatenation in the vector form, rather than concatenating text before applying <code>to_tsvector</code>, is that you can use different configurations to parse different sections of the document. Also, because the <code>setweight</code> function marks all lexemes of the given vector the same way, it is necessary to parse the text and do <code>setweight</code> before concatenating if you want to label different parts of the document with different weights.</p><p><code>setweight(&lt;vector&gt; tsvector, &lt;weight&gt; &quot;char&quot;) returns tsvector</code> : <code>setweight</code> returns a copy of the input vector in which every position has been labeled with the given <code>&lt;weight&gt;</code>, either <code>A</code>, <code>B</code>, <code>C</code>, or <code>D</code>. (<code>D</code> is the default for new vectors and as such is not displayed on output.) These labels are retained when vectors are concatenated, allowing words from different parts of a document to be weighted differently by ranking functions.</p><p>Note that weight labels apply to <strong>positions</strong>, not <strong>lexemes</strong>. If the input vector has been stripped of positions then <code>setweight</code> does nothing.</p><p><code>length(&lt;vector&gt; tsvector) returns integer</code> : Returns the number of lexemes stored in the vector.</p><p><code>strip(vector tsvector) returns tsvector</code> : Returns a vector that lists the same lexemes as the given vector, but lacks any position or weight information. The result is usually much smaller than an unstripped vector, but it is also less useful. Relevance ranking does not work as well on stripped vectors as unstripped ones. Also, the <code>&lt;-&gt;</code> (FOLLOWED BY) <code>tsquery</code> operator will never match stripped input, since it cannot determine the distance between lexeme occurrences.</p><p>A full list of tsvector-related functions is available in <a href="https://www.postgresql.org/docs/12/functions-textsearch.html" target="_blank" rel="noreferrer">Text Search Functions and Operators</a> in the PostgreSQL documentation.</p><h2 id="manipulating-queries" tabindex="-1"><a id="manipulate_queries"></a>Manipulating Queries <a class="header-anchor" href="#manipulating-queries" aria-label="Permalink to &quot;&lt;a id=&quot;manipulate_queries&quot;&gt;&lt;/a&gt;Manipulating Queries&quot;">​</a></h2><p><a href="./controlling.html#parsing-queries">Parsing Queries</a> showed how raw textual queries can be converted into <code>tsquery</code> values. WarehousePG also provides functions and operators that can be used to manipulate queries that are already in <code>tsquery</code> form.</p><p><code>tsquery &amp;&amp; tsquery</code> : Returns the AND-combination of the two given queries.</p><p><code>tsquery || tsquery</code> : Returns the OR-combination of the two given queries.</p><p><code>!! tsquery</code> : Returns the negation (NOT) of the given query.</p><p><code>tsquery &lt;-&gt; tsquery</code> : Returns a query that searches for a match to the first given query immediately followed by a match to the second given query, using the <code>&lt;-&gt;</code> (FOLLOWED BY) <code>tsquery</code> operator. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT to_tsquery(&#39;fat&#39;) &lt;-&gt; to_tsquery(&#39;cat | rat&#39;);</span></span>
<span class="line"><span>          ?column?</span></span>
<span class="line"><span>----------------------------</span></span>
<span class="line"><span> &#39;fat&#39; &lt;-&gt; ( &#39;cat&#39; | &#39;rat&#39; )</span></span></code></pre></div><p><code>tsquery_phrase(&lt;query1&gt; tsquery, &lt;query2&gt; tsquery [, &lt;distance&gt; integer ]) returns tsquery</code> : Returns a query that searches for a match to the first given query followed by a match to the second given query at a distance of exactly <code>&lt;distance&gt;</code> lexemes, using the <code>&lt;N&gt;</code> <code>tsquery</code> operator. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT tsquery_phrase(to_tsquery(&#39;fat&#39;), to_tsquery(&#39;cat&#39;), 10);</span></span>
<span class="line"><span>  tsquery_phrase</span></span>
<span class="line"><span>------------------</span></span>
<span class="line"><span> &#39;fat&#39; &lt;10&gt; &#39;cat&#39;</span></span></code></pre></div><p><code>numnode(&lt;query&gt; tsquery) returns integer</code> : Returns the number of nodes (lexemes plus operators) in a tsquery. This function is useful to determine if the <strong>query</strong> is meaningful (returns &gt; 0), or contains only stop words (returns 0). Examples:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT numnode(plainto_tsquery(&#39;the any&#39;));</span></span>
<span class="line"><span>NOTICE:  query contains only stopword(s) or doesn&#39;t contain lexeme(s), ignored</span></span>
<span class="line"><span> numnode</span></span>
<span class="line"><span>---------</span></span>
<span class="line"><span>       0</span></span>
<span class="line"><span></span></span>
<span class="line"><span>SELECT numnode(&#39;foo &amp; bar&#39;::tsquery);</span></span>
<span class="line"><span> numnode</span></span>
<span class="line"><span>---------</span></span>
<span class="line"><span>       3</span></span></code></pre></div><p><code>querytree(&lt;query&gt; tsquery) returns text</code> : Returns the portion of a tsquery that can be used for searching an index. This function is useful for detecting unindexable queries, for example those containing only stop words or only negated terms. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT querytree(to_tsquery(&#39;!defined&#39;));</span></span>
<span class="line"><span> querytree</span></span>
<span class="line"><span>-----------</span></span></code></pre></div><h2 id="rewriting-queries" tabindex="-1"><a id="rewriting"></a>Rewriting Queries <a class="header-anchor" href="#rewriting-queries" aria-label="Permalink to &quot;&lt;a id=&quot;rewriting&quot;&gt;&lt;/a&gt;Rewriting Queries&quot;">​</a></h2><p>The <code>ts_rewrite</code> family of functions search a given <code>tsquery</code> for occurrences of a target subquery, and replace each occurrence with a substitute subquery. In essence this operation is a <code>tsquery</code>-specific version of substring replacement. A target and substitute combination can be thought of as a <em>query rewrite rule</em>. A collection of such rewrite rules can be a powerful search aid. For example, you can expand the search using synonyms (e.g., <code>new york</code>, <code>big apple</code>, <code>nyc</code>, <code>gotham</code>) or narrow the search to direct the user to some hot topic. There is some overlap in functionality between this feature and thesaurus dictionaries (<a href="./dictionaries.html#thesaurus-dictionary">Thesaurus Dictionary</a>). However, you can modify a set of rewrite rules on-the-fly without reindexing, whereas updating a thesaurus requires reindexing to be effective.</p><p><code>ts_rewrite(&lt;query&gt; tsquery, &lt;target&gt; tsquery, &lt;substitute&gt; tsquery) returns tsquery</code> : This form of <code>ts_rewrite</code> simply applies a single rewrite rule: <code>&lt;target&gt;</code> is replaced by <code>&lt;substitute&gt;</code> wherever it appears in <code>&lt;query&gt;</code>. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT ts_rewrite(&#39;a &amp; b&#39;::tsquery, &#39;a&#39;::tsquery, &#39;c&#39;::tsquery);</span></span>
<span class="line"><span> ts_rewrite</span></span>
<span class="line"><span>------------</span></span>
<span class="line"><span> &#39;b&#39; &amp; &#39;c&#39;</span></span></code></pre></div><p><code>ts_rewrite(&lt;query&gt; tsquery, &lt;select&gt; text) returns tsquery</code> : This form of <code>ts_rewrite</code> accepts a starting <code>&lt;query&gt;</code> and a SQL <code>&lt;select&gt;</code> command, which is given as a text string. The <code>&lt;select&gt;</code> must yield two columns of <code>tsquery</code> type. For each row of the <code>&lt;select&gt;</code> result, occurrences of the first column value (the target) are replaced by the second column value (the substitute) within the current <code>&lt;query&gt;</code> value. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE TABLE aliases (id int, t tsquery, s tsquery);</span></span>
<span class="line"><span>INSERT INTO aliases VALUES(1, &#39;a&#39;, &#39;c&#39;);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>SELECT ts_rewrite(&#39;a &amp; b&#39;::tsquery, &#39;SELECT t,s FROM aliases&#39;);</span></span>
<span class="line"><span> ts_rewrite</span></span>
<span class="line"><span>------------</span></span>
<span class="line"><span> &#39;b&#39; &amp; &#39;c&#39;</span></span></code></pre></div><p>Note that when multiple rewrite rules are applied in this way, the order of application can be important; so in practice you will want the source query to <code>ORDER BY</code> some ordering key.</p><p>Let&#39;s consider a real-life astronomical example. We&#39;ll expand query <code>supernovae</code> using table-driven rewriting rules:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE TABLE aliases (id int, t tsquery primary key, s tsquery);</span></span>
<span class="line"><span>INSERT INTO aliases VALUES(1, to_tsquery(&#39;supernovae&#39;), to_tsquery(&#39;supernovae|sn&#39;));</span></span>
<span class="line"><span></span></span>
<span class="line"><span>SELECT ts_rewrite(to_tsquery(&#39;supernovae &amp; crab&#39;), &#39;SELECT t, s FROM aliases&#39;);</span></span>
<span class="line"><span>           ts_rewrite            </span></span>
<span class="line"><span>---------------------------------</span></span>
<span class="line"><span> &#39;crab&#39; &amp; ( &#39;supernova&#39; | &#39;sn&#39; )</span></span></code></pre></div><p>We can change the rewriting rules just by updating the table:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>UPDATE aliases</span></span>
<span class="line"><span>SET s = to_tsquery(&#39;supernovae|sn &amp; !nebulae&#39;)</span></span>
<span class="line"><span>WHERE t = to_tsquery(&#39;supernovae&#39;);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>SELECT ts_rewrite(to_tsquery(&#39;supernovae &amp; crab&#39;), &#39;SELECT t, s FROM aliases&#39;);</span></span>
<span class="line"><span>                 ts_rewrite                  </span></span>
<span class="line"><span>---------------------------------------------</span></span>
<span class="line"><span> &#39;crab&#39; &amp; ( &#39;supernova&#39; | &#39;sn&#39; &amp; !&#39;nebula&#39; )</span></span></code></pre></div><p>Rewriting can be slow when there are many rewriting rules, since it checks every rule for a possible match. To filter out obvious non-candidate rules we can use the containment operators for the <code>tsquery</code> type. In the example below, we select only those rules which might match the original query:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT ts_rewrite(&#39;a &amp; b&#39;::tsquery,</span></span>
<span class="line"><span>                  &#39;SELECT t,s FROM aliases WHERE &#39;&#39;a &amp; b&#39;&#39;::tsquery @&gt; t&#39;);</span></span>
<span class="line"><span> ts_rewrite</span></span>
<span class="line"><span>------------</span></span>
<span class="line"><span> &#39;b&#39; &amp; &#39;c&#39;</span></span></code></pre></div><h2 id="gathering-document-statistics" tabindex="-1"><a id="statistics"></a>Gathering Document Statistics <a class="header-anchor" href="#gathering-document-statistics" aria-label="Permalink to &quot;&lt;a id=&quot;statistics&quot;&gt;&lt;/a&gt;Gathering Document Statistics&quot;">​</a></h2><p>The function <code>ts_stat</code> is useful for checking your configuration and for finding stop-word candidates.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ts_stat(&lt;sqlquery&gt; text, [ &lt;weights&gt; text, ]</span></span>
<span class="line"><span>        OUT &lt;word&gt; text, OUT &lt;ndoc&gt; integer,</span></span>
<span class="line"><span>        OUT &lt;nentry&gt; integer) returns setof record</span></span></code></pre></div><p><code>&lt;sqlquery&gt;</code> is a text value containing an SQL query which must return a single <code>tsvector</code> column. <code>ts_stat</code> runs the query and returns statistics about each distinct lexeme (word) contained in the <code>tsvector</code> data. The columns returned are</p><ul><li><code>&lt;word&gt; text</code> — the value of a lexeme</li><li><code>&lt;ndoc&gt; integer</code> — number of documents (<code>tsvector</code>s) the word occurred in</li><li><code>&lt;nentry&gt; integer</code> — total number of occurrences of the word</li></ul><p>If weights is supplied, only occurrences having one of those weights are counted.</p><p>For example, to find the ten most frequent words in a document collection:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * FROM ts_stat(&#39;SELECT vector FROM apod&#39;)</span></span>
<span class="line"><span>ORDER BY nentry DESC, ndoc DESC, word</span></span>
<span class="line"><span>LIMIT 10;</span></span></code></pre></div><p>The same, but counting only word occurrences with weight <code>A</code> or <code>B</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * FROM ts_stat(&#39;SELECT vector FROM apod&#39;, &#39;ab&#39;)</span></span>
<span class="line"><span>ORDER BY nentry DESC, ndoc DESC, word</span></span>
<span class="line"><span>LIMIT 10;</span></span></code></pre></div><p><strong>Parent topic:</strong> <a href="./../textsearch/full-text-search.html">Using Full Text Search</a></p>`,51)]))}const g=t(o,[["render",i]]);export{h as __pageData,g as default};
