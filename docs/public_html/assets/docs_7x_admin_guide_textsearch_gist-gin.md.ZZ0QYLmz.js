import{_ as t,c as a,o as s,ag as n}from"./chunks/framework.Ds6Eueu6.js";const m=JSON.parse('{"title":"Preferred Index Types for Text Search","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/textsearch/gist-gin.md","filePath":"docs/7x/admin_guide/textsearch/gist-gin.md"}'),i={name:"docs/7x/admin_guide/textsearch/gist-gin.md"};function r(o,e,d,c,l,h){return s(),a("div",null,e[0]||(e[0]=[n('<h1 id="preferred-index-types-for-text-search" tabindex="-1">Preferred Index Types for Text Search <a class="header-anchor" href="#preferred-index-types-for-text-search" aria-label="Permalink to &quot;Preferred Index Types for Text Search&quot;">​</a></h1><hr><p>There are two kinds of indexes that can be used to speed up full text searches: GIN and GiST. Indexes are not mandatory for full text searching, but in cases where a column is searched on a regular basis, an index is usually desirable.</p><p>To create such an index, do one of:</p><p><code>CREATE INDEX &lt;name&gt; ON &lt;table&gt; USING gin(&lt;column&gt;);</code> : Creates a GIN (Generalized Inverted Index)-based index. The <code>&lt;column&gt;</code> must be of <code>tsvector</code> type.</p><p><code>CREATE INDEX &lt;name&gt; ON &lt;table&gt; USING gist(&lt;column&gt;);</code> : Creates a GiST (Generalized Search Tree)-based index. The <code>&lt;column&gt;</code> can be of <code>tsvector</code> or <code>tsquery</code> type.</p><p>GIN indexes are the preferred text search index type. As inverted indexes, they contain an index entry for each word (lexeme), with a compressed list of matching locations. Multi-word searches can find the first match, then use the index to remove rows that are lacking additional words. GIN indexes store only the words (lexemes) of <code>tsvector</code> values, and not their weight labels. Thus a table row recheck is needed when using a query that involves weights.</p><p>A GiST index is lossy, meaning that the index might produce false matches, and it is necessary to check the actual table row to eliminate such false matches. (PostgreSQL does this automatically when needed.) GiST indexes are lossy because each document is represented in the index by a fixed-length signature. The signature is generated by hashing each word into a single bit in an n-bit string, with all these bits OR-ed together to produce an n-bit document signature. When two words hash to the same bit position there will be a false match. If all words in the query have matches (real or false) then the table row must be retrieved to see if the match is correct.</p><p>A GiST index can be covering, i.e., use the <code>INCLUDE</code> clause. Included columns can have data types without any GiST operator class. Included attributes will be stored uncompressed.</p><p>Lossiness causes performance degradation due to unnecessary fetches of table records that turn out to be false matches. Since random access to table records is slow, this limits the usefulness of GiST indexes. The likelihood of false matches depends on several factors, in particular the number of unique words, so using dictionaries to reduce this number is recommended.</p><p>Note that GIN index build time can often be improved by increasing <a href="./../../ref_guide/config_params/guc-list.html">maintenance_work_mem</a>, while GiST index build time is not sensitive to that parameter.</p><p>Partitioning of big collections and the proper use of GiST and GIN indexes allows the implementation of very fast searches with online update. Partitioning can be done at the database level using table inheritance, or by distributing documents over servers and collecting search results using <a href="./../../ref_guide/modules/dblink.html">dblink</a>. The latter is possible because ranking functions use only local information.</p><p><strong>Parent topic:</strong> <a href="./../textsearch/full-text-search.html">Using Full Text Search</a></p>',13)]))}const p=t(i,[["render",r]]);export{m as __pageData,p as default};
