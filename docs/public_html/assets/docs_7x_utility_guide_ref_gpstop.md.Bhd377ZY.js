import{_ as t,c as s,o,ag as a}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"gpstop","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/gpstop.md","filePath":"docs/7x/utility_guide/ref/gpstop.md"}'),n={name:"docs/7x/utility_guide/ref/gpstop.md"};function i(p,e,r,c,d,l){return o(),s("div",null,e[0]||(e[0]=[a(`<h1 id="gpstop" tabindex="-1">gpstop <a class="header-anchor" href="#gpstop" aria-label="Permalink to &quot;gpstop&quot;">​</a></h1><p>Stops or restarts a WarehousePG cluster.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpstop [-d &lt;coordinator_data_directory&gt;] [-B parallel_processes&gt;] </span></span>
<span class="line"><span>       [-M smart | fast | immediate] [-t &lt;timeout_seconds&gt;] [-r] [-y] [-a] </span></span>
<span class="line"><span>       [-l &lt;logfile_directory&gt;] [-v | -q]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpstop -m [-d &lt;coordinator_data_directory&gt;] [-y] [-l &lt;logfile_directory&gt;] [-v | -q]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpstop -u [-d &lt;coordinator_data_directory&gt;] [-l &lt;logfile_directory&gt;] [-v | -q]</span></span>
<span class="line"><span> </span></span>
<span class="line"><span>gpstop --host &lt;host_namea&gt; [-d &lt;coordinator_data_directory&gt;] [-l &lt;logfile_directory&gt;]</span></span>
<span class="line"><span>       [-t &lt;timeout_seconds&gt;] [-a] [-v | -q]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpstop --version </span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpstop -? | -h | --help</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>The <code>gpstop</code> utility is used to stop the database servers that comprise a WarehousePG cluster. When you stop a WarehousePG cluster, you are actually stopping several <code>postgres</code> database server processes at once (the coordinator and all of the segment instances). The <code>gpstop</code> utility handles the shutdown of the individual instances. Each instance is shutdown in parallel.</p><p>The default shutdown mode (<code>-M smart</code>) waits for current client connections to finish before completing the shutdown. If any connections remain open after the timeout period, or if you interrupt with CTRL-C, <code>gpstop</code> lists the open connections and prompts whether to continue waiting for connections to finish, or to perform a fast or immediate shutdown. The default timeout period is 120 seconds and can be changed with the <code>-t timeout\\_seconds</code> option.</p><p>Specify the <code>-M fast</code> shutdown mode to roll back all in-progress transactions and terminate any connections before shutting down.</p><p>With the <code>-u</code> option, the utility uploads changes made to the coordinator <code>pg_hba.conf</code> file or to <em>runtime</em> configuration parameters in the coordinator <code>postgresql.conf</code> file without interruption of service. Note that any active sessions will not pick up the changes until they reconnect to the database.</p><h2 id="options" tabindex="-1"><a id="section4"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>-a : Do not prompt the user for confirmation.</p><p>-B parallel_processes : The number of segments to stop in parallel. If not specified, the utility will start up to 64 parallel processes depending on how many segment instances it needs to stop.</p><p>-d coordinator_data_directory : Optional. The coordinator host data directory. If not specified, the value set for <code>$COORDINATOR_DATA_DIRECTORY</code> will be used.</p><p>--host host_name : The utility shuts down the WarehousePG segment instances on the specified host to allow maintenance on the host. Each primary segment instance on the host is shut down and the associated mirror segment instance is promoted to a primary segment if the mirror segment is on another host. Mirror segment instances on the host are shut down.</p><p>: The segment instances are not shut down and the utility returns an error in these cases:</p><pre><code>-   Segment mirroring is not enabled for the system.
-   The coordinator or standby coordinator is on the host.
-   Both a primary segment instance and its mirror are on the host.
</code></pre><p>: This option cannot be specified with the <code>-m</code>, <code>-r</code>, <code>-u</code>, or <code>-y</code> options.</p><pre><code>&gt; **Note** The \`gprecoverseg\` utility restores segment instances. Run \`gprecoverseg\` commands to start the segments as mirrors and then to return the segments to their preferred role \\(primary segments\\).
</code></pre><p>-l logfile_directory : The directory to write the log file. Defaults to <code>~/gpAdminLogs</code>.</p><p>-m : Optional. Shuts down a WarehousePG coordinator instance that was started in maintenance mode.</p><p>-M fast : Fast shut down. Any transactions in progress are interrupted and rolled back.</p><p>-M immediate : Immediate shut down. Any transactions in progress are cancelled.</p><p>: This mode kills all <code>postgres</code> processes without allowing the database server to complete transaction processing or clean up any temporary or in-process work files.</p><p>-M smart : Smart shut down. This is the default shutdown mode. <code>gpstop</code> waits for active user connections to disconnect and then proceeds with the shutdown. If any user connections remain open after the timeout period (or if you interrupt by pressing CTRL-C) <code>gpstop</code> lists the open user connections and prompts whether to continue waiting for connections to finish, or to perform a fast or immediate shutdown.</p><p>-q : Run in quiet mode. Command output is not displayed on the screen, but is still written to the log file.</p><p>-r : Restart after shutdown is complete.</p><p>-t timeout_seconds : Specifies a timeout threshold (in seconds) to wait for a segment instance to shutdown. If a segment instance does not shutdown in the specified number of seconds, <code>gpstop</code> displays a message indicating that one or more segments are still in the process of shutting down and that you cannot restart WarehousePG until the segment instance(s) are stopped. This option is useful in situations where <code>gpstop</code> is run and there are very large transactions that need to rollback. These large transactions can take over a minute to rollback and surpass the default timeout period of 120 seconds.</p><p>-u : This option reloads the <code>pg_hba.conf</code> files of the coordinator and segments and the runtime parameters of the <code>postgresql.conf</code> files but does not shutdown the WarehousePG cluster. Use this option to make new configuration settings active after editing <code>postgresql.conf</code> or <code>pg_hba.conf</code>. Note that this only applies to configuration parameters that are designated as <em>runtime</em> parameters.</p><p>-v : Displays detailed status, progress and error messages output by the utility.</p><p>-y : Do not stop the standby coordinator process. The default is to stop the standby coordinator.</p><p>-? | -h | --help : Displays the online help.</p><p>--version : Displays the version of this utility.</p><h2 id="examples" tabindex="-1"><a id="section5"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Stop a WarehousePG cluster in smart mode:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpstop</span></span></code></pre></div><p>Stop a WarehousePG cluster in fast mode:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpstop -M fast</span></span></code></pre></div><p>Stop all segment instances and then restart the system:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpstop -r</span></span></code></pre></div><p>Stop a coordinator instance that was started in maintenance mode:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpstop -m</span></span></code></pre></div><p>Reload the <code>postgresql.conf</code> and <code>pg_hba.conf</code> files after making configuration changes but do not shutdown the WarehousePG cluster:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpstop -u</span></span></code></pre></div><h2 id="see-also" tabindex="-1"><a id="section6"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./gpstart.html">gpstart</a></p>`,45)]))}const g=t(n,[["render",i]]);export{u as __pageData,g as default};
