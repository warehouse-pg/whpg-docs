import{_ as t,c as a,o as i,ag as o}from"./chunks/framework.Ds6Eueu6.js";const d=JSON.parse('{"title":"Configuring Kerberos for Linux Clients","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/kerberos-lin-client.md","filePath":"docs/7x/admin_guide/kerberos-lin-client.md"}'),n={name:"docs/7x/admin_guide/kerberos-lin-client.md"};function s(r,e,c,l,p,h){return i(),a("div",null,e[0]||(e[0]=[o(`<h1 id="configuring-kerberos-for-linux-clients" tabindex="-1">Configuring Kerberos for Linux Clients <a class="header-anchor" href="#configuring-kerberos-for-linux-clients" aria-label="Permalink to &quot;Configuring Kerberos for Linux Clients&quot;">​</a></h1><hr><p>You can configure Linux client applications to connect to a WarehousePG cluster that is configured to authenticate with Kerberos.</p><p>If your JDBC application on Red Hat Enterprise Linux uses Kerberos authentication when it connects to your WarehousePG, your client system must be configured to use Kerberos authentication. If you are not using Kerberos authentication to connect to a WarehousePG, Kerberos is not needed on your client system.</p><ul><li><a href="#topic13">Requirements</a></li><li><a href="#topic17">Setting Up Client System with Kerberos Authentication</a></li><li><a href="#topic18">Running a Java Application</a></li></ul><p>For information about enabling Kerberos authentication with WarehousePG, see the chapter &quot;Setting Up Kerberos Authentication&quot; in the <em>WarehousePG Administrator Guide</em>.</p><p><strong>Parent topic:</strong> <a href="./client_auth.html">Configuring Client Authentication</a></p><h2 id="requirements" tabindex="-1"><a id="topic13"></a>Requirements <a class="header-anchor" href="#requirements" aria-label="Permalink to &quot;&lt;a id=&quot;topic13&quot;&gt;&lt;/a&gt;Requirements&quot;">​</a></h2><p>The following are requirements to connect to a WarehousePG that is enabled with Kerberos authentication from a client system with a JDBC application.</p><ul><li><a href="#topic14">Prerequisites</a></li><li><a href="#topic15">Required Software on the Client Machine</a></li></ul><h3 id="prerequisites" tabindex="-1"><a id="topic14"></a>Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;&lt;a id=&quot;topic14&quot;&gt;&lt;/a&gt;Prerequisites&quot;">​</a></h3><ul><li><p>Kerberos must be installed and configured on the WarehousePG coordinator host.</p><blockquote><p><strong>Important</strong> WarehousePG must be configured so that a remote user can connect to WarehousePG with Kerberos authentication. Authorization to access WarehousePG is controlled by the <code>pg_hba.conf</code> file. For details, see &quot;Editing the pg_hba.conf File&quot; in the <em>WarehousePG Administration Guide</em>, and also see the <em>WarehousePG Security Configuration Guide</em>.</p></blockquote></li><li><p>The client system requires the Kerberos configuration file <code>krb5.conf</code> from the WarehousePG coordinator.</p></li><li><p>The client system requires a Kerberos keytab file that contains the authentication credentials for the WarehousePG user that is used to log into the database.</p></li><li><p>The client machine must be able to connect to WarehousePG coordinator host.</p><p>If necessary, add the WarehousePG coordinator host name and IP address to the system <code>hosts</code> file. On Linux systems, the <code>hosts</code> file is in <code>/etc</code>.</p></li></ul><h3 id="required-software-on-the-client-machine" tabindex="-1"><a id="topic15"></a>Required Software on the Client Machine <a class="header-anchor" href="#required-software-on-the-client-machine" aria-label="Permalink to &quot;&lt;a id=&quot;topic15&quot;&gt;&lt;/a&gt;Required Software on the Client Machine&quot;">​</a></h3><ul><li><p>The Kerberos <code>kinit</code> utility is required on the client machine. The <code>kinit</code> utility is available when you install the Kerberos packages:</p><ul><li>krb5-libs</li><li>krb5-workstation<br></li></ul><blockquote><p><strong>Note</strong> When you install the Kerberos packages, you can use other Kerberos utilities such as <code>klist</code> to display Kerberos ticket information.</p></blockquote></li></ul><p>Java applications require this additional software:</p><ul><li><p>Java JDK</p><p>Java JDK 1.7.0_17 is supported on Red Hat Enterprise Linux 6.x.</p></li><li><p>Ensure that JAVA_HOME is set to the installation directory of the supported Java JDK.</p></li></ul><h2 id="setting-up-client-system-with-kerberos-authentication" tabindex="-1"><a id="topic17"></a>Setting Up Client System with Kerberos Authentication <a class="header-anchor" href="#setting-up-client-system-with-kerberos-authentication" aria-label="Permalink to &quot;&lt;a id=&quot;topic17&quot;&gt;&lt;/a&gt;Setting Up Client System with Kerberos Authentication&quot;">​</a></h2><p>To connect to WarehousePG with Kerberos authentication requires a Kerberos ticket. On client systems, tickets are generated from Kerberos keytab files with the <code>kinit</code> utility and are stored in a cache file.</p><ol><li><p>Install a copy of the Kerberos configuration file <code>krb5.conf</code> from the WarehousePG coordinator. The file is used by the WarehousePG client software and the Kerberos utilities.</p><p>Install <code>krb5.conf</code> in the directory <code>/etc</code>.</p><p>If needed, add the parameter <code>default_ccache_name</code> to the <code>[libdefaults]</code> section of the <code>krb5.ini</code> file and specify location of the Kerberos ticket cache file on the client system.</p></li><li><p>Obtain a Kerberos keytab file that contains the authentication credentials for the WarehousePG user.</p></li><li><p>Run <code>kinit</code> specifying the keytab file to create a ticket on the client machine. For this example, the keytab file <code>gpdb-kerberos.keytab</code> is in the current directory. The ticket cache file is in the <code>gpadmin</code> user home directory.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&gt; kinit -k -t gpdb-kerberos.keytab -c /home/gpadmin/cache.txt </span></span>
<span class="line"><span>   gpadmin/kerberos-gpdb@KRB.EXAMPLE.COM</span></span></code></pre></div></li></ol><h2 id="running-psql" tabindex="-1"><a id="topic12"></a>Running psql <a class="header-anchor" href="#running-psql" aria-label="Permalink to &quot;&lt;a id=&quot;topic12&quot;&gt;&lt;/a&gt;Running psql&quot;">​</a></h2><p>From a remote system, you can access a WarehousePG that has Kerberos authentication enabled.</p><h3 id="to-connect-to-warehousepg-with-psql" tabindex="-1"><a id="mh151095"></a>To connect to WarehousePG with psql <a class="header-anchor" href="#to-connect-to-warehousepg-with-psql" aria-label="Permalink to &quot;&lt;a id=&quot;mh151095&quot;&gt;&lt;/a&gt;To connect to WarehousePG with psql&quot;">​</a></h3><ol><li><p>As the gpadmin user, open a command window.</p></li><li><p>Start <code>psql</code> from the command window and specify a connection to the WarehousePG specifying the user that is configured with Kerberos authentication.</p><p>The following example logs into the WarehousePG on the machine <code>kerberos-gpdb</code> as the <code>gpadmin</code> user with the Kerberos credentials <code>gpadmin/kerberos-gpdb</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ psql -U &quot;gpadmin/kerberos-gpdb&quot; -h kerberos-gpdb postgres</span></span></code></pre></div></li></ol><h2 id="running-a-java-application" tabindex="-1"><a id="topic18"></a>Running a Java Application <a class="header-anchor" href="#running-a-java-application" aria-label="Permalink to &quot;&lt;a id=&quot;topic18&quot;&gt;&lt;/a&gt;Running a Java Application&quot;">​</a></h2><p>Accessing WarehousePG from a Java application with Kerberos authentication uses the Java Authentication and Authorization Service (JAAS)</p><ol><li><p>Create the file <code>.java.login.config</code> in the user home folder.</p><p>For example, on a Linux system, the home folder is similar to <code>/home/gpadmin</code>.</p><p>Add the following text to the file:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>pgjdbc {</span></span>
<span class="line"><span>  com.sun.security.auth.module.Krb5LoginModule required</span></span>
<span class="line"><span>  doNotPrompt=true</span></span>
<span class="line"><span>  useTicketCache=true</span></span>
<span class="line"><span>  ticketCache = &quot;/home/gpadmin/cache.txt&quot;</span></span>
<span class="line"><span>  debug=true</span></span>
<span class="line"><span>  client=true;</span></span>
<span class="line"><span>};</span></span></code></pre></div></li><li><p>Create a Java application that connects to WarehousePG using Kerberos authentication and run the application as the user.</p></li></ol><p>This example database connection URL uses a PostgreSQL JDBC driver and specifies parameters for Kerberos authentication.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>jdbc:postgresql://kerberos-gpdb:5432/mytest? </span></span>
<span class="line"><span>  kerberosServerName=postgres&amp;jaasApplicationName=pgjdbc&amp; </span></span>
<span class="line"><span>  user=gpadmin/kerberos-gpdb</span></span></code></pre></div><p>The parameter names and values specified depend on how the Java application performs Kerberos authentication.</p>`,29)]))}const b=t(n,[["render",s]]);export{d as __pageData,b as default};
