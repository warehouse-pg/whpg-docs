import{_ as o,c as r,o as t,ag as i}from"./chunks/framework.Ds6Eueu6.js";const h=JSON.parse('{"title":"Memory and Resource Management with Resource Groups","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/best_practices/resgroups.md","filePath":"docs/7x/best_practices/resgroups.md"}'),s={name:"docs/7x/best_practices/resgroups.md"};function a(n,e,u,c,m,l){return t(),r("div",null,e[0]||(e[0]=[i('<h1 id="memory-and-resource-management-with-resource-groups" tabindex="-1">Memory and Resource Management with Resource Groups <a class="header-anchor" href="#memory-and-resource-management-with-resource-groups" aria-label="Permalink to &quot;Memory and Resource Management with Resource Groups&quot;">​</a></h1><hr><p>Managing WarehousePG resources with resource groups.</p><p>Memory, CPU, and concurrent transaction management have a significant impact on performance in a WarehousePG cluster. Resource groups are a newer resource management scheme that enforce memory, CPU, and concurrent transaction limits in WarehousePG.</p><ul><li><a href="#section_r52_rbl_zt">Configuring Memory for WarehousePG</a></li><li><a href="#toolowmem">Memory Considerations when using Resource Groups</a></li><li><a href="#configuring_rg">Configuring Resource Groups</a></li><li><a href="#section113x">Low Memory Queries</a></li><li><a href="#section177x">Administrative Utilities and admin_group Concurrency</a></li></ul><h2 id="configuring-memory-for-warehousepg" tabindex="-1"><a id="section_r52_rbl_zt"></a>Configuring Memory for WarehousePG <a class="header-anchor" href="#configuring-memory-for-warehousepg" aria-label="Permalink to &quot;&lt;a id=&quot;section_r52_rbl_zt&quot;&gt;&lt;/a&gt;Configuring Memory for WarehousePG&quot;">​</a></h2><p>While it is not always possible to increase system memory, you can avoid many out-of-memory conditions by configuring resource groups to manage expected workloads.</p><p>The following operating system and WarehousePG memory settings are significant when you manage WarehousePG resources with resource groups:</p><ul><li><p><strong>vm.overcommit_memory</strong></p><p>This Linux kernel parameter, set in <a href="./../install_guide/config_os.html#topic3"><code>/etc/sysctl.conf</code></a>, identifies the method that the operating system uses to determine how much memory can be allocated to processes. <code>vm.overcommit_memory</code> must always be set to 2 for WarehousePG clusters.</p></li><li><p><strong>vm.overcommit_ratio</strong></p><p>This Linux kernel parameter, set in <a href="./../install_guide/config_os.html#topic3"><code>/etc/sysctl.conf</code></a>, identifies the percentage of RAM that is used for application processes; the remainder is reserved for the operating system. Tune the setting as necessary. If your memory utilization is too low, increase the value; if your memory or swap usage is too high, decrease the setting.</p></li><li><p><strong>gp_workfile_limit_files_per_query</strong></p><p>Set <code>gp_workfile_limit_files_per_query</code> to limit the maximum number of temporary spill files (workfiles) allowed per query. Spill files are created when a query requires more memory than it is allocated. When the limit is exceeded the query is terminated. The default is zero, which allows an unlimited number of spill files and may fill up the file system.</p></li><li><p><strong>gp_workfile_compression</strong></p><p>If there are numerous spill files then set <code>gp_workfile_compression</code> to compress the spill files. Compressing spill files may help to avoid overloading the disk subsystem with IO operations.</p></li><li><p><strong>statement_mem</strong></p><p>WarehousePG uses the <code>statement_mem</code> setting to determine the amount of memory to allocate to a query.</p></li></ul><p>Other considerations:</p><ul><li>Do not configure the operating system to use huge pages. See the <a href="./../install_guide/config_os.html#topic3/huge_pages">Recommended OS Parameters Settings</a> in the <em>WarehousePG Installation Guide</em>.</li><li>When you configure resource group memory, consider memory requirements for mirror segments that become primary segments during a failure to ensure that database operations can continue when primary segments or segment hosts fail.</li></ul><h2 id="memory-considerations-when-using-resource-groups" tabindex="-1"><a id="toolowmem"></a>Memory Considerations when using Resource Groups <a class="header-anchor" href="#memory-considerations-when-using-resource-groups" aria-label="Permalink to &quot;&lt;a id=&quot;toolowmem&quot;&gt;&lt;/a&gt;Memory Considerations when using Resource Groups&quot;">​</a></h2><p>Available memory for resource groups may be limited on systems that use low or no swap space, and that use the default <code>vm.overcommit_ratio</code>. To ensure that WarehousePG has a reasonable per-segment-host memory limit, you may be required to increase one or more of the following configuration settings:</p><ol><li>The swap size on the system.</li><li>The system&#39;s <code>vm.overcommit_ratio</code> setting.</li></ol><h2 id="configuring-resource-groups" tabindex="-1"><a id="configuring_rg"></a>Configuring Resource Groups <a class="header-anchor" href="#configuring-resource-groups" aria-label="Permalink to &quot;&lt;a id=&quot;configuring_rg&quot;&gt;&lt;/a&gt;Configuring Resource Groups&quot;">​</a></h2><p>WarehousePG resource groups provide a powerful mechanism for managing the workload of the cluster. Consider these general guidelines when you configure resource groups for your system:</p><ul><li>A transaction submitted by any WarehousePG role with <code>SUPERUSER</code> privileges runs under the default resource group named <code>admin_group</code>. Keep this in mind when scheduling and running WarehousePG administration utilities.</li><li>Ensure that you assign each non-admin role a resource group. If you do not assign a resource group to a role, queries submitted by the role are handled by the default resource group named <code>default_group</code>.</li><li>Use the <code>CONCURRENCY</code> resource group parameter to limit the number of active queries that members of a particular resource group can run concurrently.</li><li>Use the <code>MEMORY_QUOTA</code> parameter to control the maximum amount of memory that queries running in the resource group can consume.</li><li>WarehousePG assigns unreserved memory (100 - (sum of all resource group <code>MEMORY_QUOTA</code>s) to a global shared memory pool. This memory is available to all queries on a first-come, first-served basis.</li><li>Alter resource groups dynamically to match the real requirements of the group for the workload and the time of day.</li><li>Use the <code>gp_toolkit</code> views to examine resource group resource usage and to monitor how the groups are working.</li></ul><h2 id="low-memory-queries" tabindex="-1"><a id="section113x"></a>Low Memory Queries <a class="header-anchor" href="#low-memory-queries" aria-label="Permalink to &quot;&lt;a id=&quot;section113x&quot;&gt;&lt;/a&gt;Low Memory Queries&quot;">​</a></h2><p>A low <code>statement_mem</code> setting (for example, in the 10MB range) has been shown to increase the performance of queries with low memory requirements. Use the <code>statement_mem</code> server configuration parameter to override the setting on a per-query basis. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SET statement_mem=&#39;10 MB&#39;;</span></span></code></pre></div><h2 id="administrative-utilities-and-admin-group-concurrency" tabindex="-1"><a id="section177x"></a>Administrative Utilities and admin_group Concurrency <a class="header-anchor" href="#administrative-utilities-and-admin-group-concurrency" aria-label="Permalink to &quot;&lt;a id=&quot;section177x&quot;&gt;&lt;/a&gt;Administrative Utilities and admin_group Concurrency&quot;">​</a></h2><p>The default resource group for database transactions initiated by WarehousePG <code>SUPERUSER</code>s is the group named <code>admin_group</code>. The default <code>CONCURRENCY</code> value for the <code>admin_group</code> resource group is 10.</p><p>Certain WarehousePG administrative utilities may use more than one <code>CONCURRENCY</code> slot at runtime, such as <code>gpbackup</code> that you invoke with the <code>--jobs</code> option. If the utility(s) you run require more concurrent transactions than that configured for <code>admin_group</code>, consider temporarily increasing the group&#39;s <code>MEMORY_QUOTA</code> and <code>CONCURRENCY</code> values to meet the utility&#39;s requirement, making sure to return these parameters back to their original settings when the utility completes.</p><blockquote><p><strong>Note</strong> Memory allocation changes that you initiate with <code>ALTER RESOURCE GROUP</code> may not take affect immediately due to resource consumption by currently running queries. Be sure to alter resource group parameters in advance of your maintenance window.</p></blockquote><p><strong>Parent topic:</strong> <a href="./intro.html">WarehousePG Best Practices</a></p>',25)]))}const p=o(s,[["render",a]]);export{h as __pageData,p as default};
