import{_ as o,c as t,o as n,ag as s}from"./chunks/framework.Ds6Eueu6.js";const m=JSON.parse('{"title":"Troubleshooting Connection Problems","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/access_db/troubleshooting-connection-problems.md","filePath":"docs/7x/admin_guide/access_db/troubleshooting-connection-problems.md"}'),r={name:"docs/7x/admin_guide/access_db/troubleshooting-connection-problems.md"};function a(c,e,i,h,d,l){return n(),t("div",null,e[0]||(e[0]=[s('<h1 id="troubleshooting-connection-problems" tabindex="-1">Troubleshooting Connection Problems <a class="header-anchor" href="#troubleshooting-connection-problems" aria-label="Permalink to &quot;Troubleshooting Connection Problems&quot;">​</a></h1><hr><p>A number of things can prevent a client application from successfully connecting to WarehousePG. This topic explains some of the common causes of connection problems and how to correct them.</p><table tabindex="0"><thead><tr><th>Problem</th><th>Solution</th></tr></thead><tbody><tr><td>No <code>pg_hba.conf</code> entry for host or user</td><td>To enable WarehousePG to accept remote client connections, you must configure your WarehousePG coordinator instance so that connections are allowed from the client hosts and database users that will be connecting to WarehousePG. This is done by adding the appropriate entries to the <code>pg_hba.conf</code> configuration file (located in the coordinator instance&#39;s data directory). For more detailed information, see <a href="./../../client_auth.html#topic2">Allowing Connections to WarehousePG</a>.</td></tr><tr><td>WarehousePG is not running</td><td>If the WarehousePG coordinator instance is down, users will not be able to connect. You can verify that the WarehousePG cluster is up by running the <code>gpstate</code> utility on the WarehousePG coordinator host.</td></tr><tr><td>Network problems Interconnect timeouts</td><td>If users connect to the WarehousePG coordinator host from a remote client, network problems can prevent a connection (for example, DNS host name resolution problems, the host system is down, and so on.). To ensure that network problems are not the cause, connect to the WarehousePG coordinator host from the remote client host. For example: <code>ping hostname</code><br><br>If the system cannot resolve the host names and IP addresses of the hosts involved in WarehousePG, queries and connections will fail. For some operations, connections to the WarehousePG coordinator use <code>localhost</code> and others use the actual host name, so you must be able to resolve both. If you encounter this error, first make sure you can connect to each host in your WarehousePG cluster from the coordinator host over the network. In the <code>/etc/hosts</code> file of the coordinator and all segments, make sure you have the correct host names and IP addresses for all hosts involved in the WarehousePG cluster. The <code>127.0.0.1</code> IP must resolve to <code>localhost</code>.</td></tr><tr><td>Too many clients already</td><td>By default, WarehousePG is configured to allow a maximum of 250 concurrent user connections on the coordinator and 750 on a segment. A connection attempt that causes that limit to be exceeded will be refused. This limit is controlled by the <code>max_connections</code> parameter in the <code>postgresql.conf</code> configuration file of the WarehousePG coordinator. If you change this setting for the coordinator, you must also make appropriate changes at the segments.</td></tr></tbody></table><p><strong>Parent topic:</strong> <a href="./../access_db/accessing-the-database.html">Accessing the Database</a></p>',5)]))}const b=o(r,[["render",a]]);export{m as __pageData,b as default};
