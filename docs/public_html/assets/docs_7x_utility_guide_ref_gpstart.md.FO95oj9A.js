import{_ as t,c as a,o as s,ag as o}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"gpstart","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/gpstart.md","filePath":"docs/7x/utility_guide/ref/gpstart.md"}'),i={name:"docs/7x/utility_guide/ref/gpstart.md"};function n(r,e,p,l,c,d){return s(),a("div",null,e[0]||(e[0]=[o(`<h1 id="gpstart" tabindex="-1">gpstart <a class="header-anchor" href="#gpstart" aria-label="Permalink to &quot;gpstart&quot;">​</a></h1><p>Starts a WarehousePG cluster.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpstart [-d &lt;coordinator_data_directory&gt;] [-B &lt;parallel_processes&gt;] [-R]</span></span>
<span class="line"><span>        [-m] [-y] [-a] [-t &lt;timeout_seconds&gt;] [-l &lt;logfile_directory&gt;] </span></span>
<span class="line"><span>        [--skip-heap-checksum-validation]</span></span>
<span class="line"><span>        [-v | -q]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpstart -? | -h | --help </span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpstart --version</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>The <code>gpstart</code> utility is used to start the WarehousePG server processes. When you start a WarehousePG cluster, you are actually starting several <code>postgres</code> database server listener processes at once (the coordinator and all of the segment instances). The <code>gpstart</code> utility handles the startup of the individual instances. Each instance is started in parallel.</p><p>As part of the startup process, the utility checks the consistency of heap checksum setting among the WarehousePG coordinator and segment instances, either enabled or deactivated on all instances. If the heap checksum setting is different among the instances, an error is returned and WarehousePG does not start. The validation can be deactivated by specifying the option <code>--skip-heap-checksum-validation</code>. For more information about heap checksums, see <a href="./../../admin_guide/ha/enabling-high-availability-features.html">Enabling High Availability and Data Consistency Features</a> in the <em>WarehousePG Administrator Guide</em>.</p><blockquote><p><strong>Note</strong> Before you can start a WarehousePG cluster, you must have initialized the system using <code>gpinitsystem</code>. Enabling or deactivating heap checksums is set when you initialize the system and cannot be changed after initialization.</p></blockquote><p>If the WarehousePG cluster is configured with a standby coordinator, and <code>gpstart</code> does not detect it during startup, <code>gpstart</code> displays a warning and lets you cancel the startup operation.</p><ul><li><p>If the <code>-a</code> option (deactivate interactive mode prompts) is not specified, <code>gpstart</code> displays and logs these messages:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Standby host is unreachable, cannot determine whether the standby is currently acting as the coordinator. Received error: &lt;error&gt;</span></span>
<span class="line"><span>Continue only if you are certain that the standby is not acting as the coordinator.</span></span></code></pre></div><p>It also displays this prompt to continue startup:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Continue with startup Yy|Nn (default=N):</span></span></code></pre></div></li><li><p>If the <code>-a</code> option is specified, the utility does not start the system. The messages are only logged, and <code>gpstart</code> adds this log message:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Non interactive mode detected. Not starting the cluster. Start the cluster in interactive mode.</span></span></code></pre></div></li></ul><p>If the standby coordinator is not accessible, you can start the system and troubleshoot standby coordinator issues while the system is available.</p><h2 id="options" tabindex="-1"><a id="section4"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>-a : Do not prompt the user for confirmation. Deactivates interactive mode.</p><p>-B parallel_processes : The number of segments to start in parallel. If not specified, the utility will start up to 64 parallel processes depending on how many segment instances it needs to start.</p><p>-d coordinator_data_directory : Optional. The coordinator host data directory. If not specified, the value set for <code>$COORDINATOR_DATA_DIRECTORY</code> will be used.</p><p>-l logfile_directory : The directory to write the log file. Defaults to <code>~/gpAdminLogs</code>.</p><p>-m : Optional. Starts the coordinator instance only, which may be useful for maintenance tasks. This mode only allows connections to the coordinator in utility mode. For example:</p><p>: <code>PGOPTIONS=&#39;-c gp_role=utility&#39; psql</code></p><p>: The consistency of the heap checksum setting on coordinator and segment instances is not checked.</p><p>-q : Run in quiet mode. Command output is not displayed on the screen, but is still written to the log file.</p><p>-R : Starts WarehousePG in restricted mode (only database superusers are allowed to connect).</p><p>--skip-heap-checksum-validation : During startup, the utility does not validate the consistency of the heap checksum setting among the WarehousePG coordinator and segment instances. The default is to ensure that the heap checksum setting is the same on all instances, either enabled or deactivated.</p><pre><code>&gt; **Caution** Starting WarehousePG without this validation could lead to data loss. Use this option to start WarehousePG only when it is necessary to ignore the heap checksum verification errors to recover data or to troubleshoot the errors.
</code></pre><p>-t timeout_seconds : Specifies a timeout in seconds to wait for a segment instance to start up. If a segment instance was shutdown abnormally (due to power failure or killing its <code>postgres</code> database listener process, for example), it may take longer to start up due to the database recovery and validation process. If not specified, the default timeout is 600 seconds.</p><p>-v : Displays detailed status, progress and error messages output by the utility.</p><p>-y : Optional. Do not start the standby coordinator host. The default is to start the standby coordinator host and synchronization process.</p><p>-? | -h | --help : Displays the online help.</p><p>--version : Displays the version of this utility.</p><h2 id="examples" tabindex="-1"><a id="section5"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Start a WarehousePG cluster:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpstart</span></span></code></pre></div><p>Start a WarehousePG cluster in restricted mode (only allow superuser connections):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpstart -R</span></span></code></pre></div><p>Start the WarehousePG coordinator instance only and connect in utility mode:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpstart -m </span></span>
<span class="line"><span>PGOPTIONS=&#39;-c gp_role=utility&#39; psql</span></span></code></pre></div><h2 id="see-also" tabindex="-1"><a id="section6"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./gpstop.html">gpstop</a>, <a href="./gpinitsystem.html">gpinitsystem</a></p>`,37)]))}const g=t(i,[["render",n]]);export{u as __pageData,g as default};
