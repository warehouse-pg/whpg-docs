import{_ as a,c as t,o,ag as n}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"CREATE PROCEDURE","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/CREATE_PROCEDURE.md","filePath":"docs/7x/ref_guide/sql_commands/CREATE_PROCEDURE.md"}'),s={name:"docs/7x/ref_guide/sql_commands/CREATE_PROCEDURE.md"};function r(i,e,d,c,l,p){return o(),t("div",null,e[0]||(e[0]=[n(`<h1 id="create-procedure" tabindex="-1">CREATE PROCEDURE <a class="header-anchor" href="#create-procedure" aria-label="Permalink to &quot;CREATE PROCEDURE&quot;">​</a></h1><p>Defines a new procedure.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE [OR REPLACE] PROCEDURE &lt;name&gt;    </span></span>
<span class="line"><span>    ( [ [&lt;argmode&gt;] [&lt;argname&gt;] &lt;argtype&gt; [ { DEFAULT | = } &lt;default_expr&gt; ] [, ...] ] )</span></span>
<span class="line"><span>  { LANGUAGE &lt;lang_name&gt;</span></span>
<span class="line"><span>    | TRANSFORM { FOR TYPE &lt;type_name&gt; } [, ... ]</span></span>
<span class="line"><span>    | { [ EXTERNAL ] SECURITY INVOKER | [ EXTERNAL ] SECURITY DEFINER }</span></span>
<span class="line"><span>    | SET &lt;configuration_parameter&gt; { TO &lt;value&gt; | = &lt;value&gt; | FROM CURRENT }</span></span>
<span class="line"><span>    | AS &#39;&lt;definition&gt;&#39;</span></span>
<span class="line"><span>    | AS &#39;&lt;obj_file&gt;&#39;, &#39;&lt;link_symbol&gt;&#39; </span></span>
<span class="line"><span>  } ...</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>CREATE PROCEDURE</code> defines a new procedure. <code>CREATE OR REPLACE PROCEDURE</code> either creates a new procedure, or replaces an existing definition. To define a procedure, the user must have the <code>USAGE</code> privilege on the language.</p><p>If a schema name is included, then the procedure is created in the specified schema. Otherwise it is created in the current schema. The name of the new procedure must not match any existing procedure with the same input argument types in the same schema. However, procedures and functions of different argument types may share a name (overloading).</p><p>To update the current definition of an existing procedure, use <code>CREATE OR REPLACE PROCEDURE</code>. It is not possible to change the name or argument types of a procedure this way (this would actually create a new, distinct procedure).</p><p>When <code>CREATE OR REPLACE PROCEDURE</code> is used to replace an existing procedure, the ownership and permissions of the procedure do not change. All other procedure properties are assigned the values specified or implied in the command. You must own the procedure to replace it (this includes being a member of the owning role).</p><p>The user that creates the procedure becomes the owner of the procedure.</p><p>To be able to create a procedure, you must have <code>USAGE</code> privilege on the argument types.</p><p>For more information about creating procedures, refer to the <a href="https://www.postgresql.org/docs/12/xproc.html" target="_blank" rel="noreferrer">User Defined Procedures</a> section of the PostgreSQL documentation.</p><h2 id="parameters" tabindex="-1"><a id="section5"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>name : The name (optionally schema-qualified) of the procedure to create.</p><p>argmode : The mode of an argument: either <code>IN</code>, <code>INOUT</code>, or <code>VARIADIC</code>. If omitted, the default is <code>IN</code>. (<code>OUT</code> arguments are currently not supported for procedures. Use <code>INOUT</code> instead.)</p><p>argname : The name of an argument.</p><p>argtype : The data type(s) of the procedure&#39;s arguments (optionally schema-qualified), if any. The argument types may be base, composite, or domain types, or may reference the type of a table column.</p><p>: Depending on the implementation language it may also be allowed to specify pseudotypes such as <code>cstring</code>. Pseudotypes indicate that the actual argument type is either incompletely specified, or outside the set of ordinary SQL data types.</p><p>: The type of a column is referenced by writing <code>table_name.column_name%TYPE</code>. Using this feature can sometimes help make a procedure independent of changes to the definition of a table.</p><p>default_expr : An expression to be used as the default value if the parameter is not specified. The expression must be coercible to the argument type of the parameter. Each input parameter in the argument list that follows a parameter with a default value must have a default value as well.</p><p>lang_name : The name of the language that the procedure is implemented in. May be <code>SQL</code>, <code>C</code>, <code>internal</code>, or the name of a user-defined procedural language, e.g. <code>plpgsql</code>. Enclosing the name in single quotes is deprecated and requires matching case.</p><p>TRANSFORM { FOR TYPE type_name } [, ... ] } : Lists which transforms a call to the procedure should apply. Transforms convert between SQL types and language-specific data types. Procedural language implementations usually have hardcoded knowledge of the built-in types, so those don&#39;t need to be listed here. If a procedural language implementation does not know how to handle a type and no transform is supplied, it will fall back to a default behavior for converting data types, but this depends on the implementation.</p><p>[EXTERNAL] SECURITY INVOKER [EXTERNAL] SECURITY DEFINER : <code>SECURITY INVOKER</code> (the default) indicates that the procedure is to be run with the privileges of the user that calls it. : <code>SECURITY DEFINER</code> specifies that the procedure is to be run with the privileges of the user that created it. : The key word <code>EXTERNAL</code> is allowed for SQL conformance, but it is optional since, unlike in SQL, this feature applies to all procedures not just external ones. : A <code>SECURITY DEFINER</code> procedure cannot execute transaction control statements (for example, <code>COMMIT</code> and <code>ROLLBACK</code>, depending on the language).</p><p>configuration_parameter value : The <code>SET</code> clause applies a value to a session configuration parameter when the procedure is entered. The configuration parameter is restored to its prior value when the procedure exits. <code>SET FROM CURRENT</code> saves the value of the parameter that is current when <code>CREATE PROCEDURE</code> is run as the value to be applied when the procedure is entered. : If a <code>SET</code> clause is attached to a procedure, then the effects of a <code>SET LOCAL</code> command executed inside the procedure for the same variable are restricted to the procedure: the configuration parameter&#39;s prior value is still restored at procedure exit. However, an ordinary <code>SET</code> command (without <code>LOCAL</code>) overrides the <code>SET</code> clause, much as it would do for a previous <code>SET LOCAL</code> command: the effects of such a command will persist after procedure exit, unless the current transaction is rolled back. : If a <code>SET</code> clause is attached to a procedure, then that procedure cannot execute transaction control statements (for example, <code>COMMIT</code> and <code>ROLLBACK</code>, depending on the language). : See <a href="./SET.html">SET</a> for more information about allowed parameter names and values.</p><p>definition : A string constant defining the procedure; the meaning depends on the language. It may be an internal procedure name, the path to an object file, an SQL command, or text in a procedural language.</p><p>: It is often helpful to use dollar quoting (refer to <a href="https://www.postgresql.org/docs/12/sql-syntax-lexical.html#SQL-SYNTAX-DOLLAR-QUOTING" target="_blank" rel="noreferrer">Dollar-Quoted String Constants </a> in the PostgreSQL documentation) to write the procedure definition string, rather than the normal single quote syntax. Without dollar quoting, any single quotes or backslashes in the procedure definition must be escaped by doubling them.</p><p>obj_file, link_symbol : This form of the <code>AS</code> clause is used for dynamically loadable C language procedures when the procedure name in the C language source code is not the same as the name of the SQL procedure. The string obj_file is the name of the file containing the dynamically loadable object, and is interpreted as for the <a href="./LOAD.html">LOAD</a> command. The string link_symbol is the name of the procedure in the C language source code. If the link symbol is omitted, it is assumed to be the same as the name of the SQL procedure being defined. : When repeated <code>CREATE PROCEDURE</code> calls refer to the same object file, the file is only loaded once per session. To unload and reload the file (perhaps during development), start a new session.</p><h2 id="notes" tabindex="-1"><a id="section6"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>See <a href="./CREATE_FUNCTION.html">CREATE FUNCTION</a> for more details on function creation that also apply to procedures.</p><p>Use <a href="./CALL.html">CALL</a> to execute a procedure.</p><h2 id="examples" tabindex="-1"><a id="section8"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE PROCEDURE insert_data(a integer, b integer)</span></span>
<span class="line"><span>LANGUAGE SQL</span></span>
<span class="line"><span>AS $$</span></span>
<span class="line"><span>INSERT INTO tbl VALUES (a);</span></span>
<span class="line"><span>INSERT INTO tbl VALUES (b);</span></span>
<span class="line"><span>$$;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CALL insert_data(1, 2);</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section9"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section9&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p>A <code>CREATE PROCEDURE</code> command is defined in the SQL standard. The WarehousePG version is similar but not fully compatible. For details see also <a href="./CREATE_FUNCTION.html">CREATE FUNCTION</a>.</p><h2 id="see-also" tabindex="-1"><a id="section10"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section10&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./ALTER_PROCEDURE.html">ALTER PROCEDURE</a>, <a href="./DROP_PROCEDURE.html">DROP PROCEDURE</a>, <a href="./CREATE_FUNCTION.html">CREATE FUNCTION</a>, <a href="./CALL.html">CALL</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,37)]))}const m=a(s,[["render",r]]);export{u as __pageData,m as default};
