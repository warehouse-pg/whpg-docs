import{_ as a,c as e,o,ag as i}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"gpactivatestandby","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/gpactivatestandby.md","filePath":"docs/7x/utility_guide/ref/gpactivatestandby.md"}'),s={name:"docs/7x/utility_guide/ref/gpactivatestandby.md"};function n(r,t,d,c,p,h){return o(),e("div",null,t[0]||(t[0]=[i(`<h1 id="gpactivatestandby" tabindex="-1">gpactivatestandby <a class="header-anchor" href="#gpactivatestandby" aria-label="Permalink to &quot;gpactivatestandby&quot;">​</a></h1><p>Activates a standby coordinator host and makes it the active coordinator for the WarehousePG cluster.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpactivatestandby [-d &lt;standby_coordinator_datadir&gt;] [-f] [-a] [-q] </span></span>
<span class="line"><span>    [-l &lt;logfile_directory&gt;]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpactivatestandby -v </span></span>
<span class="line"><span></span></span>
<span class="line"><span>gpactivatestandby -? | -h | --help</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>The <code>gpactivatestandby</code> utility activates a backup, standby coordinator host and brings it into operation as the active coordinator instance for a WarehousePG cluster. The activated standby coordinator effectively becomes the WarehousePG coordinator, accepting client connections on the coordinator port.</p><blockquote><p><strong>NOTE</strong> Before running <code>gpactivatestandby</code>, be sure to run <code>gpstate -f</code> to confirm that the standby coordinator is synchronized with the current coordinator node. If synchronized, the final line of the <code>gpstate -f</code> output will look similar to this: <code>20230607:06:50:06:004205 gpstate:test1-m:gpadmin-[INFO]:--Sync state: sync</code></p></blockquote><p>When you initialize a standby coordinator, the default is to use the same port as the active coordinator. For information about the coordinator port for the standby coordinator, see <a href="./gpinitstandby.html">gpinitstandby</a>.</p><p>You must run this utility from the coordinator host you are activating, not the failed coordinator host you are deactivating. Running this utility assumes you have a standby coordinator host configured for the system (see <a href="./gpinitstandby.html">gpinitstandby</a>).</p><p>The utility will perform the following steps:</p><ul><li>Stops the synchronization process (<code>walreceiver</code>) on the standby coordinator</li><li>Updates the system catalog tables of the standby coordinator using the logs</li><li>Activates the standby coordinator to be the new active coordinator for the system</li><li>Restarts the WarehousePG cluster with the new coordinator host</li></ul><p>A backup, standby WarehousePG coordinator host serves as a &#39;warm standby&#39; in the event of the primary WarehousePG coordinator host becoming non-operational. The standby coordinator is kept up to date by transaction log replication processes (the <code>walsender</code> and <code>walreceiver</code>), which run on the primary coordinator and standby coordinator hosts and keep the data between the primary and standby coordinator hosts synchronized.</p><p>If the primary coordinator fails, the log replication process is shutdown, and the standby coordinator can be activated in its place by using the <code>gpactivatestandby</code> utility. Upon activation of the standby coordinator, the replicated logs are used to reconstruct the state of the WarehousePG coordinator host at the time of the last successfully committed transaction.</p><p>In order to use <code>gpactivatestandby</code> to activate a new primary coordinator host, the coordinator host that was previously serving as the primary coordinator cannot be running. The utility checks for a <code>postmaster.pid</code> file in the data directory of the deactivated coordinator host, and if it finds it there, it will assume the old coordinator host is still active. In some cases, you may need to remove the <code>postmaster.pid</code> file from the deactivated coordinator host data directory before running <code>gpactivatestandby</code> (for example, if the deactivated coordinator host process was terminated unexpectedly).</p><p>After activating a standby coordinator, run <code>ANALYZE</code> to update the database query statistics. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>psql &lt;dbname&gt; -c &#39;ANALYZE;&#39;</span></span></code></pre></div><p>After you activate the standby coordinator as the primary coordinator, the WarehousePG cluster no longer has a standby coordinator configured. You might want to specify another host to be the new standby with the <a href="./gpinitstandby.html">gpinitstandby</a> utility.</p><h2 id="options" tabindex="-1"><a id="section4"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>-a (do not prompt) : Do not prompt the user for confirmation.</p><p>-d standby_coordinator_datadir : The absolute path of the data directory for the coordinator host you are activating.</p><p>: If this option is not specified, <code>gpactivatestandby</code> uses the value of the <code>COORDINATOR_DATA_DIRECTORY</code> environment variable setting on the coordinator host you are activating. If this option is specified, it overrides any setting of <code>COORDINATOR_DATA_DIRECTORY</code>.</p><p>: If a directory cannot be determined, the utility returns an error.</p><p>-f (force activation) : Use this option to force activation of the backup coordinator host. Use this option only if you are sure that the standby and primary coordinator hosts are consistent.</p><p>-l logfile_directory : The directory to write the log file. Defaults to <code>~/gpAdminLogs</code>.</p><p>-q (no screen output) : Run in quiet mode. Command output is not displayed on the screen, but is still written to the log file.</p><p>-v (show utility version) : Displays the version, status, last updated date, and check sum of this utility.</p><p>-? | -h | --help (help) : Displays the online help.</p><h2 id="example" tabindex="-1"><a id="section5"></a>Example <a class="header-anchor" href="#example" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Example&quot;">​</a></h2><p>Activate the standby coordinator host and make it the active coordinator instance for a WarehousePG cluster (run from backup coordinator host you are activating):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gpactivatestandby -d /gpdata</span></span></code></pre></div><h2 id="see-also" tabindex="-1"><a id="section6"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./gpinitsystem.html">gpinitsystem</a>, <a href="./gpinitstandby.html">gpinitstandby</a></p>`,32)]))}const y=a(s,[["render",n]]);export{u as __pageData,y as default};
