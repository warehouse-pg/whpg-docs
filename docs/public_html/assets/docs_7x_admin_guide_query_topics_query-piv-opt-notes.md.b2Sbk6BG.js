import{_ as o,c as t,o as i,ag as a}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"Considerations when Using GPORCA","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/query/topics/query-piv-opt-notes.md","filePath":"docs/7x/admin_guide/query/topics/query-piv-opt-notes.md"}'),n={name:"docs/7x/admin_guide/query/topics/query-piv-opt-notes.md"};function s(r,e,c,l,d,p){return i(),t("div",null,e[0]||(e[0]=[a('<h1 id="considerations-when-using-gporca" tabindex="-1">Considerations when Using GPORCA <a class="header-anchor" href="#considerations-when-using-gporca" aria-label="Permalink to &quot;Considerations when Using GPORCA&quot;">​</a></h1><hr><p>To run queries optimally with GPORCA, consider the query criteria closely.</p><p>Ensure the following criteria are met:</p><ul><li><p>The table does not contain multi-column partition keys.</p></li><li><p>The server configuration parameter <code>optimizer_enable_coordinator_only_queries</code> is set to <code>on</code> when running against coordinator only tables such as the system table <em>pg_attribute</em>. For information about the parameter, see the <em>WarehousePG Reference Guide</em>.</p><blockquote><p><strong>Note</strong> Enabling this parameter decreases performance of short running catalog queries. To avoid this issue, set this parameter only for a session or a query.</p></blockquote></li><li><p>Statistics have been collected on the root partitioned table.</p></li></ul><p>If the partitioned table contains more than 20,000 partitions, consider a redesign of the table schema.</p><p>These server configuration parameters affect GPORCA query processing.</p><ul><li><p><code>optimizer_cte_inlining_bound</code> controls the amount of inlining performed for common table expression (CTE) queries (queries that contain a <code>WITH</code> clause).</p></li><li><p><code>optimizer_force_comprehensive_join_implementation</code> affects GPORCA&#39;s consideration of nested loop join and hash join alternatives. When the value is <code>false</code> (the default), GPORCA does not consider nested loop join alternatives when a hash join is available.</p></li><li><p><code>optimizer_force_multistage_agg</code> forces GPORCA to choose a multi-stage aggregate plan for a scalar distinct qualified aggregate. When the value is <code>off</code> (the default), GPORCA chooses between a one-stage and two-stage aggregate plan based on cost.</p></li><li><p><code>optimizer_force_three_stage_scalar_dqa</code> forces GPORCA to choose a plan with multistage aggregates when such a plan alternative is generated.</p></li><li><p><code>optimizer_join_order</code> sets the query optimization level for join ordering by specifying which types of join ordering alternatives to evaluate.</p></li><li><p><code>optimizer_join_order_threshold</code> specifies the maximum number of join children for which GPORCA uses the dynamic programming-based join ordering algorithm.</p></li><li><p><code>optimizer_nestloop_factor</code> controls nested loop join cost factor to apply to during query optimization.</p></li><li><p><code>optimizer_parallel_union</code> controls the amount of parallelization that occurs for queries that contain a <code>UNION</code> or <code>UNION ALL</code> clause. When the value is <code>on</code>, GPORCA can generate a query plan of the child operations of a <code>UNION</code> or <code>UNION ALL</code> operation run in parallel on segment instances.</p></li><li><p><code>optimizer_sort_factor</code> controls the cost factor that GPORCA applies to sorting operations during query optimization. The cost factor can be adjusted for queries when data skew is present.</p></li><li><p><code>gp_enable_relsize_collection</code> controls how GPORCA (and the Postgres-based planner) handle a table without statistics. By default, GPORCA uses a default value to estimate the number of rows if statistics are not available. When this value is <code>on</code>, GPORCA uses the estimated size of a table if there are no statistics for the table.</p><p>This parameter is ignored for a root partitioned table. If the root partition does not have statistics, GPORCA always uses the default value. You can use <code>ANALZYE ROOTPARTITION</code> to collect statistics on the root partition. See <a href="./../../../ref_guide/sql_commands/ANALYZE.html">ANALYZE</a>.</p></li></ul><p>These server configuration parameters control the display and logging of information.</p><ul><li><code>optimizer_print_missing_stats</code> controls the display of column information about columns with missing statistics for a query (default is <code>true</code>)</li><li><code>optimizer_print_optimization_stats</code> controls the logging of GPORCA query optimization metrics for a query (default is <code>off</code>)</li></ul><p>For information about the parameters, see the <em>WarehousePG Reference Guide</em>.</p><p>GPORCA generates minidumps to describe the optimization context for a given query. The minidump files are used by support to analyze WarehousePG issues. The information in the file is not in a format that can be easily used for debugging or troubleshooting. The minidump file is located under the coordinator data directory and uses the following naming format:</p><p><code>Minidump_date_time.mdp</code></p><p>For information about the minidump file, see the server configuration parameter <code>optimizer_minidump</code> in the <em>WarehousePG Reference Guide</em>.</p><p>When the <code>EXPLAIN ANALYZE</code> command uses GPORCA, the <code>EXPLAIN</code> plan shows only the number of partitions that are being eliminated. The scanned partitions are not shown. To show the name of the scanned partitions in the segment logs set the server configuration parameter <code>gp_log_dynamic_partition_pruning</code> to <code>on</code>. This example <code>SET</code> command enables the parameter.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SET gp_log_dynamic_partition_pruning = on;</span></span></code></pre></div><p><strong>Parent topic:</strong> <a href="./../../query/topics/query-piv-optimizer.html">About GPORCA</a></p>',17)]))}const m=o(n,[["render",s]]);export{u as __pageData,m as default};
