import{_ as a,c as s,o as t,ag as o}from"./chunks/framework.Ds6Eueu6.js";const u=JSON.parse('{"title":"createuser","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/utility_guide/ref/createuser.md","filePath":"docs/7x/utility_guide/ref/createuser.md"}'),n={name:"docs/7x/utility_guide/ref/createuser.md"};function i(r,e,p,l,c,d){return t(),s("div",null,e[0]||(e[0]=[o(`<h1 id="createuser" tabindex="-1">createuser <a class="header-anchor" href="#createuser" aria-label="Permalink to &quot;createuser&quot;">​</a></h1><p>Creates a new database role.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>createuser [&lt;connection-option&gt; ...] [&lt;role_attribute&gt; ...] [-e] &lt;role_name&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>createuser -? | --help </span></span>
<span class="line"><span></span></span>
<span class="line"><span>createuser -V | --version</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p><code>createuser</code> creates a new WarehousePG role. You must be a superuser or have the <code>CREATEROLE</code> privilege to create new roles. You must connect to the database as a superuser to create new superusers.</p><p>Superusers can bypass all access permission checks within the database, so superuser privileges should not be granted lightly.</p><p><code>createuser</code> is a wrapper around the SQL command <code>CREATE ROLE</code>.</p><h2 id="options" tabindex="-1"><a id="section4"></a>Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Options&quot;">​</a></h2><p>role_name : The name of the role to be created. This name must be different from all existing roles in this WarehousePG installation.</p><p>-c number | --connection-limit=number : Set a maximum number of connections for the new role. The default is to set no limit.</p><p>-d | --createdb : The new role will be allowed to create databases.</p><p>-D | --no-createdb : The new role will not be allowed to create databases. This is the default.</p><p>-e | --echo : Echo the commands that <code>createuser</code> generates and sends to the server.</p><p>-E | --encrypted : Encrypts the role&#39;s password stored in the database. If not specified, the default password behavior is used.</p><p>-i | --inherit : The new role will automatically inherit privileges of roles it is a member of. This is the default.</p><p>-I | --no-inherit : The new role will not automatically inherit privileges of roles it is a member of.</p><p>--interactive : Prompt for the user name if none is specified on the command line, and also prompt for whichever of the options <code>-d</code>/<code>-D</code>, <code>-r</code>/<code>-R</code>, <code>-s</code>/<code>-S</code> is not specified on the command line.</p><p>-l | --login : The new role will be allowed to log in to WarehousePG. This is the default.</p><p>-L | --no-login : The new role will not be allowed to log in (a group-level role).</p><p>-N | --unencrypted : Does not encrypt the role&#39;s password stored in the database. If not specified, the default password behavior is used.</p><p>-P | --pwprompt : If given, <code>createuser</code> will issue a prompt for the password of the new role. This is not necessary if you do not plan on using password authentication.</p><p>-r | --createrole : The new role will be allowed to create new roles (<code>CREATEROLE</code> privilege).</p><p>-R | --no-createrole : The new role will not be allowed to create new roles. This is the default.</p><p>-s | --superuser : The new role will be a superuser.</p><p>-S | --no-superuser : The new role will not be a superuser. This is the default.</p><p>-V | --version : Print the <code>createuser</code> version and exit.</p><p>--replication : The new user will have the <code>REPLICATION</code> privilege, which is described more fully in the documentation for <a href="./../../ref_guide/sql_commands/CREATE_ROLE.html">CREATE ROLE</a>.</p><p>--no-replication : The new user will not have the <code>REPLICATION</code> privilege, which is described more fully in the documentation for <a href="./../../ref_guide/sql_commands/CREATE_ROLE.html">CREATE ROLE</a>.</p><p>-? | --help : Show help about <code>createuser</code> command line arguments, and exit.</p><p><strong>Connection Options</strong></p><p>-h host | --host=host : The host name of the machine on which the WarehousePG coordinator database server is running. If not specified, reads from the environment variable <code>PGHOST</code> or defaults to localhost.</p><p>-p port | --port=port : The TCP port on which the WarehousePG coordinator database server is listening for connections. If not specified, reads from the environment variable <code>PGPORT</code> or defaults to 5432.</p><p>-U username | --username=username : The database role name to connect as. If not specified, reads from the environment variable <code>PGUSER</code> or defaults to the current system role name.</p><p>-w | --no-password : Never issue a password prompt. If the server requires password authentication and a password is not available by other means such as a <code>.pgpass</code> file, the connection attempt will fail. This option can be useful in batch jobs and scripts where no user is present to enter a password.</p><p>-W | --password : Force a password prompt.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>To create a role <code>joe</code> on the default database server:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ createuser joe</span></span></code></pre></div><p>To create a role <code>joe</code> on the default database server with prompting for some additional attributes:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ createuser --interactive joe</span></span>
<span class="line"><span>Shall the new role be a superuser? (y/n) **n**</span></span>
<span class="line"><span>Shall the new role be allowed to create databases? (y/n) **n**</span></span>
<span class="line"><span>Shall the new role be allowed to create more new roles? (y/n) **n**</span></span>
<span class="line"><span>CREATE ROLE</span></span></code></pre></div><p>To create the same role <code>joe</code> using connection options, with attributes explicitly specified, and taking a look at the underlying command:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>createuser -h coordinatorhost -p 54321 -S -D -R -e joe</span></span>
<span class="line"><span>CREATE ROLE joe NOSUPERUSER NOCREATEDB NOCREATEROLE INHERIT </span></span>
<span class="line"><span>LOGIN;</span></span>
<span class="line"><span>CREATE ROLE</span></span></code></pre></div><p>To create the role <code>joe</code> as a superuser, and assign password <code>admin123</code> immediately:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>createuser -P -s -e joe</span></span>
<span class="line"><span>Enter password for new role: admin123</span></span>
<span class="line"><span>Enter it again: admin123</span></span>
<span class="line"><span>CREATE ROLE joe PASSWORD &#39;admin123&#39; SUPERUSER CREATEDB </span></span>
<span class="line"><span>CREATEROLE INHERIT LOGIN;</span></span>
<span class="line"><span>CREATE ROLE</span></span></code></pre></div><p>In the above example, the new password is not actually echoed when typed, but we show what was typed for clarity. However the password will appear in the echoed command, as illustrated if the <code>-e</code> option is used.</p><h2 id="see-also" tabindex="-1"><a id="section7"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./../../ref_guide/sql_commands/CREATE_ROLE.html">CREATE ROLE</a>, <a href="./dropuser.html">dropuser</a></p>`,48)]))}const m=a(n,[["render",i]]);export{u as __pageData,m as default};
