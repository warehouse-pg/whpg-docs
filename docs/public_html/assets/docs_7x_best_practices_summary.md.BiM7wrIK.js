import{_ as a,c as t,o as i,ag as o}from"./chunks/framework.Ds6Eueu6.js";const p=JSON.parse('{"title":"Best Practices Summary","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/best_practices/summary.md","filePath":"docs/7x/best_practices/summary.md"}'),s={name:"docs/7x/best_practices/summary.md"};function n(l,e,r,d,c,u){return i(),t("div",null,e[0]||(e[0]=[o(`<h1 id="best-practices-summary" tabindex="-1">Best Practices Summary <a class="header-anchor" href="#best-practices-summary" aria-label="Permalink to &quot;Best Practices Summary&quot;">​</a></h1><hr><p>A summary of best practices for WarehousePG.</p><h2 id="data-model" tabindex="-1"><a id="datmod"></a>Data Model <a class="header-anchor" href="#data-model" aria-label="Permalink to &quot;&lt;a id=&quot;datmod&quot;&gt;&lt;/a&gt;Data Model&quot;">​</a></h2><p>WarehousePG is an analytical MPP shared-nothing database. This model is significantly different from a highly normalized/transactional SMP database. Because of this, the following best practices are recommended.</p><ul><li>WarehousePG performs best with a denormalized schema design suited for MPP analytical processing for example, Star or Snowflake schema, with large fact tables and smaller dimension tables.</li><li>Use the same data types for columns used in joins between tables.</li></ul><p>See <a href="./schema.html">Schema Design</a>.</p><h2 id="heap-vs-append-optimized-storage" tabindex="-1"><a id="heapapp"></a>Heap vs. Append-Optimized Storage <a class="header-anchor" href="#heap-vs-append-optimized-storage" aria-label="Permalink to &quot;&lt;a id=&quot;heapapp&quot;&gt;&lt;/a&gt;Heap vs. Append-Optimized Storage&quot;">​</a></h2><ul><li>Use heap storage for tables and partitions that will receive iterative batch and singleton <code>UPDATE</code>, <code>DELETE</code>, and <code>INSERT</code> operations.</li><li>Use heap storage for tables and partitions that will receive concurrent <code>UPDATE</code>, <code>DELETE</code>, and <code>INSERT</code> operations.</li><li>Use append-optimized storage for tables and partitions that are updated infrequently after the initial load and have subsequent inserts only performed in large batch operations.</li><li>Avoid performing singleton <code>INSERT</code>, <code>UPDATE</code>, or <code>DELETE</code> operations on append-optimized tables.</li><li>Avoid performing concurrent batch <code>UPDATE</code> or <code>DELETE</code> operations on append-optimized tables. Concurrent batch <code>INSERT</code> operations are acceptable.</li></ul><p>See <a href="./schema.html#heap_vs_ao">Heap Storage or Append-Optimized Storage</a>.</p><h2 id="row-vs-column-oriented-storage" tabindex="-1"><a id="rowcol"></a>Row vs. Column Oriented Storage <a class="header-anchor" href="#row-vs-column-oriented-storage" aria-label="Permalink to &quot;&lt;a id=&quot;rowcol&quot;&gt;&lt;/a&gt;Row vs. Column Oriented Storage&quot;">​</a></h2><ul><li>Use row-oriented storage for workloads with iterative transactions where updates are required and frequent inserts are performed.</li><li>Use row-oriented storage when selects against the table are wide.</li><li>Use row-oriented storage for general purpose or mixed workloads.</li><li>Use column-oriented storage where selects are narrow and aggregations of data are computed over a small number of columns.</li><li>Use column-oriented storage for tables that have single columns that are regularly updated without modifying other columns in the row.</li></ul><p>See <a href="./schema.html#row_vs_column">Row or Column Orientation</a>.</p><h2 id="compression" tabindex="-1"><a id="comp"></a>Compression <a class="header-anchor" href="#compression" aria-label="Permalink to &quot;&lt;a id=&quot;comp&quot;&gt;&lt;/a&gt;Compression&quot;">​</a></h2><ul><li>Use compression on large append-optimized and partitioned tables to improve I/O across the system.</li><li>Set the column compression settings at the level where the data resides.</li><li>Balance higher levels of compression with the time and CPU cycles needed to compress and uncompress data.</li></ul><p>See <a href="./schema.html">Compression</a>.</p><h2 id="distributions" tabindex="-1"><a id="dist"></a>Distributions <a class="header-anchor" href="#distributions" aria-label="Permalink to &quot;&lt;a id=&quot;dist&quot;&gt;&lt;/a&gt;Distributions&quot;">​</a></h2><ul><li>Explicitly define a column or random distribution for all tables. Do not use the default.</li><li>Use a single column that will distribute data across all segments evenly.</li><li>Do not distribute on columns that will be used in the <code>WHERE</code> clause of a query.</li><li>Do not distribute on dates or timestamps.</li><li>Never distribute and partition tables on the same column.</li><li>Achieve local joins to significantly improve performance by distributing on the same column for large tables commonly joined together.</li><li>To ensure there is no data skew, validate that data is evenly distributed after the initial load and after incremental loads.</li></ul><p>See <a href="./schema.html">Distributions</a>.</p><h2 id="resource-queue-memory-management" tabindex="-1"><a id="rqman"></a>Resource Queue Memory Management <a class="header-anchor" href="#resource-queue-memory-management" aria-label="Permalink to &quot;&lt;a id=&quot;rqman&quot;&gt;&lt;/a&gt;Resource Queue Memory Management&quot;">​</a></h2><ul><li><p>Set <code>vm.overcommit_memory</code> to 2.</p></li><li><p>Do not configure the OS to use huge pages.</p></li><li><p>Use <code>gp_vmem_protect_limit</code> to set the maximum memory that the instance can allocate for <em>all</em> work being done in each segment database.</p></li><li><p>You can use <code>gp_vmem_protect_limit</code> by calculating:</p><ul><li><p><code>gp_vmem</code> – the total memory available to WarehousePG</p><ul><li><p>If the total system memory is less than 256 GB, use this formula:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gp_vmem = ((SWAP + RAM) – (7.5GB + 0.05 * RAM)) / 1.7</span></span></code></pre></div></li><li><p>If the total system memory is equal to or greater than 256 GB, use this formula:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gp_vmem = ((SWAP + RAM) – (7.5GB + 0.05 * RAM)) / 1.17</span></span></code></pre></div></li></ul><p>where <code>SWAP</code> is the host&#39;s swap space in GB, and <code>RAM</code> is the host&#39;s RAM in GB.</p></li><li><p><code>max_acting_primary_segments</code> – the maximum number of primary segments that could be running on a host when mirror segments are activated due to a host or segment failure.</p></li><li><p><code>gp_vmem_protect_limit</code></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gp_vmem_protect_limit = gp_vmem / acting_primary_segments</span></span></code></pre></div><p>Convert to MB to set the value of the configuration parameter.</p></li></ul></li><li><p>In a scenario where a large number of workfiles are generated calculate the <code>gp_vmem</code> factor with this formula to account for the workfiles.</p><ul><li><p>If the total system memory is less than 256 GB:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gp_vmem = ((SWAP + RAM) – (7.5GB + 0.05 * RAM - (300KB *</span></span>
<span class="line"><span>      total_#_workfiles))) / 1.7</span></span></code></pre></div></li><li><p>If the total system memory is equal to or greater than 256 GB:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gp_vmem = ((SWAP + RAM) – (7.5GB + 0.05 * RAM - (300KB *</span></span>
<span class="line"><span>      total_#_workfiles))) / 1.17</span></span></code></pre></div></li></ul></li><li><p>Never set <code>gp_vmem_protect_limit</code> too high or larger than the physical RAM on the system.</p></li><li><p>Use the calculated <code>gp_vmem</code> value to calculate the setting for the <code>vm.overcommit_ratio</code> operating system parameter:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>vm.overcommit_ratio = (RAM - 0.026 * gp_vmem) / RAM</span></span></code></pre></div></li><li><p>Use <code>statement_mem</code> to allocate memory used for a query per segment db.</p></li><li><p>Use resource queues to set both the numbers of active queries (<code>ACTIVE_STATEMENTS</code>) and the amount of memory (<code>MEMORY_LIMIT</code>) that can be utilized by queries in the queue.</p></li><li><p>Associate all users with a resource queue. Do not use the default queue.</p></li><li><p>Set <code>PRIORITY</code> to match the real needs of the queue for the workload and time of day. Avoid using MAX priority.</p></li><li><p>Ensure that resource queue memory allocations do not exceed the setting for <code>gp_vmem_protect_limit</code>.</p></li><li><p>Dynamically update resource queue settings to match daily operations flow.</p></li></ul><p>See <a href="./../install_guide/config_os.html#topic3__sysctl_file">Setting the WarehousePG Recommended OS Parameters</a> and <a href="./workloads.html">Memory and Resource Management with Resource Queues</a>.</p><h2 id="partitioning" tabindex="-1"><a id="part"></a>Partitioning <a class="header-anchor" href="#partitioning" aria-label="Permalink to &quot;&lt;a id=&quot;part&quot;&gt;&lt;/a&gt;Partitioning&quot;">​</a></h2><ul><li>Partition large tables only. Do not partition small tables.</li><li>Use partitioning only if partition elimination (partition pruning) can be achieved based on the query criteria.</li><li>Choose range partitioning over list partitioning.</li><li>Partition the table based on a commonly-used column, such as a date column.</li><li>Never partition and distribute tables on the same column.</li><li>Do not use default partitions.</li><li>Do not use multi-level partitioning; create fewer partitions with more data in each partition.</li><li>Validate that queries are selectively scanning partitioned tables (partitions are being eliminated) by examining the query <code>EXPLAIN</code> plan.</li><li>Do not create too many partitions with column-oriented storage because of the total number of physical files on every segment: <code>physical files = segments x columns x partitions</code></li></ul><p>See <a href="./schema.html">Partitioning</a>.</p><h2 id="indexes" tabindex="-1"><a id="indexes"></a>Indexes <a class="header-anchor" href="#indexes" aria-label="Permalink to &quot;&lt;a id=&quot;indexes&quot;&gt;&lt;/a&gt;Indexes&quot;">​</a></h2><ul><li>In general indexes are not needed in WarehousePG.</li><li>Create an index on a single column of a columnar table for drill-through purposes for high cardinality tables that require queries with high selectivity.</li><li>Do not index columns that are frequently updated.</li><li>Consider dropping indexes before loading data into a table. After the load, re-create the indexes for the table.</li><li>Create selective B-tree indexes.</li><li>Do not create bitmap indexes on columns that are updated.</li><li>Avoid using bitmap indexes for unique columns, very high or very low cardinality data. Bitmap indexes perform best when the column has a low cardinality—100 to 100,000 distinct values.</li><li>Do not use bitmap indexes for transactional workloads.</li><li>In general do not index partitioned tables. If indexes are needed, the index columns must be different than the partition columns.</li></ul><p>See <a href="./schema.html">Indexes</a>.</p><h2 id="resource-queues" tabindex="-1"><a id="_Toc286661611"></a>Resource Queues <a class="header-anchor" href="#resource-queues" aria-label="Permalink to &quot;&lt;a id=&quot;_Toc286661611&quot;&gt;&lt;/a&gt;Resource Queues&quot;">​</a></h2><ul><li>Use resource queues to manage the workload on the cluster.</li><li>Associate all roles with a user-defined resource queue.</li><li>Use the <code>ACTIVE_STATEMENTS</code> parameter to limit the number of active queries that members of the particular queue can run concurrently.</li><li>Use the <code>MEMORY_LIMIT</code> parameter to control the total amount of memory that queries running through the queue can utilize.</li><li>Alter resource queues dynamically to match the workload and time of day.</li></ul><p>See <a href="./workloads.html#configuring_rq">Configuring Resource Queues</a>.</p><h2 id="monitoring-and-maintenance" tabindex="-1"><a id="monmat"></a>Monitoring and Maintenance <a class="header-anchor" href="#monitoring-and-maintenance" aria-label="Permalink to &quot;&lt;a id=&quot;monmat&quot;&gt;&lt;/a&gt;Monitoring and Maintenance&quot;">​</a></h2><ul><li>Implement the &quot;Recommended Monitoring and Maintenance Tasks&quot; in the <em>WarehousePG Administrator Guide</em>.</li><li>Run <code>gpcheckperf</code> at install time and periodically thereafter, saving the output to compare system performance over time.</li><li>Use all the tools at your disposal to understand how your system behaves under different loads.</li><li>Examine any unusual event to determine the cause.</li><li>Monitor query activity on the system by running explain plans periodically to ensure the queries are running optimally.</li><li>Review plans to determine whether index are being used and partition elimination is occurring as expected.</li><li>Know the location and content of system log files and monitor them on a regular basis, not just when problems arise.</li></ul><p>See <a href="./maintenance.html">System Monitoring and Maintenance</a>, <a href="./../admin_guide/query/topics/query-profiling.html#in198649">Query Profiling</a> and <a href="./logfiles.html">Monitoring WarehousePG Log Files</a>.</p><h2 id="analyze" tabindex="-1"><a id="_Toc286661612"></a>ANALYZE <a class="header-anchor" href="#analyze" aria-label="Permalink to &quot;&lt;a id=&quot;_Toc286661612&quot;&gt;&lt;/a&gt;ANALYZE&quot;">​</a></h2><ul><li>Determine if analyzing the database is actually needed. Analyzing is not needed if <code>gp_autostats_mode</code> is set to <code>on_no_stats</code> (the default) and the table is not partitioned.</li><li>Use <code>analyzedb</code> in preference to <code>ANALYZE</code> when dealing with large sets of tables, as it does not require analyzing the entire database. The <code>analyzedb</code> utility updates statistics data for the specified tables incrementally and concurrently. For append optimized tables, <code>analyzedb</code> updates statistics incrementally only if the statistics are not current. For heap tables, statistics are always updated. <code>ANALYZE</code> does not update the table metadata that the <code>analyzedb</code> utility uses to determine whether table statistics are up to date.</li><li>Selectively run <code>ANALYZE</code> at the table level when needed.</li><li>Always run <code>ANALYZE</code> after <code>INSERT</code>, <code>UPDATE</code>. and <code>DELETE</code> operations that significantly changes the underlying data.</li><li>Always run <code>ANALYZE</code> after <code>CREATE INDEX</code> operations.</li><li>If <code>ANALYZE</code> on very large tables takes too long, run <code>ANALYZE</code> only on the columns used in a join condition, <code>WHERE</code> clause, <code>SORT</code>, <code>GROUP BY</code>, or <code>HAVING</code> clause.</li><li>When dealing with large sets of tables, use <code>analyzedb</code> instead of <code>ANALYZE.</code></li><li>Run <code>analyzedb</code> on the root partition any time that you add a new partition(s) to a partitioned table. This operation both analyzes the leaf partitions in parallel and merges any updated statistics into the root partition.</li></ul><p>See <a href="./analyze.html">Updating Statistics with ANALYZE</a>.</p><h2 id="vacuum" tabindex="-1"><a id="_Toc286661609"></a>Vacuum <a class="header-anchor" href="#vacuum" aria-label="Permalink to &quot;&lt;a id=&quot;_Toc286661609&quot;&gt;&lt;/a&gt;Vacuum&quot;">​</a></h2><ul><li>Run <code>VACUUM</code> after large <code>UPDATE</code> and <code>DELETE</code> operations.</li><li>Do not run <code>VACUUM FULL</code>. Instead run a <code>CREATE TABLE...AS</code> operation, then rename and drop the original table.</li><li>Frequently run <code>VACUUM</code> on the system catalogs to avoid catalog bloat and the need to run <code>VACUUM FULL</code> on catalog tables.</li><li>Never issue a <code>kill</code> command against <code>VACUUM</code> on catalog tables.</li></ul><p>See <a href="./bloat.html">Managing Bloat in a Database</a>.</p><h2 id="loading" tabindex="-1"><a id="_Toc286661610"></a>Loading <a class="header-anchor" href="#loading" aria-label="Permalink to &quot;&lt;a id=&quot;_Toc286661610&quot;&gt;&lt;/a&gt;Loading&quot;">​</a></h2><ul><li>Maximize the parallelism as the number of segments increase.</li><li>Spread the data evenly across as many ETL nodes as possible. <ul><li>Split very large data files into equal parts and spread the data across as many file systems as possible.</li><li>Run two <code>gpfdist</code> instances per file system.</li><li>Run <code>gpfdist</code> on as many interfaces as possible.</li><li>Use <code>gp_external_max_segs</code> to control the number of segments that will request data from the <code>gpfdist</code> process.</li><li>Always keep <code>gp_external_max_segs</code> and the number of <code>gpfdist</code> processes an even factor.</li></ul></li><li>Always drop indexes before loading into existing tables and re-create the index after loading.</li><li>Run <code>VACUUM</code> after load errors to recover space.</li></ul><p>See <a href="./data_loading.html">Loading Data</a>.</p><h2 id="security" tabindex="-1"><a id="secty"></a>Security <a class="header-anchor" href="#security" aria-label="Permalink to &quot;&lt;a id=&quot;secty&quot;&gt;&lt;/a&gt;Security&quot;">​</a></h2><ul><li>Secure the <code>gpadmin</code> user id and only allow essential system administrators access to it.</li><li>Administrators should only log in to WarehousePG as <code>gpadmin</code> when performing certain system maintenance tasks (such as upgrade or expansion).</li><li>Limit users who have the <code>SUPERUSER</code> role attribute. Roles that are superusers bypass all access privilege checks in WarehousePG, as well as resource queuing. Only system administrators should be given superuser rights. See &quot;Altering Role Attributes&quot; in the <em>WarehousePG Administrator Guide</em>.</li><li>Database users should never log on as <code>gpadmin</code>, and ETL or production workloads should never run as <code>gpadmin</code>.</li><li>Assign a distinct WarehousePG role to each user, application, or service that logs in.</li><li>For applications or web services, consider creating a distinct role for each application or service.</li><li>Use groups to manage access privileges.</li><li>Protect the root password.</li><li>Enforce a strong password password policy for operating system passwords.</li><li>Ensure that important operating system files are protected.</li></ul><p>See <a href="./security.html">Security</a>.</p><h2 id="encryption" tabindex="-1"><a id="enc1"></a>Encryption <a class="header-anchor" href="#encryption" aria-label="Permalink to &quot;&lt;a id=&quot;enc1&quot;&gt;&lt;/a&gt;Encryption&quot;">​</a></h2><ul><li>Encrypting and decrypting data has a performance cost; only encrypt data that requires encryption.</li><li>Do performance testing before implementing any encryption solution in a production system.</li><li>Server certificates in a production WarehousePG cluster should be signed by a certificate authority (CA) so that clients can authenticate the server. The CA may be local if all clients are local to the organization.</li><li>Client connections to WarehousePG should use SSL encryption whenever the connection goes through an insecure link.</li><li>A symmetric encryption scheme, where the same key is used to both encrypt and decrypt, has better performance than an asymmetric scheme and should be used when the key can be shared safely.</li><li>Use cryptographic functions to encrypt data on disk. The data is encrypted and decrypted in the database process, so it is important to secure the client connection with SSL to avoid transmitting unencrypted data.</li><li>Use the gpfdists protocol to secure ETL data as it is loaded into or unloaded from the database.</li></ul><p>See <a href="./encryption.html">Encrypting Data and Database Connections</a></p><h2 id="high-availability" tabindex="-1"><a id="havail"></a>High Availability <a class="header-anchor" href="#high-availability" aria-label="Permalink to &quot;&lt;a id=&quot;havail&quot;&gt;&lt;/a&gt;High Availability&quot;">​</a></h2><blockquote><p><strong>Note</strong> The following guidelines apply to actual hardware deployments, but not to public cloud-based infrastructure, where high availability solutions may already exist.</p></blockquote><ul><li>Use a hardware RAID storage solution with 8 to 24 disks.</li><li>Use RAID 1, 5, or 6 so that the disk array can tolerate a failed disk.</li><li>Configure a hot spare in the disk array to allow rebuild to begin automatically when disk failure is detected.</li><li>Protect against failure of the entire disk array and degradation during rebuilds by mirroring the RAID volume.</li><li>Monitor disk utilization regularly and add additional space when needed.</li><li>Monitor segment skew to ensure that data is distributed evenly and storage is consumed evenly at all segments.</li><li>Set up a standby coordinator instance to take over if the primary coordinator fails.</li><li>Plan how to switch clients to the new coordinator instance when a failure occurs, for example, by updating the coordinator address in DNS.</li><li>Set up monitoring to send notifications in a system monitoring application or by email when the primary fails.</li><li>Set up mirrors for all segments.</li><li>Locate primary segments and their mirrors on different hosts to protect against host failure.</li><li>Recover failed segments promptly, using the <code>gprecoverseg</code> utility, to restore redundancy and return the system to optimal balance.</li><li>Consider a Dual Cluster configuration to provide an additional level of redundancy and additional query processing throughput.</li><li>Backup WarehousePGs regularly unless the data is easily restored from sources.</li><li>If backups are saved to local cluster storage, move the files to a safe, off-cluster location when the backup is complete.</li><li>If backups are saved to NFS mounts, use a scale-out NFS solution such as Dell PowerScale (Isilon) to prevent IO bottlenecks.</li></ul><p>See <a href="./ha.html">High Availability</a>.</p><p><strong>Parent topic:</strong> <a href="./intro.html">WarehousePG Best Practices</a></p>`,54)]))}const m=a(s,[["render",n]]);export{p as __pageData,m as default};
