import{_ as a,c as s,o as n,ag as t}from"./chunks/framework.Ds6Eueu6.js";const m=JSON.parse('{"title":"Creating and Using External Web Tables","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/external/creating-and-using-web-external-tables.md","filePath":"docs/7x/admin_guide/external/creating-and-using-web-external-tables.md"}'),o={name:"docs/7x/admin_guide/external/creating-and-using-web-external-tables.md"};function i(l,e,p,c,r,d){return n(),s("div",null,e[0]||(e[0]=[t(`<h1 id="creating-and-using-external-web-tables" tabindex="-1">Creating and Using External Web Tables <a class="header-anchor" href="#creating-and-using-external-web-tables" aria-label="Permalink to &quot;Creating and Using External Web Tables&quot;">​</a></h1><hr><p>External web tables allow WarehousePG to treat dynamic data sources like regular database tables. Because web table data can change as a query runs, the data is not rescannable.</p><p><code>CREATE EXTERNAL WEB TABLE</code> creates a web table definition. You can define command-based or URL-based external web tables. The definition forms are distinct: you cannot mix command-based and URL-based definitions.</p><p><strong>Parent topic:</strong> <a href="./../external/external-tables.html">Defining External Tables</a></p><h2 id="command-based-external-web-tables" tabindex="-1"><a id="topic32"></a>Command-based External Web Tables <a class="header-anchor" href="#command-based-external-web-tables" aria-label="Permalink to &quot;&lt;a id=&quot;topic32&quot;&gt;&lt;/a&gt;Command-based External Web Tables&quot;">​</a></h2><p>The output of a shell command or script defines command-based web table data. Specify the command in the <code>EXECUTE</code> clause of <code>CREATE EXTERNAL WEB TABLE</code>. The data is current as of the time the command runs. The <code>EXECUTE</code> clause runs the shell command or script on the specified coordinator, and/or segment host or hosts. The command or script must reside on the hosts corresponding to the host(s) defined in the <code>EXECUTE</code> clause.</p><p>By default, the command is run on segment hosts when active segments have output rows to process. For example, if each segment host runs four primary segment instances that have output rows to process, the command runs four times per segment host. You can optionally limit the number of segment instances that run the web table command. All segments included in the web table definition in the <code>ON</code> clause run the command in parallel.</p><p>The command that you specify in the external table definition is run from the database and cannot access environment variables from <code>.bashrc</code> or <code>.profile</code>. Set environment variables in the <code>EXECUTE</code> clause. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# CREATE EXTERNAL WEB TABLE output (output text)</span></span>
<span class="line"><span>    EXECUTE &#39;PATH=/home/gpadmin/programs; export PATH; myprogram.sh&#39; </span></span>
<span class="line"><span>    FORMAT &#39;TEXT&#39;;</span></span></code></pre></div><p>Scripts must be executable by the <code>gpadmin</code> user and reside in the same location on the coordinator or segment hosts.</p><p>The following command defines a web table that runs a script. The script runs on each segment host where a segment has output rows to process.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# CREATE EXTERNAL WEB TABLE log_output </span></span>
<span class="line"><span>    (linenum int, message text) </span></span>
<span class="line"><span>    EXECUTE &#39;/var/load_scripts/get_log_data.sh&#39; ON HOST </span></span>
<span class="line"><span>    FORMAT &#39;TEXT&#39; (DELIMITER &#39;|&#39;);</span></span></code></pre></div><h2 id="url-based-external-web-tables" tabindex="-1"><a id="topic33"></a>URL-based External Web Tables <a class="header-anchor" href="#url-based-external-web-tables" aria-label="Permalink to &quot;&lt;a id=&quot;topic33&quot;&gt;&lt;/a&gt;URL-based External Web Tables&quot;">​</a></h2><p>A URL-based web table accesses data from a web server using the HTTP protocol. Web table data is dynamic; the data is not rescannable.</p><p>Specify the <code>LOCATION</code> of files on a web server using <code>http://</code>. The web data file(s) must reside on a web server that WarehousePG segment hosts can access. The number of URLs specified corresponds to the number of segment instances that work in parallel to access the web table. For example, if you specify two external files to a WarehousePG cluster with eight primary segments, two of the eight segments access the web table in parallel at query runtime.</p><p>The following sample command defines a web table that gets data from several URLs.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=# CREATE EXTERNAL WEB TABLE ext_expenses (name text, </span></span>
<span class="line"><span>  date date, amount float4, category text, description text) </span></span>
<span class="line"><span>  LOCATION ( </span></span>
<span class="line"><span></span></span>
<span class="line"><span>  &#39;http://intranet.company.com/expenses/sales/file.csv&#39;,</span></span>
<span class="line"><span>  &#39;http://intranet.company.com/expenses/exec/file.csv&#39;,</span></span>
<span class="line"><span>  &#39;http://intranet.company.com/expenses/finance/file.csv&#39;,</span></span>
<span class="line"><span>  &#39;http://intranet.company.com/expenses/ops/file.csv&#39;,</span></span>
<span class="line"><span>  &#39;http://intranet.company.com/expenses/marketing/file.csv&#39;,</span></span>
<span class="line"><span>  &#39;http://intranet.company.com/expenses/eng/file.csv&#39; </span></span>
<span class="line"><span></span></span>
<span class="line"><span>   )</span></span>
<span class="line"><span>  FORMAT &#39;CSV&#39; ( HEADER );</span></span></code></pre></div>`,18)]))}const b=a(o,[["render",i]]);export{m as __pageData,b as default};
