import{_ as a,c as t,o as s,ag as o}from"./chunks/framework.Ds6Eueu6.js";const m=JSON.parse('{"title":"file:// Protocol","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/admin_guide/external/file-protocol.md","filePath":"docs/7x/admin_guide/external/file-protocol.md"}'),n={name:"docs/7x/admin_guide/external/file-protocol.md"};function l(i,e,p,c,r,h){return s(),t("div",null,e[0]||(e[0]=[o(`<h1 id="file-protocol" tabindex="-1">file:// Protocol <a class="header-anchor" href="#file-protocol" aria-label="Permalink to &quot;file:// Protocol&quot;">​</a></h1><hr><p>The <code>file://</code> protocol is used in a URI that specifies the location of an operating system file. External tables that you create that specify the <code>file://</code> protocol are read-only tables.</p><p>The URI includes the host name, port, and path to the file. Each file must reside on a segment host in a location accessible by the WarehousePG superuser (<code>gpadmin</code>). The host name used in the URI must match a segment host name registered in the <code>gp_segment_configuration</code> system catalog table.</p><p>The <code>LOCATION</code> clause can have multiple URIs, as shown in this example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE EXTERNAL TABLE ext_expenses (</span></span>
<span class="line"><span>   name text, date date, amount float4, category text, desc1 text ) </span></span>
<span class="line"><span>LOCATION (&#39;file://host1:5432/data/expense/*.csv&#39;, </span></span>
<span class="line"><span>          &#39;file://host2:5432/data/expense/*.csv&#39;, </span></span>
<span class="line"><span>          &#39;file://host3:5432/data/expense/*.csv&#39;) </span></span>
<span class="line"><span>FORMAT &#39;CSV&#39; (HEADER);</span></span></code></pre></div><p>The number of URIs you specify in the <code>LOCATION</code> clause is the number of segment instances that will work in parallel to access the external table. For each URI, WarehousePG assigns a primary segment on the specified host to the file. For maximum parallelism when loading data, divide the data into as many equally sized files as you have primary segments. This ensures that all segments participate in the load. The number of external files per segment host cannot exceed the number of primary segment instances on that host. For example, if your array has four primary segment instances per segment host, you can place four external files on each segment host. Tables based on the <code>file://</code> protocol can only be readable tables.</p><p>The system view <code>pg_max_external_files</code> shows how many external table files are permitted per external table. This view lists the available file slots per segment host when using the <code>file:// protocol</code>. The view is only applicable for the <code>file:// protocol</code>. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * FROM pg_max_external_files;</span></span></code></pre></div><p><strong>Parent topic:</strong> <a href="./../external/external-tables.html">Defining External Tables</a></p>`,10)]))}const f=a(n,[["render",l]]);export{m as __pageData,f as default};
