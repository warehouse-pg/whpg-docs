import{_ as a,c as t,o as s,ag as o}from"./chunks/framework.Ds6Eueu6.js";const h=JSON.parse('{"title":"CREATE RESOURCE QUEUE","description":"","frontmatter":{},"headers":[],"relativePath":"docs/7x/ref_guide/sql_commands/CREATE_RESOURCE_QUEUE.md","filePath":"docs/7x/ref_guide/sql_commands/CREATE_RESOURCE_QUEUE.md"}'),i={name:"docs/7x/ref_guide/sql_commands/CREATE_RESOURCE_QUEUE.md"};function n(r,e,u,l,c,d){return s(),t("div",null,e[0]||(e[0]=[o(`<h1 id="create-resource-queue" tabindex="-1">CREATE RESOURCE QUEUE <a class="header-anchor" href="#create-resource-queue" aria-label="Permalink to &quot;CREATE RESOURCE QUEUE&quot;">​</a></h1><p>Defines a new resource queue.</p><h2 id="synopsis" tabindex="-1"><a id="section2"></a>Synopsis <a class="header-anchor" href="#synopsis" aria-label="Permalink to &quot;&lt;a id=&quot;section2&quot;&gt;&lt;/a&gt;Synopsis&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE QUEUE &lt;name&gt; WITH (&lt;queue_attribute&gt;=&lt;value&gt; [, ... ])</span></span></code></pre></div><p>where queue_attribute is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    ACTIVE_STATEMENTS=&lt;integer&gt;</span></span>
<span class="line"><span>        [ MAX_COST=&lt;float &gt;[COST_OVERCOMMIT={TRUE|FALSE}] ]</span></span>
<span class="line"><span>        [ MIN_COST=&lt;float &gt;]</span></span>
<span class="line"><span>        [ PRIORITY={MIN|LOW|MEDIUM|HIGH|MAX} ]</span></span>
<span class="line"><span>        [ MEMORY_LIMIT=&#39;&lt;memory_units&gt;&#39; ]</span></span>
<span class="line"><span></span></span>
<span class="line"><span> | MAX_COST=float [ COST_OVERCOMMIT={TRUE|FALSE} ]</span></span>
<span class="line"><span>        [ ACTIVE_STATEMENTS=&lt;integer &gt;]</span></span>
<span class="line"><span>        [ MIN_COST=&lt;float &gt;]</span></span>
<span class="line"><span>        [ PRIORITY={MIN|LOW|MEDIUM|HIGH|MAX} ]</span></span>
<span class="line"><span>        [ MEMORY_LIMIT=&#39;&lt;memory_units&gt;&#39; ]</span></span></code></pre></div><h2 id="description" tabindex="-1"><a id="section3"></a>Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;&lt;a id=&quot;section3&quot;&gt;&lt;/a&gt;Description&quot;">​</a></h2><p>Creates a new resource queue for WarehousePG resource management. A resource queue must have either an <code>ACTIVE_STATEMENTS</code> or a <code>MAX_COST</code> value (or it can have both). Only a superuser can create a resource queue.</p><p>Resource queues with an <code>ACTIVE_STATEMENTS</code> threshold set a maximum limit on the number of queries that can be run by roles assigned to that queue. It controls the number of active queries that are allowed to run at the same time. The value for <code>ACTIVE_STATEMENTS</code> should be an integer greater than 0.</p><p>Resource queues with a <code>MAX_COST</code> threshold set a maximum limit on the total cost of queries that can be run by roles assigned to that queue. Cost is measured in the <em>estimated total cost</em> for the query as determined by the query planner (as shown in the <code>EXPLAIN</code> output for a query). Therefore, an administrator must be familiar with the queries typically run on the system in order to set an appropriate cost threshold for a queue. Cost is measured in units of disk page fetches; 1.0 equals one sequential disk page read. The value for <code>MAX_COST</code> is specified as a floating point number (for example 100.0) or can also be specified as an exponent (for example 1e+2). If a resource queue is limited based on a cost threshold, then the administrator can allow <code>COST_OVERCOMMIT=TRUE</code> (the default). This means that a query that exceeds the allowed cost threshold will be allowed to run but only when the system is idle. If <code>COST_OVERCOMMIT=FALSE</code> is specified, queries that exceed the cost limit will always be rejected and never allowed to run. Specifying a value for <code>MIN_COST</code> allows the administrator to define a cost for small queries that will be exempt from resource queueing.</p><blockquote><p><strong>Note</strong> GPORCA and the Postgres-based planner utilize different query costing models and may compute different costs for the same query. The WarehousePG resource queue resource management scheme neither differentiates nor aligns costs between GPORCA and the Postgres-based planner; it uses the literal cost value returned from the optimizer to throttle queries.</p></blockquote><p>When resource queue-based resource management is active, use the <code>MEMORY_LIMIT</code> and <code>ACTIVE_STATEMENTS</code> limits for resource queues rather than configuring cost-based limits. Even when using GPORCA, WarehousePG may fall back to using the Postgres-based planner for certain queries, so using cost-based limits can lead to unexpected results.</p><p>If a value is not defined for <code>ACTIVE_STATEMENTS</code> or <code>MAX_COST</code>, it is set to <code>-1</code> by default (meaning no limit). After defining a resource queue, you must assign roles to the queue using the <a href="./ALTER_ROLE.html">ALTER ROLE</a> or <a href="./CREATE_ROLE.html">CREATE ROLE</a> command.</p><p>You can optionally assign a <code>PRIORITY</code> to a resource queue to control the relative share of available CPU resources used by queries associated with the queue in relation to other resource queues. If a value is not defined for <code>PRIORITY</code>, queries associated with the queue have a default priority of <code>MEDIUM</code>.</p><p>Resource queues with an optional <code>MEMORY_LIMIT</code> threshold set a maximum limit on the amount of memory that all queries submitted through a resource queue can consume on a segment host. This determines the total amount of memory that all worker processes of a query can consume on a segment host during query execution. WarehousePG recommends that <code>MEMORY_LIMIT</code> be used in conjunction with <code>ACTIVE_STATEMENTS</code> rather than with <code>MAX_COST</code>. The default amount of memory allotted per query on statement-based queues is: <code>MEMORY_LIMIT / ACTIVE_STATEMENTS</code>. The default amount of memory allotted per query on cost-based queues is: <code>MEMORY_LIMIT * (query_cost / MAX_COST)</code>.</p><p>The default memory allotment can be overridden on a per-query basis using the <code>statement_mem</code> server configuration parameter, provided that <code>MEMORY_LIMIT</code> or <code>max_statement_mem</code> is not exceeded. For example, to allocate more memory to a particular query:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>=&gt; SET statement_mem=&#39;2GB&#39;;</span></span>
<span class="line"><span>=&gt; SELECT * FROM my_big_table WHERE column=&#39;value&#39; ORDER BY id;</span></span>
<span class="line"><span>=&gt; RESET statement_mem;</span></span></code></pre></div><p>The <code>MEMORY_LIMIT</code> value for all of your resource queues should not exceed the amount of physical memory of a segment host. If workloads are staggered over multiple queues, memory allocations can be oversubscribed. However, queries can be cancelled during execution if the segment host memory limit specified in <code>gp_vmem_protect_limit</code> is exceeded.</p><p>For information about <code>statement_mem</code>, <code>max_statement</code>, and <code>gp_vmem_protect_limit</code>, see <a href="./../config_params/guc_config.html">Server Configuration Parameters</a>.</p><h2 id="parameters" tabindex="-1"><a id="section4"></a>Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;&lt;a id=&quot;section4&quot;&gt;&lt;/a&gt;Parameters&quot;">​</a></h2><p>name : The name of the resource queue.</p><p>ACTIVE_STATEMENTS integer : Resource queues with an <code>ACTIVE_STATEMENTS</code> threshold limit the number of queries that can be run by roles assigned to that queue. It controls the number of active queries that are allowed to run at the same time. The value for <code>ACTIVE_STATEMENTS</code> should be an integer greater than 0.</p><p>MEMORY_LIMIT &#39;memory_units&#39; : Sets the total memory quota for all statements submitted from users in this resource queue. Memory units can be specified in kB, MB or GB. The minimum memory quota for a resource queue is 10MB. There is no maximum, however the upper boundary at query execution time is limited by the physical memory of a segment host. The default is no limit (<code>-1</code>).</p><p>MAX_COST float : Resource queues with a <code>MAX_COST</code> threshold set a maximum limit on the total cost of queries that can be run by roles assigned to that queue. Cost is measured in the <em>estimated total cost</em> for the query as determined by the WarehousePG query optimizer (as shown in the <code>EXPLAIN</code> output for a query). Therefore, an administrator must be familiar with the queries typically run on the system in order to set an appropriate cost threshold for a queue. Cost is measured in units of disk page fetches; 1.0 equals one sequential disk page read. The value for <code>MAX_COST</code> is specified as a floating point number (for example 100.0) or can also be specified as an exponent (for example 1e+2).</p><p>COST_OVERCOMMIT boolean : If a resource queue is limited based on <code>MAX_COST</code>, then the administrator can allow <code>COST_OVERCOMMIT</code> (the default). This means that a query that exceeds the allowed cost threshold will be allowed to run but only when the system is idle. If <code>COST_OVERCOMMIT=FALSE</code>is specified, queries that exceed the cost limit will always be rejected and never allowed to run.</p><p>MIN_COST float : The minimum query cost limit of what is considered a small query. Queries with a cost under this limit will not be queued and run immediately. Cost is measured in the <em>estimated total cost</em> for the query as determined by the query planner (as shown in the <code>EXPLAIN</code> output for a query). Therefore, an administrator must be familiar with the queries typically run on the system in order to set an appropriate cost for what is considered a small query. Cost is measured in units of disk page fetches; 1.0 equals one sequential disk page read. The value for <code>MIN_COST</code>is specified as a floating point number (for example 100.0) or can also be specified as an exponent (for example 1e+2).</p><p>PRIORITY={MIN|LOW|MEDIUM|HIGH|MAX} : Sets the priority of queries associated with a resource queue. Queries or statements in queues with higher priority levels will receive a larger share of available CPU resources in case of contention. Queries in low-priority queues may be delayed while higher priority queries are run. If no priority is specified, queries associated with the queue have a priority of <code>MEDIUM</code>.</p><h2 id="notes" tabindex="-1"><a id="section5"></a>Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;&lt;a id=&quot;section5&quot;&gt;&lt;/a&gt;Notes&quot;">​</a></h2><p>Use the <code>gp_toolkit.gp_resqueue_status</code> system view to see the limit settings and current status of a resource queue:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SELECT * from gp_toolkit.gp_resqueue_status WHERE </span></span>
<span class="line"><span>  rsqname=&#39;queue_name&#39;;</span></span></code></pre></div><p>There is also another system view named <code>pg_stat_resqueue</code> which shows statistical metrics for a resource queue over time. To use this view, however, you must enable the <code>stats_queue_level</code> server configuration parameter. See &quot;Managing Workload and Resources&quot; in the <em>WarehousePG Administrator Guide</em> for more information about using resource queues.</p><p><code>CREATE RESOURCE QUEUE</code> cannot be run within a transaction.</p><p>Also, an SQL statement that is run during the execution time of an <code>EXPLAIN ANALYZE</code> command is excluded from resource queues.</p><h2 id="examples" tabindex="-1"><a id="section6"></a>Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;&lt;a id=&quot;section6&quot;&gt;&lt;/a&gt;Examples&quot;">​</a></h2><p>Create a resource queue with an active query limit of 20:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE QUEUE myqueue WITH (ACTIVE_STATEMENTS=20);</span></span></code></pre></div><p>Create a resource queue with an active query limit of 20 and a total memory limit of 2000MB (each query will be allocated 100MB of segment host memory at execution time):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE QUEUE myqueue WITH (ACTIVE_STATEMENTS=20, </span></span>
<span class="line"><span>  MEMORY_LIMIT=&#39;2000MB&#39;);</span></span></code></pre></div><p>Create a resource queue with a query cost limit of 3000.0:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE QUEUE myqueue WITH (MAX_COST=3000.0);</span></span></code></pre></div><p>Create a resource queue with a query cost limit of 310 (or 30000000000.0) and do not allow overcommit. Allow small queries with a cost under 500 to run immediately:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE QUEUE myqueue WITH (MAX_COST=3e+10, </span></span>
<span class="line"><span>  COST_OVERCOMMIT=FALSE, MIN_COST=500.0);</span></span></code></pre></div><p>Create a resource queue with both an active query limit and a query cost limit:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE QUEUE myqueue WITH (ACTIVE_STATEMENTS=30, </span></span>
<span class="line"><span>  MAX_COST=5000.00);</span></span></code></pre></div><p>Create a resource queue with an active query limit of 5 and a maximum priority setting:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATE RESOURCE QUEUE myqueue WITH (ACTIVE_STATEMENTS=5, </span></span>
<span class="line"><span>  PRIORITY=MAX);</span></span></code></pre></div><h2 id="compatibility" tabindex="-1"><a id="section7"></a>Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;&lt;a id=&quot;section7&quot;&gt;&lt;/a&gt;Compatibility&quot;">​</a></h2><p><code>CREATE RESOURCE QUEUE</code> is a WarehousePG extension. There is no provision for resource queues or resource management in the SQL standard.</p><h2 id="see-also" tabindex="-1"><a id="section8"></a>See Also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;&lt;a id=&quot;section8&quot;&gt;&lt;/a&gt;See Also&quot;">​</a></h2><p><a href="./ALTER_ROLE.html">ALTER ROLE</a>, <a href="./CREATE_ROLE.html">CREATE ROLE</a>, <a href="./ALTER_RESOURCE_QUEUE.html">ALTER RESOURCE QUEUE</a>, <a href="./DROP_RESOURCE_QUEUE.html">DROP RESOURCE QUEUE</a></p><p><strong>Parent topic:</strong> <a href="./../sql_commands/sql_ref.html">SQL Commands</a></p>`,51)]))}const m=a(i,[["render",n]]);export{h as __pageData,m as default};
